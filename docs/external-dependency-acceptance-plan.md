# 外部依赖项验收计划（剩余 9 项门禁）

> **用途**：把 `docs/delivery-gates.md` 中 9 项未勾选门禁逐项拆成**可执行的验收计划**：需要哪些外部输入、先补哪些代码、按什么顺序跑、通过判据是什么、留什么证据、失败怎么回滚。
>
> **生成日期**：2026-09-11　**基线**：分支 `feature/planning-closure`，后端 **662 项测试通过**，`compileall` 退出码 0
>
> **与既有文档的关系**：本计划**不替代** `docs/staging-acceptance-checklist.md`（项 1–5 的执行底稿）、`docs/private-deployment-runbook.md`（私有部署与回滚）、`docs/superpowers/poc-staging-runbook.md`（外部 Runtime 联调）。本计划负责**把它们编排到 9 项门禁上**，并补齐它们没有覆盖的部分。
>
> **证据口径**：本计划本身**不构成任何验收证据**。未通过真实验收的能力不得写成「已上线 / 已发布 / 已收录」。

---

## 0. 总闸门与使用方式

**总闸门（缺一即停）**：`py scripts/staging_preflight.py` 返回 `pass`。它是聚合入口，内嵌商业化 G0 与外部 Runtime 元数据两组检查。

三个预检脚本的口径（已核实源码）：

| 脚本 | 用途 | 退出码 | 是否发网络 |
| --- | --- | --- | --- |
| `scripts/staging_preflight.py` | 聚合：基础设施隔离 + 商业化 G0 + 外部 Runtime 元数据 | `pass`→0，其余→1 | **否** |
| `scripts/commercial_g0_preflight.py` | 非开发环境、PostgreSQL、双密钥、迁移一致、保留策略、Runtime 版本 | `pass`→0，其余→1 | **否** |
| `scripts/runtime_staging_preflight.py` | RAGFlow/AgentScope 的地址、固定版本、认证注入标记、网络白名单 | 全 `pass`→0，否则 1 | **否** |

> **三条硬约束**：① 三者都**只校验部署元数据**，不发起任何网络请求、不读取也不打印密钥值，**不能替代真实验收**；② 全部 **fail-closed**：变量缺失即判 `fail`/`blocked`，不会静默跳过；③ 输出为逐行文本 `[status] 名称：消息`，**没有 JSON 模式**，请保存原文作为预检证据。

`status` 取值：`pass` / `fail` / `blocked`（`blocked` 表示元数据缺失，不是代码问题）。

---

## 1. 关键结论：这 9 项并非「给资源就能做」

这是本计划最重要的一节。**9 项里有 4 项存在代码缺口**，拿不到代码就无法进入验收：

| # | 门禁原文 | 性质 | 需先补代码？ | 主阻塞 |
| --- | --- | --- | --- | --- |
| 1 | staging 数据库实测、迁移回滚演练和真实并发压测 | 纯外部（+1 个探针脚本） | 探针脚本 | 独立 PG + 备份介质 |
| 2 | Celery Worker 实跑、Outbox 生产连接池、死信通知渠道和 staging 验收 | **含代码缺口** | **死信通知渠道未实现** | 真实 Redis + 通知渠道 |
| 3 | 设备绑定、生产密钥轮换和真实统一登录验收 | **含代码缺口** | **设备绑定、统一登录/SSO 均未实现** | 部署密钥系统 + 真实 IdP + 验证器真机 |
| 4 | 真实 PostgreSQL 商业化迁移、备份/恢复演练与客户管理员验收 | 纯外部 | 无 | 客户 staging + 客户管理员 |
| 5 | 私有部署生产预检、容量压测、独立密钥轮换与试点客户交付 | 纯外部（依赖 1/2/4） | 无 | 试点客户 + 容量阈值 |
| 6 | GEO 版本化适配器 | **含代码缺口**（且不在本仓库） | 需契约在手后才写适配器 | **GEO 侧 API 契约** |
| 7 | 真实 staging 与真实平台账号验收 | 纯外部（依赖 1） | 无 | 平台账号 + 发布授权 |
| 8 | RAGFlow/AgentScope 密钥注入、跨租户实测、并发压测、沙箱验证和真实外部服务验收 | 纯外部（依赖 1） | 无 | 外部平台地址/版本/账号/密钥注入 |
| 9 | 真实模型、网页抓取和公众号自动发布验收 | **含代码缺口** | **网页抓取与自动发布未实现**（真实模型接入代码已具备） | 真实模型密钥 + 平台凭据 |

**推论**：项 3、6、9 的「先补代码」是本计划第 6 节登记的阻塞缺陷，**不属于外部资源范畴**；在它们补完前，给再多外部资源也无法通过验收。

---

## 2. 通用约定

### 2.1 前置条件（缺一即停）

1. `WORKBENCH_ENV` 非 `development`、`WORKBENCH_STORAGE_BACKEND=postgres`，禁止内存回退。
2. PostgreSQL / Redis / 对象存储**均非 localhost**，且租户、命名空间、Redis 逻辑库彼此隔离。
3. 双密钥（`WORKBENCH_AUTH_SECRET` 与 `WORKBENCH_BACKUP_ENCRYPTION_KEY`）≥ 32 位、互不相同，**只由部署密钥系统注入**。
4. `WORKBENCH_APPLIED_MIGRATIONS`、`WORKBENCH_RETENTION_POLICY`、`WORKBENCH_RUNTIME_VERSIONS` 已按目标库与交付记录登记。
5. `py scripts/staging_preflight.py` 返回 `pass`。
6. 管理员已绑定 TOTP（`WORKBENCH_REQUIRE_ADMIN_TOTP` 为真时，未绑定只发受限会话）。

### 2.2 证据规范（本计划新增约定）

- **原始证据不入库**。证据目录固定为仓库根的 `.acceptance/`（已加入 `.gitignore`），命名 `.acceptance/<日期>-<项号>-<slug>/`。
- 仓库内**只提交结论**：验收完成后把结论行追加到 `docs/acceptance-log.md`（**首次验收时创建**，一行一项，含日期、项号、结论、证据目录名、执行人），**不提交原始证据文件**。
- **禁止进入仓库或粘贴进结论**的内容：密码、`Authorization` 头、API Key、Cookie、验证码、浏览器会话、客户原文、数据库连接串。
- 证据若必须含请求/响应，一律**脱敏摘要**（去掉上述字段）；预检脚本输出本身已不含密钥，可直接留存。

### 2.3 通过判据通则

- 每项的判据必须是**可判定**的（响应码分布、记录条数、字段是否存在），不接受「看起来正常」。
- 出现未解释的 4xx/5xx、跨租户可读、密钥落盘落日志 → **判失败**，不豁免。
- 阈值类判据（容量、延迟）**必须先书面确认**，否则记为「不可判定」而不是「通过」。

### 2.4 回滚与失败处理通则

- 失败即停，**不在原库试错**；回滚只使用**已演练过的备份与对象清单**。
- **禁止手工修改用量账本或审计记录**。
- 涉及越权、数据泄露、提示词注入、未知发布回执时立即暂停并转人工接管。
- 关闭外部 Runtime 的注册项即可回退到 Mock，**不改动工作台的任务事实源**；**禁止直接重放外部副作用**。

### 2.5 证据记录模板（复制到 `.acceptance/<目录>/README.md`）

```markdown
# 验收记录 · 项 <N> · <标题>

- 日期：
- 执行人 / 复核人：
- 目标环境：staging 标识 =           目标库 =            Runtime 版本 =
- 预检结果：`py scripts/staging_preflight.py` → pass / fail / blocked（附原文文件）
- 本轮范围：
- 未覆盖范围与原因：

## 判据逐条核对

| 判据 | 实测结果 | 证据文件 | 结论 |
| --- | --- | --- | --- |
|      |          |          | pass / fail |

## 证据文件清单

| 文件 | 内容 | 是否已脱敏 |
| --- | --- | --- |

## 失败项与处理

- 现象：      定位：      处理：      是否回滚：

## 回滚记录（如发生）

- 使用的备份/对象清单：      操作人：      时间：      结果：
```

---

## 3. 逐项验收计划

### 项 1 · staging 数据库实测、迁移回滚演练和真实并发压测

- **目标**：证明 PostgreSQL 仓储在真实 PG 上的租户隔离、唯一约束、原子审批、迁移 runner 与**回滚**都正确，并证明并发下不出现越权或重复生效。
- **需要的输入**：独立 staging PostgreSQL（非 localhost）与凭据；备份介质与独立加密密钥；维护窗口；一台能访问 staging 的探针运行主机。
- **探针脚本（已实现）**：`scripts/staging_concurrency_probe.py` 覆盖三个场景：① 同一手机号并发失败登录（验证限流跨进程一致性 → 期望 `429`）；② 同一幂等键并发创建任务（期望只产生 1 条记录）；③ 同一计划提案并发审批（期望恰好 1 个 `200`、其余 `409`），输出脱敏 JSON 统计。真实命令示例：

  ```powershell
  py scripts/staging_concurrency_probe.py --base-url https://staging.example.com --token $env:WORKBENCH_PROBE_TOKEN --phone +8613800000000 --password $env:WORKBENCH_PROBE_PASSWORD --proposal-id <pending_review 且发起人≠令牌用户的提案 id> --concurrency 8 --timeout 10 --output .acceptance/probe.json
  ```

  说明：默认强制 HTTPS 且拒绝本地主机（越界以退出码 `2` 失败，可临时用 `--allow-insecure` / `--allow-local` 覆盖）；退出码为「配置错误 `2` / 全部非 skipped 场景 `pass` 为 `0` / 否则 `1`」。`plan_approval` 场景未提供 `--proposal-id` 时记为 `skipped` 且不影响整体判定。**该脚本只产出并发行为统计，报告仅含主机与响应码分布、不含口令/令牌，本身不构成验收证据。**
- **验收步骤**（执行底稿：`docs/staging-acceptance-checklist.md` 执行顺序 1–9）：
  1. 跑 `py scripts/staging_preflight.py` → 必须 `pass`，留存原文。
  2. 维护窗口前做一致性备份，记录备份校验值；启动应用让迁移 runner 应用 `001`–`014`，记录实际应用的迁移版本。
  3. 冒烟：`GET /api/v1/health`、租户读取、客户管理员权限检查、**跨租户 404 检查**。
  4. 创建一条低风险任务，验证 PostgreSQL 持久化、审计写入与**幂等重复提交**。
  5. 运行并发探针（上述三场景），记录响应码分布与 P95 延迟。
  6. **回滚演练**：按备份把隔离库恢复到迁移前状态，验证应用可启动、租户/审计可读、隔离仍成立，记录耗时。
- **通过判据**：迁移清单与登记一致；跨租户一律 `404`、越权一律 `403`；同幂等键并发只留 1 条记录；并发审批只有一个成功；回滚后数据可读且隔离成立；探针无未解释 5xx。
- **证据清单**：预检原文；迁移清单打印；备份校验值；探针原始 JSON 与统计；审计片段；回滚操作记录（操作人/时间/结果）。
- **失败与回滚**：见 2.4；回滚只用已演练备份。
- **关联**：`docs/security-attack-surface-report.md` 第 5 节「未覆盖项」1/2/5。

### 项 2 · Celery Worker 实跑、Outbox 生产连接池、死信通知渠道和 staging 验收

- **目标**：证明 Outbox → Redis Streams → Worker 的真实链路、幂等消费、有限重试与死信登记，以及**死信通知可达**。
- **需要的输入**：真实 Redis（独立实例或独立逻辑库）；Worker 进程运行环境；**死信通知渠道**（企业 IM webhook / SMTP 任一）与收件人。
- **需先补的代码（阻塞项）**：
  1. **死信通知渠道未实现** —— 全仓 `app/` 内 `notification` / `notify` / `webhook` / `push` 0 命中；现有 `DeadLetterStore` 只做登记与人工重放。需新增：死信产生时经配置渠道发送**脱敏**通知（失败静默降级但必须写审计）、对应 `app/settings.py` 配置项、以及测试。
  2. `docs/private-deployment-runbook.md` **未覆盖 Celery Worker / Outbox / 死信环节**，项 2 缺执行依据，需补手册章节。
- **验收步骤**：
  1. 启动 Worker：`celery -A app.worker:celery_app worker --loglevel=INFO`（生产模式自动绑定 Outbox 发布器）。
  2. 创建任务 → 验证 Outbox 写入 → Worker 发布 → Redis Stream 消费 → **重复投递不重复生效**。
  3. 制造失败：把外部适配器指向不可达地址，使事件重试至 `WORKBENCH_OUTBOX_MAX_ATTEMPTS` 后进入死信。
  4. 验证死信可查（`GET /api/v1/dead-letters`）、可人工重放（`POST /api/v1/dead-letters/{event_id}/replay`），且**通知渠道收到脱敏通知**。
  5. 短时批量写入观察消息堆积与回落。
- **通过判据**：幂等消费者不重复生效；实际重试次数与配置一致；死信可查可重放；通知到达且不含密钥/PII；积压能回落；无未解释失败。
- **证据清单**：Worker 日志片段（脱敏）；Stream 长度记录；死信记录；通知渠道截图；重放结果。
- **失败与回滚**：关闭外部 Runtime 注册项回退 Mock；禁止直接重放外部副作用。

### 项 3 · 设备绑定、生产密钥轮换和真实统一登录验收

- **目标**：证明设备绑定、密钥轮换、统一登录三件事在生产环境成立。
- **需先补的代码（阻塞项，且是 9 项里最大的代码缺口）**：
  1. **设备绑定未实现** —— 门禁清单 B 节明确标 ❌（当前只有 `RuntimeContext.device_id` 这种运行时字段，没有面向账号的设备绑定与异地登录拦截）。
  2. **统一登录 / SSO 未实现** —— 当前只有自建「手机号 + 口令 + TOTP」（`docs/delivery-readiness-checklist.md` 已如实登记「SSO 与设备绑定未做」）。
- **需要的输入**：部署密钥系统（可注入并轮换双密钥）；真实 IdP 的 client id/secret 与回调域名（若做 SSO）；真实验证器设备。
- **验收步骤**（三小项分别判定）：
  - **3.1 设备绑定**：绑定一台设备 → 换设备/异地登录应按设计被拒或要求二次验证；替换设备走 `POST /api/v1/auth/accounts/{account_id}/totp-reset` 后重登。
  - **3.2 密钥轮换**：用部署密钥系统轮换 `WORKBENCH_AUTH_SECRET`（与备份加密密钥保持分离且不同），验证轮换后旧令牌按既定策略失效、重登可获新令牌、且轮换期间服务可用性符合维护窗口约定；**轮换后必须重跑预检**。
  - **3.3 统一登录**：与 IdP 联调首次登录绑定、失败回退到口令登录、越权拒绝。
- **通过判据**：轮换后旧令牌按预期失效、新令牌可用、预检仍 `pass`；异地登录按设计被拒；SSO 首次绑定与失败回退均符合设计。
- **证据清单**：轮换操作记录与预检原文；令牌失效的请求/响应（脱敏）；IdP 侧截图；验证器真机截图。
- **失败与回滚**：轮换失败时按 2.4 用密钥系统回退到上一版本密钥并重跑预检。

### 项 4 · 真实 PostgreSQL 商业化迁移、备份/恢复演练与客户管理员验收

- **目标**：在客户 staging 上证明商业化 G0（租户、用量、生命周期）可迁移、可备份恢复，且客户管理员能独立完成本租户操作。
- **需要的输入**：客户 staging PostgreSQL 与凭据；备份介质；客户管理员账号与联系人；维护窗口。
- **验收步骤**：
  1. 用 `py scripts/commercial_g0_preflight.py` 复核（期望 `pass`）。
  2. 按 `docs/staging-acceptance-checklist.md` 执行顺序 4–8：租户读取、客户管理员权限、创建任务与写入用量账本（含幂等重复写入）、申请导出与删除（冷静期）、**把备份恢复到隔离库验证记录可读**。
  3. 客户管理员验收：查租户与用量、申请导出、申请删除后撤销或等冷静期；按 `docs/private-deployment-runbook.md`「客户交接」核对交付清单。
- **通过判据**：租户/用量/生命周期记录可读；导出**只含授权范围**且不含密码/Cookie/令牌/原始密钥/客户原文；删除走冷静期且保留最小审计；恢复后记录一致；客户管理员可独立完成上述动作。
- **证据清单**：预检原文；导出文件的**元数据**（不含原文）；生命周期作业记录；恢复演练记录；客户管理员验收签认。
- **失败与回滚**：见 2.4；**严禁手工改用量账本或审计记录**。

### 项 5 · 私有部署生产预检、容量压测、独立密钥轮换与试点客户交付

- **目标**：在试点客户环境完成预检、容量验证、密钥轮换与正式交付。
- **需要的输入**：试点客户环境（独立 PG/Redis/对象存储命名空间）；**容量与延迟阈值（书面确认）**；部署密钥系统；支持联系人。
- **验收步骤**：
  1. `py scripts/commercial_g0_preflight.py` → `pass`。
  2. 容量压测：复用项 1 探针的扩容版（并发用户 / 任务量 / 用量写入），记录 P95 延迟、错误率与资源水位。
  3. 独立密钥轮换（同 3.2 流程），轮换后重跑预检与冒烟。
  4. 按 `docs/private-deployment-runbook.md` 完成交付与客户交接，逐项标注**尚未通过真实验收的能力**（第三方发布渠道、自动化浏览器协助、外部 Runtime、GEO 适配器）。
- **通过判据**：预检 `pass`；压测在**客户确认的**容量下无未解释 5xx 且延迟达标；轮换不影响交付约定；交付清单齐备且未验收项已如实标注。
- **失败与回滚**：压测不达标即暂停交付，回到容量评估；轮换异常按 2.4 回退密钥。
- **注意**：容量/延迟阈值目前**没有基线**，验收前必须与客户书面确认，否则该判据记为「不可判定」。

### 项 6 · GEO 版本化适配器

- **局面**：**不能仅靠本仓库落地**。GEO 是独立 Git 仓库的外部系统，本仓库内 `app/`、`tests/` 对 `GEO` **零命中**，仅有 `README.md` 的边界原则与 `docs/architecture.md` 的一个组件框。
- **需要的输入（必须先拿到）**：
  1. GEO 的 API 契约：端点、认证方式、数据模型、错误语义、幂等键；
  2. **固定版本号**与版本兼容/升级规则；
  3. 明确的读写边界（只读或有界调用，`README.md` 已定「禁止跨库写入」）；
  4. 端点与密钥注入方式（如 `GEO_ENDPOINT` / `GEO_API_KEY`，由部署环境注入）。
- **需先补的代码（拿到契约后）**：适配器本体；**固定版本校验**（可复用 `app/runtime/registry.py` 的 `_validate_version` 范式，拒绝 `latest`/`main`/`head`）；`architecture.md` 提到的关联字段落地（任务号 / 项目号 / 产物版本 / 证据引用）；契约测试（`FakeTransport`）+ staging 元数据预检扩展。
- **验收步骤**：契约测试（MockTransport）→ 真实联调（只读边界冒烟 + 错误语义核对）→ 跨租户拒绝验证。
- **通过判据**：版本为固定值；跨租户调用被拒；**不跨库写入**；错误语义与契约一致。
- **未拿到契约前**：**不做占位实现**（本计划维持「外部依赖阻塞」，不臆造接口形状）。

### 项 7 · 真实 staging 与真实平台账号验收

- **目标**：证明真实平台账号可被工作台安全使用（凭据注入、账号可达、权限最小），**不含发布动作**（发布在项 9）。
- **需要的输入**：staging 独立主机与凭据（依赖项 1）；真实平台账号与发布授权；回执核对人。
- **验收步骤**：登录并注入平台凭据（仅由部署环境注入）→ 验证账号可达与权限范围最小 → 做一次**不产生外部副作用**的只读调用 → 核对失败回执路径 → 检查日志与事件中**不含凭据**。
- **通过判据**：账号可达且权限最小；失败留可核对回执；日志/事件/配置中无凭据；无凭据落盘落库。
- **证据清单**：脱敏请求摘要；平台侧截图；审计记录；凭据泄漏排查结果（grep 关键字 + 人工复核）。
- **失败与回滚**：任何凭据泄漏迹象立即暂停并轮换凭据。

### 项 8 · RAGFlow/AgentScope 密钥注入、跨租户实测、并发压测、沙箱验证和真实外部服务验收

- **目标**：证明外部知识层与受控执行器在真实环境下范围校验正确、失败可控、可人工接管。
- **需要的输入**：RAGFlow / AgentScope 各自的 **HTTPS 地址 + 固定版本 + 认证注入 + 隔离测试账号**；最小网络白名单。
- **验收步骤**：
  1. `py scripts/runtime_staging_preflight.py` → `pass`（只验证元数据，不发起网络）。
  2. 本地冒烟：`py -m app.runtime.staging`，期望 `status: pass`（只证明适配边界与脱敏规则）。
  3. 真实接入后按 `docs/superpowers/poc-staging-runbook.md` 的 ⑥⑦⑧ 执行：RAGFlow 两租户知识库白名单、引用字段、空范围拒绝、**跨租户结果整批拒绝**；AgentScope `/health`、事件游标、暂停/恢复/取消、审批、usage、replay，并注入未知事件/超时/取消确认统一失败、无自动越权、可人工接管。
  4. 并发压测与沙箱验证（依赖项 1 的探针）。
- **通过判据**：跨租户结果**整批拒绝**；空范围拒绝；非 2xx / 超时 / 缺运行号 / 事件格式错误一律判失败；无自动越权；证据不含 `Authorization`/API Key/Cookie/客户原文。
- **证据清单**：预检原文；冒烟输出；请求摘要与脱敏响应；指标；失败回放；人工接管记录。
- **失败与回滚**：关闭对应 Runtime 注册项回退 Mock，保留事件摘要与策略版本，**禁止直接重放外部副作用**。

### 项 9 · 真实模型、网页抓取和公众号自动发布验收

- **需先补的代码（阻塞项）**：**网页抓取已实现（可测部分）；公众号自动发布仍未实现** —— 内容工作台 Alpha 明确「不自动发布」（`README.md`）。真实模型接入代码**已具备**（`OpenAICompatibleContentGenerator` + `WORKBENCH_CONTENT_GENERATION_BACKEND`），只缺真实密钥联调。
  ① **抓取器（含 robots 遵守、域名白名单、限速、合规边界）：已实现** —— `app/content/scraper.py` 与 `POST /api/v1/content-sources/scrape`，离线可测：白名单为空即关闭（`503`，不放行）、非白名单域名/robots 不允许或不可用（fail-closed）一律 `403`、非 2xx/正文为空/类型不符 `502`，成功与拒绝均写审计 `content.source.scraped`。**真实抓取仍属未验收**：需外部提供抓取白名单与合规确认后方可进入验收。
  需要新增：② 发布器（幂等、回执核对、失败进人工接管、**绝不自动重发**）；③ 内容安全评估用例（提示词注入 / 数据泄露 / 越权输出）。
- **需要的输入**：真实模型 `base_url` / `model` / `key`；目标平台开发者凭据与发布授权；抓取白名单与合规确认；回执核对人。
- **验收步骤**：内容安全评估（注入/泄露用例全过）→ 真实模型生成质量与合规抽检 → 抓取仅限白名单且留痕 → 发布一次并核对回执 → 注入失败确认进人工接管且不重发。
- **通过判据**：内容安全用例全过；抓取不越白名单且留痕；发布有可核对回执；失败不自动重发；无凭据泄漏。
- **证据清单**：内容安全评估结论；抓取白名单配置与日志；发布回执与核对记录；人工接管记录。

---

## 4. 依赖关系与推荐执行顺序

```text
[外部输入就绪] → py scripts/staging_preflight.py = pass   ← 总闸门
                         │
        ┌────────────────┼─────────────────┬───────────────┐
        ▼                ▼                 ▼               ▼
      项 1            项 2              项 6           项 9 的代码补齐
   （PG/迁移/压测）  （Worker/死信）   （需 GEO 契约）  （抓取器 + 发布器）
        │                │                 │               │
   ┌────┼────┐           │                 └──── 契约到位后再做适配器
   ▼    ▼    ▼           ▼
  项 4  项 8  项 7      项 5（还依赖项 4）
        ▲
   项 3 需先补「设备绑定 + SSO」代码，之后才可验收
```

**推荐顺序**：① 先修复第 6 节的阻塞缺陷（不需外部资源）→ ② 项 1 → ③ 项 2 → ④ 项 4 → ⑤ 项 8 → ⑥ 项 7 → ⑦ 项 5 → ⑧ 并行推进项 3 / 项 6 / 项 9 的**代码补齐**，代码就绪后再各自验收。

---

## 5. 需外部方一次性提供的输入清单（已去重）

| # | 输入 | 用于哪几项 |
| --- | --- | --- |
| 1 | 独立 staging 主机与 PostgreSQL / Redis / 对象存储实例 + 凭据（**非 localhost**） | 1, 2, 4, 5, 7, 8 |
| 2 | 部署密钥系统：注入 ≥32 位且互不相同的 `WORKBENCH_AUTH_SECRET` 与 `WORKBENCH_BACKUP_ENCRYPTION_KEY`，并支持轮换 | 3, 5 |
| 3 | 备份介质 + 独立备份加密密钥 + 维护窗口 | 1, 4, 5 |
| 4 | 死信通知渠道（IM webhook / SMTP）与收件人 | 2 |
| 5 | RAGFlow / AgentScope：HTTPS 地址、固定版本、认证注入、隔离测试账号、网络白名单 | 8 |
| 6 | 真实平台账号（公众号）与发布授权、回执核对人 | 7, 9 |
| 7 | 真实模型 `base_url` / `model` / `key` 与内容安全评估口径 | 9 |
| 8 | 客户管理员账号与试点客户环境 | 4, 5 |
| 9 | **GEO 契约文档**（端点、认证、数据模型、错误语义、版本规则、读写边界） | 6 |
| 10 | 真实 IdP 的 client id/secret 与回调域名；验证器真机 | 3 |
| 11 | **容量与延迟的验收阈值（书面确认）** | 1, 5, 8 |

> 11 项里第 1、2、9、11 项是硬前置：缺任一项，总闸门或对应项无法判定。

---

## 6. 本计划编制时发现的阻塞性缺陷

以下 4 条是**代码/文档层面**的缺陷，不需要外部资源即可修复，但不修复会导致验收无法进行或验收结论不可信：

1. **`.env.staging.example` 迁移清单过期（已修复）**：`WORKBENCH_APPLIED_MIGRATIONS` 只登记到 `012_account_totp`，而 `migrations/` 实际已有 `001`–`014`。按该模板执行时 `staging_preflight.py` / `commercial_g0_preflight.py` 的「迁移状态」会判 `blocked`，**总闸门永远无法 `pass`**。已补 `013_run_records`、`014_orchestration_proposals`，并新增 `tests/staging_assets` 静态契约测试防止再次漂移。
2. **私有部署手册未覆盖异步链路**：`docs/private-deployment-runbook.md` 全文未提及 Worker / Outbox / 死信，项 2 缺少执行依据，验收前需补手册章节。
3. **无任何通知能力**：项 2 的「死信通知渠道」是**代码缺口**而非配置项（`app/` 内相关关键字 0 命中）。
4. **设备绑定与统一登录未实现**：项 3 在补齐这两块代码前**无法进入验收**。

---

## 附录 A · 客户端发布链（**不计入**上述 9 项门禁）

客户端的门禁项已勾选，但以下两步同样是外部依赖，需与正式发布一起做：

- **桌面端**：固定依赖版本（当前 devDependencies 用 `latest` 占位）→ 代码签名与可信时间戳 → `npm run dist` 产出 NSIS 安装包 → **干净 Windows 机器**跑「安装 → 打开 → 更新」全流程（宪法 2.5）→ 观察 SmartScreen 信誉积累。详见 `desktop/README.md`。
- **PWA 伴侣端**：真机（iOS / Android）安装性验证（当前图标为 SVG，部分平台要求位图）；若要真推送，需引入 HTTPS 域名与 VAPID 密钥。详见 `companion-pwa/README.md`。
- **共同前置**：跨源部署需后端 CORS 放行 `Authorization` 头（当前仅 `development` 注册 CORS 且未含该头）。

## 附录 B · 预检脚本速查

```powershell
# 总闸门（聚合：基础设施隔离 + 商业化 G0 + 外部 Runtime 元数据）
py scripts/staging_preflight.py

# 分组复核
py scripts/commercial_g0_preflight.py
py scripts/runtime_staging_preflight.py

# 演示 fail-closed（传空配置，全部走失败分支）
py scripts/staging_preflight.py --example

# 外部 Runtime 本地冒烟（只证明适配边界与脱敏，不代表外部服务可用）
py -m app.runtime.staging

# Worker（生产模式自动绑定 Outbox 发布器）
celery -A app.worker:celery_app worker --loglevel=INFO
```

> 预检脚本**只验证部署元数据**：不发起网络请求、不读取也不打印密钥值、退出码为 `pass`→0 / 其余→1。**不得**把预检 `pass` 当作真实验收证据。
