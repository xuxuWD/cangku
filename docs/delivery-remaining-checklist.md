# 交付剩余清单（可勾选 · 需外部输入）

> **本文件的作用**：只说「**还差什么、谁提供输入、用什么命令验证、判据是什么**」。
> **状态真相仍在** [`docs/delivery-readiness-checklist.md`](delivery-readiness-checklist.md)（现状与 ✅/❌），本文件是它的**执行层**，编号与它的「阻塞项 1–8」一一对应，避免两处各说一套。
> **口径**：以下所有事项**代码无法代替**；代码侧缺口已归零，唯一例外是 GEO 适配器（需对端契约，见组 5）。
> **红线**：写操作（迁移演练、并发压测、密钥轮换、启动应用服务）**必须单独授权**并在专用账号下执行；只读部分用 [`docs/readonly-verification-runbook.md`](readonly-verification-runbook.md)。
> **本机已验证（2026-09-12，一次性本机环境，未触碰 staging）**：1.7 跨租户探测、1.8 并发压测三场景、2.2 密钥轮换两阶段 —— 命令与判据均按实测结论写成，可直接照抄。**2026-09-16 追加（本机 Docker，独立 project + 独立端口，演练后已 `down -v` 清理）**：容器编排三服务（app / worker / beat）起栈、镜像版本标签、容器日志上限与**容量测算**取证见 [`docs/private-deployment-runbook.md`](private-deployment-runbook.md)「容量与并发」「监控与告警」；组 1 的 1.9 因此获得**本机级**证据（见该项注记），**但告警渠道与 staging 侧仍未验收**，勾选框一律不动。

---

## 组 1 · 真实环境验收（阻塞项 1）—— 解锁项最多，建议先做

**需要谁提供**：基础设施方 / 运维 —— 一台独立主机（Linux）+ **PostgreSQL（必须预装 pgvector）** + Redis + 对象存储实例 + 全部凭据；一个可登录的租户与超管账号。

**输入索取表（2026-09-16，可直接转发给基础设施方 / 运维）**：[`docs/infra-input-request.md`](infra-input-request.md) —— 14 项逐项要求与验证方式（I1 主机 / I2 PG 客户端 / I3 PG 实例 / I4 pgvector / I5 只读账号 / I6 迁移清单 / I7 Redis / I8 对象存储 / I9 双密钥注入 / I10 超管账号 / I11 备份介质与窗口 / I12 探针账号 / I13 死信渠道 / I14 变量对照单）+ 回执模板 + 安全红线。**未回执前组 1 的判据不做（只读命令待值到位后执行）**；预检 pass 路径与 runbook 漂移核对已于 2026-09-16 完成（见下方 1.3 / 1.4 / 1.4.1 / 1.5 子项）；**1.6–1.12 的「命令 / 参数 / 前置条件」逐项可执行性核对亦已完成（2026-09-16，见下方各子项）**——**1.12 的复测清单已于 2026-09-16 落文档（立项规格 + 复测手册，见下方 1.12 子项）**，各项均「值一到即可执行」；**写操作命令本身一条未跑**（runbook §5 未动）。

**转交与催办材料（2026-09-16 备）**：`docs/infra-input-request.md` §7 新增「转交说明与催办附言（发送人自用）」——含可复制的催办附言模板（回传渠道 / 期望回执时间留空待填）、发出人自查三条红线，以及「先回 I1–I6 + I10 即可先解锁只读核验面」的分批口径；**转交状态＝已发出（2026-09-16 · 飞书）：首发 19:39 存档至本人私聊〔消息 ID `om_x100b6593b329aca0b2a22aba839549c`，客户端不可见、用户反馈未收到〕→ 21:58 经应用机器人通道补发至用户可见会话「公司工作台」〔说明条 `om_x100b659e47fc58a4b039acd50b34684` 21:58:43、材料条+附件 `om_x100b659e478f88a8b48044c3688a5c3` 21:58:44，均已回读校验〕→ 用户确认已转发运维（**转交渠道＝微信（文件转发）**〔2026-09-16 登记〕；转发在体系外、无收件 / 回执证据）→ **催办附言定稿版已发**（回传渠道＝徐君〔飞书〕；2026-09-16 用户确认）；运维回执未返回、I1–I14 待提供**（回执到达后按索取表 §6 流程登记）。

**单机 staging 建置方案（2026-09-16 备 · 待执行 · 用户拍板方案 A）**：[`docs/staging-singlebox-build-plan.md`](staging-singlebox-build-plan.md) —— 用**闲置台式机**（Win10 22H2 · i5-14400 / 16 GB / 512 GB · Docker Desktop/WSL2）自建单机 staging 的 step-by-step（Step 0 机器自查 → Step 8 备份演练，每步带自查命令与期望输出；写操作三授权点已标注），配套新增端口覆盖编排 [`docker-compose.staging.yml`](../docker-compose.staging.yml)（只把 5432/6379/9000/8000 **追加**绑到该机局域网 IP，9001 控制台不外扩；基础编排零改动）、embedding 编排 [`docker-compose.embedding.yml`](../docker-compose.embedding.yml)（**E1 定案 A**〔2026-09-16〕：TEI CPU 承载 Qwen3-Embedding-0.6B，镜像钉 digest、模型只读挂载、仅宿主回环）与守护测试 `tests/test_staging_singlebox_assets.py`（**12 条，全绿**）。**状态＝未执行**：台式机未动、命令一行未跑；覆盖文件、embedding 编排与方案命令**均未经真机验证**（方案 §12 D3/D6 已登记待实测项）。**阻塞项**：E1（embedding 服务落点）**已定案（2026-09-16，方案 A 台式机自包含）**、该项阻塞已解除；I11（备份介质与第三把密钥）/ U6（手机号分配）/ 外部 Runtime 就绪实况待拍板。**口径**：1.3/1.4 预检的 Runtime 段在外部 Runtime 接入前**不可能诚实全绿**（方案 §12 D4 如实登记，**禁止为凑 pass 填假值**）。**勾选框一律未动**。

> **两个已验证的实操前置（2026-09-12 本机实测，避免到 staging 白跑）**
> ① **部署机必须安装 PostgreSQL 客户端**（`pg_dump` / `pg_restore`）：缺客户端时 `scripts/migration_backup_drill.py --phase backup` 会在「pg_dump 可用性」这一步直接 `fail`（本机即如此）。
> ② `migration_backup_drill.py` 的**任何阶段（含 `--phase list`）都要求先设 `WORKBENCH_DATABASE_URL`**——安全护栏先于动作执行，缺它会以 `exit=2`「必须配置 WORKBENCH_DATABASE_URL」拒绝；本地演练另需显式 `--allow-local`（实测：本地地址不加该开关会被拒，`exit=2`）。另外 `WORKBENCH_APPLIED_MIGRATIONS` **必须从目标库读取**，否则 `--phase list` 会报「迁移清单不一致（缺少 22 项）」。

- [ ] 1.1 目标 PostgreSQL 已装 `pgvector`
      验证：`psql "$DSN" -c "SELECT extname, extversion FROM pg_extension WHERE extname='vector'"`
      判据：**有一行返回**（官方 `postgres` 镜像不带该扩展，迁移 001 会直接失败）
- [ ] 1.1.1 只读账号已建（**怎么建：见 [`readonly-verification-runbook.md`](readonly-verification-runbook.md) §1.1**，含已在真实 PG 上验证的 `CREATE ROLE` + `pg_read_all_data` 语句，该角色可自动覆盖未来迁移新建的表）
- [ ] 1.2 填好生产/预发配置（**不进仓库**）：`WORKBENCH_ENV≠development`、`WORKBENCH_STORAGE_BACKEND=postgres`、`WORKBENCH_DATABASE_URL`、`WORKBENCH_AUTH_SECRET`（≥32）、`WORKBENCH_BACKUP_ENCRYPTION_KEY`（与前者分离）、从目标库读出的 `WORKBENCH_APPLIED_MIGRATIONS`
- [ ] 1.3 `python scripts/staging_preflight.py` → `pass`
      本期实测（未配置环境）会输出 32 条 `fail`（另有 3 条 `blocked`），可当作「你需要准备哪些配置」的清单
      - **2026-09-16 本机只读预演「pass 路径」（仍不勾选）**：以一套**合成非本地值**（变量集与 `.env.staging.example` 同口径；该模板的迁移清单已同步为仓内 34 项）实跑 ⇒ `Staging 前置预检结果：pass`、**36 项 `[pass]`、`exit=0`**。三个预检脚本均为**纯元数据校验、零网络请求**（`staging_preflight.py:3-5` 自述；`worker_preflight.py` 的 `--offline` 分支 `:317-318`）⇒ 未配置环境的 `fail` 只是缺值、不是脚本能力问题。**判据仍未达成：staging 上一项未执行**。
- [ ] 1.4 `python scripts/runtime_staging_preflight.py` → `pass`（未配置环境实测输出 20 条 `fail`；五类 Runtime 各 3 条 + 环境/标识/网络白名单）
      - **2026-09-16 本机只读预演「pass 路径」（仍不勾选）**：同组合成值实跑 ⇒ `外部 Runtime staging 预检结果：pass`、**20 项 `[pass]`、`exit=0`**。**判据仍未达成：staging 上一项未执行**。
- [ ] 1.4.1 `python scripts/worker_preflight.py --offline` → `pass`（本机实测输出 7 条，含「迁移清单不一致」；联网校验另需 `--base-url` 与 `--token`）
      - **2026-09-16 本机只读预演「pass 路径」（仍不勾选）**：同组合成值实跑 ⇒ `Worker 运行态前置预检结果：pass`、**6 项 `[pass]` + 1 项 `[skipped]`（`联网运行态校验`按 `--offline` 跳过）、`exit=0`**；迁移清单按仓内 34 项比对一致。**判据仍未达成：staging 上一项未执行**。
- [ ] 1.5 **只读核验清单全绿**（`docs/readonly-verification-runbook.md`）：pgvector 存在、迁移 022 已落地且复合外键存在、**归一风险 0 行**、**绑定侧未纳管为空**（`candidates.roles == []` 且 roster 绑定侧为空）
      - **2026-09-16 只读漂移核对（仍不勾选）**：runbook 命令与当前代码**逐面无漂移**——端点（`app/main.py`：`/api/v1/auth/sessions` `:3572`、`/auth/registrations` `:3412`（`bootstrap_token` 为请求体字段 `:3327`）、`/auth/me/totp` `:3721` 与 `/confirmation` `:3733`、`/workforce/roster` `:1072`、`/workforce/candidates` `:1343`、`/dead-letters` `:1021`、`/api/v1/health` `:616`、§1.3 默认探针 `/api/v1/approvals/pending` `:3504`）；SQL 面（`workbench_tasks`＝`migrations/001_initial.sql:3`、`binding_type`/`binding_key`＝`004:3-4`、目录两表与复合外键 `(tenant_id, role_key)`＝`022:9/24/35`、`workbench_schema_migrations` 由 `app/migrations.py:30` 创建＝§1.2 口径成立、`001:1` 含 `CREATE EXTENSION vector`＝1.1 判据前提）；§4 五个只读脚本全部存在、跨租户 6 类 KIND（`cross_tenant_probe.py:64-69`，缺 `--resource` `exit=2` `:336-338`）、并发三场景（`staging_concurrency_probe.py:49`）、`migration_backup_drill.py:452-462` 与 `secret_rotation_drill.py:336-347` 参数均与文档一致 ⇒ **命令一到 staging 即可执行**。**判据仍未达成：staging 上一项未执行**。
      - **2026-09-16 本机 Docker 全链路只读预演（仍不勾选）**：在**一次性本机 Docker 环境**（`pgvector/pgvector:pg16`〔PG 16.10 / vector **0.8.0**〕+ Redis 容器 + `uvicorn` 真实 HTTP 服务；**34 项迁移全部应用**、3 租户与最小种子、跑完即销毁）上，把 runbook §1–§4 命令**逐条实跑**并留档 `exit` 码与关键输出：① **§1.1 / §1.2**：`CREATE ROLE` + `pg_read_all_data` 建只读账号 ⇒ 属性 `workbench_ro|f|f|t`；从 `workbench_schema_migrations` 读出 **34 项（001–034）** 拼接清单，与仓内比对一致（`worker_preflight` 判「迁移清单一致（34 项）」）；② **§2 A–H 与 §7 样例逐项一致**（A 有行〔版本号差异见下〕；B 两表 + 复合外键 `FOREIGN KEY (tenant_id, role_key) REFERENCES workbench_job_roles(tenant_id, role_key)`；C **1 行** `role|Content-Operator|content-operator`；D `agent_bindings 2 / digital_employees 2 / job_roles 2 / role_bindings 3 / tasks 1`〔D 先于 §4 造数取得〕；E `Content-Operator, legacy-role`；F 空；G `role|geo-operator`；H `t-1|geo-analyst|geo-operator|disabled`）+ 只读包装**负例**（同会话 `CREATE TABLE` ⇒ `ERROR: cannot execute CREATE TABLE in a read-only transaction`、`exit=1`；`SELECT` `exit=0`）+ **未来表覆盖复验**（管理员建表 ⇒ 只读账号**免授权**可 `SELECT` 到行 ⇒ 删表）；③ **§1.3 令牌三情形**：引导注册 **201**（`super_admin`/`approved`/`reviewed_by=bootstrap`）→ 边界（已有超管后错口令 + 缺 `tenant_id`）**201 `pending`** → 无码登录 **200 `scope=totp_enrollment`/`expires_in=300`** → 受限令牌访问 roster **403**「账号需要先完成动态口令绑定」→ `POST /auth/me/totp` **200**（SHA1 / 6 位 / 30 秒）→ 确认绑定 **200 `{"status":"confirmed"}`** → 已消费码登录 **401** → 跨步长新码登录 **200 `scope=full`/`expires_in=900`** → 已绑不带码 **401**「需要动态验证码」；普通注册 **201 `pending`** → 超管审批（`department_lead`/`t-b`）**200** → B 登录 **200 `scope=full`**；④ **§3**：`candidates` **200 `{"roles":[],"agents":[]}`**、`roster` **200 `total=0`**、非超管 **403**「只有超级管理员可以查看岗位与数字员工清单」、无令牌 **401**「缺少登录身份信息」；⑤ **§4**：五个预检脚本实跑（`staging_preflight` 6 `[pass]`/28 `[fail]`/2 `[blocked]`；`runtime_staging_preflight` 0/20/0；`commercial_g0` 6/1/2 且「迁移状态一致」`[pass]`；`worker_preflight --offline` 4/2/1 且「迁移清单一致（34 项）」`[pass]`；`sso_preflight --offline` `[skipped]` **`exit=0`**——`fail` 项均为**本机开发态缺 staging 配置**的 fail-closed，非脚本能力问题）+ `cross_tenant_probe`（两个租户各一真实 task ⇒ **`exit=0`**、报告 `A→A=200 B→B=200 A→B=404 B→A=404`；漏 `--resource` ⇒ **`exit=2`**）；⑥ **审计佐证（只读）**：`workbench_audit_log` 有 `account.registration.requested×3` / `approved×2`（引导两条即 `requested`+`approved` 且 `detail={"bootstrap": true}`）、`login.succeeded×3` / `failed×3`、`totp.enrolled` / `confirmed` / `enrollment_required` 各 1，手机号一律脱敏。**差异与新发现**：A2 的 `extversion` 为 **0.8.0**（§7 样例 0.8.6 ⇒ 镜像自带版本差异，判据「有行」成立）；**同一步长内重复使用动态码（含确认绑定已消费的那一个）会被计为登录失败**（默认 `5` 次 / `300` 秒窗口 ⇒ 锁定 `900` 秒），按 runbook「等验证器翻到下一个码（≤30 秒）」执行即可（本机踩到并按此通过）。**判据仍未达成：staging 上一项未执行**。
- [ ] 1.6 **迁移回滚演练**（写操作，需授权）：`scripts/migration_backup_drill.py`
      判据：能按备份恢复到指定版本，且 `WORKBENCH_APPLIED_MIGRATIONS` 与库内一致
      - **2026-09-16 只读可执行性核对（仍不勾选）**：四阶段（`list` / `backup` / `restore` / `verify`，`scripts/migration_backup_drill.py:389`）与参数（`:452-462`）和判据口径一致；**默认 dry-run，`--execute` 才真跑**。前置可枚举：`WORKBENCH_DATABASE_URL`（须 `postgresql+psycopg://`，缺 `exit=2` `:103`）、`pg_dump` / `pg_restore`（I2；缺即 `fail` `:196-205` / `:277-287`）、`WORKBENCH_BACKUP_ENCRYPTION_KEY` ≥32（I11 / I9；缺即 `fail` `:211-223`）、隔离恢复库 `--restore-dsn`（缺则 `skipped` `:258-267`）、`WORKBENCH_APPLIED_MIGRATIONS` 自目标库读（I6）。**口径差异登记**：脚本无「回退到指定历史版本」参数，其恢复口径＝恢复到隔离库 → `apply_migrations` 前滚至最新 → 清单逐一比对（`:310-317`）；回退能力旁证为 10.12 本机一次性容器 027 回退（run `35080905772`），staging 同款需另行授权。**判据仍未达成：需 I1 / I2 / I3 / I6 / I9 / I11 到位 + 授权**。
- [ ] 1.7 **跨租户只读探测**（需两个不同租户的令牌 + **每个 KIND 在两租户各一个真实资源 ID**）：
      `python scripts/cross_tenant_probe.py --base-url … --token-a … --token-b … --resource "task:<A_ID>:<B_ID>"`（**漏掉 `--resource` 会 `exit=2`**）
      判据：`exit=0` 且报告 `A→A=200 B→B=200 A→B=404 B→A=404`（本机已实测通过）
      - **2026-09-16 只读可执行性核对（仍不勾选）**：脚本参数与 runbook 一致（`scripts/cross_tenant_probe.py:309-320`；缺 `--resource` ⇒ `exit=2` `:335-337`）；6 类 KIND ↔ 端点逐一对应（`:63-70` ↔ `app/main.py`：`task :3028` / `plan_proposal :3853` / `content_task :741` / `run_metrics :3128` / `orchestration_proposal :3995` / `commercial_lifecycle :1000`）。**对照组造数路径已可枚举**：task → `POST /api/v1/tasks`；content_task → `POST /api/v1/content-tasks`（`:679`）；plan_proposal → `POST /api/v1/tasks/{task_id}/plan-proposals`（`:3834`，需 `WORKBENCH_PLANNER_TOOLS` 非空）；run_metrics → 建 run（`:3103` / `:3905`）；orchestration_proposal → `POST /api/v1/orchestration-proposals`（`:3966`）；commercial_lifecycle → `POST /api/v1/commercial/exports` / `deletion-requests`（`:934` / `:976`）。**执行注意**：I10 只含「一个租户 + 超管」⇒ 第二租户须由超管走「注册 + 审批」自建（本机已验证路径，需额外一个手机号）；建任务角色限 `employee` / `department_lead` / `ceo` / `super_admin`（`customer_admin` `403`）。**判据仍未达成：需 I1 / I3 / I10 + 第二租户 + 逐 KIND 造数**（本机已实测 task 一种全绿）。
- [ ] 1.8 **并发压测**（写操作，需专用账号）：

      ```bash
      python scripts/staging_concurrency_probe.py --base-url "$BASE" \
        --token "$TOKEN" --phone "$PROBE_PHONE" --password "$PROBE_PASSWORD" \
        --scene login_throttle --scene task_idempotency --scene plan_approval \
        --proposal-id "$PROPOSAL_ID" --concurrency 8
      ```

      - `--base-url` / `--token` / `--phone` / `--password` 四个**必填**；**`--phone` 必须是专用探针账号**（登录场景会把它锁定一段时间），**绝不能用真人账号**。
      - **`login_throttle` 第一次跑必然 `fail`（`{401:8}`），这不是缺陷**：8 个并发请求在账号被锁定之前**同时通过了「未锁定」检查**，而锁定是在失败计数之后才生效。**紧接着再跑一次即 `pass`（`{429:8}`）**，报告还会自己推出「部署最大失败次数约为 5」（与默认 `WORKBENCH_LOGIN_MAX_FAILURES=5` 一致）——**不要因为这个 fail 去改代码或调配置**。
      - **`plan_approval` 的前置**：需要一个处于 `pending_review`、且**发起人 ≠ 令牌用户**的提案（自审会被拒）；另外**创建提案要求 `WORKBENCH_PLANNER_TOOLS` 非空**（默认空时建提案直接 `422`「未配置任何可用工具」，本机实测），所以要在 staging 上先备好真实提案，**不能临时现造**。
      判据（2026-09-12 本机实测三场景全部跑通）：`login_throttle` 第二次 `{429:8}`；`task_idempotency` `{200:7,201:1}`（同一幂等键只产生 1 条记录）；`plan_approval` `{200:1,409:7}`（并发审批保持原子）
      - **2026-09-16 只读可执行性核对（仍不勾选）**：参数与实现一致（`scripts/staging_concurrency_probe.py:421-436`；`--base-url` 强制 HTTPS 且非本地、越界 `exit=2` `:63-74`；`--concurrency` 限 1–64）；三场景端点逐一对应：`login_throttle` → `POST /api/v1/auth/sessions`（故意错口令、不带令牌 `:254-268`）、`task_idempotency` → `POST /api/v1/tasks`（带令牌 + 同一幂等键 `:271-295`）、`plan_approval` → `POST /api/v1/plan-proposals/{id}/approval`（`:297-318`）。**缺 `--proposal-id` 时 `plan_approval` 记 `skipped` 而非 `fail`（`:300-308`）——不计失败，但也拿不到该场景判据证据**。前置可枚举：I12 专用探针账号（口令不进报告）、`pending_review` 且发起人 ≠ 令牌用户的提案、`WORKBENCH_PLANNER_TOOLS` 非空（建提案前置）。**执行前核对项**：同一 `--token` 同时用于 `task_idempotency`（建任务）与 `plan_approval`（审批），探针账号角色须同时覆盖「建任务 + 审批提案」两种权限。**判据仍未达成：需 I12 + 真实提案 + 角色核对 + 授权**。
- [ ] 1.9 **审计落 PG 的实跑验证 + 日志采集告警**
      判据：库里能查到审计行；采集侧有告警规则（此前只有假连接静态断言）
      - **2026-09-16 第二批进展（仍不勾选）**：9 条规则从「成文」推进到「可执行」——只读探针 `scripts/monitoring_probe.py` 按 runbook 逐条同口径判定（信号不可用显式 `skipped`，不会静默算作通过），并在本机对真库实跑取证（Outbox 0 条 / 死信 0 条 / 磁盘 67.3% / PG 连接 10-100 等实测值；未接渠道时 `--notify` 不生效）；客户侧「渠道接入 + 触发一次真实告警」的核对清单与配套只读探针见 `docs/customer-side-acceptance-runbook.md` + `scripts/customer_acceptance_probe.py`。**仍缺**：渠道未接入、无真实触发证据。
      - **2026-09-16 只读可执行性核对（仍不勾选）**：三份资产齐备且与判据同口径——9 条告警规则表 ↔ `docs/private-deployment-runbook.md:65-75`（部署侧采集配置）；只读采样探针 `scripts/monitoring_probe.py`（参数 `:924-947`；信号不可用显式 `skipped`，不静默算通过）；客户侧「渠道接入 + 真实触发」清单 `docs/customer-side-acceptance-runbook.md`（§2.5 `:144-179` / §2.6 `:181-212` / §3 `:216-246`）+ 配套探针 `scripts/customer_acceptance_probe.py`（含 `_check_audit_log:521`；参数 `:659-683`）。**判据口径**：以**渠道侧收到一次真实告警**为准——只跑采样探针不算取证；「库里能查到审计行」须在真实 PG 上查（本机内存仓储不算）。**判据仍未达成：需 I5 / I7 到位 + I13 渠道接入 + 客户侧执行 + 授权**。
- [ ] 1.10 **Outbox / Celery Worker 实跑**：Redis + Worker 进程 + 死信通知渠道地址
      判据：事件经 Outbox 投递成功；失败进死信并能通知
      - **2026-09-16 只读可执行性核对（仍不勾选）**：资产齐备——异步链路与 worker/beat 命令见 `docs/private-deployment-runbook.md:43-54`（beat 必须独立进程 `:27`、启动命令 `:46-49`）；死信查询/回放端点 `GET /api/v1/dead-letters`（`app/main.py:1021`）；通知渠道配置项 `WORKBENCH_DEAD_LETTER_WEBHOOK_URL`（`app/settings.py:33`）与实现 `app/notifications.py` 均已存在。**差异登记**：`docs/external-dependency-acceptance-plan.md` 项 2（`:141-157`）仍写「死信通知渠道未实现 / runbook 未覆盖异步链路」两项待补代码——**现均已落地**，该文本滞后，本轮只登记不改动。前置可枚举：I7 Redis + Worker/beat 部署（判据前半「Outbox 投递成功」）；I13 渠道地址未到位时可暂缺，但判据后半「能通知」无法取证。**判据仍未达成：需 I7 + I13（可跳过）+ 部署 + 授权**。
- [ ] 1.11 **商业化迁移与备份/恢复演练**：真跑一次恢复（不是只看脚本）
      - **2026-09-16 只读可执行性核对（仍不勾选）**：执行资产与 1.6 同源（`scripts/migration_backup_drill.py` 四阶段；「真跑恢复」＝`--execute` + `--restore-dsn` 指向隔离恢复库后前滚比对清单，`:248-317`）；补 `scripts/commercial_g0_preflight.py` 复核商业化 G0 与迁移状态一致；步骤口径对齐 `docs/staging-acceptance-checklist.md` 执行顺序 4–8（`:29-33`：健康/租户读取/低风险任务持久化/用量账本/导出与删除冷静期/备份恢复到隔离库后记录可读）。前置同 1.6：I1 / I2（`pg_dump`、`pg_restore`）/ I3 / I6（清单自目标库读）/ I9 / I11（备份介质与窗口）+ 客户管理员（I10）走导出/删除链路。**判据仍未达成：需 I1 / I2 / I3 / I6 / I9 / I11 + 客户管理员 + 授权**。
- [ ] 1.12 **攻击面八类检查在真实环境复测**（本机报告 `docs/security-attack-surface-report.md` 已有，需真实环境结论）
      - **2026-09-16 只读可执行性核对（仍不勾选）**：**本项是 1.6–1.12 中唯一「无现成可执行资产」的判据**——仓内无对应复测脚本、也无成文的 staging 请求清单；报告 §5（`:223-234`）逐条列出未覆盖范围与真实环境要求，§6（`:236-240`）明确不代表生产验收，`:12` 已声明未在真实环境执行。**可复用映射**：第 1 类（水平越权）↔ `scripts/cross_tenant_probe.py`；第 8 类（资源滥用）中的登录限流并发部分 ↔ `scripts/staging_concurrency_probe.py`（`login_throttle` 场景）；第 7 类（信息泄露）中的脱敏/审计部分 ↔ `scripts/customer_acceptance_probe.py`。**其余类别（第 2–6 类）需把本机 `TestClient` 用例转写为 staging 请求清单，多数为写操作**。**判据仍未达成：复测清单已于 2026-09-16 落文档（见下子项）；执行仍待 I1–I14 到位 + 单独写授权**。
      - **2026-09-16 立项 + 复测清单落文档（仍不勾选）**：**V1 已产出两份文档**——立项规格 [`docs/superpowers/specs/2026-09-16-attack-surface-retest-design.md`](superpowers/specs/2026-09-16-attack-surface-retest-design.md)（**草案待评审**；分期 **V1 清单落文档＝本轮 / V2 staging 执行＝待 I1–I14 + 单独写授权 / V3 结论回写**；复测口径＝**严格重放八类不扩面**；不可造场景替代口径〔过期令牌→登出撤销后复用 / 生成器伪造 `kind`→观察式 / dev 态 `200`→不复测〕；**5 条待裁决点 A1–A5** 各附推荐）+ 复测手册 [`docs/attack-surface-retest-runbook.md`](attack-surface-retest-runbook.md)（八类逐项：复用资产 / staging 请求清单〔端点行号齐备〕/ 前置账号角色 / 判据 / 读写标记 / 证据模板；命名对齐 `readonly-verification-runbook.md`）。**判据仍未达成：清单已成文，执行待 I1–I14 到位 + 单独写授权**。
      - **2026-09-16 立项评审通过 + A4 指针落地（仍不勾选）**：用户审阅立项规格后**批准** ⇒ **A1–A5 逐条按推荐落定为裁决**（A1 采纳替代口径〔过期令牌→登出撤销后复用、生成器伪造 `kind`→观察式、dev 态 `200`→不复测〕/ A2 严格重放八类不扩面 / A3 造数落演练租户 + I12 专用账号 / A4 报告 §5 加指针 / A5 V2 先手工 / curl 不新增脚本）；立项规格状态更新为「**已评审**」并回填审批记录（§4 尾注）；**A4 已落地**——`docs/security-attack-surface-report.md` §5 尾注加「复测指针」（不改报告任何结论）。**判据仍未达成：执行待 I1–I14 到位 + 单独写授权**。

**解锁**：就绪清单中 12 条 ❌ 里的绝大多数，以及 65 条「代码完成但未验收」中的大部分。

---

## 组 2 · 生产密钥与轮换（阻塞项 2）

**需要谁提供**：部署密钥系统（Vault/KMS/云密钥服务）与轮换流程。

- [ ] 2.1 两把密钥由密钥系统注入（`WORKBENCH_AUTH_SECRET`、`WORKBENCH_BACKUP_ENCRYPTION_KEY`，≥32 位且互不相同），仓库内无明文
- [ ] 2.2 **轮换演练**（写操作，需授权）：`scripts/secret_rotation_drill.py`
      两阶段，**`before` 与 `after` 之间必须替换 `WORKBENCH_AUTH_SECRET` 并重启应用**（密钥只在启动期读取）：

      ```bash
      # ① 轮换前：登录取令牌，写入证据文件（含令牌，用完即删）
      python scripts/secret_rotation_drill.py --phase before --base-url "$BASE" \
        --phone "$PHONE" --password "$PASSWORD" --output /tmp/rot-token.json

      # ② 替换 WORKBENCH_AUTH_SECRET 并重启应用

      # ③ 轮换后：断言旧令牌失效 / 重登成功 / 新令牌可用
      python scripts/secret_rotation_drill.py --phase after --base-url "$BASE" \
        --phone "$PHONE" --password "$PASSWORD" --output /tmp/rot-token.json
      ```

      - **前置：应用必须使用持久化账号仓储**（`WORKBENCH_STORAGE_BACKEND=postgres`）。默认 `memory` 下账号只存在于进程内，重启即丢，会导致 `after` 阶段「重新登录」返回 `401 手机号或密码不正确`——这是**运行配置问题，不是轮换缺陷**（本机已踩过一次，见下）。staging 本就是 postgres，正常不会遇到。
      - 令牌是无状态 HMAC-SHA256（`auth_secret` 参与签名），因此**重启本身不会**让旧令牌失效；`after` 阶段的 401 只可能来自密钥变化。本机做了对照实验确认这一点。
      - 受保护探针默认 `GET /api/v1/approvals/pending`（任意已登录角色均 200），可用 `--probe-path` 覆盖；报告**只打印令牌指纹（SHA-256 前 8 位），绝不打印令牌原文**。
      判据（2026-09-12 本机 PG16 + `WORKBENCH_STORAGE_BACKEND=postgres` 实测）：
      - 对照（**同密钥**重启）：旧令牌 `200`、重新登录 `200` → 证明重启不影响会话
      - 轮换（S1→S2）后：旧令牌 `401`、重新登录成功（新指纹 `sha256:aa523ec5`）、新令牌 `200`，`exit=0` 全 `pass`
      - 落库旁证：`workbench_accounts` 中该账号 `scrypt$` 哈希与轮换前一致（改密钥不动口令哈希）

---

## 组 3 · 外部 Runtime 联调（阻塞项 3）

**需要谁提供**：RAGFlow / AgentScope 各自的 HTTPS 地址、**固定版本号**、认证注入方式、隔离测试账号。

- [ ] 3.1 capability 白名单已登记（模板由 `tests/test_staging_assets.py` 守护）
- [ ] 3.2 真实联调 + 沙箱验证
      判据：`GET /api/v1/runtimes/health` 返回 `ok`；知识检索返回**带引用的**片段；不直连工作台数据库、不决定审批结果

---

## 组 4 · 真实模型与发布验收（阻塞项 5）

**需要谁提供**：真实模型密钥；公众号平台账号与**发布授权**。

- [ ] 4.1 内容生成走真实模型（`content_model_*`）
- [ ] 4.2 网页抓取真实站点（域名白名单 / robots / 限速 / 体积上限）
- [ ] 4.3 公众号自动发布 + 回执核对（幂等键 `task:revision`、失败转人工接管、**绝不自动重发**）
      判据：每项留一条真实证据（回执 ID / 检索引用 / 抓取留痕），并把脱敏结论写回就绪清单

---

## 组 5 · GEO 版本化适配器（阻塞项 6）—— 唯一未闭合的代码缺口

**需要谁提供**：GEO 侧 API 契约（端点、认证、数据模型、错误语义）、固定版本号与兼容/升级规则、读写边界。索取表见 `docs/geo-contract-request.md`（**草案，尚未回执**）。

- [ ] 5.1 索取表全部项拿到回执（**没有回执不得开工**，也不做占位实现）
- [ ] 5.2 适配器 + 契约测试（`FakeTransport`）+ 版本固定校验（拒绝 `latest`/`main`/`head`）
- [ ] 5.3 staging 真实联调

---

## 组 6 · 桌面端签名 / 公证 / 干净电脑测试（阻塞项 7）

**需要谁提供**：Windows 代码签名证书（OV/EV）+ 可信时间戳服务 + 一台干净 Windows 机器。

- [ ] 6.1 固定依赖版本 → 构建安装包（NSIS）
- [ ] 6.2 代码签名 + 时间戳
- [ ] 6.3 干净机器跑「安装 → 打开 → 自动更新（旧版→新版）」全流程

---

## 组 7 · PWA 真机安装与推送（阻塞项 8）

**需要谁提供**：真机（iOS/Android）；若要真推送，还需 HTTPS 域名 + VAPID 密钥。

- [ ] 7.1 真机安装性验证（manifest、图标、离线壳；**不缓存 `/api/`**）
- [ ] 7.2 （可选）Web Push 真推送；不做则如实说明「提醒为轮询」

---

## 组 8 · 真实统一登录（IdP）与密钥轮换验收

**需要谁提供**：真实 IdP 的 `client_id` / `client_secret` / endpoints / 回调白名单。

- [ ] 8.1 SSO 真实联调（自建账号 + TOTP 二次验证已在开发期验证）
- [ ] 8.2 生产密钥轮换见组 2

---

## 组 9 · 明确不做（口径已变更，避免重复询问）

- [ ] **设备绑定**：**不做**。理由与变更记录见 `docs/delivery-gates.md`「门禁口径变更记录」（产品采用「注册申请 + 管理员审批」制）。
- 不做即视为该项关闭，不再列入剩余。

---

## 组 10 · 我方实现自查（**无需外部输入** · 2026-09-13 新增）

> **来源**：[OpenMausBot 源码研读报告](file:///d:/徐徐AI学习/公司工作台/docs/openmausbot-source-study-and-adaptation-plan.md) §5.2——从**对方暴露的缺口**反查「我们是否同类风险」。以下各条**此前均未逐条自查**，**不构成对我们现状的定性结论**（沿用"没验证的必须写未验证"）；**2026-09-16 已查的条目在各项下附「结论 + 证据（`文件:行`）」**（10.1 已查〔应用层无 DELETE/清理路径通过；数据库层 REVOKE 未做 ⇒ 差距，仍不勾选〕；10.2 通过；10.4 通过（键名归一 + 值形态扫描 + 掩码幂等）；10.5 已查〔用量账本 / `daily_budget_cents` / 评测费用 / 展示层全整数分，均通过；任务预算 `budget`（`NUMERIC(18,6)` 元 + 接口 float 链路）为差距，运行时「元→分」换算已最小加固并登记 ⇒ 仍不勾选〕；10.6 已查〔生产强制 `state_postgres` 成立（配置层 + 装配层双保险 + 部署口径 + 预检 + 守护测试）；唯一边界「`build_runtime_service` 缺省兜底不带 env 校验」已按 2026-09-16 拍板做 fail-closed 最小加固 + 先红后绿 + 反假两轮 ⇒ 勾选〕；10.8 已查〔8 类人工决议入口逐条核对：计划提案 / 运行内审批 / 编排优化提案 / 技能版本复核 / 对话入口（复用运行内审批）均成立；账号注册审批与知识文档复核语义 N/A（申请人无账号 / 系统到期触发）；未接线入口已登记；唯一缝隙「任务审批允许发起人自审」已按 2026-09-16 拍板收紧（403 + 列表剔除）+ 先红后绿 + 反假两轮 ⇒ 勾选〕；10.7 已查〔导出 / 删除路径均存在但覆盖不全（导出 15 类仅 `memories` 已接线；删除清场仅记忆层、`skills` / `knowledge_governance` 的 `delete_all_for_tenant` 已具备未接线；「记录确认人」未实现；保留策略无服务侧执行器；用户级个人导出 / 删除无）；本轮按 2026-09-16 拍板加固两项——新增导出包取回端点（admin-only、租户服务端解析、跨租户统一 404、过期 404）+ worker 周期任务按 `expires_at` 清理过期包（beat 间隔默认 3600），先红后绿 + 反假两轮 ⇒ 仍不勾选（差距如实登记，不改码）；**2026-09-19 B-1 补齐「包号发现 + 管理台导出闭环」**（见 10.7 条），**B-2a 接线 6 类真实数据**、**B-2b 再接线 3 类**、**B-2c 再接线 2 类（11/15）**（见 10.7 条），剩余差距面＝**4 类无实体类别** / 删除清场扩围 / 确认人 / 保留策略执行器〕；10.3 的「运行事件有界」「日志有界」两条已于 2026-09-16 闭合，但因「磁盘容量告警渠道未接入」仍不勾选；10.11 已查〔五层证据（构建机制 / 源码 grep / 本机构建产物实测扫描 / 仓库卫生 / 守护测试 42 项全绿）：构建产物与静态资源**无密钥、无内部地址、`VITE_*` 变量名零泄漏**；开发 fallback（`localhost:8000` / `super_admin`）非密钥非内部地址，且后端非 development 只信 Bearer（`app/main.py:548-554`）⇒ 勾选〕；10.9 已查〔**判据成立 ⇒ 勾选**：① 容器侧零外网出口五层证据——装配 fail-closed（`--network` 恒内网桥 + 生产构造不传 `network_name` + 起容器前校验 `Internal=True`，`internal=False` 拒绝执行）、工具面 ④-1 只读白名单 + ④-2 A4 网络黑名单双闸门、真容器实测零出网（DNS 不外泄 / `1.1.1.1:443` / `8.8.8.8:53` / 裸 UDP DNS / `169.254.169.254` 全 `Network is unreachable`）、真容器 + 真实供应商双轮对照（`supplierDomain=BLOCKED` / `gateway REACHABLE status=403`）、13 项联网 / 遥测插件禁用 + `read-only` 剖面锁死；② 容器内无供应商密钥（env 白名单仅网关地址 + 短期令牌两项）；③ 工作台侧出网调用点逐条枚举 12 处（受控抓取 / 知识适配器 / 模型 4 / 发布集成 3 / 内部受控 3），全外置配置 + 显式超时；④ 三项阴性核查（无硬编码第三方域名 / 无遥测埋点 / 无第二类出网原语）；⑤ C1/C2 不立项明写「不引入第二套出网归口」〕；10.12 已查〔**判据成立 ⇒ 勾选**（用户授权全链路写操作）：一次性独立容器（digest 钉死、仅回环端口、无卷）六步全过——前滚 34 项（含 027）→ 对象核对（约束 1 + 两表 21/10 列、CHECK 4/2、FK 1/3）→ 备份/恢复（`pg_dump`/`pg_restore` 退出码 0，恢复库逐项一致）→ 回退 027（两条 `DROP TABLE` + `DROP CONSTRAINT` + `DELETE 1`，不用 CASCADE）⇒ 归零 → 再前滚（仅 1 项＝`027`，计数全同）→ 容器销毁无残留〕；10.10 已查〔**判据成立 ⇒ 勾选**：工具面 4 个审计落点 `reason` 全为受控枚举或空（`executed` 恒 `None` / `blocked` 取 `ReasonCode.value` / 令牌守卫固定 `"not_authorized"` / 到期清理不写 `reason` 键）；9 值 `ReasonCode` 枚举 + 写入侧 `_validate` 运行时校验（InMemory / Postgres 同日径）+ 5 组正负向守护测试；动作码为 `AuditAction` 枚举 + 查询侧 422 + 读取侧水合三重闸门；边界登记：DB 层无 CHECK、非工具类审批驳回理由为自由文本（有意设计，超本条范围）〕），**未附结论的仍未查**。
> **与本文件口径的偏差说明**：本文件其余各组均为「代码无法代替、需外部输入」；本组是**纯内部核查**，放在此处仅为集中可勾选，**不代表需要外部资源**。

- [ ] 10.1 审计是否有 **DELETE / 清理路径**（`app/audit/store.py` 与数据库层是否 REVOKE）——判据：**可删即不合规**
      - **结论（2026-09-16 自查，两分）：应用层「无 DELETE / 清理路径」已查实（强证据）；数据库层「REVOKE」未做（差距）⇒ 按判据「可删即不合规」仍不勾选。** ① **应用层无删除能力**：`app/audit/store.py` 的 `AuditStore` 协议只有 `append` / `list_recent` / `query`（`:12-27`），两个实现只有 INSERT + SELECT（`:55-114` / `:117-271`；`app/audit/service.py` 同为只有 `record` / `query`）⇒ 接口级不可删；`app/` 全目录 **18 处 `DELETE FROM` 逐条核对，无一指向 `workbench_audit_log`**（清单见 change-record 本条），`scripts/` 无 DELETE ⇒ 语句级亦无。② **保留策略无执行器**：`WORKBENCH_RETENTION_POLICY` 的 `audit:730` 仅被 `scripts/commercial_g0_preflight.py` 部署预检读取、**不被服务侧消费**（`app/commercial/lifecycle.py:14-17`）⇒ 不是清理路径。③ **租户删除不删审计**：物理清场只清记忆层（`app/commercial/lifecycle.py:339`），且删除执行**本身写审计**（`:342-349`，`commercial.deletion.executed`）。④ **worker 清理只动运行事件**：`app/worker.py:235-236`（守护 `tests/test_runtime_events_postgres.py::test_purge_never_touches_the_audit_log`）。⑤ **检测层**：审计为空告警 + 客户验收探针「不轮转、不删除」（`scripts/monitoring_probe.py:157` / `scripts/customer_acceptance_probe.py:521-535`）。
      - **数据库层差距（未做，不得读成已验）**：全仓 `*.sql` 无 `REVOKE` / `GRANT`、`migrations/010_audit_log.sql` 无删除/改保护 ⇒ 按当前部署口径（应用 DSN 兼作迁移账号＝表属主）账号天然有 DELETE / UPDATE 权限 ⇒「不可篡改」目前靠**应用层无路径 + 检测层**，**不靠 DB 硬约束**；且 **REVOKE 生效前提是账号分离**（属主角色 vs 应用 DML 角色），非单条 SQL 可毕 ⇒ **处置（2026-09-16 用户拍板）：仅登记，暂不做 DB 加固**（不写 runbook 加固预案、不加迁移/触发器）；待组 1 真实环境验收时一并考虑。
      - **附：任务域事件流 `workbench_audit_events`（迁移 001，非通用审计）存在删除路径（设计内）**：结构级 `ON DELETE CASCADE`（`migrations/001_initial.sql:25/31`）＋ 应用级失败补偿（`app/repository.py:194`），由 `app/conversation/execution.py:406-411` 在 ⑥ 落库失败（503）时触发；真库守护 `tests/test_dsh_execution_postgres.py:454-474`（零残留、含 `audit_events=0`）⇒ 定性＝**失败请求痕迹清零**（该请求「从未发生」，也不写通用审计），非删除已生效的审计证据。034 注释「审计表（`workbench_audit_log` / `workbench_audit_events`）不可删除」的准确口径应读作「**保留期清理/后台任务不得触碰**：`workbench_audit_log` 全口径不可删；`workbench_audit_events` 仅随任务生命周期/失败补偿删除」——迁移已应用，**不回改**。
- [x] 10.2 审计与运行日志的**轮转策略**是否避免「只留一份 `.1`、覆盖历史」；是否有**日期分段或外部归档**
      - **结论（2026-09-16 自查）：无「只留 `.1`」风险，且日志有界。** 证据：① 应用侧审计日志只有 `StreamHandler(sys.stdout)`、**无 `FileHandler`**（`app/audit/logging.py:22`）⇒ 不存在应用内「覆盖式保留 `.1`」的机制（由 `tests/test_audit_logging.py` 守护）；② 落盘与轮转交给容器运行时：三服务已设 `json-file` / `max-size=10m` / `max-file=5`（`docker-compose.app.yml`，2026-09-16 本机演练 `docker inspect` 实查生效；`tests/test_compose_worker_assets.py::test_app_services_bound_container_log_growth` 守护）；③ 数据库里的审计行**不轮转、不删除**（不可篡改口径，见 10.1）。**未做**：外部归档与日期分段——长期留存需由客户侧日志采集承担（runbook「监控与告警」接入方式）。
- [ ] 10.3 运行事件 / 日志是否**有界**（容量与保留），并配**磁盘容量告警**
      - **结论（2026-09-16 自查，两批）：前两条已闭合，仅剩「磁盘容量告警」的渠道接入 ⇒ 仍不勾选。** ① **日志有界 ✓**（同 10.2 证据②）；② **运行事件有界 ✓（2026-09-16 第二批闭合）**——事件已从状态行的行内 JSONB（改造前 `app/runtime/state.py` 的无界 list，经 `app/runtime/serialization.py` 整段序列化）迁到 **append-only 表 `workbench_runtime_events`**（迁移 `034`：主键 `(run_id, sequence)`，一次追加只写一行，状态行只留计数 `event_count` ⇒ 行大小有界、且消除写放大），并由 worker 周期任务 `runtime-events-purge` 按 **保留期**（`WORKBENCH_RUNTIME_EVENTS_RETENTION_DAYS`，默认 30 天）清理 `occurred_at` 超期的行 ⇒ **容量与保留都有上限**；清理**不触碰审计**（`tests/test_runtime_events_postgres.py::test_purge_never_touches_the_audit_log` 守护），`GET /api/v1/runs/{run_id}/events` 的 cursor 断点读取语义不变（同一文件另有游标与序号单调断言）；③ **磁盘容量告警**：规则已写入 runbook「监控与告警」第 7 条（≥80% 预警 / ≥90% 严重），并已落成只读探针 `scripts/monitoring_probe.py`（本机实跑取证），但**渠道仍未接入任何环境** ⇒ 尚未生效，故本条不勾选（与 1.9 同因）。
- [x] 10.4 `redact_payload` 是否覆盖**值的形态**（不只看键名：工具标题 / 命令摘要 / 回复正文里可能带 key），且掩码**幂等**（重复脱敏不改变 payload hash）
      - **结论（2026-09-16 自查 + 修补）：两条判据均已覆盖。** 改造前只按**键名精确匹配**（`key.lower() in 白名单`）：判据①**未覆盖**（只读取证：值形态 **6/6 全漏**、键名另漏 5 个变体 `apiKey` / `X-Api-Key` / `authToken` / `api-key` / `clientSecret`）；判据②当时成立但无守护。现按「**键名归一 + 值扫描**」收口（用户拍板）；**读取输出（`to_public_dict`）与持久化写入（`serialization.encode_event`）共用同一入口**这一点未变（`app/runtime/contracts.py:180`）。
      - **① 键名归一**：`app/runtime/contracts.py:105-130` 复用审计侧 `key_tokens`（`app/audit/redaction.py:23-26`，先例已跨模块复用）做词元归一 ⇒ `apiKey` / `X-Api-Key` / `api-key` / `authToken` / `clientSecret` 与下划线小写同口径；**裸 `key` 单列不纳入**（`{"key": "plan-42"}` 是正常业务字段），`api` + `key` 组合另行命中。原 `SENSITIVE_PAYLOAD_KEYS`（精确匹配用）随之删除（全仓仅本文件两处引用，已核实）。
      - **② 值形态扫描**：`app/runtime/contracts.py:136-152` 三段**有限模式集**，字符串值统一替换为 `[已隐藏]`——`Bearer <token>` / 敏感词 `k[:=]v`（含引号包裹的 JSON 形态，负向断言避免与前者重复替换）/ 已知凭据前缀（`sk-` / `ghp_` / `glpat-` / `xox?-` / `AKIA`）；未命中的文本原样返回。
      - **③ 幂等取证**：掩码取值字符类排除 `[` `]`（`:122`）⇒ 已掩码片段不再被任一模式命中（不依赖「替换结果恰好相同」）；混合样本实测 `raw 5e724d83…` → `once = twice = thrice = 8e592fb1…`（逐字节一致），5 个凭据形态 `leaks` 为空。
      - **测试与反假**：`tests/test_runtime_contracts.py:115-237` 共 8 条（值形态 / 键名形态 / 反误伤 ×2 / 幂等 ×2 / 端到端 / 边界登记），**先红**（3 红 5 绿）后绿；**反假三轮**均按要求变红并还原——绕过值扫描 ⇒ 2 红、裸 `key` 纳入判定 ⇒ 反误伤红、去掉 `api`+`key` 组合 ⇒ 2 红（含既有 `api_key` 守护用例）。写库路径由真库用例 `tests/test_runtime_state_postgres.py:144-148`、`tests/test_runtime_events_postgres.py:158-184` 守护。
      - **全量回归**：`2147 passed / 0 failed / 0 skipped`（junit `tests="2147" errors="0" failures="0" skipped="0"`；基线 2139 ⇒ ＋8，无既有用例被跳过或删除）；`compileall` 退出码 0。
      - **未覆盖边界（登记，属有意保留）**：① `-p<password>` 短选项形态不覆盖（覆盖它必然误伤 `-production` 之类，`tests/test_runtime_contracts.py:214-222` 钉住该边界）；② `Bearer` 后不足 4 字符的 token 不替换（阈值取舍：过松会把普通词当凭据）；③ 值扫描是**有限模式集**（不认识的形态不替换），**不构成**「任意值内凭据都能识别」的承诺。
      - **未做（不得读成已验）**：历史已落库事件仍是**旧规则**产物（值内凭据可能仍在库内）⇒ 读取路径会按新规则再次收敛，但**库内存量数据未清洗**（无回溯重扫）；本批**已推送并取得 CI 结论**（2026-09-16 销账：提交 `7107fae` + `2a4bb82`，run `35064405339` **6/6 job success**，后端 `2051 passed, 96 skipped` = 2147 与本机一致、真库 `tests=68 skipped=0 failed=0`）。
- [ ] 10.5 金额 / 费用是否**全部整数分**（`usage_ledger` 与展示层；`daily_budget_cents` 已合规，其余待核）
      - **结论（2026-09-16 自查 + 最小加固，两分）：用量账本 / `daily_budget_cents` / 评测费用 / 展示层「全整数分」已查实（强证据）；任务预算 `budget`（`NUMERIC(18,6)`「元」+ 接口 float 链路）为**差距**，本轮按用户拍板做**最小加固**（运行时「元→分」换算精确化）并如实登记 ⇒ 按判据仍不勾选。** ① **用量账本**：`cost_cents` 为 `BIGINT NOT NULL`（`migrations/006_commercial_g0.sql:40`），应用层全 `int` 链路（`app/commercial/usage.py:18` 契约、`:31`/`:79` 拒绝负值、`:52`/`:120` 冲正写 `-cost_cents`、`:95`/`:102`/`:104-109` 读回与累计均 `int(...)`）；契约口径「`cost_cents` 为**整数分**」（`docs/api-contract.md:128`）。② **`daily_budget_cents`**：`BIGINT NOT NULL DEFAULT 0 CHECK (>= 0)` + 迁移注释「金额按宪法用整数分」（`migrations/023_conversational_agent.sql:58`/`:65-66`）；服务端校验拒绝非整数（`app/workforce/config.py:210-212` `_validate_non_negative_int`；真值用例含 `1.5` 拒绝，`tests/test_agent_config_store.py:220-221`）；前端按「元」输入、提交前 `Math.round(yuan * 100)`（`admin-web/src/features/workforceSettings/WorkforceSettingsPage.tsx:62`/`:78`，测试钉住 `12.34 → 1234`）、展示 `(cents / 100).toFixed(2)`（`:58`）。③ **评测费用**：`INTEGER NOT NULL DEFAULT 0 CHECK (>= 0)` + 注释「整数分（不用浮点）」（`migrations/033_evolution_eval.sql:47`）；runner 全整数（`app/evolution/runner.py:57`/`:161` 预算闸门 `int(...) * len(cases) * repeats`、`:180` fail-closed 文案）。④ **记忆预算闸门**：整数分累计（`app/memory/service.py:43`/`:49-51`/`:57`）。⑤ **展示层**：`formatCents` 纯整数运算（`admin-web/src/features/billing/state.ts:7-13`，含冲正负值；测试 `UsageBillingPage.test.tsx:19-30` 钉住整数分换算）⇒ 管理台金额面仅两处（用量与费用页 / 每日预算字段）已逐一核过；`companion-pwa` / `desktop` 全目录金额类键 grep **零命中**（无金额代码）。⑥ **运行时契约本身是整数分**：`RuntimeContext.budget_cents: int`（`app/runtime/contracts.py:62`）+ 反序列化强制 int（`app/runtime/serialization.py:148`）+ 策略闸门按分比较（`app/runtime/policy.py:44`）。
      - **差距（登记，不得读成已验）**：**任务预算 `budget` 不是整数分**——存储为「元」`NUMERIC(18, 6) NOT NULL CHECK (budget >= 0)`（`migrations/001_initial.sql:11`），链路全程 float（`app/domain.py:67` `budget: float`、`:187`/`:190`/`:196` 闸门比较；`app/main.py:332` `TaskCreate.budget: float`、`:345` `TaskView.budget: float`；`app/repository.py:223` `budget=float(row[7])`），仅在运行时换算为分（`app/runtime/service.py:97`；阴性核查：`app/` 全目录无其他「元→分」换算点——`* 100` 命中仅本处金额，其余 `* 1000` 为时延 / 过期时间）。**本轮最小加固（2026-09-16 用户拍板）**：换算由 `int(task.budget * 100)`（二进制浮点截断：`0.29 × 100 = 28.999…` ⇒ **28**，少 1 分）改为 `_yuan_to_cents`（`Decimal(str(amount)) * 100` + `ROUND_HALF_UP`，`app/runtime/service.py:29-36`）；测试钉住（`tests/test_runtime_adapters.py:219-243`：`0.29 → 29` / `19.99 → 1999`，**先红**（28/1998）后绿，反假变红后还原）。**未做**：存储改整数分、API 契约改分（`budget` 仍为「元」）、前端任务表单口径、亚分精度（`NUMERIC(18,6)` 可表示 `0.005` 元）拦截 —— 均属「地基改动」（宪法 §1.4），建议**另立专项**。
      - **全量回归**：`2149 passed / 0 failed / 0 skipped`（junit `tests="2149" errors="0" failures="0" skipped="0"`；基线 2147 ⇒ ＋2，无既有用例被跳过或删除）；`compileall` 退出码 0。
      - **未验证（不得读成已验）**：① 真实环境（staging / 生产）的存量任务预算值**未回溯核**（`budget` 为「元」、亚分精度未被拒绝；本机测试库无生产数据）；② ~~「任务预算改整数分」的迁移方案与兼容影响**未评估**~~ ⇒ **已出专项方案（见下条 D）**；③ ~~前端任务创建入口的存在性未核~~ ⇒ **已核（2026-09-19）**：管理台**有**任务创建入口（首页草稿 → 任务，`admin-web/src/features/home/api.ts:76` **写死 `budget: 0`**），运行详情页类型含 `budget: number`（`features/runDetail/types.ts:27`）但**无金额输入控件**；`companion-pwa` / `desktop` 仍零命中 ⇒ **前端无「元」输入面**，改分**不触发前端表单改造**（这是选项 A 成本低的关键事实）。
- **D · 10.5「任务预算改整数分」专项方案（2026-09-19，本轮只出方案、不改码）**：**结论：本项属宪法 §1.4「地基改动」（数据模型 + 接口契约），按绝对禁止 #9 不列选项不动手 ⇒ 本轮出 A/B/C 三选项供拍板。**
      - **现状取证（逐处，2026-09-19 复核）**：① **存储**：`budget NUMERIC(18,6) NOT NULL CHECK (budget >= 0)`，单位**元**（`migrations/001_initial.sql:11`）。② **领域对象**：`Task.budget: float`（`app/domain.py:67`）。③ **治理闸门**：`ensure_can_create` 按**元**比较（`app/domain.py:203` `budget < 0`、`:208-209` 普通员工高风险 **`budget > 1000`** 元）。④ **接口契约**：`TaskCreate.budget: float = Field(default=0, ge=0)`（`app/main.py:456`）、`TaskView.budget: float`（`:469`、回填 `:750`）；**无 `budget_cents` 字段**。⑤ **落库唯一路径**：`_store_new_task`（`app/main.py:3789-3832`）把 `budget` 计入 **`request_fingerprint`**（`:3815`）⇒ **改单位会改变指纹**，直接影响幂等语义（见选项 A 的兼容处理）。⑥ **运行时换算**：`app/runtime/service.py:100` `budget_cents=max(0, _yuan_to_cents(task.budget))`（`_yuan_to_cents` = `Decimal(str(x))*100` + `ROUND_HALF_UP`，`:29-36`）⇒ **运行时下游已是整数分**（`RuntimeContext.budget_cents: int`，`app/runtime/contracts.py:70`；策略闸门按分比较 `app/runtime/policy.py:44`）。⑦ **其它写入方**：会话承载任务 `budget=CONVERSATION_TASK_BUDGET`（`app/conversation/execution.py:734`，口径为 0）、内容工作台建任务 `budget=0`（`app/content/service.py:157`）、S2「存成任务」沿用源任务预算（`app/main.py:4244`/`:4250`）、管理台首页建任务 `budget: 0`（`admin-web/src/features/home/api.ts:76`）。⑧ **导出包**：`tasks` 类别当前导出 `budget`（`app/commercial/export_readers.py:304`）⇒ 改口径需同步契约（`docs/api-contract.md` B-2b 段）。
      - **选项（按破坏面从小到大）**：
        - **A. 加法式（推荐）**：迁移新增 `budget_cents BIGINT NOT NULL DEFAULT 0 CHECK (budget_cents >= 0)`，**回填** `ROUND(budget * 100)`；服务端**以 `budget_cents` 为唯一权威**（闸门 `1000` 元 → `100000` 分、运行时直接用分不再换算），旧列 `budget` **降级为只读兼容视图**（`TaskView` 同时回 `budget_cents` 与 `budget`，后者由分反算，标注**deprecated**）。`TaskCreate` 新增可选 `budget_cents`，与 `budget` **互斥**（同时给且不一致 ⇒ `422`；只给 `budget` ⇒ 服务端 `Decimal` 精确换算，**拒亚分**）。**指纹**：新写入用 `budget_cents` 参与，**读旧行时沿用存量 `request_fingerprint` 不动** ⇒ 存量幂等键不回退。**代价**：一列冗余 + 一段兼容期；**风险最低、可回滚**。
        - **B. 彻底切换**：迁移 `DROP COLUMN budget`，`TaskCreate.budget_cents` / `TaskView.budget_cents` **替换**旧字段（破坏性契约变更，前端三端与导出包同步改）。**代价**：存量指纹语义变更（幂等键需重算或作废）、旧客户端 422、导出包字段变更需与已交付客户对齐；**收益**：无冗余、口径唯一。
        - **C. 仅加固（不达标）**：不加列，只在 `TaskCreate` 上**拒绝亚分**（`Decimal` 校验 ≤2 位小数）+ 契约明确写「单位：元」。**代价**：判据「全部整数分」**仍不成立**（存储与链路仍是 float 元）⇒ **不解决 10.5**，仅缩小误用面。
      - **推荐 A 的理由**：① 运行时下游**本来就是整数分**（取证 ⑥），A 只是把「权威口径」从换算点前移到存储，**改动面最小且与既有架构同向**；② 前端**无「元」输入控件**（见上条 ③）⇒ 不触发前端改造；③ 存量幂等键与指纹**不受影响**（A 的关键设计）；④ 可回滚（旧列保留）。
      - **若选 A：迁移与验证计划（草案，未执行）**：迁移 `044_task_budget_cents.sql`（新增列 → `UPDATE ... SET budget_cents = ROUND(budget * 100)` 回填 → `SET NOT NULL`；**不动** `budget` 列，回滚即 `DROP COLUMN`）；服务端改动点收敛为 `domain.Task` / `_store_new_task` / `ensure_can_create`（闸门改分）/ `TaskView` / `task_export_reader`；**先写失败测试**（亚分拒绝、互斥 `422`、闸门按分、存量指纹不变、导出含 `budget_cents`），**反假至少三轮**（去亚分校验 / 闸门仍按元 / 指纹改成分 ⇒ 各自必须变红）；全量回归 + 真库门控 + `api-contract.md` 同步 + 导出契约口径更新。
      - **未验证（D）**：① 真实环境**存量 `budget` 值未回溯核**（是否有亚分值 / 超大值，需真库统计）；② **迁移时长未测**（大表 `UPDATE` 需评估锁与耗时，`workbench_tasks` 规模未知）；③ 前端三端对 `TaskView` 新增字段的**渲染影响未核**（仅确认无金额输入控件）；④ **未批准前不动任何代码**。
- **新登记差距（2026-09-19 审计发现）：真源 §7 要求的「客户管理员手册 / 员工手册 / 故障手册」在仓库内不存在**。真源 `docs/superpowers/specs/2026-09-06-commercial-g0-design.md:129` 明写「私有部署交付包必须包含：…客户管理员手册、员工手册和故障手册」，而全仓 grep `客户管理员手册|员工手册|故障手册` **仅命中该真源本身**（`docs/` 下现有的是运维向 runbook：`private-deployment-runbook.md` / `customer-side-acceptance-runbook.md` / `readonly-verification-runbook.md`，**不是**面向客户管理员与员工的手册）。本清单组 1–8 亦未跟踪该项 ⇒ **补登为未完成交付项**（属交付打包环节，组 7 邻域）。**未做**：三份手册的受众 / 范围 / 与现有 runbook 的分工需先定口径，本批未动。
- **✅ 已产出：三份交付手册（2026-09-19）—— 真源 §7「私有部署交付包必含」项闭合**。落点 `docs/handbooks/`：[`员工手册`](file:///d:/徐徐AI学习/公司工作台/docs/handbooks/employee-handbook.md)（面向 `employee` / `department_lead`：三个入口 / 登录与口令策略 / 一句话派活 / 对话与分享 / 本人会话导出与删除 / 任务页出稿 / 运行详情（叫停 · 验收 · 沉淀）/ 通知 / **你能看到哪些审批**（审批集中在 CEO 与超管，发起人不得自审）/ 常见问题 / 未交付边界）、[`客户管理员手册`](file:///d:/徐徐AI学习/公司工作台/docs/handbooks/customer-admin-handbook.md)（首个管理员初始化 / 账号与登录安全（限流 · 登出即撤销 · TOTP 受限会话）/ 岗位与数字员工九个执行参数 / 知识权限与治理（未登记不可检索 · 过滤 fail-closed）/ 技能与记忆治理 / 用量与费用整数分 / 数据导出（异步 + 7 天 + 11/15 类）/ 租户删除两步流程与确认人 / 保留策略与审计 / **无界面入口清单** / 每周巡检）、[`故障手册`](file:///d:/徐徐AI学习/公司工作台/docs/handbooks/troubleshooting-handbook.md)（三条纪律（报错原文不改 · 不吞异常 · 三轮刹车）/ 现象→自助排查矩阵 / 报障四样信息与只读探针 / 四类高频问题定位 / **监控现状如实**（9 条规则已定但**渠道未接入**）/ 恢复与回滚只引用 runbook 不另立口径 / 明确不做清单 / L1→L3 升级路径）。
      - **口径与纪律**：三份手册**逐条对应已交付能力**，未交付 / 未验收项一律单列（自动化调度、任务中心列表页、公众号自动发布、手机端系统推送、桌面端签名与更新、真实模型与抓取、C1/C2 未立项、保留策略无执行器、告警渠道未接入、迁移回滚演练未做）；**不承诺**未证实的接入与效果（宪法 §9）；故障手册**不预置联系方式**（避免过期信息被当真），指向客户交付记录。
      - **守护与反假**：新增 `tests/test_delivery_handbooks.py` **11 条**（三份存在且非占位 / 各有适用范围 / **三份互链** / 每份都有未交付·未验收声明 / 管理员手册覆盖 9 个必需主题 / 员工手册覆盖三端 / 故障手册指向 runbook 与两个只读探针且**脚本真实存在** / 故障手册含报错原文与三轮刹车纪律）。**反假两轮真变红后还原**：① 临时移除故障手册 ⇒ 5 条红（存在性 + 互链 + 探针引用 + 纪律）；② 去掉「三轮刹车」纪律 ⇒ 对应用例红。
      - **全量回归**：后端 `2532 passed / 0 failed / 0 skipped`（真库 DSN；基线 2521 ⇒ **＋11 恰为本批新用例**）。
      - **未验证（手册）**：① **未逐条复核手册与实现的逐句一致性**（守护只钉结构性事实与边界声明，文案正确性仍需人读）；② ~~三份手册**未纳入任何自动化打包**~~ ⇒ **已闭合（见下条：交付包组装脚本）**；③ 手册为 Markdown，**未出 PDF / 未做客户侧排版**；④ 客户交付记录里的联系人与响应时间**未填**（属客户侧信息）。
- **交付打包：真源 §7 六项必含 → 机器核对（2026-09-19）**。新增 [`scripts/build_delivery_package.py`](file:///d:/徐徐AI学习/公司工作台/scripts/build_delivery_package.py)（组装 + 核对 + 客户侧只读校验三合一）与守护 `tests/test_delivery_package.py`（13 条）。**此前「交付包必含哪六项」只写在真源里、靠人工记忆组装**（漏一项不会有任何提示）；现改为机器核对。
      - **真源 §7 六项 → 仓库落点（缺失即 fail-closed，退出码 1、不产出任何内容）**：① 数据库迁移脚本 = `migrations/`（实测 **43 个文件**）；② 配置模板 = `.env.example` / `.env.staging.example` / `.env.sso.example`；③ 备份 / 恢复 / 健康检查 / 回滚脚本 = `scripts/migration_backup_drill.py`（`--phase list|backup|restore|verify`，带安全护栏与 DSN 脱敏）+ `monitoring_probe.py`（9 条监控规则）+ `customer_acceptance_probe.py`；④ 部署运行手册 = `docs/private-deployment-runbook.md`；⑤ **三份手册** = `docs/handbooks/`；⑥ 第三方许可 = `THIRD-PARTY-NOTICES.md` + `licenses/{LGPL-3.0,GPL-3.0,MPL-2.0}.txt`。另含 Python 发行包口径（`requirements.lock` + `pyproject.toml`）。
      - **生成物**：随包生成 `CONFIG-AND-SECRETS.md`（**只列键名与注入规则，不出现任何「键=值」形态**；含起栈前必跑的三条预检）+ `MANIFEST.json`（**版本清单 + 每个文件 sha256**）+ `INCOMPLETE.md`（缺项与补齐方式）。
      - **版本清单（真源 §7 五维度）**：控制平面（`--version` 或 `git describe`）、数据库迁移（实测 `043_tenant_deletion_record` / 43 项）、桌面端（`desktop/package.json` = 0.1.0）、Runtime（取自 `.env.staging.example`，**已标注为模板示例值须替换**）、**岗位能力包 = missing**（真源 §6 定义的该产物在本仓库内**不存在**：入口 / 默认 Agent / 技能 / 提示词版本 / 模型范围 / 知识范围 / 动作白名单 / 审批规则 / 评测集分散在数字员工目录、技能层与知识范围绑定中）⇒ **如实标 missing 并写明原因，不臆造版本号**。
      - **红线：密钥不进交付物**。只复制 `*.example` 模板；`.env` / `.env.*` 真值文件**逐文件复核**，命中即 fail-closed（`is_forbidden_config_name` 显式两条判据——首版用正则 `^\.env($|\.(?!.*\.example$).*)` **把 `.env.example` 也误拦了**，实测暴露后改为函数并加判据用例）。输出目录默认 `dist/delivery`（`.gitignore` 已排除，实测 `git status` 不出现）。
      - **退出码语义**：`0` 完整（或 `--allow-incomplete`）；`1` 仓库内必备项缺失（**未组装任何东西**）；`2` 已组装但缺外部产物（控制平面镜像 / 桌面端安装包）⇒ 包不完整。`--allow-incomplete` **只放宽退出码，不删 `INCOMPLETE.md`**（不隐藏事实）。`--check-only <dir>` 供**客户侧**核对既有交付包：按 MANIFEST 逐文件验 sha256，损坏或不是交付包一律非 0。
      - **测试与反假**：13 条（六项齐备 / **缺项 fail-closed 且不产出内容** / 无外部产物时安全生成且必备落点齐全 / INCOMPLETE 列出全部外部缺项 / 版本五维度且迁移版本取自实际清单 / 每文件 sha256 与文件一致 / **无真值配置入包** / **判据本身准确**（拦 `.env`·`.env.local`·`.env.production`，放行 `*.example`）/ 配置说明无赋值形态 / check-only 发现被改文件 / check-only 原样包不误报 / 非交付包显式拒绝 / `--allow-incomplete` 不删声明）。**反假两轮真变红后还原**：① 去掉 fail-closed 退出码（1→0）⇒ 对应用例红（`assert 0 == 1`）；② 让密钥文件判据恒放行 ⇒ 判据用例红。
      - **真机实跑**：`py scripts/build_delivery_package.py --version 0.1.0-dev` ⇒ `incomplete`（缺镜像与安装包，符合预期）、**61 个文件**入包、`MANIFEST.json` 记 43 项迁移与五维度版本；`--check-only dist/delivery` ⇒ `incomplete` 且**未报任何文件损坏**。
      - **未验证（打包）**：① ~~控制平面镜像与桌面端安装包未提供~~ ⇒ **镜像已构建并首次入包（见后条）**，**桌面端安装包仍缺**（需签名证书）⇒ 仍**没有 `status=complete` 的包**（`status=complete` 这条路径**只有单测覆盖**）；② **岗位能力包产物未定义**（需先决定是否把分散配置抽成独立产物）；③ ~~交付包未做压缩 / 签名 / 校验和分发~~ ⇒ **整包指纹 + 签名状态 + 压缩包与校验和已补**；④ ~~客户侧 `--check-only` 未实跑~~ ⇒ **已用模拟客户流程实跑**（复制到临时目录 → 校验和 → 解包 → 包内自查）。
- **交付包整包指纹 + 签名状态（2026-09-19）—— 补上打包未验证第 ③ 项**。① **为什么需要**：原设计只有 `MANIFEST.json` 逐文件 sha256，但**清单本身也在包里**——改了文件再顺手把清单里的 sha256 改成新值，逐文件校验就会"通过"，形同虚设。② **实现**：新增 `PACKAGE-DIGEST.txt`（对包内**除本文件以外**的全部文件按「posix 路径 + 文件 sha256」排序拼接计算的单一哈希），并在 `--check-only` 里**重算比对**；新增可选 `--signature-file`（外部密钥对指纹的签名，随包为 `PACKAGE-DIGEST.sig`）。③ **签名状态如实标注（关键）**：无证书时指纹文件与报告**都明写「未签名」**，并写明含义——「**只能**证明包内内容与生成时一致，**不能**证明本包由我方发布」；**绝不暗示已签**。④ **确定性**：计算不掺时间戳 / 本机信息 ⇒ 同输入同结果，可用于「两次构建是否一致」。⑤ **退出码**：签名文件路径不存在 ⇒ **fail-closed（退出 1）**；未签名本身**不**算损坏（否则每包都失败），但必须在报告里看得见。
      - **测试与反假**：`tests/test_delivery_package.py` 13 → **17 条**（新增：指纹与重算一致且幂等 / **改了文件又同步改清单仍被抓到**（这条专门证明指纹的**独立价值**——并断言此轮逐文件校验确实是"通过"的，否则用例没在验指纹）/ 未签名必须在报告与指纹文件里可见 / 提供签名则随包且标记已附、签名路径不存在则 fail-closed）。**反假一轮真变红后还原**：关掉 `check_only` 里的指纹比对 ⇒ 上述篡改用例红（且日志显示只剩逐文件校验，证明抓到它的确实是指纹）。
      - **真机实跑**：`--version 0.1.0-dev --force` ⇒ 指纹 `sha256:66617fe9…`、报告含 `[missing] 交付包签名…未签名`；**两次构建（不同输出目录）指纹逐字节一致**（`两次构建一致: True`）；`--check-only dist/delivery` ⇒ 逐文件与指纹均通过，只报预期中的 `INCOMPLETE.md`。
      - **未验证（指纹 / 签名）**：① **未用真实密钥做过一次签名与验签**（无受控密钥环境）⇒ 签名路径**只有单测覆盖**；② ~~整包压缩（tar/zip）与其校验和未做~~ ⇒ **已补（见下条）**；③ 指纹算法**未做跨平台 / 跨 Python 版本的兼容性声明**（实现用标准库 sha256 与 posix 路径，跨平台理论上一致，但**未实测**）。
- **交付包第一入口 + 压缩包与校验和（2026-09-19）**。① **`README-FIRST.md`（包内第一入口）**：此前客户拿到包要先自己判断「先看哪份」——手册、清单、预检散落在多个目录里容易漏。现随包生成第一页，含**三步上手**（① 核对完整性 `--check-only`；② 按角色手册配置 + 跑三条预检；③ 按 runbook 部署 + 客户侧验收）、**包内结构表**、**问题上报四样信息**；且**状态如实**——不完整时置顶「先读 `INCOMPLETE.md`」，未签名时明写「本包未签名…不能证明本包由我方发布」。② **`--archive`**：额外产出 `<输出目录>.tar.gz` 与 `<archive>.sha256`（**压缩包放在输出目录同级**，不把自己装进自己）⇒ 「手上这一个文件有没有传坏」在**解包之前**即可回答。③ **两个顺序坑（已修并留注）**：`README`/`MANIFEST`/`PACKAGE-DIGEST` 必须**最后**写（否则清单与指纹漏登记先前生成的说明文件——README 可被随意替换而无人发现）；签名文件校验**提前**到写 README 之前（否则 README 会先写死「未签名」再发现签名缺失）。④ 归档用 `output.name + ".tar.gz"` 而非 `with_suffix`（目录名自带点如 `delivery.v2` 时 `with_suffix` 会把它替换掉）。
      - **测试与反假**：`tests/test_delivery_package.py` 17 → **21 条**（新增：README 必须含三步上手 / 手册入口**逐个验证文件真实存在** / 版本与 MANIFEST 一致不写死 / 未签名显式声明 / 有签名时不得再写「本包未签名」/ **README 必须被 MANIFEST 与指纹覆盖**（改了 README 不改清单 ⇒ 校验必须报错）/ 压缩包在同级且校验和与文件一致、包内不再含压缩包）。**反假一轮真变红后还原**：把 README 的签名状态写死为「未签名」（不读 `signed` 参数）⇒ 对应用例红。
      - **真机实跑**：`--version 0.1.0-dev --archive` ⇒ 包内 `README-FIRST.md` 生成正确（含三步上手与四类角色手册入口）、`dist/delivery.tar.gz`（170,585 字节）+ `dist/delivery.tar.gz.sha256`（`93e2ef26…  delivery.tar.gz`）。
      - **未验证（本批）**：① ~~压缩包校验和未在另一台机器上实跑验证~~ ⇒ **已用「模拟客户」流程实跑（见下条）**；② `README-FIRST.md` 的链接为**相对路径**，在 GitHub / 编辑器外（如压缩包内直接双击 md）**未必可点**——属 Markdown 阅读器差异，未逐一实测；③ 归档**未做可复现性声明**（gzip 内嵌时间戳 ⇒ **同一目录两次归档的字节可能不同**；可复现的是**目录形态的整包指纹**，不是压缩包本身，已在文档口径上区分）。
- **模拟客户视角端到端验证 —— 抓出 2 个真实交付缺陷（2026-09-19，本轮最有价值的验证）**。做法：**不用仓库里的脚本**，而是像客户那样走一遍——`dist/delivery.tar.gz` **复制到临时目录**（模拟另一台机器）→ 校验 `.sha256` → `tar -xzf` 解包 → **从解包目录里**跑包内脚本自查 → 再跑 README 让客户跑的预检命令。
      - **🔴 缺陷 ①（客户照做即失败）**：`README-FIRST.md` 让客户跑 `python scripts/build_delivery_package.py --check-only .`，但**打包脚本自身没被复制进包**（`ops_scripts` 只列了三个运维脚本）⇒ 实测报 `can't open file ... scripts/build_delivery_package.py`。
      - **🔴 缺陷 ②（同源，第二条命令同样失效）**：README「第 2 步」让客户跑的三条预检（`commercial_g0_preflight.py` / `staging_preflight.py` / `worker_preflight.py`）**也都不在包里**。
      - **根因与收敛做法**：`ops_scripts` 是**逐个列清单**——新增脚本时没人会回来改这份清单，**必然漂移**。⇒ 改为**整目录纳入 `scripts/`**；并新增守护「**README / PACKAGE-DIGEST 里让客户跑的每个 `scripts/*.py` 都必须在包里**」——它持续生效，未来 README 提到新脚本时会立刻红。**已核查 `scripts/` 内无硬编码密钥**（阴性 grep），这些脚本本就是给部署侧 / 客户侧的只读或演练工具。
      - **守护缺口（登记）**：此前 21 条守护**全绿却没发现这两个缺陷**——因为只断言了「README 提到的手册存在」，**没断言「README 提到的脚本存在」**。⇒ 本轮补 2 条（README 与 DIGEST 两处落点各一条）；`tests/test_delivery_package.py` 21 → **23 条**。
      - **修复后复跑（全部通过）**：① 压缩包校验和一致（`True`）；② 解包成功；③ **从解包目录**跑 `--check-only` ⇒ 六项必备全 `齐备`、**无任何文件损坏**、只报预期中的 `INCOMPLETE.md`；④ README 的预检命令**真能跑且 fail-closed 正确**——`commercial_g0_preflight.py` 在未配置环境下 `[blocked] 迁移状态 / 保留策略 / Runtime 版本` 且**退出码 1**；`worker_preflight.py --offline` 报「迁移清单不一致（缺少 43 项）」且**退出码 1**（未设 `WORKBENCH_APPLIED_MIGRATIONS` 时的正确行为）。**临时模拟目录已清理。**
      - **未验证（本条）**：① 模拟是在**同一台机器**上做的（临时目录 ≠ 真另一台机器）⇒ 「跨机器传输」只验证了**文件层**（校验和），未验证跨 OS / 跨文件系统差异；② `staging_preflight.py` 未单独跑（另两条已跑，且它需联网配置）；③ ~~桌面端安装包 / 控制平面镜像仍缺 ⇒ 完整包仍未产出~~ ⇒ **镜像已构建并入库（见下条）**，仍缺桌面安装包。
- **首份引用真实镜像的交付包 + 镜像引用钉 digest（2026-09-19）**。① **本机构建应用镜像**（消除「本仓库只能构建、需 docker」这条缺项）：`docker build --build-arg WORKBENCH_IMAGE_VERSION=743e3ba-dirty --build-arg WORKBENCH_IMAGE_REVISION=743e3baf…` ⇒ 成功（退出码 0），得 `workbench-app:743e3ba-dirty`，`RepoDigest = workbench-app@sha256:59b3fc6a591d00af6540a0114ebff1bfdc40e577d1b8417ef7db8ae395f72472`。② **镜像内自检**（容器内实跑，非静态核对）：`docker run --rm --entrypoint python … -c "import app.main, app.worker"` ⇒ **`IMPORT_OK`**（依赖装全、代码可导入）+ `migrations` 目录在位。③ **镜像引用**：`CONTROL-PLANE-IMAGE.txt` **优先钉 digest**（`workbench-app@sha256:59b3fc6a…`）并写明「tag 可变、digest 不可变，部署请用这一行」；**取不到 digest 时如实标注原因 + 补救命令**，绝不编造（`_image_digest` 取不到返回空串，有单测钉住）。④ **产出**：`--version 743e3ba-dirty --image-ref workbench-app:743e3ba-dirty --archive` ⇒ 报告 `[included] 控制平面镜像…（digest sha256:59b3fc6a591d…）`；仅剩三项 `[missing]`：**桌面端安装包** / **岗位能力包版本**（产物不存在）/ **签名**（无受控密钥）。
      - **测试与反假**：`tests/test_delivery_package.py` 23 → **25 条**（新增：镜像引用文件**要么钉 digest 要么如实说没取到**（含原因与补救命令）/ `_image_digest` 取不到时返回**空串而非编造值**）。**客户视角端到端复跑**：压缩包校验和一致 → 解包 → 从解包目录 `--check-only` ⇒ 仅报预期中的 `INCOMPLETE.md`（退出码 2）。
      - **未验证（本条）**：① **镜像构建基于 `-dirty` 工作区**（155 个文件未提交）⇒ 镜像标签与 `git rev-parse HEAD`（`743e3ba`）**不覆盖未提交改动**，镜像内容与该提交**不完全对应**——这是本批最重要的诚实边界，正式交付必须**先提交再构建**；② ~~镜像未跑起来过（只做了导入自检）~~ ⇒ **已起容器实跑通（见下条）**；③ 镜像**未推送任何 registry** ⇒ `RepoDigest` 是本机 `docker images` 口径，**收方无法据此拉取**；④ 桌面端安装包仍未产出（需 Windows 代码签名证书）⇒ 仍**没有 `status=complete` 的包**。
- **镜像启动冒烟：真起容器跑通健康检查（2026-09-19）—— 补上「可运行产物」这一层**。做法：用构建出的镜像起**一次性容器**（`-p 127.0.0.1:18111:8000`，连本机测试库 `workbench_test`，用完即删），逐层取证：
      - **① fail-closed 行为正确（首次启动失败是**正确**的）**：只给数据库与双密钥时启动**失败**并报 `ValueError: 生产环境必须配置 WORKBENCH_EMBEDDING_BASE_URL（本地 embedding 服务地址）`（`app/bootstrap.py:1352`）——非 development 缺关键配置即拒启动，**不是镜像缺陷**；补齐该项后启动成功。
      - **② 容器内建健康检查通过**：状态 `Up 18 seconds (healthy)`，日志 `GET /api/v1/health HTTP/1.1" 200 OK`。
      - **③ 宿主机端口映射可用**（部署关键，单独取证）：`curl.exe http://127.0.0.1:18111/api/v1/health` ⇒ **`{"status":"ok","service":"company-workbench"}` + `HTTP_CODE=200`**。
      - **④ 非 root 运行**：容器内 `id -u` = **999**（`Dockerfile` 的 `USER workbench` 生效）。
      - **⑤ 应用启动完整**：`Application startup complete` / `Uvicorn running on http://0.0.0.0:8000`；app 进程按设计**自动应用迁移**（连的测试库迁移已在 043，无新增）；**容器与临时目录均已清理**（`docker rm -f`）。
      - **未验证（本条）**：① 冒烟只覆盖 **`/api/v1/health`**，**未跑任何业务链路**（未登录、未建任务、未调工具）⇒ 不构成「镜像功能可用」的结论；② **未按加固形态跑**（无 `--cap-drop ALL` / `--read-only` / `--pids-limit` / 非内网桥）——生产执行容器的加固口径属门禁 §B17，**本冒烟不覆盖**；③ 容器连的是**本机测试库**（非真实 staging 库）；④ ~~未验证 worker / beat 两个入口命令在该镜像内的启动~~ ⇒ **已验证（见下条）**。
- **同一镜像三入口全部启动验证（2026-09-19）—— app / worker / beat 逐个真跑**。compose 三服务**共用同一镜像、仅 command 不同** ⇒ 必须验证三个入口都能起来，否则「镜像构建成功」是假象。取 `docker-compose.app.yml` 的**权威命令**逐个起一次性容器（连本机测试 PG `55433` + Valkey `6380`，用完即删）：
      - **app（默认 CMD = uvicorn）**：`healthy` + 宿主机 `HTTP 200`（见上条）。
      - **worker**：`--entrypoint celery -A app.worker worker --loglevel=INFO` ⇒ **`Connected to redis://host.docker.internal:6380/0`** + **`celery@32ffc1b49644 ready.`**，且日志列出**全部周期任务已注册**（`purge_run_artifacts` / `purge_runtime_events` / `recompute_crm_health` / `remind_crm_activities` / `remind_crm_renewals` / `run_lifecycle_jobs` / `scan_knowledge_review_due` 等）。
      - **beat**：`--entrypoint celery -A app.worker beat --loglevel=INFO --schedule=/var/lib/celery/beat-schedule` ⇒ `scheduler -> celery.beat.PersistentScheduler`、`db -> /var/lib/celery/beat-schedule`（**镜像预建的可写目录生效**）、**`beat: Starting...`**，并**实际派发**：`Scheduler: Sending due task outbox-publisher (app.worker.publish_outbox)`（15 秒周期任务，证明调度真的在跑而非只启动）。
      - **三入口共用的已知边界（口径澄清）**：容器状态对 worker / beat 显示 `health: starting` —— 因为**镜像级 HEALTHCHECK 探的是 HTTP 8000**（uvicorn），对 worker / beat **不适用**；`docker-compose.app.yml` 里 worker 另有 `celery inspect ping` 的 healthcheck、beat **显式禁用**继承 healthcheck（测试 `test_beat_disables_inherited_http_healthcheck` 守护）。⇒ **本冒烟的「health: starting」不代表异常**，不代表两入口通过容器健康检查。
      - **清理**：三个一次性容器均已 `docker rm -f`，`docker ps -a | grep smoke` **无残留**。
      - **未验证（三入口）**：① ~~未验证「worker 真能执行任务」~~ ⇒ **已验证执行链路（见下条）**，但「是否真接线」仍不可区分；② ~~未验证 beat 与 worker 的联动~~ ⇒ **已验证（见下条）**；③ 未在 `WORKBENCH_ENV=production` 下跑（用的是 `staging`）；④ 未验证优雅退出 / 信号处理。
- **beats → worker 联动链路实跑（2026-09-19）—— 真实异步链路验证**。**同时起两个容器**（同一镜像、同一 redis、同一 postgres），观察 40 秒内的完整链路（不是分别验证启动）：
      - **① beat 派发**：`Scheduler: Sending due task lifecycle-jobs (app.worker.run_lifecycle_jobs)` + `Sending due task outbox-publisher (app.worker.publish_outbox)`（两个 15/30 秒周期任务）。
      - **② worker 接收**：`Task app.worker.run_lifecycle_jobs[ea8f8ed2-…] received`、`Task app.worker.publish_outbox[03937b47-…] received`。
      - **③ worker 执行成功**：`Task app.worker.publish_outbox[…] succeeded in 0.0083s: 0`、`Task app.worker.run_lifecycle_jobs[…] succeeded in 0.0125s: {'exports': 0, 'deletions': 0}` ⇒ **beat → redis → worker → 执行 → 返回结果** 全链路打通，且返回值形态与任务契约一致。
      - **🔴 本条最重要的诚实边界（~~必须登记~~ ⇒ 已于同日消除，见下条）**：**「返回值」无法区分「已接线」与「未接线」** —— `publish_outbox` 未接线时返回 `0`、`run_lifecycle_jobs` 未接线时返回 `{"exports": 0, "deletions": 0}`（`app/worker.py` 的显式契约：未接线即零值、不伪造结果）⇒ 当时观测到的返回值与「worker 已完成生产装配」**在证据上不可区分**。
      - **清理**：两个容器均已 `docker rm -f`，`docker ps -a` 无残留。
      - **未验证（联动）**：① ~~上述「接线与否不可区分」~~ ⇒ **已消除（见下条）**；② 未观察**失败路径**（任务抛错 / 重试 / 死信）；③ 未验证**周期稳定性**（只观察了 40 秒内的一轮派发，未验证长时间运行下的重复派发与幂等）。
- **构造真实待处理作业 —— 证明 worker 在生产装配下真处理（2026-09-19，消除上条边界）**。做法：**不再靠返回值形态推断**，而是**制造一条真实作业**并观察端到端效果：
      - **① 构造**：向测试库 `workbench_event_outbox` 插一条待发布事件（`dedupe_key='smoke-dedupe-1'`、`published_at IS NULL`）⇒ 查库确认「待发布 = 1 条」。
      - **② 观察 worker 真处理**：同时起 worker + beat（同镜像、同 PG、同 redis）45 秒 ⇒ `Task app.worker.publish_outbox[…] succeeded in 0.3747s: **1**`（**发布了 1 条**），**下一轮** `succeeded in 0.2996s: 0`（队列已空）。
      - **③ 查库确认状态改变**（最关键的一环）：`SELECT published_at IS NULL, attempts` ⇒ **`published_at_is_null=false`、`attempts=0`** —— 该行**真的被标记为已发布**，且**一次成功无重试**。
      - **④ 结论**：未接线时 `publisher is None` 只会返回 `0` 且**不会改动数据库**；本观测中**返回值 1 + 库内状态改变**同时成立 ⇒ **worker 在该镜像里确实完成了生产装配**（连上 PG、持有真正的事件发布器、成功发布事件）。**上条登记的「接线与否不可区分」边界就此消除。**
      - **清理**：测试数据 `DELETE 1`、两个容器 `docker rm -f`，`docker ps -a` 无残留（测试库无遗留行）。
      - **未验证（本条）**：① ~~只覆盖 outbox 发布这一条链路；`run_lifecycle_jobs`（导出/删除执行器）仍未用真实作业验证过~~ ⇒ **已验证（见下条）**；② 未观察**失败路径**（投递失败 / 重试计数 / 死信登记）；③ 用的仍是 `staging` 环境与测试库。
- **构造待执行导出作业 —— 证明生命周期执行器真在工作（2026-09-19，同日收口）**。同法验证**第二条链路**（导出执行器）：① **构造**：先在测试库建冒烟租户（首次尝试失败——`inserted=0` 暴露**测试库原本没有任何租户**，故先 `INSERT workbench_tenants` 再插作业），插一条 `workbench_lifecycle_jobs(kind='export', status='queued')` ⇒ 查库确认 `job=queued`。② **观察**：worker+beat 跑 45 秒后 ⇒ **`job_status=completed`**（作业被推进到完成）且 **`packages=1`**（`workbench_export_packages` 里该作业名下**真的生成了 1 个导出包**）。③ **结论**：未接线时 `_lifecycle_runner is None` 只会返回零值且**不动数据库**；本观测中**作业状态推进 + 导出包落库**同时成立 ⇒ **生命周期执行器（含导出载荷生成）在该镜像里确实在工作**。（worker 日志那一轮显示 `{'exports': 0, ...}` 属时序——该轮运行于作业被完成之后，故不再计为待处理。）
      - **清理（全部已核对无残留）**：`DELETE` 顺序按外键：导出包 1 行 → 作业 1 行 → outbox 行（`DELETE 0`，上一条已清）→ 冒烟租户 1 行；复查 `smoke%` 的 jobs / tenants / packages **均为 0**；容器 `docker ps -a` 无 `wb-*` 残留。
      - **未验证（本条）**：① 只验证了 **export** 作业，**未验证 delete 作业**（删除执行器涉及三面清场，验证成本与破坏性更高，且**同一执行器入口**已由本观测证明可达）；② ~~导出包内容未逐字段核对（只核对了「生成了 1 个包」这一事实）~~ ⇒ **已核对（见下条）**；③ 未验证 `run_pending_jobs` 的 **limit 截断**与**多作业并发**行为。
- **worker 生成的导出包内容逐字段核对（2026-09-19）—— 闭合 B-2 系列登记的「worker 侧装配未在 PG 进程实跑」**。做法：建冒烟租户 + **种一条 `workbench_tasks` 行** + 插 `kind='export'` 作业 ⇒ worker+beat 跑 45 秒生成导出包 ⇒ **直接查库读 payload**：
      - **`tasks_rows=1`** —— 我种的那条任务**真的出现在导出包里** ⇒ **worker 侧的 `tasks` 读取器确实接线并取到了数据**（不是空数组）。这一条正是 B-2b 登记缺口（「worker 侧新装配（账号 / 任务读取器）未在 PG 进程实跑」）的**直接反证**，缺口就此闭合。
      - **`unimplemented=[approvals, growth_proposals, knowledge_references, knowledge_versions]`** —— **恰好是 B-2c 收口时判定的那 4 类无实体类别**，与契约口径逐字一致（**未接线的不造假**）。
      - **`resource_keys=15`** / **`has_tenant=true`** / **`redaction=true`** —— 顶层契约字段齐备（15 类键、租户标识、脱敏声明）。
      - **清理（已核对无残留）**：按外键顺序 DELETE 导出包 / 作业 / 任务 / 租户（各 1 行）⇒ 复查 `smoke%` 计数 **`0/0/0`**；容器 `docker ps -a` 无 `wb-w5/wb-b5` 残留。
      - **未验证（本条）**：① 只核对了 **`tasks` 一类**的「有数据」与**未实现类别的集合**，**未逐类核对**其余 10 类已接线类别在 worker 侧也真有数据（memory 中 B-2 的真库端到端用例覆盖了多类，但那是**测试进程**而非 worker 进程）；② ~~未核对脱敏是否真的生效（只核对了 `redaction` 声明存在，未检查包内无敏感值）~~ ⇒ **已核对（见下条）**；③ 未核对 `truncated_categories` / `unavailable_categories` 在真实数据下的行为。
- **脱敏真值标记验证 —— 证明 worker 侧脱敏真的生效（2026-09-19）**。做法（**关键在「种真值」**）：向测试库 `workbench_accounts` 插一条账号，**把敏感列填成可被文本检索的唯一标记值**（`password_hash='SCRYPT-SECRET-DO-NOT-LEAK'`、`totp_secret='TOTP-SECRET-DO-NOT-LEAK'`、`sso_subject='SSO-SUBJ-DO-NOT-LEAK'`、`rejection_reason='REJ-REASON-DO-NOT-LEAK'`、`phone='13800138000'`）⇒ 生成导出包 ⇒ **直接在 payload 全文上做字符串检索**（任一处出现即证明该列漏进包）：
      - **`users_rows=1`**（账号入了包）+ **`mask_ok=138****8000`**（**手机号是掩码形态**，非原值）。
      - **`has_pwd_key=false`** —— 包内**无 `password_hash` 键**。
      - **`leak_scrypt=false` / `leak_totp=false` / `leak_sso=false` / `leak_rej=false`** —— 口令哈希、TOTP 种子、SSO 主体、驳回理由**四项敏感真值全部未出现在 payload 任何位置**。
      - **`leak_phone_full=false`** —— **手机号明文也未泄漏**（连掩码之外的原始形态都查了）。
      - **结论**：因为种的是「DO-NOT-LEAK」唯一标记，**读取器漏掉任一敏感列都会现形**；四项全 false + 掩码正确 ⇒ **worker 侧脱敏确实生效**（不只是声明了 `redaction`）。
      - **清理（已核对无残留）**：DELETE 导出包 / 作业 / 账号 / 租户 ⇒ 复查 `smoke%` 计数 **`0/0/0`**；容器无 `wb-w6/wb-b6` 残留。
      - **未验证（本条）**：① 只覆盖 **`users` 一类**的敏感列（`memories` / `audit` 等类别的脱敏未做同法真值验证）；② ~~未验证**值形态脱敏**（如任务标题里人为写入 `sk-…` 形态的串是否被替换——那属 `redact_payload` 的**值扫描**能力，10.4 已在单测层验证，但**未在 worker 生成的真实包上验过**）~~ ⇒ **已验证（见下条）**；③ 未验证 `truncated_categories` / `unavailable_categories` 的真实数据行为。
- **值形态脱敏验证 —— `redact_payload` 的值扫描在 worker 真实链路生效（2026-09-19）**。为什么单列：10.4 只在**单测层**验证过「值内凭据会被替换」，而**生产链路（worker 生成的真实导出包）从未验过**——两者可能因装配差异而不一致。做法：种一条**标题里含凭据形态**的任务（`title='测试 sk-ABCDEFGHIJKLMNOPQRSTUV 令牌泄漏'`）⇒ worker 生成导出包 ⇒ 读取包内该行的 title 字段：
      - **`title_val=测试 [已隐藏] 令牌泄漏`** —— 标题里的 `sk-ABCDEFGHIJKLMNOPQRSTUV` **被替换为 `[已隐藏]`**（值扫描真的在起作用，而非只按键名屏蔽）。
      - **`leak_raw_cred=false`** —— 原始凭据串**未出现在 payload 任何位置**；`masked_present=true` —— 掩码文本存在。
      - **结论**：宪法「敏感信息防线」要求的**值形态脱敏**在**真实 worker 链路**上得到验证（此前只有单测层证据）。
      - **清理（已核对无残留）**：DELETE 导出包 / 作业 / 任务 / 租户 ⇒ 复查 `smoke%` 计数 **`0/0/0`**；容器无 `wb-w7/wb-b7` 残留。
      - **未验证（本条）**：① 只验了 **`sk-` 前缀**这一种凭据形态（10.4 登记的模式集还有 `Bearer` / `ghp_` / `glpat-` / `xox?-` / `AKIA` 与 `k[:=]v` 形态，**未逐个在真实包上验**）；② 只验了 `tasks.title` 一个字段位置（其他类别的自由文本字段未验）；③ 未验证**掩码幂等**在真实链路上的表现（单测层已验）。
- **🔴🔴 真机验证抓出第二个生产向真缺陷并已修复：worker 侧删除租户**漏清记忆层**（2026-09-19）**。这是本轮最有价值的发现，也是「真机验证」相对「单测全绿」的又一次证明。
      - **现象（查库所得）**：用冒烟租户构造一条满足全部前置的删除作业（`cooling_down` + `execute_after` 已过 + `final_exported=true` + `confirmed_by='smoke'`，租户状态 `deleting`），worker+beat 跑 45 秒后：`job=completed`、`tenant=deleted`、知识文档 `docs_left=0`（**清场本身在工作**），**但审计 `cleared=["knowledge_governance","skills"]` —— `memories` 不在列表里**。
      - **根因**：`app/worker.py` 的 `configure_runtime` 里，`memory_store=build_memory_store(...)` **只传给了 `build_export_readers`（导出读取器）**，**没有传给 `build_commercial_components`** ⇒ 生命周期服务 `memory_store=None` ⇒ `execute_delete` 清场循环跳过记忆层。**同一函数里两处都用它，漏了一处**。后果：**生产删除租户时记忆层数据残留**（导出包能出 `memories` 数据、删除却不清它 ⇒ 两面不一致，属合规问题）；B-3 之前更严重（`memory_store` 一直是 `None` ⇒ 当时"只清记忆层"的注释与实际相反）。
      - **修复**：把记忆层仓储提为局部变量、**一次构造两处复用**（导出读取器 + 生命周期清场），并给 `build_commercial_components` 补 `memory_store=memory_store`；同时更正 worker.py 里那句过时注释。
      - **真机复验（决定性证据）**：重建镜像（`workbench-app:fixmem`）后同法重跑 ⇒ **`cleared=["knowledge_governance","memories","skills"]` 三面齐全**、`job=completed`、`tenant=deleted`。**修复前两面、修复后三面**，同一脚本同一比对。
      - **守护与反假（含一次「弱判据」自暴露）**：新增 `tests/test_worker_runtime_wiring.py::test_production_runtime_injects_all_three_tenant_purge_channels`（静态钉住 `configure_runtime` 装配：三个 `*_store` 参数**必须存在且不得传 `None`**；且记忆层仓储只构造一次）。**反假三轮**：① 首版判据只查 `memory_store=` 子串 ⇒ 写 `memory_store=None` **仍然通过**（**反假当场暴露判据太弱**）；② 收紧为「非 None」但**未限定调用体** ⇒ 被同函数里 `build_export_readers(memory_store=...)` 那一行蒙混通过；③ 最终用正则圈定 `build_commercial_components(...)` 调用体 + 排除 `None` ⇒ **探针精确变红**（`worker 装配的 memory_store 缺失或传了 None`），还原后复绿。**这条用例的价值在反假过程中才真正建立**。
      - **清理（已核对无残留）**：审计 / 作业 / 知识文档 / 租户按序删除 ⇒ `smoke%` 计数 **`0/0`**；容器无 `wb-w9/wb-b9` 残留。
      - **未验证（本条）**：① 复验用的仍是**冒烟租户 + 测试库 + staging 环境**；② ~~只验证了 `cleared_categories` **包含三面**，**未核对每面"确实被清空"**（仅知识文档查了 `docs_left=0`；记忆层与技能层的清空未逐面取证）~~ ⇒ **已兑现（2026-09-19，见下条「第三个生产向真缺陷」）**：逐表取证后发现**每面只清了一张主表**（记忆层的规则 / 画像、技能层的绑定均残留），已修复并真机复验「六张表全 0」；③ ~~API 侧是否也有同类漏注入未核~~ ⇒ **已横向排查（同日）**：`app/main.py:278-286` 的 `build_commercial_components` 调用**三个参数齐全**（`memory_store=memory_service.store` / `skills_store=skills_service.store` / `knowledge_store=knowledge_governance_service.store`）⇒ **API 侧无同类缺陷**。
      - **全量回归**：后端 `2558 passed / 0 failed / 0 skipped`（真库 DSN；基线 2557 ⇒ **＋1 恰为本批守护用例**）。
      - **同类缺陷横向排查（2026-09-19，逐项核对，未发现其他）**：按「worker 执行的每个周期任务所需依赖是否都已注入、且**面是否齐全**」逐条核对：① **10 个周期任务全覆盖**——`outbox-publisher`（publisher ✓）/ `lifecycle-jobs` 与 `export-packages-purge`（lifecycle ✓，含本轮修复的三面清场）/ `knowledge-review-scan`（governance ✓）/ `runtime-events-purge`、`conversation-stream-purge`、`run-artifacts-purge`（三个 purger ✓）/ `crm-health-recompute`、`crm-activity-reminder`、`crm-renewal-window`（**CRM 的 service 与 notifier 都真注入**，`app/worker.py:219-222`，不是传 `None`）。② **两侧读取器参数集一致**：`main.py:267-277` 与 worker 的 `build_export_readers` **同为 9 个具名读取器**（memory / workforce / knowledge / audits / runs / accounts / tasks / artifacts / steps；`usage` 均由 `build_commercial_components` 用本进程账本自接）⇒ **无"一侧少一类"**。③ 结论：**「装配漏注入」这一类缺陷仅本轮修复的那一处**，其余装配点均已核对。
- **🔴🔴 第三个生产向真缺陷（同类但更隐蔽）：清场「审计报面名齐全、实际每面只清一张表」——已修复（2026-09-19）**。本条正是上面那条**登记未验证项②（「未核对每面『确实被清空』」）**的兑现：把「面」按**迁移里的实体表**展开逐表取证，就抓到了它。
      - **做法（逐表取证）**：新增一次性脚本 `tmp/delete-purge-faces.py`（`seed` / `count` / `clean` 三模式），种**六张表**各一行——记忆层 `workbench_memory_facts` / `_rules` / `_profile_keys`、技能层 `workbench_skills` / `workbench_skill_bindings`、知识层 `workbench_knowledge_documents`——外加一条满足全部前置的删除作业（`cooling_down` + `execute_after` 已过 + `final_exported` + `confirmed_by`，租户 `deleting`）⇒ 真实 worker/beat 跑一轮（`deletions=1`）⇒ **逐表查库计数**。
      - **现象（决定性证据，修复前）**：`job=completed`、`tenant=deleted`、审计 `cleared_categories=["knowledge_governance","memories","skills"]`（**三面齐全**），但逐表计数为 **`facts=0 / rules=2 / profile_keys=2 / skills=0 / bindings=1 / docs=0`** ⇒ **记忆层只清了事实表、技能层只清了技能包表**。危害比上一条更隐蔽：审计里**面名一个不缺**，看审计根本无法发现残留（而 `list_all_for_tenant` 这类读方法本身也只读一张表，单测与走查会「看起来全对」）。
      - **根因**：两处 `delete_all_for_tenant` 都只删「主表」——记忆层在 `029` 迁移里是**三张表**（事实 / 规则 / 身份类画像），而 `Postgres|InMemoryMemoryStore` 只删 `workbench_memory_facts`；技能层在 `030` 迁移里是**两张表**（技能包 / 数字员工绑定，`PostgresSkillStore` 的 docstring 亦自述两表），而 store 只删 `workbench_skills`。
      - **修复（四实现）**：`app/memory/store.py`、`app/skills/store.py` 的**内存 + PG 两个实现**一律改为**整层清场**（PG 在同一事务内多表 `DELETE` 并累计 `rowcount`；内存同清对应字典），返回值语义统一为「删除行数合计」；两个 Protocol（`MemoryExportStore` / `TenantPurgeStore`，`app/commercial/lifecycle.py`）的 docstring 写明 **「整层 = 该层在迁移里的全部租户级表」**，避免后来者再按「主表」理解。
      - **测试与反假（先红 → 绿）**：新增 **4 条** + 加严 **1 条**——真库记忆层逐表查库（`test_memory_postgres.py::test_lifecycle_delete_clears_whole_memory_layer`）、真库技能层逐表（`test_skills_postgres.py::test_lifecycle_delete_clears_bindings_too`）、内存记忆层（`test_memory_layer.py`）、内存技能层（`test_skills_layer.py`）**各 1 条新增**；B-3 集成用例加严 1（`test_commercial_lifecycle.py`：清场助手由「各层一行」扩为**整层各表一行**，断言按表逐张核）。**先红 5 failed**（报错精确指向残留表）→ 修复后 **5 passed**；**反假一轮真变红后还原**：① PG 记忆清场只留事实表 ⇒ 真库用例红；② 内存技能清场去掉绑定 ⇒ 内存用例红 **且** B-3 集成用例同时红（正是「面名盲区」被打中）。受牵连文件级回归 `163 passed`。
      - **真机复验（决定性证据）**：重建镜像（`workbench-app:fixlayer`）⇒ 同一脚本、同一比对 ⇒ **六张表全 0**（修复前 3 张残留 2/2/1）；`deletions=1`、`job=completed`、`tenant=deleted`、审计三面不变。⚠️ 过程中先出现过一次 `deletions: 0` —— 原因是**上一轮的旧镜像容器（`wb-w10`）仍在跑并抢先消费了同一 broker 上的作业**；停掉旧容器后重跑即得预期结果。**登记为纪律：真机复验前必须先确认没有其它 worker 容器仍在消费同一 broker/队列**（否则复验结论可被上一版镜像的行为污染）。
      - **清理（已核对无残留）**：脚本 `clean` 后六表 `0/0/0/0/0/0`、作业/租户/审计均为空；容器 `wb-w10` / `wb-b10` / `wb-w11` / `wb-b11` 全部 `docker rm -f`。
      - **全量回归（改动冻结后）**：后端 **`2563 passed / 0 failed / 0 skipped`**（真库 DSN；基线 2558 ⇒ **＋5 = 本批 4 条新用例 + 1 条静态缺陷守护**；`tmp/pytest-purge-faces-final.log` 为本次输出落盘）。
      - **顺带修掉的第三处（同一文件内的静态缺陷）**：`InMemorySkillStore.list_bindings` 的排序键原写作 `b.created_at or parse_epoch(b)`，而 **`parse_epoch` 在本仓库并不存在**（静态检查 `F821 undefined name`；同文件另三处同类排序用的是 `_dt_or_min`）⇒ 只要某行 `created_at` 为空就抛 `NameError`。正常路径因 `SkillBinding.created_at` 有 `default_factory=now` 而**从不触发** ⇒ 一直潜伏。已改为 `_dt_or_min(b.created_at)`（与三处同类排序一致；正常路径排序结果不变），并加用例 `test_skills_layer.py::test_list_bindings_tolerates_missing_created_at`（**先红**：`NameError: name 'parse_epoch' is not defined` → 修复后绿）。**登记：本仓库未接 ruff（无配置、CI 无该 job）⇒ `F821` 这类静态缺陷不会被任何既有机制拦下**（本次是顺手跑一次静态检查才发现的）。
      - **未验证（本条）**：① 仍是**冒烟租户 + 测试库 + staging 环境**（未在 production / 真实集群复跑）；② 「整层」的圈定口径 = **该 store 在迁移里拥有的租户级表**；同域但**属于别的 store** 的表不计入（下条登记）；③ 未在真库上演练「大租户多行清场」的耗时与锁行为（本次每表 1–2 行）。
- **清场 / 导出覆盖面普查：55 张租户级表里清场覆盖 6 张（三面）—— 差距登记 + 待拍板选项（2026-09-19，本轮只登记不改码）**。按「面名 ✕ 表」同一方法做了全库普查（依据：测试库 `information_schema.columns` 实测，**全平台含 `tenant_id` 的表 55 张**）。
      - **① 清场覆盖（6/55）**：`workbench_memory_facts` / `_rules` / `_profile_keys`、`workbench_skills` / `_skill_bindings`、`workbench_knowledge_documents`（本轮修复后**层内完整**）。
      - **② 未覆盖但同属「知识」域的一张表**：`workbench_knowledge_access_bindings`（迁移 `004`，`(tenant_id, binding_type, binding_key, knowledge_base_id)` + `granted_by`）是**租户级授权配置**，归 `app/knowledge_policy.py` 的仓储，**不在**商业化生命周期注入的三面里 ⇒ 租户删除后授权绑定残留（与本轮修复的「绑定残留」同类，只是归属另一个 store）。同域的 `workbench_knowledge_access_audits`（`005`）为**追加式审计**，与 `workbench_audit_log` 同口径（10.1/10.3 判据：不可删），建议**明确登记「不清」并写进口径**而不是静默留着。
      - **③ 剩余 47 张租户级表**（= 55 − 6 − 2，**逐表枚举**：`accounts` / `audit_events` / `audit_log` / `content_publications` / `conversations` / `conversation_members` / `conversation_messages` / `conversation_stream_frames` / `conversation_stream_state` / `customer_admins` / `dead_letters` / `digital_employees` / `eval_cases` / `eval_runs` / `eval_case_results` / `event_outbox` / `execution_idempotency` / `export_packages` / `inbox_items` / `job_roles` / `lifecycle_jobs` / `orchestration_proposals` / `plan_proposals` / `plan_versions` / `retention_policies` / `run_records` / `run_artifacts` / `run_acceptance_decisions` / `run_promotions` / `runtime_states` / `runtime_events` / `tasks` / `tool_actions` / `usage_ledger` / `workspaces` / `crm_accounts` / `crm_activities` / `crm_contacts` / `crm_contracts` / `crm_field_defs` / `crm_insights` / `crm_leads` / `crm_opportunities` / `crm_opportunity_stage_events` / `crm_quote_lines` / `crm_quotes` / `crm_targets`，均为 `workbench_` 前缀）**均不在清场范围内** —— 该项**此前已登记**为「其余属后续专项」（见 10.7 未验证段③、真源 `commercial-g0-design.md:113`「业务数据、对象存储文件、向量索引和缓存按策略清理」），本行只是把**表级证据与数量**补全。**建议按面分批推进（每批：契约面名 → store 的整层清场 → 逐表取证 → 反假）**：第 1 批 会话/消息/流帧；第 2 批 任务/运行/产物/步骤/验收/沉淀；第 3 批 CRM；第 4 批 其余小表；**审计类（`workbench_audit_log` / `workbench_audit_events` / `workbench_knowledge_access_audits`）按真源 §6.2「保留最小必要元数据」单独口径**，不做物理删。
      - **④ 导出面与删除面口径不一致（同一盲区的另一侧）**：真源 §6.1 的导出项含**「记忆」**，而 `memories` 类别实际**只导出事实表**（`memory_export_reader` → `list_all_for_tenant`）—— 规则表与身份类画像表**不入包**，与本轮已改为「整层三张表」的删除面**口径不一致**。修法形状经核查**只有一个合规选项**：真源 §6.1 + `EXPORT_RESOURCE_CATEGORIES` **把 15 类集合固定**（`app/commercial/lifecycle.py:24-44` 注释「真源 `commercial-g0-design.md:102-108`（§6.1）的导出类别清单」）⇒ 新增独立类别与真源冲突，**只能在 `memories` 行内加判别字段**（`kind`）。⇒ **已按该形状实现（见下条）**。
- **导出面补齐：`memories` 由「只出事实」改为**整层三类**（2026-09-19，承接上条 ④）**。判据：真源 §6.1 的导出项「记忆」= 记忆层，而 `029` 迁移自述记忆层是「三类记忆（身份 / 规则 / 事实）」；删除面已按整层三张表清场 ⇒ 两面口径必须一致。
      - **契约先行**（`docs/api-contract.md`）：新增「`memories` 整层口径（2026-09-19）」段——三类行的字段与判别字段 `kind`（`fact` / `rule` / `profile`）、分组与组内排序（`fact` → `rule` → `profile`；组内 `created_at, memory_id` / `owner_kind, owner_id, profile_key`）、**只出 `active`**（`superseded` 历史版本不入包，与既有事实类口径一致）、全量读取无分页的边界登记；上一条「已接线的类别与字段口径」里的 `memories` 同步改为三类口径（**事实类字段逐字不变**，只新增 `kind`）。
      - **实现**：`MemoryStore` 协议新增 `list_rules_for_tenant`（只 `active`）/ `list_profiles_for_tenant`，**内存 + PG 两实现**（PG 规则读取沿用 `_RULE_COLUMNS` + `version` 两段拼装与 `_hydrate_rule_with_version`；画像读取按 `(owner_kind, owner_id, profile_key)` 排序）；**PG 侧 `list_all_for_tenant` 顺带补 `ORDER BY created_at, memory_id`**（原先**无排序** ⇒ 同一份数据两次导出可能顺序不同，与契约「顺序确定」不符）；`memory_export_reader` 合并三类、总数 = 三类之和（**不拿返回条数冒充**）、上限按合并后的整层列表切片。
      - **测试与反假（先红 → 绿）**：新增 **3 条**（真库三类读取 1〔含 `superseded` 不入包 + 版本号 + 两次读取逐字相同〕、内存三类读取 1、读者三类 1〔三类字段集各不相同 + 分组顺序 + 上限切片 + 不跨租户 + 两次读取逐字相同〕）；另**改造既有装配用例的假仓储**：原先只实现 `list_all_for_tenant` 的假对象在读者改读三张表后 `AttributeError`，被既有断言当场抓到（`assert ['memories'] == []` —— 该类被如实放进 `unavailable_categories`，**fail-loud 生效**）⇒ 改为**真实内存仓储**（该文件 docstring 本就要求「用真实存储而不是假对象」）。**反假两轮真变红后还原**：① 读者还原成「只出事实」⇒ 读者用例红（`['fact','fact'] != ['fact','fact','rule','rule','profile','profile']`）；② 内存规则读取去掉「只出 active」⇒ 内存用例红（旧版进了列表）。
      - **真机取证（决定性证据，脚本 `tmp/memory-export-kinds.py`）**：种 `active` 租户 + 导出作业（`kind=export`、`status=queued`）+ 三类数据（事实 1、规则 2 版、画像 1）⇒ **真实 worker/beat** 跑一轮（worker 日志 `{'exports': 1, 'deletions': 0}`）⇒ 读 `workbench_export_packages.payload`：**`rows=3`、`kinds=['fact','rule','profile']`**、`fact_content_ok=True`、`rule_only_active=True`（只出 v2）、**`superseded_absent=True`**（旧版确未入包）、`profile_fields_ok=True`、**`unavailable=[]`**（worker 侧三类读取全部成功，不是「读不到 ⇒ 空」）。**清理已核对无残留**：包/作业/事实/规则/画像/租户 计数 `0/0/0/0/0/0`；容器 `wb-w12` / `wb-b12` 已删。
      - **全量回归（改动冻结后）**：后端 **`2565 passed / 0 failed / 0 skipped`**（真库 DSN；上一基线 2563 ⇒ **＋2 = 本批新增的内存 / 真库两条读取用例**（读者那条是**改写**既有用例、净增 0）；`tmp/pytest-export-kinds-final.log` 为本次输出落盘）。
      - **未验证（本条）**：① 仍是**测试库 + staging 环境**（未在 production 复跑）；② 只核 `memories` 一类（其余类别的真机核对见前序条目）；③ **「导出 `superseded` 历史版本」未做**（已写入契约作为口径变更项，需另议）；④ 前端**未改**（导出包是文件下载、载荷形状变化不影响页面；前端套件本次未跑——本批不涉前端代码）。
- **修复：运行详情页「使用指南」文案过期（2026-09-19，用户可见缺陷）**。① **现象**：`admin-web/src/app/guides.ts` 的 `run` 指南写着「『交付』里点不动 → **验收确认与沉淀功能尚未交付**，本页只做展示」，且第 1 段称「你能发起的动作**只有**暂停 / 恢复 / 取消」——而 S2 早已交付验收决议与沉淀入口（`features/runDetail/AcceptanceDecisionPanel.tsx`：「确认完成」/「打回重做」/「存成任务」，并有 `RunDetailPage.test.tsx` 守护）。② **影响**：指南**比实际能力少说** ⇒ 用户读了会以为功能不存在而不去用（属「能点却不知道能点」的可用性缺陷，非功能缺陷）。③ **修复**：改第 1 / 3 段与退出路径，逐字对齐按钮标签，并写明前置条件（运行已结束 + 仅发起人 / CEO / 超管可验收）；**未交付的「设为自动化」仍如实标注**（留在页面内行说明，不进指南）。
      - **守护与反假**：新增 `admin-web/src/app/guides.test.ts`（3 条：run 指南必须提到三个已交付动作标签 / **不得把已交付的验收能力说成未交付**（正则钉住「验收」与「尚未交付」不同句）/ 每份指南都是完整四段）。**反假一轮真变红后还原**：把过期句子塞回 exit ⇒ 第 2 条红（`× 运行详情指南不得把已交付的验收能力说成未交付`）。**为什么原有机制守不住（登记）**：`check:ui-copy` 只查开发术语泄漏，`tsc` 只保证每页都有指南（`Record<AppView, Guide>`），**都不校验「指南说的能力」与「实际交付的能力」是否一致**。
      - **前端回归**：`vitest` **353/353**（基线 350 ⇒ ＋3 恰为本批新用例）、`tsc --noEmit` 干净、`check:ui-copy` 通过、`check:contrast` 通过、`build` 成功。
      - **未验证（本条）**：~~其余 19 份指南未逐份复核~~ ⇒ **已复核（见下条：指南逐份审计）**；`companion-pwa` / `desktop` 两端的用户文案**未纳入本次核查**。
- **使用指南逐份审计：16 份指南查出 10 处缺陷并修正（2026-09-19）**。做法：把 `GUIDES` 每份指南的四段文案与**对应页面组件的真实渲染分支 / 按钮文案 / 空态**逐条对照（`AppShell.VIEW_META` + `App.tsx` 路由 → `features/*` 页面组件），只认代码里真实存在的文案。**缺陷分两类**：**A 类「少说」**（页面已交付的能力指南完全没提，用户读了以为不存在）与 **B 类「多说」**（指南承诺了页面上并不存在的按钮/动作，用户按指南找不到）。
      - **A 类 6 处（已补进指南）**：① `home` 未提「**或直接建任务**」入口与「待我审批」指标卡；② `conversation` 未提会话头部「**归档 / 导出 / 删除**」与会话分享；③ `billing` **只讲只读汇总，完全没提 B-1 已交付的「数据导出」区**（申请导出 / 生成状态 / 列表 / 下载 / 7 天有效）；④ `crmAccounts` 未提详情页「**生成跟进计划**」与联系人「**查看明文**」；⑤ `crmContracts` 未提「**提交待签**」（登记签署的前置动作）与「**作废合同**」；⑥ `crmProgress` 未提「**我的 / 全量**」统计范围切换。
      - **B 类 4 处（已改正）**：⑦ `knowledge` 原称「开关改完**立即生效**」——实际须点「保存」（`canSave = selectedIds !== initialIds`，无改动时保存按钮禁用）⇒ **这是最危险的一处**：管理员会以为权限已改完；⑧ `crmQuotes` 原称单价「**按分输入**」——实际输入框是**按「元」**（表头即「单价（元）」）；同页还承诺「新建一份报价」，而**列表只有刷新、没有新建入口**；⑨ `audit` 原称「点开一条看明细」——行**不可点击**，明细本就内联展示；⑩ `settings` 退出路径称「在帮助与反馈里**说一声**」——该分组只有起步路径/快捷键/各页指南标题/版本，**没有反馈入口**。
      - **子代理结论须复核（本轮实测教训）**：审计由子代理产出，其给出的两处文案**与代码不符**（把「或直接建任务」写成「新建任务」；称知识页「刷新」不存在——实际 `:190` 有「刷新」）⇒ 我**逐条回读页面代码后才落笔**，并把「改动未保存时页面会提示」改为有据可依的「没有改动时保存按钮不可点」。**登记为纪律：子代理给的文案标签一律要回读原文件确认。**
      - **守护与反假**：`admin-web/src/app/guides.test.ts` 从 3 条扩到 **4 条**，新增一条锚点用例（billing 必含「数据导出 / 申请导出」；knowledge **不得**含「立即生效」；crmProgress 必含「我的 / 全量」；audit **不得**含「点开一条看明细」；settings **不得**含「说一声」；conversation 必含「删除 / 导出」）。**反假一轮真变红后还原**：把「立即生效」塞回 knowledge ⇒ 该用例红。
      - **前端回归**：`vitest` **354/354**（基线 353 ⇒ ＋1）、`tsc --noEmit` 干净、`check:ui-copy` 通过、`check:contrast` 通过、`build` 成功。
      - **未验证（指南审计）**：① 只核了**管理台 16 份指南**——`companion-pwa`（伴侣端）与 `desktop`（桌面端）的用户可见文案**未纳入**；② 判定依据是**页面代码里的渲染分支与文案**，**未在真实浏览器逐页点过**（属走查项，本机未跑）；③ 指南是 Markdown 纯文本渲染，本次修正**未加粗/未加链接**（避免出现字面 `**`）。
- **伴侣端 / 桌面端用户文案审计：查出并修正 2 处登录语义缺陷（2026-09-19）**。把同一套「文案 vs 已交付能力」审计（A 类少说 / B 类多说）应用到另两个客户端：`companion-pwa/src` 约 40 个文案落点、`desktop/src` 全部字符串。
      - **🔴 修正 ①（B 类，登录提示误导）**：`companion-pwa/src/features/session/LoginPage.tsx:11` 原先**所有 401 一律显示**「手机号、密码或动态验证码不正确」；而服务端把两种情形区分成**受控 detail**——`app/main.py:4774`「需要动态验证码」（已绑定 TOTP 却未填码，并非填错）与 `:4776`「动态验证码不正确」。**影响**：用户会把「只是没填码」误当成「密码错了」而反复重试，**越试越触发登录限流**（同一手机号 5 分钟窗口失败达阈值即 `429`）。**修法**：按服务端受控 detail 分别给出可操作提示；其余 401 与接口契约一致，统一为「手机号或密码不正确」（不区分原因，避免泄露账号是否存在）。
      - **🔴 修正 ②（A 类，受限会话被当成功）**：`LoginPage.tsx:75` 原标签写「动态验证码（**如已绑定**）」暗示可选；而管理员（`ceo` / `super_admin`）未绑定 TOTP 时服务端签发的是 **受限会话 `scope=totp_enrollment`**（`app/auth.py:14`，只放行 `/auth/me/totp*` 等），而伴侣端**既丢弃 `scope`（`app/session.ts:5-11` 的 `Session` 无该字段）又没有绑定入口** ⇒ 登录"成功"后**待办与通知全部 403**，用户完全不知道为什么。**修法**：`scope === 'totp_enrollment'` 时**不保存会话**并如实说明「请先在网页管理台完成绑定」；标签改为「已绑定的账号必填」。
      - **守护与反假**：`companion-pwa/src/features/session/LoginPage.test.tsx` 从 2 条扩到 **4 条**（新增：`需要动态验证码` ⇒ 提示含「已绑定动态验证码」且**不再**显示成密码错；`动态验证码不正确` ⇒ 如实区分；`scope=totp_enrollment` ⇒ **不保存会话**、不回调、提示含「需要先绑定动态验证码」）。**既有 1 条用例的期望文案同步更正**（原期望正是那处误导文案）。**反假一轮真变红后还原**：去掉 TOTP 分支 ⇒ 新用例红。
      - **回归**：伴侣端 `vitest` **60/60**（基线 58 ⇒ ＋2）、`tsc --noEmit` 干净、`build` 成功；后端全量（真库 DSN）**2545 passed / 0 failed / 0 skipped**（本批**未改后端**，为确认无牵连而复跑）。
      - **未验证（伴侣端 / 桌面端）**：① `desktop/src` **零用户可见字符串**（只有 2 条控制台日志）⇒ **无文案可审**；其「未签名 / 更新链路未验收」已由 README 与 `electron-builder.yml` 如实标注（非界面文案）；② 伴侣端其余文案（审批 / 通知 / 对话 / 外壳）经代码对照**未发现缺陷**，但**未在真机浏览器逐页点过**；③ 本次只核**文案与实现是否一致**，**未核**伴侣端功能是否满足交付标准（如真机安装、Web Push——后者属组 7 外部项）。
- [x] 10.6 生产是否**强制 `state_postgres`**（内存态仅限 development）
      - **结论（2026-09-16 自查 + 最小加固）：判据成立——生产强制链完整（配置层 + 装配层 + worker + 部署口径 + 预检 + 守护测试），边界已按用户拍板 fail-closed 收口 ⇒ 勾选。** ① **配置层**：`validate_runtime_settings` 非 development 强制 `storage_backend == "postgres"`（`app/settings.py:667-689`；`:676-677` 抛「生产环境必须使用 PostgreSQL 持久化仓储」、`:678-679` 内容仓储禁 memory、`:680-681` 数据库地址必须 PostgreSQL）；`env` 为**精确比较** ⇒ 任何非 `"development"` 值（含拼错 / 大小写偏差）都走强制分支（fail-closed 方向）。② **API 主进程（顺序保证）**：`app/main.py:154` 模块级先校验 ⇒ `:202` 再 `build_runtime_state_store(settings)` ⇒ `:213-220` 显式注入 `build_runtime_service(state_store=…)` ⇒ 生产必为 `PostgresRuntimeStateStore`。③ **装配层双保险**：`build_runtime_state_store` 的 memory 分支非 development 直接 `ValueError("生产环境禁止使用内存运行时状态仓储")`（`app/bootstrap.py:584-587`）；postgres 分支返回 `PostgresRuntimeStateStore`（`:588-598`）。④ **worker**：`configure_runtime` 非 postgres ⇒ `ValueError("Worker 必须使用 PostgreSQL")`（`app/worker.py:66-67`；守护 `tests/test_outbox.py:195`）；运行事件 purger 同为 Postgres（`:91-96`）；惰性装配 `_ensure_runtime`（`:170-185`）development 不装配、非 development 首次任务时装配、失败不吞。⑤ **beat**：不装配 runtime（`docker-compose.app.yml:128-134`）。⑥ **部署口径与预检**：compose 三服务（app `:38-39` / worker `:89-90` / beat `:139-140`）全部 `WORKBENCH_ENV=production` + `WORKBENCH_STORAGE_BACKEND=postgres`；`scripts/commercial_g0_preflight.py:63-73`、`scripts/worker_preflight.py:136-146` 双强制；`.env.staging.example:10` 为 postgres。⑦ **测试守护**：`tests/test_persistence_contract.py:12-16`、`tests/test_runtime_state_write_through.py:223-231`/`:234-245`、`tests/test_outbox.py:195`。
      - **边界与最小加固（2026-09-16 用户拍板）**：`build_runtime_service` 原 `state_store or RuntimeStateStore()` 缺省兜底**不带 env 校验** ⇒「生产强制」此前靠装配注入而非 fail-closed（唯一生产调用点 `app/main.py` 已注入、无实际触发点，但未来新增生产入口漏传会**静默回内存且无报错**）。现改为：非 development 且未注入 ⇒ 装配期抛 `ValueError("生产环境必须显式注入运行时状态仓储（禁止内存回退）")`（`app/bootstrap.py:631-632`，docstring 同步 `:623-624`）；守护 `tests/test_runtime_state_write_through.py:248-260`（**先红** `DID NOT RAISE` 后绿；**反假两轮**均按要求变红并还原：去抛错 ⇒ 本用例红；条件反转 ⇒ 本用例 + `tests/test_runtime_registry_wiring.py` 3 用例红）。`registry.py:130` / `service.py:68` 内层兜底**未改**（生产链路均经 `build_runtime_service` 注入；单测直接构造场景保留）。**保留边界（登记，属设计选择）**：`app/runtime/staging.py:31` 冒烟脚本用内存 store（`python -m app.runtime.staging` 工具，非生产链路）；worker 强制时机为「首次任务执行」而非进程启动（fork 安全的惰性装配）。
      - **全量回归（含真库）**：`2150 passed / 0 failed / 0 skipped`（junit `tests="2150" errors="0" failures="0" skipped="0"`；基线 2149 ⇒ ＋1，无既有用例被跳过或删除）；`compileall` 退出码 0。
      - **未验证（不得读成已验）**：① staging / 生产**真实环境**的部署配置未验收（属组 1.2 外部任务；本仓库 compose 已硬编码 production + postgres）；② worker 惰性装配的**生产路径本机未实跑**（静态核查 + 守护测试覆盖异常文本）。
- [ ] 10.7 是否存在**用户数据导出 / 删除**路径（宪法九章）；若无，明确归档与保留策略的补位口径
      - **结论（2026-09-16 自查 + 加固两项）：导出 / 删除路径均存在但覆盖不全；本轮按用户拍板补「导出包取回端点 + 过期清理」⇒ 仍不勾选（差距如实登记，不改码）。** ① **导出面**：真源 15 类导出类别已定义（`app/commercial/lifecycle.py:25-41`），但**仅 `memories` 已接线**（记忆层生命周期通道 `:264-276`），其余 14 类一律空数组（`UNIMPLEMENTED_EXPORT_CATEGORIES`，`:42-43`，注释自证「均无现成读取方法……不臆造字段」）。② **删除面**：`execute_delete`（`:355-393`）物理清场**仅清记忆层**（`:380-383`）；`skills` / `knowledge_governance` 的租户级 `delete_all_for_tenant`（`app/skills/store.py:80`、`app/knowledge_governance/store.py:80`）**已具备但未接线**；**「记录确认人」未实现**（真源 `docs/superpowers/specs/2026-09-06-commercial-g0-design.md:114` 要求「删除前必须生成最终导出包并记录确认人」，全仓无确认人落库点）。③ **保留面**：`DEFAULT_RETENTION_POLICY`（`:19`）**仅被部署预检读取、不被服务侧消费**（`:16-18` 注释自证）⇒ **保留策略无服务侧执行器**（与 10.1 同证据）。④ **用户级面**：commercial 路由面（`app/main.py:900-1000`）**均为租户级**，**无用户级个人导出 / 删除路径**（宪法九章「用户可删除、可导出」属差距）。
      - **本轮加固（用户 2026-09-16 拍板「两项都做」）**：**a) 导出包取回端点** `GET /api/v1/commercial/exports/{package_id}`（契约先行 `docs/api-contract.md:146-148`）——admin-only、租户由服务端从登录上下文解析、**跨租户与不存在统一 `404`**（不泄露他租户资源是否存在）、`expires_at <= now` 过期返 `404`「导出包已过期」；实现：异常 `ExportPackageExpired`（`app/commercial/lifecycle.py:57-64`，继承异常族但语义为 `404`、路由**先捕获**；先例 `DeletionNotPending → 409`）+ 服务 `get_export_package`（`:303-317`，`_ensure_admin` + 租户限定取包）+ 路由（`app/main.py:945-973`）。**b) 过期清理**：worker 任务 `purge_export_packages`（`app/worker.py:253-267`，复用既有 `_lifecycle_runner` 注入点、**未接线返回 0**）+ beat 排程 `export-packages-purge`（`:137-140`）+ `export_package_purge_interval_seconds`（默认 3600、范围 30–604800，`app/settings.py:538-546`）+ 两存储实现（InMemory `:202-209` / Postgres `:599-611` 单条 DELETE，迁移 028 既有索引支撑）；过期口径 = 包自身 `expires_at`（完成时刻 + 7 天，`EXPORT_PACKAGE_TTL` `:48-50`），清理后与「从未存在」不可区分；compose / 两 env 模板已同步（beat 与 worker 同值）。
      - **测试与反假**：+13 条（lifecycle 6 / API 2 / worker 4 / 真库 1）；**反假两轮真变红后还原**——① 去过期校验 ⇒ `DID NOT RAISE ExportPackageExpired` + `assert 200 == 404`；② 去租户归属限定 ⇒ `DID NOT RAISE ResourceNotFound` + `assert 200 == 404`；**全量回归（含真库）`2165 passed / 0 failed / 0 skipped`**（基线 2152 ⇒ ＋13）、`compileall` 退出码 0。
      - **B-1 补链（2026-09-19）：导出包「发现」路径 + 管理台接入取回**。原缺口（本条 2026-09-16 加固后仍存在）：取回端点要 `package_id`，而申请响应（`LifecycleJobView`）与作业视图**都不携带**它、也没有列表端点 ⇒ **客户端无法发现包号**，取回能力实际不可用。本轮：契约先行新增 `GET /api/v1/commercial/exports`（`docs/api-contract.md:146-148`）——**admin-only + 租户服务端解析 + `limit` 1–200 / `offset` ≥ 0 分页（非法值 `422`）+ 列表不含载荷**，顺序 `created_at DESC, id DESC`（同刻按包号倒序 ⇒ 确定），且**如实包含已过期但未被清理的包**（客户端按 `expires_at` 标注，取回仍 `404`）；实现：`ExportPackageSummary`（列表面元数据，`app/commercial/lifecycle.py:95-108`）+ `ExportPackageStore.list_for_tenant` 内存/PG 双实现（`:232-243` / `:655-685`，PG 侧 SELECT **不含 `payload` 列**）+ 服务 `list_export_packages`（`:362-373`）+ 路由（`app/main.py:1054-1089`）。管理台「用量与费用」页新增「数据导出」区块（`admin-web/src/features/billing/UsageBillingPage.tsx`）：申请导出 → **认服务端作业状态**确认生成（首次即时查 + 每 3 秒复查、最多约 2 分钟，超时如实告知「仍在生成」）→ 列表回流 → 按包号取回并**落盘为文件**（载荷不进页面）；四态齐备（加载 / 空 / 错误 / 无权限），过期包标注「已过期」且**不提供下载**，租户删除申请与保留策略设置**不摆假按钮**、在页面如实说明属下一期。**无迁移**（复用 028 既有索引 `(tenant_id, created_at)`），**无权限模型变更**（与取回端点同一 admin 口径）。
      - **测试与反假（B-1）**：后端 +5（服务 2 / API 2 / 真库 1，含「列表含过期包但取回 404」）、前端 +11（页面 6 / 客户端 5）；**先红**（5 条 `AttributeError` / `405` / `KeyError`）后绿；**反假两轮真变红后还原**——① 去 admin 校验 ⇒ `DID NOT RAISE CommercialPolicyError` + `assert 200 == 403`；② 去租户过滤（内存与真库两处）⇒ `assert 4 == 3` / `assert (5 == 2)` / 真库跨租户行泄漏进列表。前端另有两处反假：去掉过期判定 ⇒ 过期用例红；去掉「响应不符合契约按可重试错误处理」的边界校验 ⇒ 对应用例红。**全量回归**：后端 `2470 passed / 0 failed / 0 skipped`（真库 DSN 已设）、前端 `350/350` + `tsc` 0 + `vite build` + `check:ui-copy` + `check:contrast`；**真机走查 28/28、0 控制台错误、0 个 4xx/5xx**（`tmp/b1-export-walkthrough.mjs`：空态 → 申请 → 等生成 → 列表回流 → **真实下载事件落盘并校验包内 15 类 / 脱敏声明 / 数据面无敏感词元** → 刷新仍在 → 关联点检用量区块未破坏；歪路「普通员工查列表 / 取回双双 403」「不存在包号 404」「limit=0 ⇒ 422」），截图与下载包归档 `docs/screenshots/ui-v2-b1-export/`。
      - **B-2a 导出接线首批（2026-09-19）：6 类接真实数据 + 上限 / 截断 / 失败三类如实标注**。原缺口：15 类里只有 `memories` 接线，其余 14 类恒空数组。本轮新增读取通道模块 [`export_readers.py`](file:///d:/徐徐AI学习/公司工作台/app/commercial/export_readers.py)（无状态适配器，契约 `reader(tenant_id, *, limit) -> (rows, total)`）+ **6 类**接线：`roles` / `agents`（workforce 目录，字段同既有对外视图）、`knowledge_documents`（知识治理层全量读）、`audits`（通用审计分页读）、`runs`（运行记录：**新增 `count_for_tenant`** 提供总数）、`memories`（既有实现归一到同一读取器）。口径三条（已写入契约 `docs/api-contract.md` 的「导出包内容口径（B-2）」段）：① **字段只取既有读模型/视图已有字段**并**按类别硬排除凭据列**（口令哈希 / TOTP 种子 / SSO 主体 / 系统提示词 / 模型键 / 工具白名单 / 记忆策略 / 任务幂等键与请求指纹 / 运行执行授权位）——数字员工**只导目录字段**（PG 列表读模型不返回配置列，导配置等于导默认值）；② **每类行数上限 5000**，超出在 `truncated_categories` 给 `{"exported", "total"}`（**总数取自存储计数，不拿返回条数冒充**）；③ 读取器报错的类别进 **`unavailable_categories`** 且不写入 `resources`（「读不到」≠「没有」），其余类别照常；全部行统一过既有脱敏器 `redact_payload`。
      - **顺带修掉一个真缺陷（生产向）**：导出作业在 **worker** 进程执行，而 worker 装配 `build_commercial_components` 时**既没注入记忆层仓储、也没有任何读取通道** ⇒ 生产里生成的导出包会是「所有类别皆空」的**假包**（此前 2026-09-16 的加固只在 API 进程接线）。现 worker 与 API **两侧同源注入** `build_export_readers`（worker 侧用共享连接构造 PG 仓储），契约补「⚠️ 部署口径：两个进程都必须注入」。
      - **测试与反假（B-2a）**：+16 条（读取器 13 / 运行记录计数 2〔内存 + SQL 形状〕/ **真库端到端 1**：PG 仓储 + 读取器 → 载荷只含本租户真实行、上限压到 1 时 `truncated_categories` 精确为 `{"runs": {"exported": 1, "total": 2}}`）；**反假五轮真变红后还原**——① 去掉行数上限 ⇒ 2 条红（含真库）；② 去掉统一脱敏 ⇒ 值内 `sk-…` 原样出现；③ 吞掉读取失败 ⇒ `assert [] == ['roles']`；④ 读者不按租户取数 ⇒ 7 条红（含真库）；⑤ 用返回条数冒充总数 ⇒ 2 条红。**全量回归**：后端 `2486 passed / 0 failed / 0 skipped`（真库 DSN；基线 2470 ⇒ ＋16）。
      - **真机走查 36/36、0 控制台错误、0 个 4xx/5xx**（`tmp/b1-export-walkthrough.mjs`，走查 API 每类种一行 + **他租户对照行**）：真实下载的文件里 6 类都取到了种子行（岗位 / 数字员工 / 知识文档 / 运行 / 审计逐类断言）、`unimplemented_categories` 恰为**剩余 9 类**、`unavailable_categories`/`truncated_categories` 为空、**他租户种子一行未出现**；截图与下载包归档 `docs/screenshots/ui-v2-b1-export/`。
      - **未验证（B-2a）**：① 走查栈仍是内存 + 假执行器 + 脚本自带「代替 beat 的 2 秒线程」（**不是真实 worker/beat**）；② **worker 侧新装配未在 PG 进程上实跑**（真库往返由门控用例覆盖，但生产进程的 `configure_runtime` 组合只做了静态核对）；③ **数字员工的配置明细**（系统提示词 / 模型 / 工具白名单 / 自治档 / 预算）**仍未进包**（缺口已登记：PG 列表读模型不返回配置列，逐条 `read_agent_config` 属 N+1，留待后续）；④ 剩余 9 类未接线的**原因逐类登记**：`tasks` / `users` / `usage`（只有状态受限列出、跨租户列出、或仅有汇总 ⇒ 需新增按租户读取方法）、`steps`（无独立存储，只能借 `RuntimeState` 且内存态不完整）、`artifacts`（只有 per-run 列出）、`knowledge_versions`（无版本实体，仅文档上的单值列）、`knowledge_references`（无引用关系表，只在内容 brief 里）、`growth_proposals`（无租户字段、仅内存实现、无列出方法）、`approvals`（只有待审批聚合，无历史记录表）。
- **B-2b 导出接线第二批（2026-09-19）：`tasks` / `users` / `usage` 三类接真实数据**（9/15 类已接线）。本轮为三类各加一个**按租户列出**的读方法（内存 + PG 双实现，含 `COUNT(*)` 过滤后总数）：任务 [`TaskStore.list_for_tenant`](file:///d:/徐徐AI学习/公司工作台/app/domain.py) / [`PostgresTaskRepository.list_for_tenant`](file:///d:/徐徐AI学习/公司工作台/app/repository.py)（`ORDER BY id`）、账号 [`InMemoryAccountRepository.list_for_tenant`](file:///d:/徐徐AI学习/公司工作台/app/accounts/repository.py) / `PostgresAccountRepository.list_for_tenant`（`ORDER BY requested_at, account_id`；**未审批（`tenant_id IS NULL`）的申请账号不属于任何租户** ⇒ 天然不入包）、账本 [`InMemoryUsageLedger.list_for_tenant`](file:///d:/徐徐AI学习/公司工作台/app/commercial/usage.py) / `PostgresUsageLedger.list_for_tenant`（`ORDER BY occurred_at, id`，复用 006 的 `(tenant_id, occurred_at)` 索引）；读取器 [export_readers.py](file:///d:/徐徐AI学习/公司工作台/app/commercial/export_readers.py) 增三类（`tasks` 不含幂等键与请求指纹；`users` **手机号只出掩码**、不含口令哈希 / TOTP 种子 / SSO 主体 / 驳回理由；`usage` 不含内部幂等键、冲正为负值并可追 `reversal_of`）。装配口径：`usage` 读取器由 `build_commercial_components` 用**本进程账本实例**自行接线（账本归它持有），其余由 API / worker 两侧按可用上游注入（API 侧 `account_service` 装配**提前**到商业化组件之前，注释已说明原因）。
      - **测试与反假（B-2b）**：+12 条（三类存储的按租户列出 6〔内存 + PG SQL 形状〕/ 读取器 5〔字段集与掩码 / 计数 / 装配 / 载荷填充〕/ **真库端到端 1**〔PG 上种任务 + 账号 + 用量 → 载荷只含本租户行、手机号掩码、无 `tenant_id` 行内列〕）；**先红**（收集期 `ImportError`：读取器与读方法尚不存在）→ 实现后绿；**反假五轮真变红后还原**——① 手机号不掩码 ⇒ 2 条红（含真库）；② 用量计数用返回条数 ⇒ 1 条红；③ 任务导出内部幂等键 ⇒ 字段集用例红；④ PG 账号查询去掉租户过滤 ⇒ SQL 形状用例红；⑤ 内存任务列表去掉租户过滤 ⇒ 3 条红。**全量回归**：后端 `2498 passed / 0 failed / 0 skipped`（真库 DSN；基线 2486 ⇒ ＋12）。
      - **真机走查 40/40、0 控制台错误、0 个 4xx/5xx**（同一个 `tmp/b1-export-walkthrough.mjs`，走查 API 新增三类种子 + 他租户对照）：真实下载文件里**九类**种子行逐一命中（含 `tasks` / `users`〔`137****0001` 掩码〕/ `usage`〔units 7 / cost 70〕）、`unimplemented_categories` 恰为**剩余 6 类**（`steps` / `artifacts` / `knowledge_versions` / `knowledge_references` / `growth_proposals` / `approvals`）、他租户种子零出现、用户行无 `password_hash`。
      - **未验证（B-2b）**：① 走查栈同前（内存 + 假执行器 + 脚本自带「代替 beat 的 2 秒线程」，非真实 worker/beat）；② **worker 侧新装配（账号 / 任务读取器）未在 PG 进程实跑**（真库往返由门控用例覆盖，`configure_runtime` 组合仍为静态核对）⇒ **已闭合（2026-09-19，见下条）**；③ 剩余 6 类的未接原因同 B-2a 条（`steps` 无独立存储只能借 `RuntimeState` 且内存态不完整、`artifacts` 只有 per-run 列出、`knowledge_versions` 无版本实体、`knowledge_references` 无引用关系表、`growth_proposals` 无租户字段且仅内存、`approvals` 只有待审批聚合）；④ 数字员工配置明细仍未进包。
- **B-2c 导出接线第三批（2026-09-19）：`artifacts` / `steps` 两类接真实数据 ⇒ 11/15 类已接线**。① 产物登记：新增 [`InMemoryRunArtifactStore.list_for_tenant`](file:///d:/徐徐AI学习/公司工作台/app/runtime/artifacts.py) / `PostgresRunArtifactStore.list_for_tenant`（**过期口径与 `list_for_run` 逐字一致**：`expires_at` 已过的不入包也不计入总数；排序 `created_at, artifact_id`；PG 侧 `COUNT(*)` 与 `LIMIT/OFFSET` 同条件）；读取器在运行级视图之上**补 `run_id`**（租户级快照必须能看出产物属于哪次运行）。② 计划步骤：[`step_export_reader`](file:///d:/徐徐AI学习/公司工作台/app/commercial/export_readers.py) 把运行状态的计划**展开成每个步骤一行**（`run_id` / `step_id` / `kind` / `tool` / `requires_approval` / `completed`，`completed` 由 `completed_steps` 判定；**不含**步骤正文与执行输出）。装配：API 侧 `run_artifact_store` **装配提前**到商业化组件之前（注释说明），`runtime_state_store` 直接复用；worker 侧用共享连接构造 `PostgresRunArtifactStore`（保留期同源自 settings）与 `PostgresRuntimeStateStore`。
      - **测试与反假（B-2c）**：+6 条（**真库**产物按租户列出 1〔含过期行不入包 + 到期边界〕/ 读取器 3〔产物字段集与 run_id / 步骤展开与完成判定 / 装配〕/ 载荷 1〔全上游接上 ⇒ 只剩 4 类无实体未实现〕/ **真库端到端 1**〔PG 上种产物与状态 → 只出本租户行〕）；**先红**（收集期 `ImportError`）→ 绿；**反假四轮真变红后还原**——① 内存侧去掉过期判定 ⇒ 2 条红；② 内存侧去掉租户过滤 ⇒ 2 条红；③ 步骤计数值用取到的行数冒充总数 ⇒ 1 条红；④ **PG 侧去掉租户过滤 ⇒ 真库用例红**（结果集变含他租户）。
      - **走查还暴露一处测试缺口（已补）**：首轮反假①（内存去过期判定）**没红** ⇒ 说明内存侧没有「过期产物」样本（只有 PG 用例覆盖了过期语义）；已在读取器用例里补种一行保留期外的产物，重跑反假①即精确变红。
      - **真机走查 42/42、0 控制台错误、0 个 4xx/5xx**：真实下载文件里**十一类**种子行逐类命中（含 `artifacts`〔`/workspace/walk.txt` + `run_id`〕与 `steps`〔两个计划步骤各一行〕）、`unimplemented_categories` 恰为**剩余 4 类**（`approvals` / `growth_proposals` / `knowledge_references` / `knowledge_versions`）、他租户种子零出现。
      - **未验证（B-2c）**：① 走查栈同前（内存 + 假执行器 + 脚本自带「代替 beat 的 2 秒线程」，非真实 worker/beat）；② worker 侧新装配（产物 / 状态仓储）未在 PG 进程实跑（真库往返由门控用例覆盖）；③ **剩余 4 类判定为「无实体 / 无按租户读通道」⇒ 明确不接线**（逐类原因见契约「剩余 4 类」段），不造数据；④ 数字员工配置明细仍未进包。
- **B-3 删除清场扩围 + 记录确认人（2026-09-19）**（真源 `commercial-g0-design.md:114`「删除前必须生成最终导出包**并记录确认人**；业务数据……按策略清理」）。① **确认人**：新增迁移 [`043_tenant_deletion_record.sql`](file:///d:/徐徐AI学习/公司工作台/migrations/043_tenant_deletion_record.sql) 在 `workbench_lifecycle_jobs` 上只增 `confirmed_by` / `confirmed_at` 两列；新增动作码 `COMMERCIAL_DELETION_CONFIRMED`（`commercial.deletion.confirmed`，明细只记受控值 `kind` / `status`，确认人由 `actor_id` 承载）；新增显式端点 [`POST /api/v1/commercial/deletion-requests/{job_id}/confirm`](file:///d:/徐徐AI学习/公司工作台/app/main.py)（admin-only + 需处冷静期 + 需已完成最终导出 + **fail-closed 审计**）；删除执行前置**缺一即拒**（`final_exported` / `confirmed_by` / 审计通道三闸门，[`execute_delete`](file:///d:/徐徐AI学习/公司工作台/app/commercial/lifecycle.py)）。② **清场扩围**：`execute_delete` 在记忆层之外，把技能层（`workbench_skills`）与知识治理层（`workbench_knowledge_documents`）也纳入**租户物理清场**（逐面 `delete_all_for_tenant`），并在审计 `cleared_categories` 里**如实只列真正清到的面**（未注入的面不参与也不列出）；worker 装配补注入 `build_skills_service` / `PostgresKnowledgeGovStore`（worker 是执行删除的进程）。③ **worker 自动确认兜底**：冷静期可能长达数十天、管理员常遗忘最后一步 ⇒ 到期且已完成最终导出时，worker **按申请自动确认**（确认人 = 请求人，写同一审计动作），避免删除静默卡死；`confirmed_by` 进了 SELECT 与 UPDATE（`PostgresLifecycleJobStore._COLUMNS` 集中列序 + `_hydrate`）。
      - **测试与反假（B-3）**：**＋5 条新用例**（lifecycle 4〔未确认拒绝 + 确认后执行 + 确认审计恰 1 条 / 非管理员·未导出·冷静期内仍受执行闸门约束 / 无审计通道拒绝确认 / 三面清空 + `confirmed_by` + `cleared_categories`〕、API 1〔409 → 200（`confirmed_by` + 仍 `cooling_down`）→ 员工 403 → 他租户 in (403,404)〕）；另**回写既有 4 条**用例（直接调 `execute_delete` 的补录确认人 / 无审计通道用例改为先验确认闸门）与**加严 2 条**（worker 到期自动确认的确认审计与 `detail["confirmed_by"]` 断言；PG `_COLUMNS` 的 SELECT/UPDATE 含两列断言）；**先红后绿**；**反假四轮真变红后还原**——① 去掉确认人闸门 ⇒ 3 条红；② 去掉技能清场面 ⇒ 1 条红（技能残留）；③ 去掉确认审计 fail-closed ⇒ 1 条红；④ 去掉 worker 自动确认 ⇒ 1 条红（`确认人` 拒绝，worker 不自动确认即卡死）。
      - **全量回归（含真库）**：后端 `2509 passed / 0 failed / 0 skipped`（真库 DSN；基线 2504 ⇒ **＋5 恰为本批新用例**，无既有用例被跳过或删除）；前端 `350/350`、`tsc --noEmit`、`ui-copy`、`contrast`、`build` 全过（`AUDIT_ACTION_LABELS` 已同步新动作码，`tests/test_frontend_audit_labels.py` 守护两端一致）；`.env.staging.example` 的 `WORKBENCH_APPLIED_MIGRATIONS` 已追加 `043_tenant_deletion_record`（`tests/test_staging_assets.py` 校验清单等于 `migrations/` 全清单）。
      - **未验证（B-3）**：① 确认 / 清场走查未在**真实浏览器 + 真实 worker/beat** 现场跑（本步为**后端能力 + 契约**，管理台未接入删除确认界面——按纪律不摆假按钮）；② 真库上 `confirmed_by` / `confirmed_at` 的**集群并发**（同租户同时多个删除申请取最近那条）未做高并发演练；③ 清场对**对象存储文件 / 向量索引 / 缓存**（真源 `commercial-g0-design.md:114` 提到的其余面）本期未接（业务数据内存/技能/知识治理已接，其余属后续专项）。
- **B-4 保留策略服务侧执行器 —— 专项方案（2026-09-19，本轮只出方案、不改破坏性代码）**：**结论：本项不能按「机械接线」落地。** 逐类别核过后，真源 §6.3 的五类里**三类撞上「地基 / 资金 / 合规」级决策、一类已被更严格的既有机制覆盖、一类与既有不可篡改硬化直接冲突** ⇒ 按宪法 §1.4（地基不随手翻）与绝对禁止 #9（复杂决策不列选项就动手），**本轮改为出选项供拍板**，不擅自落地破坏性清理。
      - **逐类别核查（证据）**：① **`tasks`（180 天）技术上可清但会级联销毁事件流**——`workbench_tasks` 只含 `queued` / `pending_approval` / `cancelled`（`migrations/001_initial.sql:14`），除 `workbench_audit_events` 外无入向外键，而该表外键是 **`ON DELETE CASCADE`**（`001_initial.sql:26` / `:32`，10.1 已定性为「随任务生命周期删除」）⇒ 清任务即**级联删掉任务域事件流**，并留下孤儿运行记录（`workbench_run_records.task_id` **无外键**，`013_run_records.sql:4`）。② **`runs`（180 天）被 5 处无级联外键挡住**——`workbench_run_records` 被 `027_dsh_tool_execution.sql:75` / `:125`、`037_run_artifacts.sql:26`、`040_run_acceptance_decisions.sql:32`、`042_run_promotions.sql:25` 引用，**均无 `ON DELETE CASCADE`** ⇒ 直接清会外键失败；要清必须先决定销毁**工具执行记录**（10.10 的受控 reason 码证据）/ **运行验收决议**（040 追加式验收证据）/ **运行→任务沉淀**（042）/ **产物登记**（037）⇒ 属「销毁证据」的合规决策，**不得默认**。③ **`usage`（365 天）会改掉已计费累计**——账本累计口径是**全表 `SUM(...)`**（`app/commercial/usage.py:61` / `:114` / `:122`）⇒ 按龄删行会**改变 `GET /api/v1/commercial/usage` 的累计与费用**，且冲正行（`reversal_of`）可能与其原行分离 ⇒ 须先定「结转 / 汇总快照」语义（**资金语义**，宪法 §1.3 金额红线）。④ **`events`（365 天）已有更严格的既有清理方**——同表由**全局** `runtime-events-purge` 按 `WORKBENCH_RUNTIME_EVENTS_RETENTION_DAYS`（默认 **30** 天）清理（10.3 已闭合）；再加逐租户路径会在同表互相打架。口径上 30 < 365 = **更短**保留，真源 §6.3「客户可以缩短期限」⇒ **不构成违规**，登记为「实际由更严格的全局保留期决定」。⑤ **`audit`（730 天）无删除路径**——`034` 迁移注释「审计表不可删除」+ 10.1 判据（可删即不合规）+ 10.3 各清理器的 `never_touches_the_audit_log` 守护 ⇒ 保留期**更长**（安全方向），登记为「730 天只作留存上限声明，不产生删除路径」。
      - **选项（待拍板，按破坏面从小到大）**：
        - **A. 只清 `tasks`**：执行器逐租户读策略，仅驱动 `workbench_tasks` 按 `created_at` 清理；其余四类保持现状并逐类登记。代价：任务域事件流随任务级联消失、孤儿运行记录残留。
        - **B. A + `usage` 结转**：先为 `usage` 定义**结转行**（净额单行、受控 `reason`，保证累计逐分不变）再按龄清明细；`runs` 仍另案。代价：新增资金语义（须评审），结转行自身也需保留期。
        - **C. B + `runs` 显式「先子后父」删除**：不改 schema，在同一事务内按 037→040→042→027 顺序删子行再删运行记录。代价：**销毁验收决议与工具执行证据**，须用户明确接受。
        - **D. 暂不做**（保持「保留策略为声明式、无执行器」现状，作为已知差距登记）。**本轮取 D + 本方案**。
      - **未验证 / 待办**：本方案的三处阻断均来自**静态核查**（schema 外键 / 累计 SQL 口径 / 既有清理器守护），**未**在真库上实跑「按龄删行」验证外键行为（按纪律不先动破坏性代码）；`tasks` 级联删事件流的**真实影响面**未量化（需真库统计）。
- **C 第三方许可清单与 copyleft 义务（2026-09-19）—— `THIRD-PARTY-NOTICES` 已产出 + 查出未登记的 LGPL**（真源 `docs/dsh-integration-preflight-checklist.md` §B13 遗留②③ 与 §F7.4；口径来源 `docs/superpowers/poc-license-checklist.md:18`「任何复用须另过许可评审并产出 `THIRD-PARTY-NOTICES`」）。① **产出物**：根目录 [`THIRD-PARTY-NOTICES.md`](file:///d:/徐徐AI学习/公司工作台/THIRD-PARTY-NOTICES.md)（**由脚本生成、勿手工编辑**）+ 许可正文目录 [`licenses/`](file:///d:/徐徐AI学习/公司工作台/licenses) + 生成器 [`scripts/generate_third_party_notices.py`](file:///d:/徐徐AI学习/公司工作台/scripts/generate_third_party_notices.py)（**可重跑**：交付口径要求「每次升级都要重新生成 SBOM、核对许可证」）。② **覆盖面**：四类**随交付物分发**的产物 —— 应用镜像（`requirements.lock` 53 项 + `dist-info/METADATA`）、执行镜像（dsh Linux `npm ci` 安装树）、管理台 / 伴侣端 / 桌面端（各自 `package-lock.json`）⇒ **共 1152 个组件**；口径明写「锁文件 / 安装树全量、**保守从宽**（含 devDependencies 与 optional 平台包），**不声称**等于最终镜像内的精确集合」。③ **🔴 本轮查出此前未登记的 copyleft**：**`psycopg-binary@3.3.5` 与 `psycopg-pool@3.3.1` 为 LGPL-3.0-only**（在**应用镜像**内、随交付物分发）——此前只登记了 `sharp` 生态的 2 条 LGPL ⇒ §B13/§F7.4 的「必须含 2 条 LGPL」口径**漏了这两条**；现清单共列 **4 条 LGPL**，并按 `sharp` / `psycopg` **两族**分别写明「许可正文 + 可重链接 + 源码获取途径」三项义务。④ **正文取证（不手写、不凭记忆）**：`LGPL-3.0` / `GPL-3.0` / `MPL-2.0` 为**预置权威原文**（`gnu.org` / `mozilla.org`）且**缺失即 fail-closed**（上游往往不随带正文：`@img/sharp-libvips-*` 无 LICENSE 文件、`certifi` 的 MPL 只有 989 字节「指路说明」）；其余由脚本从安装树**按内容特征校验后**原文收集 —— 实测拦下 `cryptography` 的 197 字节「指路说明」被当成 Apache 正文。
      - **测试与反假（C）**：新增 `tests/test_third_party_notices.py` **12 条**（产物由脚本生成 / LGPL 四项义务与 4 条条目（含 psycopg 两条）/ 正文权威性（首行特征 + 长度下限）/ 锁文件与三个前端 lock **逐条覆盖** / **copyleft 正文缺失时 fail-closed 且不产出半成品** / 归一不猜测未知值 / `licenses/` 无未引用散落文件 / 正文非占位符 / **正文非「指路说明」**）。**先红后绿**（产物不存在 ⇒ 5 红）；**反假四轮真变红后还原**——① 关闭正文特征校验 ⇒ 197 字节「指路说明」被收录，`test_collected_texts_are_not_mere_pointers` 红；② 漏掉桌面端 ⇒ `test_notices_lists_every_frontend_lock_entry` 红（289 包缺失）；③ `REQUIRED_LICENSE_TEXTS` 清空 ⇒ fail-closed 用例红（**探针暴露 fail-open**）；④ LGPL 只登记 `@img` 族 ⇒ psycopg 断言红。
      - **全量回归**：后端 `2521 passed / 0 failed / 0 skipped`（真库 DSN；基线 2509 ⇒ **＋12 恰为本批新用例**，无既有用例被跳过或删除）。
      - **未验证（C，不得读成已验）**：① **未逐包比对「声明值与许可证正文一致」**（1152 条，仅登记声明值）；② **镜像内二次扫描仍未做** —— 执行镜像（含 dsh）**尚未构建**（`WORKBENCH_EXEC_IMAGE_DIGEST` 留空、仓库内无该镜像定义）⇒ 清单扫的是**锁文件与安装树**而非镜像层，**一致性未验证**（脚本已支持 `--dsh-tree` 指向镜像内树，待镜像冻结后复跑）；③ BlueOak-1.0.0（10 个组件）/ CC0-1.0 / WTFPL 等**正文未齐**（已在产物「未验证与边界」逐条登记为需人工补权威原文）；④ **未评估商标 / 专利条款**（Apache-2.0 §6 等）对交付形态的影响，对外交付前建议法务复核；⑤ **交付打包未接**：`THIRD-PARTY-NOTICES.md` 与 `licenses/` **未进应用镜像**（`Dockerfile` 只 COPY `app` / `migrations` / `scripts` / `pyproject.toml`）——按义务它们应随**交付包**分发，属交付打包环节（组 7），本批未动 `Dockerfile`。
- **未验证（不得读成已验）**：① staging / 生产**未验收**（属组 1 外部任务）；② ~~管理台前端未接入取回端点（本机未跑前端套件）~~ ⇒ **已闭合（2026-09-19 B-1）**：管理台可申请 / 列表 / 取回并落盘下载，前端全量 `350/350` 已跑、真机走查 28/28（见上条 B-1）；③ 真库清理用例只覆盖「删过期行」语义，**未做跨租户大规模数据演练**；④ CI **不覆盖真实 beat 排程下的「到点清理」现场**；⑤ 差距面（**剩余 4 类导出类别**——B-2a/B-2b/B-2c 已接 11 类，剩 4 类为「无实体 / 无按租户读通道」，逐类原因见契约「剩余 4 类」段；**保留策略服务侧执行器**）**本轮明确不改码**，属后续专项（~~删除清场扩围 / 确认人~~ ⇒ **已由 B-3 交付**；**用户级「导出 / 删除本人会话」已由 P2c-4 交付并接入会话页**，不在此列；本批前端全量套件覆盖该页用例）；⑥ ~~本批**尚未推送 ⇒ CI 未取证**~~ ⇒ **已销账（2026-09-16）**：两提交已推送（`0de21be..fd564bd`），run `35077056683` **6/6 job success**（后端 `2068 passed, 97 skipped` = 2165 与本机一致；真库 `tests=69 skipped=0 failed=0`；沙箱加固与逃逸回归〔真容器〕/ 桌面端 / 手机伴侣端 / 网页管理台均 ✓）；⑦ 走查栈是**内存 + 假执行器 + 走查脚本自带的「代替 beat 的 2 秒线程」**（`tmp/s1-deeplink-api.py` 头部说明），**不是真实 worker / beat 排程**（CI 亦不覆盖真实 beat 下的到点生成，口径同 ④）；⑧ 管理台仅接入**导出**面：租户删除申请 / 删除确认 / 保留策略设置仍**无界面入口**（~~后端能力已在~~ ⇒ 删除申请与**确认**的后端能力已交付〔B-3〕，保留策略执行器属后续专项 B-4；按纪律不摆假按钮，故无界面入口）。
- [x] 10.8 「**发起人不得自审**」在**所有审批入口**都成立（含段二新增的对话入口）
      - **结论（2026-09-16 自查 + 加固）：8 类人工决议入口逐条核对，其余均已成立；唯一缝隙「任务审批允许发起人自审」已按用户拍板收紧（`403` + 待办剔除）+ 先红后绿 + 反假两轮 ⇒ 勾选。** ① **已成立入口（逐条证据）**：计划提案「发起人 ≠ 审批人」（`app/planner/service.py:163-167`，`_ensure_approver`）；运行内审批（`app/runtime/service.py:311-316`，`_ensure_decider`；且 `:91-100` 的 `_context` 以 `user_id=task.created_by` 构造运行身份 ⇒ 比对对象从根上就是发起人）；编排优化提案（`app/orchestration/service.py:159-163`）；技能版本复核（`app/skills/service.py:147-152`，`owner_id == user_id` ⇒ `PolicyError`）。② **对话入口（段二新增）复用运行内审批 ⇒ 同样成立**：对话创建运行（`app/conversation/execution.py:260-276`）时承载任务 `created_by` = 触发者；其审批的**唯一决议路径**是运行审批端点（`app/main.py:3200-3237`，`approval_id` = 承载计划步 id）⇒ 由 `_ensure_decider` 覆盖，**不存在绕开自审校验的独立入口**。③ **语义 N/A（登记理由）**：账号注册审批（`app/accounts/service.py:178-214`）仅 `super_admin` 可办、申请人**尚无账号** ⇒ 不存在「本人 = 审批人」；知识文档复核（`app/knowledge_governance/service.py:174-213`）为 `ensure_can_manage` + **系统到期触发** ⇒ 无「发起人」概念。④ **未接线入口已登记**（不构成入口）：`app/agent_services.py:101-152`、`app/capabilities.py:34-56`（无实例化）。⑤ **唯一缝隙与加固**：判据来源 `docs/openmausbot-source-study-and-adaptation-plan.md:155` §5.2 第 3 条（已定「仅 ceo/超管 + 发起人不得自审」），但任务审批入口原仅 `ensure_can_approve`（`app/repository.py:35-64` 无自审校验）⇒ CEO 自建需审批任务可自批（**先红值 `assert 200 == 403`**）。现按 2026-09-16 用户拍板收紧：路由**先取任务做归属比对**、`task.created_by == context.user_id` ⇒ `403`「发起人不能审批自己创建的任务」（`app/main.py:2989-2999`；检查顺序「403 先于 409」与既有审批入口一致；`store.approve` 生产唯一调用点即本路由 ⇒ 无误伤面）；待办列表**同口径剔除**（`app/approvals.py:64-71`，**先红值 `assert 2 == 1`**），避免展示点不动的待办。⑥ **影响面（如实）**：该缝隙原影响为「状态闸门自放行」（`pending_approval → queued` + 事件/通知），**不解锁工具执行**——执行解锁仅在运行内审批决议后的重跑（`app/main.py:3246-3254`）⇒ 无执行绕过面。⑦ **测试与反假**：`tests/test_control_plane.py:73-105`（CEO 自建 → 本人 `403` 且 detail 含「发起人」→ 另一审批人 `200`）＋ `tests/test_approvals_service.py:184-193`（CEO 视角列表仅剩他人条目）；**先红后绿**；**反假两轮**均按要求变红并还原——① 去路由校验 ⇒ 用例红（`assert 200 == 403`）；② 列表过滤条件反转（`!=` → `==`）⇒ 用例红（items 由 `['task-other']` 变 `['task-own']`）。
      - **全量回归（含真库）**：`2152 passed / 0 failed / 0 skipped`（junit 原文 `tests="2152" errors="0" failures="0" skipped="0" time="122.495"`；DSN 指向本机测试库 `wb-test-postgres-1:55433/workbench_test`）——基线 2150（10.6 收口）⇒ **＋2 恰为本批两用例**，无既有用例被跳过或删除；静态 `compileall -q app` 退出码 0。
      - **保留边界（登记，属设计选择；可用性代价已由用户拍板接受）**：单管理员部署（仅一名 `ceo`/`super_admin`）下，该管理员自建的需审批任务**无人可批**（须另有第二名 `ceo`/`super_admin` 审批）——与计划提案 / 运行内审批的既有锁死口径一致（非本批引入的新语义）。
      - **未验证（不得读成已验）**：① staging / 生产环境**未验收**（属组 1 外部任务）；② 前端**未改动**（`companion-pwa` 任务待办仍走既有端点 `/tasks/{id}/approve` 路径不变），本机未跑前端套件（CI 前端 job 覆盖）；③ 文档改动无独立测试（不构成对外证据）。
- [x] 10.9 **出网归口唯一性**：除受控抓取 / 知识适配器外，是否还存在第二条出网路径（宪法 4.8 + 段二 A4）
      - **结论（2026-09-16 自查，纯只读）：判据成立 ⇒ 勾选。** 判据来源＝段二 A4（`docs/dsh-integration-preflight-checklist.md:27`：工具进程**不允许出网**；联网需求由**工作台侧受控抓取 / 知识适配器**归口；模型调用由工作台侧发起、容器不持有模型密钥）。① **容器侧零外网出口（五层证据）**：a) **装配 fail-closed**——网络面恒为内网桥（CLI `--network` `app/tool_execution/executor.py:156-157`、SDK `network` `:179`），生产装配 `from_settings` **不传** `network_name` ⇒ 恒用默认 `INTERNAL_NETWORK_NAME="workbench-exec-internal"`（`:51` / `:268-284`），且每次起容器前校验既有网络 `Internal=True`（`:450-467`：`internal=False` ⇒ `ToolExecutionConfigError`「非内网桥（internal=False），拒绝执行」；缺失则 `create(internal=True)`）；部署侧同口径（`scripts/ensure_exec_internal_network.py:39-50`）。b) **工具面双闸门**：④-1 只读白名单 `ls/cat/head/tail/wc/stat/file`（`app/tool_execution/blacklist.py:35`）＋ ④-2 A4 网络与外联黑名单（`curl/wget/nc/ncat/netcat/socat/telnet/ssh/scp/sftp/ftp/tftp/ping/traceroute/dig/nslookup/host/ntpdate/iptables/nft/ip/ifconfig/route/tcpdump`，`:45-47`）；dsh 侧工具面另收窄至只读检视集（`app/runtime/adapters/dsh.py:110`）。c) **真容器实测零出网**：DNS 解析不外泄（`gaierror`）、`1.1.1.1:443` / `8.8.8.8:53` / 裸 UDP DNS / 云元数据 `169.254.169.254` 全部 `Network is unreachable`（`docs/sandbox-boundary-decision.md:78-80`，按 §3.3 加固口径 Probe 1）。d) **真容器 + 真实供应商双轮对照**：容器内 `supplierDomain = BLOCKED`、`gateway = REACHABLE status=403`（`docs/dsh-integration-preflight-checklist.md:912`；网关挂双网「只有它能出网」、执行容器只在内部网 `:902`）。e) **联网类插件与遥测兜底关闭**：13 项禁用插件含 `web` / `web-search-deepseek` / `web-fetch-http` / `tool-web` / `session-telemetry-otel`（`app/runtime/adapters/dsh.py:91-105`），`DSH_PERMISSION_MODE=read-only`（`:24`）、`DSH_TELEMETRY_DISABLED=1`（`:26`）、启动期剖面锁死自检（`:167-177`）。
      - **② 容器内无供应商密钥（不存在「有 key 自行联网」形态）**：容器 env 白名单仅「网关内网地址 + 短期网关令牌」两项（`dsh.py:45`），装配期逐键 fail-closed（`executor.py:121-133`），`build_token_env` 剔除供应商密钥字段名（`dsh.py:69-70`）并自检白名单（`:73-77`）。
      - **③ 工作台侧出网调用点逐条枚举（12 处，全部外置配置 + 显式超时）**：**受控抓取归口（A4 允许）** `app/content/scraper.py:71-75`/`:242`（域名白名单 `:238`、robots fail-closed `:186-202`、限速 `:212-221`、字节上限随调用传入 `:242-244`）；**知识适配器归口（A4 允许）** `app/knowledge.py:69`（共享 `httpx.Client`，构造见 `app/bootstrap.py:1229`）；**模型类 4**：内容模型 `app/content/openai_compatible.py:32`、规划模型 `app/planner/generator.py:106`、Embedding `app/memory/embedding.py:36`、网关上游 `app/model_gateway/upstream.py:83-88`/`:112`（传输级重试上限、收到响应后不重试）；**发布 / 集成类 3**：公众号发布 `app/content/publisher.py:50`/`:52`、Webhook 通知 `app/notifications.py:53`、SSO `app/accounts/sso.py:105`（https 强制 `:80`）；**内部受控 3**：外部 Runtime 传输 `app/runtime/adapters/common.py:68`、边车回传 `app/exec_callback/server.py:86`（预共享密钥）、网关控制面令牌 `app/tool_execution/gateway_token.py:67`。
      - **④ 三项阴性核查**：全仓无硬编码第三方域名（仅文档样例 `app/content/safety.py:108-184`、`app/model_gateway/server.py:369` 自身绑定址、`app/runtime/staging.py:49-68` 冒烟桩）；无遥测埋点（`analytics|telemetry|sentry|datadog|usage_report` 仅命中 dsh 禁用清单）；无第二类出网原语（`smtplib|ftplib|paramiko|websocket` 零命中）；两前端 apiBase 仅本产品 API（admin-web 12 处 / companion-pwa 2 处），desktop 仅本产品 URL + 构建期注入更新源。
      - **⑤ 治理闭环**：C1（电脑控制）/ C2（浏览器操作）不立项，明写「**不引入第二套出网归口**」（`docs/capability-ownership-map.md:48`/`:76`、`docs/feature-inventory.md:94`）。
      - **未验证（不得读成已验）**：① 真实生产宿主 / staging **未复测**（`--internal` 行为不得由本机结论外推生产拓扑，`docs/sandbox-boundary-decision.md:139`；属组 1 外部任务）；② §B17「加固形态下复跑」**未做**（`docs/dsh-integration-preflight-checklist.md:920`，门禁项独立成立）；③ **网关自身出网面未独立自检**（本次核的是「容器侧零出网 + 工作台侧调用点枚举」；网关作为唯一模型出网归口的 host 白名单 / 出网加固未单独演练）；④ 用例 41（云元数据）**反假不可构造**（`docs/sandbox-boundary-decision.md:135`）；⑤ 阴性 grep 结论**限于当前快照**，不构成运行时阻断证据（运行时阻断依赖 ①②③ 的机制与实测）；⑥ **宪法 4.8 逐依赖登记表未逐条核对**（全仓仅有外部依赖验收 / 执行计划类文档，未发现逐依赖的用途 / 密钥归属 / 重试 / 降级 / 验签 / 对账登记表 ⇒ 作为边界登记）。
      - **推送与 CI 取证（2026-09-16 销账）**：本批回写提交 `22b4903` 已推送，run `35079935924` **6/6 job success**（后端 `2068 passed, 97 skipped` = 2165 与本机一致；真库 `tests=69 skipped=0 failed=0`；沙箱加固与逃逸回归〔真容器〕/ 桌面端 / 手机伴侣端 / 网页管理台均 ✓）。⚠️ 边界保留：本销账回写提交（docs-only）触发的后续 run 不再另行登记（口径同 10.4–10.11）。
- [x] 10.10 **`reason` 不含自由文本**：工具类动作码的**取值**是否有枚举校验（而非只做键名白名单）
      - **结论（2026-09-16 自查，纯只读）：判据成立 ⇒ 勾选。** 核查范围＝**工具类**面（`tool.executed` / `tool.blocked` 两个动作码 + `027` 表 `reason_code`）。
      - **① `reason` 取值全为受控枚举或空，绝无自由文本**（工具面全部 4 个审计落点逐一核对）：`tool.executed` 恒 `None`（`app/tool_execution/service.py:528-541`）；`tool.blocked` 取 `ReasonCode.value`（`service.py:603-647`，两分支 `:625` / `:644`）；回传令牌守卫复用 `tool.blocked` 写固定字面量 `"not_authorized"`（`app/tool_execution/token_binding.py:142-159`，值＝`ReasonCode.NOT_AUTHORIZED`）；到期清理复用 `run.approval_decided` 只写 `status="expired"`、**连 `reason` 键都不写**（`app/tool_execution/cleanup.py:137-144`）。失败语义表 9 个键的 `reason_code` 全部是 `ReasonCode` 枚举成员（`service.py:68-82`）。
      - **② `reason_code` 取值有真正的枚举校验（而非只做键名白名单）**：9 值受控枚举 `ReasonCode(StrEnum)`，docstring 明写「自由文本一律不得落库 / 落审计」（`app/tool_execution/store.py:25-36`）；写入侧运行时校验 `_validate` → `ReasonCode(action.reason_code)`，未知值抛 `ValueError`（`store.py:77-90`），**InMemory 与 Postgres 两个实现共用同一校验**（Postgres `upsert` `:275`；读取侧水合 `ReasonCode(str(row[20]))` 往返归一 `:245`）。守护测试齐备：9 值精确集合（`tests/test_tool_action_store.py:56-68`）、负向「`free_text_reason` 被拒」（`:70-74`）、9 值逐值接受（`:76-86`）、PG 负向「`free_text_日本語` 被拒」（`tests/test_tool_action_store_postgres.py:200`）、PG 枚举往返（`:329-341`）。
      - **③ 动作码取值本身是枚举 + 三重闸门**：`tool.*` 均为 `AuditAction(StrEnum)` 成员（`app/audit/models.py:11-90`），工具面 4 个落点逐一无裸字符串（见 ①）；**查询侧**未知动作码 `AuditAction(raw)` → `422`（`app/main.py:2887-2897`，端点 `:2909-2925`）；**读取侧**水合 `AuditAction(str(row[1]))` 未知值即抛（`app/audit/store.py:140`）。
      - **④ 键名白名单与取值枚举的分工**：`ALLOWED_DETAIL_KEYS`（`app/audit/models.py:97-163`）是**键名**白名单（拒未声明字段 + 嵌套敏感键递归拒绝 `:196-200`）；**取值**的枚举收口在产生侧（①②③）——即本判据问的「而非只做键名白名单」在工具类面**不成立**（键名白名单之外，取值另有枚举校验）。
      - **边界登记（不改码，如实登记）**：a) **DB 层无 CHECK 约束**——迁移 `027` 的 `reason_code` 列仅注释声明「受控枚举码（9 值）…自由文本一律不落」（`migrations/027_dsh_tool_execution.sql:73`，无列级 CHECK；`workbench_audit_log.action` 同为 `TEXT NOT NULL` 无 CHECK，`migrations/001_initial.sql:28`）⇒ 应用层 `_validate` 是唯一闸门（绕过应用层直写 SQL 才可能落非枚举值，与 10.1 已登记的「DB 层账号权限未管」同域）。b) **非工具类的审批驳回理由落审计 `reason` 键为管理员输入自由文本**（≤500 字：账号驳回 `app/accounts/service.py:197-213`、计划提案驳回 `app/planner/service.py:114-127`、编排提案驳回 `app/orchestration/service.py:131-146`，均同时落业务列 `rejection_reason`）——**超出本条「工具类」范围**，属审批决议业务数据的有意设计；若未来要求审计零自由文本需专项口径变更。c) 非工具面受控字面量（`"totp_required"` / `"no_binding"` / `"task_unavailable"`，`app/accounts/service.py:265`/`:440`、`app/main.py:2797`/`:2823`/`:3085`）无统一枚举定义——值受控但散落。d) 写入侧无 `AuditAction(...)` 归一转换（依赖类型标注 + 调用点自律；查询侧与读取侧有枚举闸门）。
      - **未验证（不得读成已验）**：① staging / 生产环境**未复测**（属组 1 外部任务）；② 文档改动无独立测试（不构成对外证据）。
      - **推送与 CI 取证（2026-09-16 销账）**：本批回写提交 `ae14a9d` 已推送，run `35081745668` **6/6 job success**（后端 `2068 passed, 97 skipped` = 2165 与本机一致；真库 `tests=69 skipped=0 failed=0`；沙箱加固与逃逸回归〔真容器〕/ 桌面端 / 手机伴侣端 / 网页管理台均 ✓）。⚠️ 边界保留：本销账回写提交（docs-only）触发的后续 run 不再另行登记（口径同 10.4–10.12）。
- [x] 10.11 **密钥不进前端产物**：构建产物 / 静态资源中不得出现密钥或内部地址（宪法 4.1 禁止项 + 8.6 上线自检）
      - **结论（2026-09-16 自查）：判据成立 ⇒ 勾选。** 五层证据：① **机制层**：两前端 `vite.config.ts` 均无 `define` / 无 `envPrefix` 覆盖（`admin-web/vite.config.ts:1-9`、`companion-pwa/vite.config.ts:1-8`；默认仅 `VITE_` 前缀可进客户端）；桌面端打包 `files` 白名单仅 `src/**` / `web/**` / `package.json`（`desktop/electron-builder.yml:11-14`），更新源构建时 fail-closed 注入（`:22-27`）。② **源码层（grep 全扫描）**：三包 `src/` / `scripts/` / `public/` 密钥类模式（`sk-` / `ghu_` / `ghp_` / `AKIA` / `PRIVATE KEY` / `postgres://` / `redis://`）**零实质命中**（全部命中项为业务代码：登录密码输入框 `companion-pwa/src/features/session/LoginPage.tsx:21-72`、审计文案 `admin-web/src/features/auditLog/types.ts:53-54`、安全测试串 `desktop/src/config.test.cjs:98`）；内部地址类（`192.168.` / `10.x` / `172.16-31` / 容器名）**零命中**；静态资源仅两个 SVG 的 `xmlns="http://www.w3.org/2000/svg"`（标准命名空间）。③ **产物层（实测，最强证据）**：本机 `npm run build`（两前端均成功）⇒ dist 全文件正则扫描：密钥类 5 模式 **0**、内部地址类 4 模式 **0**、`VITE_*` 变量名 **0**（构建期完全替换）；产物内 fallback 值仅 `http://localhost:8000/api/v1`（admin-web 11 处 / pwa 2 处）与 `super_admin`（11 / 2 处）——开发便利默认，**非密钥非内部地址**。④ **仓库卫生层**：根 `.gitignore` 排除 `dist/` / `.env` / `.env.*` / `*.pem` / `*.key` / `desktop/web/`（`companion-pwa/dist/` 由 `tests/test_pwa_assets.py:132-133` 守护）；三 env 模板全占位符（`.env.sso.example:59` / `.env.staging.example:23-24` / `.env.example:14-15`）；CI 三个前端 job **无 secrets / env 注入**（`.github/workflows/ci.yml:49-106`）。⑤ **守护测试层（42 项全绿）**：Dockerfile **不得**烤入 5 个密钥赋值（`tests/test_container_assets.py:56-66`）、**不得** `COPY tests` / `COPY admin-web`（`:69-73`）、`.dockerignore` 排除 `.env*` 与 `admin-web`（`:91-105`）；CI 三前端 job 与 build 守护（`tests/test_ci_assets.py:47-57`）。
      - **关联核实（开发身份 fallback 的风险链，已闭合）**：产物内 `super_admin` fallback 是否可利用——后端 `current_user` 在**非 development 环境只信 `Bearer` 令牌**（`app/main.py:548-554`；X-Tenant-Id / X-User-Id / X-User-Role 仅 development `elif` 分支生效），与「only for development configuration」口径一致（`docs/superpowers/plans/2026-09-06-knowledge-access-admin.md:105`）。`settings.env` 默认 `development`（`app/settings.py:11`）的残余面（非编排部署漏配）已由四重缓解覆盖：compose 三服务硬编码 `production`（`docker-compose.app.yml:39/90/142`）+ 三个预检脚本强制非 development（`scripts/commercial_g0_preflight.py:64-65`、`scripts/worker_preflight.py:136-140`、`scripts/runtime_staging_preflight.py:108`）+ `validate_runtime_settings` 全套强制（`app/settings.py:687-700`）+ `docs/private-deployment-runbook.md:10` 条款 ⇒ 与 N2 已登记未验证项（开发环境 X-User-Role 伪造）关联，不重复立条。
      - **未验证（不得读成已验）**：① staging / 生产**真实构建与部署未跑**（属组 1 外部任务；CI 构建为干净环境、无 API 地址注入）；② **前端生产产物注入流程未定义**（`VITE_API_BASE_URL` 指向生产 API 的注入点无真源定义）——CI 产物带开发 fallback `localhost:8000`，直接用于部署会指向 localhost（**功能问题、非泄漏**），登记待组 1.2 一并验收；③ 桌面端**安装包本体未实测扫描**（受签名 / 打包环境限制，属组 6；内置 web 部分即 admin-web/dist 已扫描、壳源码已 grep）；④ 文档改动无独立测试（不构成对外证据）。
      - **推送与 CI 取证（2026-09-16 销账）**：本批回写提交 `980dd32` 已推送，run `35078247188` **6/6 job success**（后端 `2068 passed, 97 skipped` = 2165 与本机一致；真库 `tests=69 skipped=0 failed=0`；沙箱加固与逃逸回归〔真容器〕/ 桌面端 / 手机伴侣端 / 网页管理台均 ✓）。⚠️ 边界保留：本销账回写提交（docs-only）触发的后续 run 不再另行登记（口径同 10.4–10.8）。
- [x] 10.12 **`027` 迁移的升级与回退演练**（含对既有表 `workbench_run_records` 的约束增补；宪法 4.4 备份与恢复；登记口径见 `docs/change-record.md`）
      - **结论（2026-09-16 演练，用户已授权全链路写操作）：判据成立 ⇒ 勾选。** 环境：**一次性独立容器**（digest 钉死 `pgvector/pgvector:0.8.0-pg16@sha256:a132765…`、仅绑 `127.0.0.1:55600`、无卷、`POSTGRES_HOST_AUTH_METHOD=trust`；库 `workbench_drill` + 恢复库 `workbench_restore`），**非生产、不碰** 既有 `wb-test-*` / `infra-*` 栈。① **前滚**：真实迁移器 `app.migrations.apply_migrations` 对空库应用**全部 34 项**迁移（`001_initial`→`034_runtime_events`，含 `027_dsh_tool_execution`）。② **对象核对**（只读 SQL）：约束 `workbench_run_records_run_tenant_unique`=1；`workbench_tool_actions`（21 列 / CHECK 4 / FK 1 / PK 1 / 索引 4）、`workbench_execution_idempotency`（10 列 / CHECK 2 / FK 3 / PK 1 / 索引 2）。③ **备份 / 恢复（宪法 4.4）**：容器内 `pg_dump --format=custom`（退出码 0、102550 字节）→ `createdb workbench_restore` → `pg_restore --clean --if-exists`（退出码 0；首次因缺 `-U postgres` 以 OS 用户连库被拒 `role "root" does not exist`，补角色后成功）→ 恢复库核对与源库**逐项一致**（34 项 / has_027=1 / 约束与两表计数全同）。④ **回退 `027`**（按 change-record 2026-09-13 已登记口径、**不用 CASCADE**）：`DROP TABLE workbench_tool_actions` → `DROP TABLE workbench_execution_idempotency` → `ALTER TABLE workbench_run_records DROP CONSTRAINT workbench_run_records_run_tenant_unique` → `DELETE FROM workbench_schema_migrations WHERE version='027_dsh_tool_execution'`（`DELETE 1`）⇒ 核对归零：33 项 / has_027=0 / 约束=0 / 两表 `<none>`。⑤ **再前滚（回退可逆）**：重跑迁移器**仅应用 1 项＝`027_dsh_tool_execution`**，对象计数与 ① 完全一致。⑥ **销毁**：`docker rm -f wb-027-drill-pg`（退出码 0），无容器 / 卷残留；dump 仅存于容器 `/tmp`、随销毁消失，未落宿主、未进仓库。
      - **未验证（不得读成已验）**：① **staging / 生产目标库未演练**（AI 不连生产；本演练为一次性本地空库；生产库上的升级 / 回退仍须按组 1 外部流程在服务器执行并回传）；② 回退演练**不覆盖有数据场景**（异构回填风险不在本条范围）；③ **宿主 `pg_dump` / `pg_restore` 路径未跑通**（宿主无客户端 ⇒ 备份恢复走容器内镜像自带客户端等价命令；`scripts/migration_backup_drill.py` 的宿主执行面未演练）；④ 备份**未落盘加密**（`WORKBENCH_BACKUP_ENCRYPTION_KEY` 面未演练；空库无敏感数据且随容器销毁 ⇒ 边界登记）；⑤ 文档改动无独立测试（不构成对外证据）。
      - **推送与 CI 取证（2026-09-16 销账）**：本批回写提交 `a312182` 已推送，run `35080905772` **6/6 job success**（后端 `2068 passed, 97 skipped` = 2165 与本机一致；真库 `tests=69 skipped=0 failed=0`；沙箱加固与逃逸回归〔真容器〕/ 桌面端 / 手机伴侣端 / 网页管理台均 ✓）。⚠️ 边界保留：本销账回写提交（docs-only）触发的后续 run 不再另行登记（口径同 10.4–10.11）。

**判据**：每条给出「结论 + 证据（`文件:行`）」；未查的写「未验证」。

---

## 开工顺序建议

1. **组 1 先行**（只读部分今天就能做：1.1 → 1.5），它一次解锁最多 ❌，且会暴露配置/迁移层面的真实问题；
2. 组 2、组 3 并行（都需要密钥/账号）；
3. 组 4（平台授权到齐后）；
4. 组 6、组 7（采购证书/真机）；
5. 组 5 只等对端契约，拿到即做。

## 回传与判读

每组完成后，把命令输出与证据（**脱敏**）回传；由我判读并更新 `docs/delivery-readiness-checklist.md` 的对应行（状态真相只在该文件维护，本文件只勾选进度）。
