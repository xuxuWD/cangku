# P2a 段二（dsh 接入段）规格评审记录（C1）

> **评审对象**：`docs/superpowers/specs/2026-09-12-dsh-integration-design.md`（送审时状态：**待评审**）
> **关联送审材料**：`docs/dsh-integration-preflight-checklist.md`、`docs/api-contract.md` 的「工具执行（P2a 段二）」章节、`docs/superpowers/specs/2026-09-12-conversational-agent-platform-design.md`（立项文档）
> **评审日期**：2026-09-12
> **结论**：**不予放行**（可送审，须先修订；见 §2 阻断项）
> **性质**：评审记录存档。**本记录不含修订**，规格修订结果另行落进规格正文与 §3.8。评审期间**未修改任何文件、未执行 git 命令**。

## 0. 评审方式与边界

- 采用**三个独立只读视角**（互为盲评：各自不共享其它视角的推理，也未见起草过程的对话）：
  1. **安全与闸门正确性**（九步闸门顺序与不变式、黑名单绕行、容器边界、授权位、fail-closed）
  2. **一致性与可测性**（规格↔契约↔清单↔代码事实；测试计划与验收可判定性；迁移/配置/模板/回滚）
  3. **宪法合规与流程**（逐条对照 `D:\徐徐AI学习\AI知识库\wiki\concepts\AI编程开发宪法.md` 母版）
- 汇总方**亲自复核了 7 条**：`workbench_tasks` 无 `origin` 列、`decide_approval` 以 `state.plan` 全量写授权位、系统无任务列表接口、`plan_digest` 不含参数、`ensure_execution_authorized` 两处 `return`（fail-open）、`AuditAction` 无 `tool.*`、规格 §前置事实仍写后端 1417。
- **未复核**条目照实标注。属推演性结论的（shebang 绕过、TOCTOU/硬链接）**未做实验验证**，须以用例或实验收口。
- **独立性偏差声明**：汇总方参与过本规格部分章节（§3.2.1 危险命令黑名单、§3.7 Y1–Y3、契约新增章节）的撰写，故**不构成本规格的独立评审人**；独立性由上述三个视角承担。
- 本记录**不替代**开工门禁：即使规格评审通过，`docs/dsh-integration-preflight-checklist.md` §B（除 B12 外）未取证前仍不得动代码。

## 1. 结论

三个视角**一致认定：不予放行**。规格的整体方向被认定为正确（闸门先于执行器、黑名单为兜底而容器边界为主防线、无新迁移、承诺边界未夸大），但存在会**直接导致安全事故或实现无从落地**的缺陷，必须先修订并重审：

- 安全问题 2 条：授权位不含参数（「批准 A、执行 B」）、黑名单 `basename` 判定可被工作卷内同名可执行文件一步绕过。
- 文档互斥 1 条：`origin=conversation` 与「零新迁移」不能同时成立。
- 流程空洞 1 条：审批通过后如何推进到执行未定义。
- 幂等缺失 1 条：对话端点开始触发真实副作用却无幂等键。

## 2. 阻断项与裁决

| # | 阻断项 | 证据 | 状态 |
| --- | --- | --- | --- |
| **B1** | 要求承载任务带 `origin=conversation`，但 `workbench_tasks` **无 origin 列**，而规格 §4 声称「无新迁移」→ 三份文档与 schema 无法同时成立 | `migrations/001_initial.sql:3-19`（列清单无 origin）；规格 §3.7 / §4；契约变更点 4 | ✅ 已复核 · **裁决：撤销该要求**（系统本无任务列表接口，承载任务对用户不可见，无需落库标记） |
| **B2** | **授权位摘要不含参数** → 批准「写入 A」后可把参数改成 B，摘要不变，⑦ 放行、⑧ 执行 | `app/runtime/authorization.py:48-63`（仅 `step_id/kind/tool/requires_approval` 四字段）；`app/runtime/contracts.py:70-80`（`PlanStep` 无参数字段）；清单 A5 已要求「执行前二次校验参数（防『批准 A 执行 B』）」 | ✅ 已复核 · **待修订**（拟：把规范化参数摘要并入授权比对） |
| **B3** | 可执行名只按 `basename` 判定、且未禁止工作卷内可执行文件 → `executable=/workspace/ls`（脚本，basename 命中 Q2 只读白名单）经 shebang 拉起 `sh`，A7/A8/Q1/Q3 同时失效；③ 路径校验还会放行 | 规格 §3.2.1 R1/R2（只讲 basename）、③（只判是否在允许目录内）、局限 3（只讲「改名」，未讲「同名替换」） | ✅ 逐条核对成立 · **待修订**（拟：④ 前增「可执行文件必须解析自受信任且不可写根」） |
| **B4** | **审批通过后如何推进到 ⑧ 未定义**：⑤ 命中即返回 `202` 并终止请求链路，全文无「审批后恢复执行」入口；配合 Y2「每条消息新建承载任务 + Run」，重发消息得到**新 run → 无授权位 → 又一次 202**，⑦ 可能永不可达 | 契约 ⑤ 行仅 `202`；`app/main.py:1836-1879`（审批端点响应不含执行推进）；规格 §3.2 ⑤⑥⑦ | ✅ 已复核 · **待修订**（拟：决议通过后同请求内重跑 ①–⑨，且**禁止**跳过 ②③④） |
| **B5** | 对话消息端点开始触发**真实副作用**，但请求体仅 `{"content": string}`，**无幂等键** → 重复 POST 重复建承载任务并重复执行副作用（宪法 4.2「关键写接口必须幂等」） | 契约请求体定义；规格全文 grep「幂等」0 命中 | ✅ 已复核 · **裁决：以 `message_id` 作执行幂等键** |

## 3. 重要项（11 条）

### 3.1 文档间矛盾（会误导实现）

| # | 问题 | 证据 |
| --- | --- | --- |
| M1 | 契约 ⑦ 行把「授权来源不在白名单」列为 `ensure_execution_authorized` 的失败条件，但该函数**只比对摘要、不校验来源**（来源校验发生在审批写入阶段）→ 契约超范围 | 契约 ⑦ 行；`app/runtime/service.py:136-160`；`app/runtime/authorization.py:41-45` |
| M2 | 契约把 `tool.executed` / `tool.blocked` 列为**必须**新增动作码，规格 §3.4 只写「建议」；且 `AuditAction` **没有 `tool.*`**，漏扩则 `GET /api/v1/audits?action=tool.executed` 直接 **422** | `app/audit/models.py:11-56`；`:63-100`；`app/main.py:1478-1488` |
| M3 | §4 声称新增配置由 `tests/test_env_templates.py` 类静态契约守护；该测试实际只覆盖 `sso_*` 与 5 个 Runtime 的固定字段，**容器镜像/工作目录/超时/pids/内存/总开关都不会被守护** | `tests/test_env_templates.py:49-115`；`.env.staging.example:1-93`；规格 §4 |
| M4 | §2.3 称「其余 **245** 个 `@deepseek-ai/*` 子包为 rc.2」，与清单 F1 实测（条目 246、rc.2 230、另有 5 个 `node-addon-system*@0.1.2`）不一致，而 §4 又按 245 界定许可证核对范围 | 规格 §2.3、§4；清单 F1（`checklist:149-152`） |
| M5 | `agent_key` 校验落点不对称：契约挂在 `POST /api/v1/conversations`，**真实执行发生在 `POST .../messages`**；规格未写端点与状态码 | 契约变更点 2；`app/main.py:1305-1326`；清单 B7 |
| M6 | 规格**自身基线不一致**：§前置事实写后端 **1417**，§3.6 写 **1434** | 规格 §前置事实 与 §3.6；清单 B11 |

### 3.2 闸门与安全缺口

| # | 问题 | 证据 |
| --- | --- | --- |
| G1 | Q2 只读白名单**未进九步闸门**、无失败语义；A 类漏 `env`/`busybox`/`toybox`/`timeout`/`setsid`/`nice`/`stdbuf` 等**包装器** → `env rm -rf /workspace/x` 中真实命令对 R1（只取顶层可执行名）不可见 | 规格 §3.2.1 Q2 与 A 类；契约九步闸门 |
| G2 | 黑名单参数口径缺口：未定义长选项 `=` 形态（`--in-place=.bak`、`--directory=/`）；`tar` 只列 `--absolute-names` 漏 `-P`；`chmod` 只列 `777/666/+s` 漏 `0777`/`a+rwx`/`--reference`；未防「先释放符号链接再经链接写出」，未列 `cpio`/`7z`/`bsdtar` | 规格 §3.2.1 R1 与 B 类逐条原文 |
| G3 | **工具目录清单与各工具风险档缺失** → 清单 B5「每个工具都有风险档、并说明什么动作算 critical」无法验收，用例 5（`full_auto` + `critical` 工具）无所指 | 规格 §3.1（只有字段定义，「草案，待评审」）；清单 B5；规格 §5 用例 5 |
| G4 | §3.4 的**文件级落库字段**（`摘要 + args_digest + sha256 + 字节数`）**无落点**，且与 Y3「不落这些」冲突；既有审计明细白名单也不含这些键 | 规格 §3.4、§3.7 Y3；`app/audit/models.py:63-100`；契约审计口径 |
| G5 | 「明细走白名单，**不含自由文本**」与**复用 `reason`** 自相矛盾：`reason` 是白名单内的自由文本键，实现时会把命令串/路径/异常 message 落进审计 | `app/audit/models.py:65`；规格 §3.4；契约审计口径 |
| G6 | §3.5「`restrict` 空交集 → **降级为最小集**」非 fail-closed：空交集本意常是「不开放任何工具」，回落等于**给出比预期更多**的能力，且 dsh 默认最小集不由我方定义 | 规格 §3.5 |
| G7 | **闸门后补**：段二-3 就含「一次真实工具执行」，`agent_key` 校验收紧却排在**段二-4** → 违反清单 B7「路由到真实执行**前**必须校验存在且启用」 | 规格 §1.4；清单 B7；契约变更点 2 |

### 3.3 未复核但影响设计的前置

| # | 问题 | 证据 | 备注 |
| --- | --- | --- | --- |
| P1 | 「容器内不得持有模型密钥」与「模型调用由工作台侧发起」缺乏可判定前提：实测显示模型调用由 **dsh 进程**解析并发出，而规格从未写明 dsh 进程跑在**工作台宿主**还是**工具容器**内 | 规格 §3.2/§3.3；清单 F3.2 实测报文（`provider/model`、`code=AUTH`、`status=401`、密钥掩码） | **未复核**：需先定 dsh 进程位置与凭据来源，才能声称「容器内不持有模型密钥」 |
| P2 | 执行**同步在请求链路内**完成（容器起停 + 工具执行），与宪法 4.5「耗时操作丢给队列或后台任务」冲突；「本段不做流」不等于「必须同步」 | 规格 §3.2；契约「同步、非流式」 | 需给同步时长上限，或改后台 + 轮询 |

## 4. 建议与待核实（合并三视角）

**容器与执行环境**
- 补：`--security-opt no-new-privileges`；工作卷 `nosuid,nodev,noexec`（`noexec` 兼作 B3 的第二道缓解）；mount propagation；`/dev/shm` 与 `/tmp` 可写可执行面；镜像按 **digest** 钉死；执行器崩溃后**孤儿容器回收**（「任务结束即销毁」只覆盖正常路径）
- 明确工作卷**设备隔离**与校验→执行的**原子性**（fd 传递 / `openat2 RESOLVE_BENEATH`）；补 TOCTOU 与硬链接用例（**待核实**：硬链接不改变 realpath，若与敏感文件同设备可绕过 ③ 与 C 类）
- 明确工作目录 **per-run 唯一性**、销毁时机、失败残留清理，以及跨租户不可见的证据判据

**黑名单与路径匹配**
- 明确 R1 规范化口径：长选项 `=`、短选项粘连、大小写归一；补齐等价别名与数值形态（`-P`、`0777`、`a+rwx`、`--reference`）；解包默认不跟随符号链接
- 明确 C 类**匹配算法**（realpath 绝对路径前缀 / 基名 glob / 是否大小写归一 / `~` 是否展开），补齐 `/proc/self/maps|mem`、`/proc/sys/**`、`/sys/**`、`/dev/**`
- R2 声称「兼容 Windows 侧 `pwsh`/`cmd`」不成立（未剥离 `.exe/.cmd/.bat/.ps1`）→ 要么补剥离，要么在规格里**限定本段只在 Linux 容器运行**

**流程与验收**
- 用例 4 的「必须红」**不可复现**（④ 挪到 ⑦ 之后仍在 ⑧ 之前）→ 观测点应改为「审批通过后的第二次执行尝试是否**重跑 ②③④**」
- 用例 2/8/10 的「必须红的方式」为空，需补具体改坏构造；用例 10 的「切回 mock 后全量全绿」无判定口径；§3.6 外置开关未给配置项名/取值/默认值
- §5 未覆盖已锁定决策点：Q4（启动期断言）、Y2、Y3（不落 args_digest 等）、§3.5 剖面锁死、超时=终止容器、`--cap-drop ALL`/pids/memory/无 `docker.sock`/容器无凭据
- 补一条**聚合测试命令**（等价 CI 四个 job），现无脚本落点；§6 的泛化条目不能替代用例
- 清单自认「最高优先级待验项」——`bash/write/edit` 的 `sandbox_permissions`/`justification` 升级通道真实行为（自动拒 / 挂住 / 静默放行）——在 §5 无用例，须列为段二-1 明确交付判据

**合规与流程**
- 许可证：14 个 LGPL（全为 `sharp`）与「一旦分发镜像/桌面产物即触发 LGPL 义务，须产出 `THIRD-PARTY-NOTICES`」应列为**交付物与验收项**，并明确本段是否分发镜像
- 环境分开：规格只写「与工作台分主机」，未写开发/测试/生产三套环境的程序/配置/数据/权限如何分开；清单 A2 的「开发期本机 Docker」与规格的无条件分主机约束不对称（F4 取证本身即在本机 Docker 完成）
- 收尾纪律：段二-2 的**一次性假执行器/测试桩**须纳入清理清单并经用户确认后删除
- 写代码与验收分离：四个子段的验收应明确由**只读独立方**执行
- 前端：对话页触发真实执行后，加载/空/错误/无权限**四态**与前端用例未纳入验收
- **待核实**：单会话最大轮次上限（宪法 4.9）由谁承担；模型/dsh 调用的重试上限与单次失败降级路径；真源三件套中「功能清单」是否齐备（若以立项文档 §14.1 充当，需显式引用）

## 5. 附录：三个视角的完整 findings

### 附录 A 视角一：安全与闸门正确性（18 条）

| # | 严重度 | 问题 | 证据 | 建议处置 |
| --- | --- | --- | --- | --- |
| 1 | 阻断 | 可执行名只按 basename 判定，且未禁止「可执行文件位于可写工作卷内」。`executable=/workspace/ls`（realpath→`/workspace/ls`，basename=`ls` 命中 Q2 只读集），文件真实内容是脚本或恶意 ELF → 经 shebang 拉起 `sh` 执行任意命令，`sh` 从未出现在结构化命令里，A7/A8/Q1/Q3 全部同时失效；③ 路径校验还会放行该路径 | 规格 §3.2 ③、§3.2.1 局限 3；`app/runtime/contracts.py:70-80` | ④ 前增加 fail-closed 步骤：可执行名必须解析自**受信任且不可写**的固定根（如 `/usr/bin`），校验 realpath 落在该根内、属主 root、非 group/world-writable；拒绝 shebang 与非 ELF；工作卷不得在 `PATH` 内。补用例：允许名 + 工作卷内同名脚本 → 必须拒 |
| 2 | 阻断 | **授权位摘要不含任何参数**，无法实现「防批准 A 执行 B」。`plan_digest` 只取 `step_id/kind/tool/requires_approval`，`PlanStep` 无参数字段 → 改参数后摘要不变 | `app/runtime/authorization.py:48-63`；`app/runtime/contracts.py:70-80`；清单 A5 | 批准对象必须绑定**参数集合**：把规范化参数摘要并入计划/授权比对，或在 ⑦ 额外比对「审批时冻结的 args_digest」。补用例：批准后改参数 → 必须 409 |
| 3 | 重要 | **审批通过后如何推进到 ⑧ 未定义**。⑤/⑥ 命中即 202 并终止请求；契约无「审批后恢复执行」端点；Y2 下重发消息得到新 run → 新授权位 NULL → 又一次 202，⑦ 可能永不生效。若改为「审批后从 ⑦ 续跑」，则跳过 ②③④ | 规格 §3.2；契约 ⑤ 行；`app/main.py:1836-1879` | 规格必须写明「审批后由谁、经哪个入口、从 ① 重跑全套闸门再到 ⑧」，并明确**禁止**任何跳过 ②③④ 的「已校验」缓存 |
| 4 | 重要 | 规格省略既有函数**两处 fail-open**：`run_records is None` 直接 return（闸门整体不生效）；`authorization is None` 直接 return（无法区分「本不需要审批」与「应审批但缺授权位」） | `app/runtime/service.py:141,150-160`；规格 §3.2 ⑦ | 要求执行器装配后做**启动期断言**（`run_records` 必须存在）；明确 `authorization is None` 只允许出现在「无需审批」分支，且该分支不得触达 ⑧ |
| 5 | 重要 | **一次审批 = 授权整个 plan**：`decide_approval` 通过时用 `state.plan` 全量写授权位，与具体 `approval_id` 无关 → 多个审批项时任一被批准即放行全计划 | `app/runtime/service.py:116-134`（L130）、`:162-171` | 授权位必须与被批准的审批项及其对应步骤子集绑定；多审批项运行逐项授权，未批准步骤在执行器里逐步骤校验 |
| 6 | 重要 | 「可执行名」正向白名单只写在 Q2，**未进九步闸门**也无失败语义；④ 是黑名单（命中即拒）。`env`/`busybox`/`toybox`/`timeout`/`setsid`/`nice` 等包装器不在 A 类，其参数里的真实命令对 R1 不可见 | 规格 §3.2.1 R1/R2/R3、A 类、Q2；契约九步闸门 | 把「可执行名 ∈ 白名单」写成 ④ 的**前置 fail-closed 判定**并给失败码；A 类补包装器作第二道。补用例：`env rm -rf /workspace/x`、`busybox rm -rf /` → 必须拒 |
| 7 | 重要 | 黑名单**参数口径缺口**：① R1 未定义长选项 `=` 形态（`--in-place=.bak`、`--directory=/`），B 类按 `-i`/`-C /` 字面写；② `tar` 漏 `-P`；③ `chmod` 漏 `0777`、`a+rwx`、`--reference=`；④ 未防「先释放符号链接再经链接写出」的 tar 逃逸，未列 `cpio`/`7z`/`bsdtar` | 规格 §3.2.1 R1、B 类 | 补齐规范化口径与等价别名/数值形态，要求解包默认不跟随符号链接；每条补「必须红」样本 |
| 8 | 重要 | **规格没有工具目录清单，也没有各工具的风险档**。B5 要求每个工具有风险档并说明何为 `critical`，用例 5 依赖 `critical` 工具，但规格找不到任何具体工具条目与定档 → 「风险定档」这一半闸门无法验收 | 规格 §3.1、§5 用例 5；清单 B5 | 评审前把工具清单 + 每工具 `risk_level/has_side_effect/reversible` 作为规格附录写死；明确 `critical` 的承担工具 |
| 9 | 重要 | §3.4 的**文件级落库字段没有落点**，且与「无新迁移」矛盾：「只落摘要 + args_digest + sha256 + 字节数」需要表或对象存储元数据位，但 §3.4 末条与 §4 判定「本段不新建表」，Y3 又明确不落这些键 | 规格 §3.4、§3.7 Y3；`app/audit/models.py:63-100`；契约审计口径 | 明确文件元数据的唯一落点（既有某表某列 / 对象存储元数据 / 承认本段不落 sha256），三者择一并回改契约，不得两套并存 |
| 10 | 重要 | 「明细不含自由文本」与**复用 `reason`** 自相矛盾：`reason` 是白名单内的自由文本键，实现者塞拒绝原因时会把命令串/路径/异常 message 落审计 | `app/audit/models.py:65`；规格 §3.4、§3.7 Y3；契约审计口径 | 把 `reason` 约束为**受控枚举码**（如 `path_denied`/`blacklisted`/`param_invalid`），在 build 侧加枚举校验 + 测试；自由文本一律不落 |
| 11 | 重要 | 「容器内不得持有模型密钥」与「模型调用由工作台侧发起」缺乏可判定前提：实测显示模型调用由 dsh 进程解析并发出，而规格未写明 dsh 进程跑在工作台宿主还是工具容器内 | 规格 §3.2/§3.3；清单 F3.2 实测 | 明确 dsh 进程位置、凭据来源与生命周期（短期凭据 / 工作台侧代理），据此重述「无凭据」口径；未写明前不得声称「容器内不持有模型密钥」 |
| 12 | 重要 | 「`restrict` 空交集 → 降级为最小集」是非 fail-closed 回落：空交集本意常是「不开放任何工具」，回落成「最小集」等于给出**更多**能力，且 dsh 默认最小集由上游定义 | 规格 §3.5 | 改为 fail-closed：空交集 ⇒ 不装配任何工具或**拒绝启动**并告警；「降级」只能降到我方显式空集/白名单，不得使用 dsh 默认集 |
| 13 | 重要 | **闸门后补**：真实执行落在段二-3，`agent_key` 校验收紧却在段二-4 → 违反 B7「开始路由到真实执行**前**必须校验存在且启用」 | 规格 §1.4；清单 B7；契约变更点 2 | 把 `agent_key` 收紧并入段二-3 的开通前置，或明确段二-3 不接任何用户入口（仅内部离线触发） |
| 14 | 建议 | §3.3 遗漏：未要求 `--security-opt no-new-privileges`；工作卷未要求 `nosuid,nodev,noexec`；未规定 mount propagation；未提 `/dev/shm` 与 `/tmp` 可写可执行面；未要求镜像按 digest 钉死；未规定崩溃后**孤儿容器回收** | 规格 §3.3；清单 F4 | 补进 §3.3 作为硬要求，并对 `noexec` 工作卷 + shebang 场景补负向用例 |
| 15 | 建议 | C 类路径**匹配语义未定义**（绝对路径与基名 glob 混列），漏 `/proc/self/maps`、`/proc/self/mem`、`/proc/sys/**`、`/sys/**`、`/dev/**`；大小写不敏感文件系统上 `.ENV`/`ID_RSA` 可绕过 | 规格 §3.2.1 C 类、R2；§5 用例 11 | 明确匹配算法（realpath 绝对路径前缀/glob、基名 glob、平台大小写归一），补齐路径族并补「必须红」样本 |
| 16 | 建议 | R2 声称覆盖「Windows 侧 `pwsh`/`cmd`」不成立：只做 basename 大小写不敏感，未剥离 `.exe/.cmd/.bat/.ps1` | 规格 §3.2.1 R2、A7 | 规范化里剥离可执行扩展名；若本段只在 Linux 容器运行，应在规格里**限定适用范围**，不要以「兼容 Windows」为已堵住的理由 |
| 17 | 待核实 | ③ 的 realpath 校验与 ⑧ 执行之间存在 **TOCTOU** 窗口；且**硬链接不改变 realpath**，若敏感文件与工作卷同设备，`/workspace/x` 硬链接到 `/etc/shadow` 可同时绕过 ③ 与 C 类。缺证据：工作卷挂载类型/设备隔离、校验→执行的原子性保证 | 规格 §3.2 ③、§3.3 | 规格补写：校验与打开动作原子化（fd 传递 / `openat2`），工作卷与容器根**不同设备**；§5 补 TOCTOU/硬链接用例 |
| 18 | 待核实 | 用例 4 的预期「把黑名单挪到授权之后 → 必须红」**可能无法复现**：④ 挪到 ⑦ 之后仍在 ⑧ 之前，黑名单命令依然被拒，测试不会变红 → 不变式缺少可观测断言点 | 规格 §3.2 不变式、§5 用例 4；契约九步闸门 | 把观测点改为「审批通过后的第二次执行尝试是否重新经过 ③④」，并据此写断言 |

**视角一结论**：不建议放行。方向正确（闸门先于执行器、黑名单为兜底、容器边界为主防线、无新迁移；且规格 §8.1 明确写了「闸门先于执行器」，**没有**「先跑起来再补闸门」的表述）。但存在 B3（shebang 绕过）与 B2（授权不含参数）两条阻断级缺陷；放行前至少解决 #1、#2、#3，并同步修好 #4、#5、#6、#7、#9（其中 #9 是自相矛盾的落库口径）；#8 应在段二-2 开工前补齐，否则该子段按 B5 无法验收。

### 附录 B 视角二：一致性与可测性（16 条）

| # | 严重度 | 问题 | 证据 | 建议处置 |
| --- | --- | --- | --- | --- |
| 1 | 阻断 | §3.7 Y2 要求承载任务带 `origin=conversation`，但 §4 声明「无新迁移」，而任务表**无 origin 列**；契约又把 origin 写成硬要求、规格写成「例如」→ 三份文档 + schema 无法同时成立 | 规格 §3.7/§4；契约变更点 4；`migrations/001_initial.sql:3-19`；`migrations/025` | 先定死 origin 的落点（新增列须推翻「无新迁移」走迁移 027 / 复用既有字段并写死约定），二者择一后回改契约 |
| 2 | 重要 | Y2 称承载任务「会出现在任务列表与任务维度指标中」，但系统**根本没有任务列表接口**（只有 `GET /tasks/{task_id}`），指标只按 `runtime_key` 聚合 → 契约的「标注或过滤」无落点、不可验收 | `app/main.py:1578/1621/1629`；`app/runtime/run_metrics.py:88-101`；契约变更点 4；规格 §3.7 | 规格点名承载任务会出现在哪个现存面（如 `/api/v1/collaboration-dynamics`）或明确需新增列表接口 |
| 3 | 重要 | Y3 只承诺扩 `ALLOWED_DETAIL_KEYS`，§3.4 把新增动作码写成「建议」，契约却列为必须；不扩 `AuditAction` 时 `GET /api/v1/audits?action=tool.executed` → **422** | `app/audit/models.py:11-56`、`:63-100`；`app/main.py:1478-1488`；规格 §3.4/§3.7；契约 | 规格把 `AuditAction` 与 `ALLOWED_DETAIL_KEYS` 扩展并列为硬要求，并补「漏扩即 422」的负向测试 |
| 4 | 重要 | 契约 ⑦ 行把「授权来源不在白名单」列为 `ensure_execution_authorized` 的失败条件；段一已交付的该函数**只比对 plan_digest、不校验来源** | 契约 ⑦ 行；`app/runtime/service.py:136-160`；`app/runtime/authorization.py:41-45` | 契约 ⑦ 删除「授权来源」，或注明来源校验发生在审批写入阶段 |
| 5 | 重要 | §4 称新增配置「由 `tests/test_env_templates.py` 类的静态契约守护」；该测试实际只覆盖 `sso_*` 与 5 个 Runtime 字段，**无「全部 Settings 字段必须进 staging 模板」断言** → 新增容器/超时/资源/总开关不会被守护 | `tests/test_env_templates.py:49-115`；`.env.staging.example:1-93`；规格 §4 | 维持承诺则须同步扩测试；否则 §4 改为「新增配置人工登记 + 补测试」，不要宣称已被守护 |
| 6 | 重要 | §5 用例未覆盖多个已锁定决策点：Q4（启动期断言）、Y2、Y3（不落 args_digest/sha256/虚拟路径/字节数）、§3.5 剖面锁死、§3.3 超时=终止容器、`--cap-drop ALL`/pids/memory/`docker.sock`/容器无凭据 | 规格 §5、§3.2.1 Q4、§3.7、§3.5、§3.3 | §5 逐项补用例并给「必须红」构造 |
| 7 | 重要 | §5 用例 12 要求 `python app.py`（Q1 禁脚本）被拒，但 A/B/C 三层**均无裸解释器脚本条目**（A8 只列 `python -c`/`node -e`）→ 该锁定值无落实点，契约 ④ 也只覆盖 A/B/C | 规格 §3.2.1 A8/Q1、§5 用例 12；契约 ④ 行 | A 类补「裸解释器可执行名」，或在 ②/④ 增设「脚本执行」判定，并在契约 ④ 注明状态码 |
| 8 | 重要 | §3.6 的「外置开关」未给配置项名、取值与判定口径；§5 用例 10「切回 mock 后全量全绿」的「必须红的方式」为**空**，无法与「从未启用 dsh」区分 | 规格 §3.6、§5 用例 10；`app/settings.py:254-255` | 明确环境变量名/取值/默认值与断言方式，并给用例 10 可复现的改坏→变红构造 |
| 9 | 重要 | Y2「自动创建承载任务 + Run」在现有代码下的可行性未写清：承载任务 `created_by` 必须等于操作者（否则运行可见性与 `/tasks/{task_id}` 直接 404）、`employee_key`/`risk_level`/`budget`/`idempotency_key` 取值、是否过 `ensure_can_create` 均未定 | `app/domain.py:112-119`、`:157-167`；`app/runtime/service.py:69-78`；`app/main.py:1578-1618` | §3.7 补：`created_by`=操作者、`employee_key` 来源、风险档/预算口径、幂等键推导、是否复用 `ensure_can_create` |
| 10 | 建议 | 规格内部基线不一致：§前置事实写 1417，§3.6 写 1434 | 规格 §前置事实/§3.6；清单 B11 | 统一到同一时点数值并注明取证时间 |
| 11 | 待核实 | §2.3「其余 245 个子包为 rc.2」与清单 F1 实测（246 条目、rc.2 230、另有 5 个 `node-addon-system*@0.1.2`）不一致；§4 又按 245 界定核对范围 | 规格 §2.3/§4；清单 F1 | 按 lockfile 实测重述数量（lockfile 不在本仓库 → 精确数字**待核实**） |
| 12 | 建议 | `agent_key` 收紧落点不对称：契约挂在 `POST /conversations` 并承诺 422，实际执行在 `POST .../messages`；规格未写端点/状态码/「历史会话停用后仍可读」 | 契约变更点 2；清单 B7；`app/main.py:1305-1326` | 规格补明校验端点、状态码与历史可读口径，三份对齐 |
| 13 | 建议 | §4「`package.json` 写精确版本」在本仓库无落点：根目录无 `package.json`，dsh 探针目录在仓库之外 | Glob 结果；规格 §4；立项 §14.1 | 明确 dsh 依赖与精确版本的登记位置（容器镜像构建文件/独立目录） |
| 14 | 建议 | §3.3 把「与工作台分主机」写成无条件硬约束，未保留清单 A2 的「开发期本机 Docker」，而 F4 取证本身即在本机 Docker 完成 | 规格 §3.3；清单 A2/F4 | §3.3 区分「开发期（本机 Docker，仅验证机制）」与「生产（分主机）」，与 A2 对齐 |
| 15 | 待核实 | §2.1「只有 `initialize`/`session.prompt`/`shutdown` 三个请求 + 4 个通知」属静态勘察；仓库内无 dsh 源码/协议文件可核对 | 规格 §2.1；清单 F3.2；立项 §15 #7 | 该枚举作为设计前提时须附原始协议报文；缺则标「待核实」 |
| 16 | 建议 | 清单自述状态已过期：头部称「段二自身还没有规格（§C1）」，而 A5/E5 又引用已存在的规格；规格现已含 §1.2 禁止事项与 §6 验收标准（正对应 C3） | 清单头部、§A5/§E5；规格 §1.2/§6 | 更新清单头部与 §E.5 的 C1/C3 状态描述 |

**视角二结论**：文档之间存在会误导实现的矛盾，建议暂不放行。需点名裁决的矛盾：①origin 三向互斥（阻断）；②承载任务影响面写错（无任务列表、指标只按 runtime 聚合）；③审计动作码强制程度不一致；④⑦ 闸门职责被契约放大；⑤模板守护承诺不成立；⑥基线数字自相矛盾；⑦子包版本数字冲突；⑧分主机约束与 A2/F4 不对称。此外 §5 未覆盖 Q4/Y2/Y3/剖面锁死/超时终止/容器加固，用例 10 无判定口径、`python app.py` 无落实点，属「无法判定通过/不通过」的验收条目。

### 附录 C 视角三：宪法合规与流程（18 条）

| # | 严重度 | 违反/缺失条款 | 问题 | 证据 | 建议处置 |
| --- | --- | --- | --- | --- | --- |
| 1 | 阻断 | 一章 1.4 变更控制；十章 10.2 新增字段三问；四章 4.4 结构变更走 migration | Y2 要求承载任务带 `origin=conversation`，但 `workbench_tasks` 无该列，插入语句列清单亦无；规格同时宣称「零新迁移」，自相矛盾 | 规格 §3.7/§4；`migrations/001_initial.sql:3-19`；`app/repository.py:72-78` | 先定死 origin 落点并回改 §4 与 §3.7 的迁移口径；若加列，按地基变更走专项评审 |
| 2 | 阻断 | 四章 4.2 关键写接口必须幂等 | 对话消息端点在本段变为触发真实副作用，请求体仍只有 `content`，**无幂等键**；重复 POST 将重复派生承载任务并重复执行副作用 | 契约请求体；`migrations/001_initial.sql:12`（任务有 `idempotency_key NOT NULL` + 唯一约束）；规格全文无「幂等」 | 定义服务端幂等口径（由 message_id / conversation_id + 发起人推导），§5 增「同一请求重放不产生第二次执行」反假用例 |
| 3 | 重要 | 四章 4.5 耗时操作不放请求链路同步做 | 规格与契约明确「同步、非流式」，容器起停与工具执行在 HTTP 请求链路内完成 | 规格 §3.2；契约（同步非流式） | 给出同步执行时长上限与超时降级口径，或改后台任务 + 轮询；「本段不做流」≠「必须同步」 |
| 4 | 重要 | 四章 4.2 接口三答；4.8 第三方集成（超时/重试/降级） | 规格称「工作台与执行环境之间只经一个受控执行接口」，但该接口的认证/授权/超时/重试/失败降级在契约中**完全缺失** | 规格 §3.3；契约无该接口定义 | 先在 `api-contract.md` 补执行接口契约（调用方身份与认证、超时与重试上限、失败降级），契约先行 |
| 5 | 重要 | 五章 数据归属；四章 4.4/4.7 | 规格只写「挂一个专用空工作目录」，未规定该目录是否按租户/按 run 唯一、运行后如何销毁与残留回收；跨租户不可见缺可验证判据 | 规格 §3.3、§6 异常用例 | 写明 per-run 目录唯一性、销毁时机与失败残留清理；补「A 租户运行看不到 B 租户目录」的证据判据 |
| 6 | 重要 | 十二章 12.3 开源与内容合规；三章 3.1 判据 5 | 规格把许可证只写成「逐项核对 245 个子包」，未把已查出的 14 个 LGPL（全为 `sharp`）与「若打进交付镜像/桌面产物须产出 `THIRD-PARTY-NOTICES`」设为交付物或开工闭环 | 规格 §4；清单 F5 | 把许可证结论与 NOTICES 产出列为交付物与验收项，明确本段是否分发镜像 |
| 7 | 重要 | 六章 6.1 风险分级 | §5 十二条用例未标 P0/P1/P2 与风险类别，无法证明本段（权限/数据）满足「每次必测 + 三类全覆盖」 | 规格 §5/§6 | 给用例标注风险级别，明确本段属 P0/P1 并声明三类覆盖映射 |
| 8 | 重要 | 六章 6.5 验收看证据；十三章 13.6 不确定标「待核实」 | 门禁清单登记的「最高优先级待验项」（`bash/write/edit` 的 `sandbox_permissions`/`justification` 升级通道真实行为）在 §5 无对应用例 | 规格 §3.5、§1.4；清单 F3.2 | 把该复验列为段二-1 明确交付判据，并在 §5 增加「升级通道被拒/不挂起」的用例与原始报文证据要求 |
| 9 | 重要 | 十三章 13.6 不编造数据 | 规格称「245 个子包」，清单实测为 246 条目，数字不一致且未标来源 | 规格 §2.3；清单 F1 | 以取证原始输出统一数字，或标「待核实」并说明口径差异 |
| 10 | 建议 | 六章 6.4 一条命令跑完全量 | §6 全量回归并列 5 类命令，未给**一条聚合命令**；`scripts/` 下无聚合脚本 | 规格 §6；`scripts/` 清单 | 明确一条聚合命令（等价 CI 四个 job），并在汇报中给出存档路径 |
| 11 | 建议 | 六章 6.3 反假测试有效性 | §5 用例 2、8、10 的「必须红的方式」为「—」，无法证明能区分正确与错误 | 规格 §5 | 对这三条补「改坏哪一处必须变红」的具体操作 |
| 12 | 建议 | 八章 8.1 环境五分开 | 规格只写「与工作台分主机」，未写开发/测试/生产三套环境的程序/配置/数据/权限如何分开；清单所述「开发期本机 Docker」未落进配置节 | 规格 §3.3/§4；清单 A2 | §4 配置登记区分三环境的镜像/主机/凭据，避免开发口径带进生产 |
| 13 | 建议 | 十章 10.5 清理纪律 | 段二-2 用「假执行器」离线验收，规格未把一次性假执行器/测试桩纳入收尾清理清单与用户确认流程 | 规格 §1.4/§6 | 验收标准加「临时假执行器清单经确认后清理，删完重跑正式测试」 |
| 14 | 建议 | 十一章 11.2 规则 3 写代码与验收分离 | §1.4 为四个子段给出独立验收判据，但未要求验收由独立只读方执行 | 规格 §1.4 | 写明各子段验收只读、不修改文件，并规定证据形式 |
| 15 | 建议 | 四章 4.1 前端四态齐备 + 统一请求层 | 规格让现有对话页触发真实工具执行，但未要求把加载/空/错误/无权限四态与前端用例纳入验收 | 规格 §1.1 E、§6 | 验收标准补对话页四态与前端用例 |
| 16 | 待核实 | 四章 4.9 AI 集成（超时/单会话最大轮次/额度上限） | §1.2 排除「单轮预算与成本熔断」，§3.3 有超时上限，但 4.9 另要求「单会话最大轮次」，规格未提由谁承担 | 规格 §1.2、§3.3 | 确认会话轮次上限承担方，或如实登记为「未覆盖」 |
| 17 | 待核实 | 四章 4.8 第三方集成 | 规格列了 dsh 无 cancel/无版本协商、超时由我们实现，但未写模型调用的重试上限与单次失败降级口径（整体开关回 mock ≠ 单次降级） | 规格 §3.5/§3.6 | 补模型/dsh 调用的重试上限与失败降级路径 |
| 18 | 待核实 | 二章 2.1 真源三件套 | 规格上位真源只列「立项文档 + 段一规格」，`docs/` 下无独立「功能清单」文档；若以立项文档充当功能清单，未显式说明 | 规格头部；`docs/**/*.md` | 确认三件套齐备并显式引用功能清单 |

**视角三结论**：**可送审，但不可放行**。规格在宪法二章「做什么/不做什么/影响什么/验收标准」、禁止事项可检查性、三类用例与反假测试纪律、五道防线覆盖，以及「沙箱不是安全边界」上明显优于一般规格，**承诺边界未被夸大**（不存在「沙箱=安全边界」式越界承诺），此项合规。必须先补第 1、2 条（阻断，均触及地基与真实副作用正确性）；放行前还需闭环第 4、5、6、8 条（重要）。本评审结论**不替代**清单 §B 的取证要求。

---

## 6. 修订要求（进入下一轮规格修订须闭环的清单）

**已裁决（按裁决改）**
1. 撤销 `origin=conversation` 要求；规格 §3.7、契约变更点 4 同步回改，与「零新迁移」保持一致（B1）
2. 以 `message_id` 作执行幂等键，并补「重放不产生第二次执行」的反假用例（B5）

**待修订（修法由规格给出，重审时确认）**
3. 授权对象绑定**规范化参数摘要**（B2）
4. ④ 前增设「可执行文件必须解析自受信任且不可写根」的 fail-closed 判定（B3）
5. 写明**审批通过后的推进链路**（谁、经哪个入口、从 ① 重跑全套闸门），并**禁止**跳过 ②③④（B4）
6. 修正 M1–M6（契约 ⑦ 职责、`AuditAction` 扩展、模板守护承诺、子包数字、`agent_key` 落点、基线 1417→1434）
7. 修正 G1–G7（Q2 白名单进闸门、黑名单参数口径与包装器、工具目录 + 风险档附录、落库落点、`reason` 枚举化、`restrict` fail-closed、`agent_key` 收紧并入段二-3）
8. 处理 P1/P2（dsh 进程位置与凭据来源；同步执行的时长上限或后台化）
9. 按 §4 建议补齐容器加固、路径匹配语义、用例可判定性、聚合测试命令、许可证交付物、四态验收、清理与验收分离等条目

> **重审触发**：以上闭环后，规格状态由「待评审」更新为「已评审（附评审记录）」，方可按清单 §C1 判据视为通过。

> **修订结果（2026-09-12，本轮已执行）**：规格已完成 §6 全部 9 项修订，逐条对应关系见规格 **§9「评审记录与本轮修订说明」**；规格状态更新为「**已修订 · 待重审**」，契约与门禁清单已同步回改。**本记录保持原样不改**（存档性质），后续结论以规格 §9 与重审为准。

---

## 7. 第二轮：规格重审结论（2026-09-12）

**送审对象**：第一轮修订后的规格。**方式**：3 个独立只读视角（A 阻断项闭合【对抗式】/ B 一致性与可测性 / C 宪法合规与回归风险），均被明确要求**不得采信规格 §9 的自述结论**，须回代码与原文证伪。

**结论：不予放行**（A 判「不予放行」；B 判「不建议放行」；C 判「可放行（附条件）」——**以更严为准**）。

### 7.1 对 5 条阻断项的独立判定

| 阻断项 | 重审判定 | 关键证据 |
| --- | --- | --- |
| **B1** 撤销 `origin` | ✅ **真闭合** | 规格与契约均无残留要求；`migrations/001_initial.sql:3-19` 确无该列 |
| **B2** 参数摘要 | ❌ **表面闭合**（阻断） | `PlanStep` 无参数字段（`app/runtime/contracts.py:70-75`）、`plan_digest` 只取 4 字段（`app/runtime/authorization.py:48-63`）、迁移 `026` 仅**单列** `authorized_plan_digest`（`026:9-12`），而规格声明「无新迁移」→ **现有代码下不可实现** |
| **B4** 审批后重跑 | ❌ **表面闭合**（阻断） | 待批动作**无结构化落点**（`app/runtime/state.py` 无该字段；`mock.py:98-103` 只把 action 塞进事件）；审批端点无执行推进（`app/main.py:1836-1879`）；且与 Y2「每条消息新建承载任务+Run」实质冲突——「禁止重发推进」**无强制手段** |
| **B5** `message_id` 幂等 | ❌ **表面闭合（机制整体失效）** | `message_id` 由 `append_message` 内 `uuid4` **每次新建**（`app/conversation/store.py:135`、`:333`）→ 重放拿不到相同值，幂等永不命中；请求体 `extra=forbid`（`app/main.py:1165-1167`），客户端无法提供键。（附带澄清：因无输入通道，**不存在**伪造他人 `message_id` 的攻击面） |
| **B3** ④-0 可执行来源 | ⚠️ **表面闭合**（重要） | 受信任根写成「如 `/usr/bin`」非规范值且**未进 §4 配置项**；PATH 语义自相矛盾；**`dsh-subprocess-local` 未禁用**（`docs/dsh-integration-preflight-checklist.md:193`）→ 模型可经它自起子进程，**④-0/④-1/④-2 整段闸门被整体绕过** |

### 7.2 第一轮修订**新引入**的问题

| # | 问题 | 证据 |
| --- | --- | --- |
| N1 | `reason` 若按**全局键级**枚举化，会打断既有 5 处自由文本写入 | `app/accounts/service.py:212`、`app/content/service.py:116`、`app/workforce/config.py:269`、`app/planner/service.py:127`、`app/orchestration/service.py:146` |
| N2 | **自相矛盾**：规格同时写「⑦ 单一 `plan_digest` 比对」与「逐项授权」——单三元组无法表达逐项 | 规格 §3.2（同段两处）；`migrations/026:9-21` |
| N3 | `restrict` 空交集「拒绝启动」作用域不清，可能因 `tool_allowlist` 默认 `[]` 打挂整个服务 | 规格 §3.5；`migrations/023:56` |
| N4/N5/N6 | 承载任务字段口径缺失；`202` 响应体未定义；受信任根未进配置项 | 规格 §3.7/§5；`migrations/001_initial.sql:3-19` |
| — | 契约子段映射写反（把 G7 又装回去）；清单 §B1 残留「245 个子包」；LGPL「压到 2 条」构成错误（`@img/sharp-linux-x64` 实为 **Apache-2.0**）；「唯一手段」绝对化 | 均为**修订者本轮引入**，已于第二轮更正 |

### 7.3 C 视角的正面结论（如实记录）

- **未发现任何一条被模糊措辞弱化**（无「必须→建议」「拒绝→告警」「数值→待定」式降级）；B2/B3/B4/B5/M2/M5/G1/G2/G5/G6/G7 均为**实质变严**。
- §3.1.1 工具清单与 `artifact.export` **可溯源**（立项 D8/D23/§15#9、清单 A3/A5、首轮评审 G3 的显式要求），**不属于凭空造需求**。

### 7.4 重审裁决项与用户决议（2026-09-12）

| # | 裁决项 | 决议 |
| --- | --- | --- |
| **R1** | B2/B4 落点 + 授权粒度 | **新增迁移 `027`**（授权项 + 待批动作）→ 支持**逐项授权**；**推翻「无新迁移」**，属地基变更须专项评审 |
| **R2** | 执行幂等键载体 | **请求头 `Idempotency-Key`** |
| **R3** | `dsh-subprocess-local` | **显式禁用**（与 `tool-bash` 同等） |
| **R4** | `restrict` 空交集作用域 | **只禁「真实执行」并告警**，不拒绝服务进程启动 |

### 7.5 第二轮修订与后续

第二轮修订已完成（逐条见规格 **§9.1**）：新增迁移 `027`、幂等改 `Idempotency-Key`、禁用 `subprocess-local`、`restrict` 作用域收窄、`fs.write` 拆分、`artifact.export` 闸门落点与导出侧校验、Y2 字段级口径、§3.3/§4 补 `--cpus`/受信任根/环境分开、§5 用例 14/16/21 改为可判定并新增 22–25、§8 补 U9–U11。

**后续：待第三轮独立复核**（须同时确认**迁移 `027` 这一地基变更的评审是否充分**）。复核通过后规格状态方可改为「已评审（附记录）」，并按清单 §C1 判据视为过闸。

---

## 8. 第三轮：独立复核结论（2026-09-13）

> **送审对象**：第二轮修订后的 `docs/superpowers/specs/2026-09-12-dsh-integration-design.md`（送审状态：**已修订（第二轮） · 待第三轮独立复核**）。
> **含送达的旁证**：`docs/api-contract.md`（「工具执行（P2a 段二）」）、`docs/dsh-integration-preflight-checklist.md`、`docs/delivery-gates.md`、`docs/delivery-readiness-checklist.md`。
> **方式**：**4 个独立只读视角**（A 对抗式阻断项闭合【以证伪为目标】/ B 迁移 `027` 地基变更专项 / C 一致性与可测性 / D 宪法合规与回归风险）+ 汇总方亲自复核。四视角**均被明确要求不得采信规格 §9、§9.1 与记录 §7 的自述结论**，须回源码与原文证伪；全程**未修改任何文件、未执行 git 命令、未连接数据库**（汇总方仅执行只读的 `pytest --collect-only`）。
> **结论**：**不予放行**（A/B/C/D 四视角**一致**）。
> **性质**：评审记录存档。本记录**不含修订**；规格与契约**本轮未做任何改动**（符合「写代码与验收分离」）。

### 8.1 对第三轮专项问题的答复：迁移 `027` 的「地基变更评审」是否充分

**答：不充分（阻断）。** 判据与证据：

1. 规格 §4 对 `027` 的**全部定义只有一句职责**（「承载 ⑥ 的待批动作与 ⑦ 的授权项…」），**无表名、无字段名与类型、无主键/唯一/外键/非空/CHECK、无索引、无租户列与隔离方式、无时间戳、无软删除、无回滚口径**，也无对既有表的结构变更。规格正文 grep「`CREATE TABLE`」「列清单」「草案」**均 0 命中**。
2. `027` 的**完整 DDL 只存在于 `docs/superpowers/plans/2026-09-12-dsh-foundation-design-draft.md`**，而该文件自述「**设计草案，不是阶段规格**」「状态：**待评审**」「**通过后才**把本草案 §1–§5 逐条并入规格与契约」「本文件本身不改任何代码、不建任何表」；且**规格/契约/清单三处均未引用它**。
3. 即：**第二轮重审的送审对象里没有数据模型本体**，无法构成宪法铁律四与 4.4 六步⑥所要求的那次专项评审（被评审对象不含 DDL，评审者无从评审）。
4. 对照宪法：铁律四「地基不随手翻…数据模型改动必须专项评审」；1.4「变更留痕：影响地基的变更必须写进变更记录（时间、原因、影响面、回退方式）」；4.4 六步⑥「**先出设计文件（`schema.sql` 或 migration）再建表**」；4.10.x「新增字段必答三问（旧数据如何兼容）」。

### 8.2 阻断项与证据

| # | 阻断项 | 证据 | 状态 |
| --- | --- | --- | --- |
| **R3-1** | **`027` 地基变更评审信息不足**（见 §8.1）：权威真源内无数据模型本体，DDL 仅在不被引用的待评审草稿中 | 规格 `:334`；`plans/2026-09-12-dsh-foundation-design-draft.md:1-6,46-124`；规格正文 grep「草案」= 0 | ✅ 四视角一致 · **阻断** |
| **R3-2** | **规格正文残留已推翻的旧口径，与 §4 互斥**：§3.4「**本段不新建表**（…授权位已有迁移 `026`）」、§3.7 Y2「**零新迁移**；**幂等键由 `message_id` 派生**（B5）」、§7 X5「§3.4 与 §4 迁移口径已同步为「**无新迁移**」」；契约同一段内 `:644-645` 已改、`:693`（Y2 决议行）**未改** | 规格 `:267`、`:305`、`:406`；`api-contract.md:644-645` vs `:693` | ✅ 已复核 · **阻断**（违反单一可信来源） |
| **R3-3** | **草稿 ⊃ 规格（规格-实现漂移）**：草稿多出两项**未进规格**的结构变更——① 对既有表 `workbench_run_records` 增补 `UNIQUE (run_id, tenant_id)`；② 新表 `workbench_execution_idempotency`（幂等落点）。规格 §4 只写 `027` 承载「待批动作 + 授权项」 | 草稿 `:84-98`、`:100-125`；规格 `:334` | ✅ 已复核 · **阻断**（①② 均属地基变更，未登记影响面与回退） |
| **R3-4** | **B2 / B4 / B5 在第三轮仍属「表面闭合」**（规格写了、按现有代码实现不了）：<br>· B2：`PlanStep` **无参数字段**；`plan_digest` 仅取 4 字段；`migrations/` 最大编号 **026**（`027` 不存在）<br>· B4：**待批动作无结构化落点**（`RuntimeState.approvals` 仅 `id→status`）；决议端点 `POST /runs/{run_id}/approvals/{approval_id}/approval` **不取回动作、不推进执行**<br>· B5：请求头通道可用（`CORS_HEADERS` 已含 `Idempotency-Key`），但「**返回既有结果**」**无存储落点**（该表仅存在于草稿）；且该头的**必填性与缺省语义未定义** | `app/runtime/contracts.py:70-75`；`app/runtime/authorization.py:54-63`；`app/runtime/state.py:12-23`；`app/runtime/mock.py:98-103`；`app/main.py:1834-1877`；`app/settings.py:448`；`migrations/` 目录（001–026） | ✅ 已复核 · **阻断** |
| **R3-5** | **`artifact.export` 缺「跨租户归属校验」的可验收项**（宪法五道防线第四道·数据归属）：§5 用例 25 只覆盖类型/大小/数量/嗅探/命名；契约只泛写「仅当属工作台受控范围且租户隔离/权限受控时才落」 | 规格 `:371`、`:384`；契约 `:672` | ✅ 已复核 · **阻断（合规）** |

### 8.3 重要项（16 条，按严重度）

| # | 问题 | 证据 |
| --- | --- | --- |
| **P1** | **N5 未闭合**：`202` 响应体在两份真源均标「待定」（契约自述「须在实现前定死并回改本节」）；规格全文 grep「`202`」= 0 命中 | 契约 `:641`、`:657` |
| **P2** | **N3 复活**：§3.5「空交集 **不得**拒绝整个服务进程启动」与 §3.6「设为 `dsh` 时启动期**必须断言**…`restrict` 非空集」方向相反（`tool_allowlist` 默认 `[]`） | 规格 `:272` vs `:296`；`migrations/023:56` |
| **P3** | **M5 只落了 2/3**：契约与清单已写 `agent_key` 收紧落点（执行入口 + `422` + 历史会话可读），**规格侧无端点与状态码** | 契约 `:642`；清单 `:82`；规格 `:43`、`:54` |
| **P4** | **扩 `AuditAction` 会打断既有守护测试**：`tests/test_frontend_audit_labels.py` 要求前端 `AUDIT_ACTION_LABELS` 与后端 `AuditAction` **全集相等**；规格 §4/§6 未把「同步前端动作标签」列为交付物 | `tests/test_frontend_audit_labels.py:30-36` |
| **P5** | **10 项新增配置无落点、无守护**：`WORKBENCH_AGENT_RUNTIME_BACKEND` / `WORKBENCH_EXEC_*` / `WORKBENCH_DSH_VERSION` / `WORKBENCH_ARTIFACT_EXPORT_ENABLED` 在 `app/settings.py`、`.env.staging.example`、`tests/test_env_templates.py` **三处零命中**；`WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值仍未定（U11） | 规格 `:335`、`:337`、`:443` |
| **P6** | **门禁清单与规格口径相反**：清单仍写 `dsh-subprocess-local`「未禁用…需评估」，规格 §3.5 已裁决「**显式禁用** + 启动断言」 | 清单 `:193` vs 规格 `:288` |
| **P7** | **⑨ 之前各步的「通过路径」无留痕定义** → 用例 16「审计中重现 ②③④ 的判定记录」**无可观测对象**（契约只定义失败路径写 `tool.blocked`） | 用例 16（规格 `:362`）；契约 `:669` |
| **P8** | **用例 10 的「必须红」未点名构造**（未指定缺哪一项剖面断言及注入方式）；用例 18 受 U11（超时默认值未定）阻塞；用例 24 真实层依赖段二-3 环境 | 规格 `:356`、`:364`、`:370`、`:443` |
| **P9** | **聚合测试命令无落点**：规格要求「一条命令等价 CI 四个 job」，`scripts/` 12 个脚本无聚合脚本，根目录无 `Makefile`/`package.json` | 规格 `:394` |
| **P10** | **真源三件套形式不达标**：以立项 §14.1（`期/内容/前置` 分期表）充当「功能清单」，不含「谁用/输什么/得什么结果/角色与权限/正常流程/异常边界」（宪法 2.1）；且规格 §8 U9 仍标「待核实」与头部「已裁决」并存 | 规格 `:4` vs `:441`；立项 `:440-453` |
| **P11** | **`restrict` 的「第二道防线」与 §8 U10 自认未取证并存**；§3.6 的开关断言在无 `restrict` 取证前无法判定（未表述为夸大，但验收口径需明确「未取证即未通过」） | 规格 `:272`、`:442` |
| **P12** | **`ensure_execution_authorized` 的 fail-open 处置在规格与草稿间措辞冲突**：规格「段二**不得**沿用 `run_records is None` 直接 return」 vs 草稿「**缺省保持旧行为**（保证段一测试不破）」 | 规格 `:170`；草稿 `:155` |
| **P13** | **§4「环境分开」未覆盖宪法 8.1 五分开全项**（只登记镜像/主机/工作卷根/凭据来源/开关）；`docs/private-deployment-runbook.md` 全文无执行主机章节，规格未把更新 runbook 列为交付物 | 规格 `:336` |
| **P14** | **许可证结论措辞强于证据**：规格称「逐包核对**已完成**」，清单 §F5 自认「**未逐包比对**许可证正文与声明值（583 条目）」，NOTICES 未产出、镜像内二次扫描未做；该遗留**未进** §8 U 清单 | 规格 `:339`；清单 `:294-297` |
| **P15** | **前端 422 文案错配**：`admin-web` 对话页把 `422` 固定映射为「消息内容不符合要求…」，`agent_key` 收紧后提示会误导 | `admin-web/src/features/conversation/state.ts:29` |
| **P16** | **Σ 残留旧数字/旧状态**：`docs/delivery-readiness-checklist.md`（1159/975/admin-web 60/108）、清单头部「待评审·一轮评审不予放行」与 §E.2/§E.5、立项 `:447`「待重审」，均与现状（1434 基线、规格第二轮已修订）不一致 | 见各文件行号 |

### 8.4 汇总方亲自复核的条目（8 条，不采信子视角结论）

1. 草稿文件**确实存在**且含完整 DDL（`workbench_tool_actions`、`workbench_execution_idempotency`、对既有表 `workbench_run_records` 的 `UNIQUE` 增补），且自述「不是阶段规格／待评审／通过后才回填规格」——**R3-1/R3-3 成立**。
2. 规格正文对草稿的引用：grep「草案」**0 命中**——**R3-1 成立**（DDL 在权威真源之外）。
3. 规格 `:267` / `:305` / `:406` 三处旧口径与 §4 `:334` 互斥；契约 `:644-645` 已改而 `:693` 未改——**R3-2 成立**。
4. `PlanStep` 确无参数字段（`app/runtime/contracts.py:70-75`）——**R3-4/B2 成立**。
5. `migrations/` 目录最大编号为 **026**，`027` 不存在——**R3-4/B2 落点未落地成立**（本段未开工，属"规格承诺"）。
6. `tests/test_frontend_audit_labels.py:30-36` 确为**全集相等**断言——**P4 成立**。
7. `app/settings.py:448` 的 `CORS_HEADERS` 确含 `Idempotency-Key`（请求头通道可行）；`app/` 下无任何 `Idempotency-Key` 解析逻辑——**R3-4/B5 的"通道可行、落点缺失"成立**。
8. 清单 `:193` 仍写 `dsh-subprocess-local`「未禁用…需评估」——**P6 成立**。

> **推翻的子视角结论 1 条**：视角 A 引用记录 §7.1 的「PATH 语义自相矛盾」经复核**不成立**（规格全文仅一处 PATH，且为「不使用 PATH 解析」，与「工作卷不得出现在该根内」不冲突）。R4 的 `restrict` 作用域在 §3.5 内**真闭合**（与 §3.6 的冲突见 P2）。R3 的 `dsh-subprocess-local` 禁用**规格侧真闭合**，未闭合的是**门禁清单**（P6）。

### 8.5 已证实为真的正面结论（如实记录）

- **后端基线 1434 属实**：汇总方实测 `pytest --collect-only` 逐文件加总 = **1434**（管理台 147 / 伴侣端 37 / 桌面 19 为文档原值，本次**未复核**）。
- 首轮 M1/M2/M3/M4/M6 的修正**确已落到契约/清单**；`origin=conversation` **无活跃残留**；段二子段映射**未写反**；Y2 承载任务字段与 `migrations/001`/`013`/`025`、`app/domain.py:157-167` **对得上**；`reason` 作用域收窄（仅 `tool.*`）与既有 5 处自由文本写入**不冲突**；`.github/workflows/ci.yml` 确有**四个 job**。
- **未发现任何一条被模糊措辞弱化**（无「必须→建议」「拒绝→告警」「数值→待定」式降级）；**承诺边界未被夸大**——规格 §3.3/§3.5/§8 明确「沙箱不是安全边界」「不得据此声称隔离达成」「不承诺容器内不持有模型密钥」，无「沙箱=安全边界」式越界承诺。

### 8.6 未验证（不得当作已解决）

1. **无任何运行时取证**：真实 dsh、容器、Docker、模型凭据、`restrict` 运行期拦截、沙箱升级通道真实行为**一律未验证**；按规格口径，用例 21 **当前状态为"未通过"**（不是"无法判定"）。
2. lockfile **不在本仓库内**，`246 / rc.2 230 / 5` 精确计数与逐包许可证正文**未核**。
3. `027` 尚未落地，其**可重复执行性、升级路径、对既有 1434 项测试零破坏**均无从验证。
4. 前端 147 / 伴侣端 37 / 桌面 19 用例数未复核（U8）。
5. 草稿的 git 跟踪状态未核（本轮未执行 git 命令）。

### 8.7 复核裁决项（需用户决议）与最小闭环清单

**待用户裁决（4 项）**

| # | 裁决项 | 说明 |
| --- | --- | --- |
| **R5** | 草稿地位的处置 | **A**：把草稿 §1–§5 并入规格与契约（含两份 DDL、对既有表 `UNIQUE` 增补、参数规范化算法、装配与失败语义表），草稿转为归档；**B**：把草稿升格为正式的《迁移 027 地基设计》独立真源并由规格显式引用。二者择一，不得维持"规格无 schema、DDL 在未引用草稿里"的现状 |
| **R6** | `Idempotency-Key` 必填性 | 草稿设为「缺省 = 幂等关闭」，与宪法 4.2「关键写接口必须幂等」相悖 → 需裁定：**必填**（缺省 `422`）／**可选但缺省时不得触发真实执行**／**可选且缺省关闭**（须写明例外依据） |
| **R7** | `026` 与 `027` 的权威与事务边界 | 草稿 §1.4 已给「`027` 为唯一授权权威、`026` 降级为快照、同事务写入」的**草案口径**，需用户确认后写入规格正文 |
| **R8** | 功能清单形式 | 维持「以立项 §14.1 充当」需补足宪法 2.1 的能力维度（另附能力清单），或另建独立文档并显式引用 |

**最小闭环清单（闭环后触发第四轮独立复核）**

1. 按 R5 的裁决处置草稿；在规格内给出可评审的 `027` 定义（表名/字段/类型/约束/索引/租户列/时间戳/软删除/回滚/前进兼容），并对**既有表 `workbench_run_records` 的结构变更**单列影响面与回退方式；
2. 清除规格 `:267`/`:305`/`:406` 与契约 `:693` 四处旧口径；
3. 定死 `202` 响应体、`Idempotency-Key` 必填性与缺省语义（R6）、`026↔027` 主从与事务边界（R7）；
4. 统一 §3.5/§3.6 的 `restrict` 空交集语义，并同步门禁清单 §F3；
5. 按 §8.2/§8.3 补齐交付项与用例：`artifact.export` 跨租户归属校验、前端动作标签同步、`WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值、10 项配置的模板登记与守护测试、聚合命令、⑨ 之前各步的判定留痕；
6. 由**未参与起草的独立方**就数据模型本体（`027`）再做一次只读专项评审并留痕（宪法 1.4/铁律四）。

> **重审触发**：上述 6 项闭环后，规格状态方可由「已修订（第二轮） · 待第三轮独立复核」改为「**已修订（第三轮） · 待第四轮独立复核**」；**复核通过前不得写第一行实现代码**，清单 §C1 不得判为过闸。

### 8.8 独立性声明（本轮）

- 四个视角**互不共享推理**，也未见起草与修订过程的对话；均被要求**不得采信**规格 §9/§9.1 与记录 §7 的自述结论，先证伪后判定（本轮据此推翻记录 §7.1 的「PATH 语义自相矛盾」1 条）。
- **汇总方偏差声明**：本记录汇总方与既往前两轮的汇总方（曾参与本规格 §3.2.1、§3.7、契约新增章节的撰写）为同一助手身份，故**不构成本规格的独立评审人**；本轮独立性由上述四个只读视角承担。汇总方在 §8.4 标注的"亲自复核"仅覆盖列出的 8 条，其余条目**未逐条复现**。
- 本记录**不替代**开工门禁：即使规格评审通过，`docs/dsh-integration-preflight-checklist.md` §B 的取证要求仍需独立闭环。

### 8.9 本轮裁决结果（2026-09-13，用户决议）

| # | 裁决项 | 决议（用户采纳推荐项） | 修订时的落地要求 |
| --- | --- | --- | --- |
| **R5** | 草稿地位处置 | **并入规格**（方案 A） | 草稿 §1–§5 作为规格附录写入（建议落点：规格新增 §4.1「迁移 `027` 设计（附录）」，含两份 DDL、对既有表 `workbench_run_records` 的 `UNIQUE (run_id, tenant_id)` 增补、参数规范化算法、装配与失败语义表）；草稿文件保留并标注「**已并入规格 §4.1，本文件转归档**」；契约同步引用。**不得**在规格只写"见某文档"而不含本体 |
| **R6** | `Idempotency-Key` 必填性 | **头可选；缺键 ⇒ 不触发真实执行** | 规格与契约须写明：带键 ⇒ 真实执行 + 幂等（命中返回既有结果）；**缺键 ⇒ 沿用既有 stub 语义**，不创建承载任务/运行、不产生任何真实副作用。补反假用例：**缺键时不得新增承载任务与运行** |
| **R7** | `026` 与 `027` 的授权权威 | **`027` 为唯一授权权威；`026` 降为运行级快照** | 写入规格 §3.2/§3.7：执行放行**只看 `027` 逐项**；`026` 三列**保留、不删、不回填**，语义降级为「运行级授权快照」；`decide_approval` 的「写 `027` 状态 + 写 `026` 快照 + 适配器决议」**同事务**；`authorization is None` **仅允许**出现在"无需审批"分支且该分支**不得触达 ⑧** |
| **R8** | 真源三件套·功能清单 | **另建独立《功能清单》文档** | 在 `docs/` 下新建独立功能清单，按宪法 2.1 六要素（谁用 / 输什么 / 得什么结果 / 角色与权限 / 正常流程 / 异常与边界）逐项写；**先补齐 P2a-2 与已交付期次**，未开工期次标「待细化」；规格头部与门禁清单显式引用；规格 §8 的 **U9 状态更正为已闭环** |

> **R8 属对既有裁决的逆转（须留痕）**：2026-09-12 曾裁决「本项目**不另建**独立的功能清单文档，以立项 §14.1 充当」。本轮复核（视角 D）判定该处理不满足宪法 2.1 的功能清单内容要求，**R8 据此推翻原裁决**，改以独立文档承载。原裁决自本决议起失效。
> **R6 属对草稿口径的更正**：草稿 §3 的「缺省 = 幂等关闭」不采纳，改为「缺省不触发真实执行」——既守住宪法 4.2（真实副作用必须幂等），又不破坏既有对话行为。
> **以上四项为第三轮修订的前置输入**；修订须连同 §8.7 的 6 条最小闭环清单一并执行，完成后再触发第四轮独立复核。

---

## 9. 第四轮：独立复核结论（2026-09-13）

> **送审对象**：第三轮修订后的 `docs/superpowers/specs/2026-09-12-dsh-integration-design.md`（状态：**已修订（第三轮） · 待第四轮独立复核**），含新增 **§4.1 迁移 `027` 设计附录**。
> **方式**：**4 个独立只读视角**（A `§4.1` 数据模型本体专项【地基变更，权重最高】/ B 对抗式：第三轮 9 项改动真闭合与旧口径零残留 / C 一致性与可测性 / D 宪法合规与回归风险 + 本轮新增登记物）+ 汇总方**亲自复核 4 条**。四视角均被要求**不得采信规格 §9/§9.1/§9.2 的自述**；全程只读，未修改文件、未执行 git、未连数据库。
> **结论**：**不予放行**（A 判「不予放行」；B/C/D 判「附条件放行」——**以更严为准**）。
> **性质**：评审记录存档；**本记录不含修订**，规格与契约本轮**未改动**。

### 9.1 结论摘要

第三轮修订把 `027` 从「一句话职责」提升为**可评审的数据模型本体**（§4.1 含两份 DDL、字段规范对照、既有表 `UNIQUE` 增补、参数规范化算法、主从/事务/回退、迁移工程约定、装配与失败语义），并要求第四轮专项确认。**本轮专项结论：信息量已够评审，但本体本身存在阻断级缺陷**，且并入过程**遗漏了归档草稿的关键一节**，导致第三轮判定的 B2「参数摘要/逐项授权」**重新失去落点**。

**正面结论（如实记录）**：第三轮 9 项改动经对抗式逐条证伪后**均为真闭合**（R5 的五要素"一项不缺"、R6 四处＋契约 Y2 已改、R7、R8 独立功能清单真存在并被三处引用）；旧口径在**规范正文与契约/清单活跃文本内行为层零残留**；§5 用例**1–27 连续且均可构造、可判定**；§6 的 `scripts/verify_all.py` 属规格明写"本段须新增"，**不构成缺口**；基线 1434 在四处文档一致。

### 9.2 阻断项与证据

| # | 阻断项 | 证据 | 汇总方复核 |
| --- | --- | --- | --- |
| **R4-1** | **`027` 文件内语句顺序错误**：§4.1.1 的 `CREATE TABLE` 声明 `FOREIGN KEY (tenant_id, run_id) REFERENCES workbench_run_records (tenant_id, run_id)`，而父表侧 `UNIQUE (run_id, tenant_id)` 到 §4.1.2 才 `ADD`；按 §4.1.1→§4.1.2→§4.1.3 落盘，PostgreSQL 建外键时即报 *there is no unique constraint matching given keys for referenced table*。规格**未写死顺序** | 规格 §4.1.1（DDL 首行标"文件名：migrations/027…"）、§4.1.2；`migrations/013:2`（仅 `PRIMARY KEY (run_id)`） | ✅ **我亲自复核**：§4.1.1 在 §410 行前、§4.1.2 的 `ALTER … ADD CONSTRAINT` 在 §L415–419 → **成立** |
| **R4-2** | **§4.1 遗漏归档草稿 §2 的关键内容**：`ensure_execution_authorized` 的**签名变更**（新增 `actions: Sequence[ToolAction]` 与"缺省保持旧行为"）未并入；另三条语义（⑥ 落库失败 → `503` 不进入等待 / ⑥ 落库本身不写 `tool.*` 审计 / 执行器只执行 `status='approved'` 且 `plan_digest` 一致的行）亦未承接。**直接后果：⑦「计划摘要 + 参数摘要比对 + 逐项校验」在现有签名下无接口落点**（`PlanStep` 无参数字段、`authorization.py` 只比对 4 字段摘要）——第三轮 B2 的根因**重现** | 草稿 `plans/2026-09-12-dsh-foundation-design-draft.md:145-157`；规格 §4.1 全节无该表；`app/runtime/contracts.py:70-76`、`authorization.py:54-63`、`service.py:136-160` | ✅ **我亲自复核**：草稿 L151–157 的签名变更表**确实存在**，§4.1 确无 → **成立** |
| **R4-3** | **枚举三处不一致**：① `reason`（§3.4／契约 `:671`）为 **6 值**，§4.1.1 的 `reason_code` 为 **9 值**（多 `not_in_catalog`/`approval_denied`/`approval_expired`）却自称"与 §3.4 同源"，而 §4.1.6 失败表**实际用到** `not_in_catalog` → 用例 20 判定歧义；② `outcome` 在 §4.1.3 的 `CHECK` 为 **3 值**，§4.1.6-7 的 `execution.outcome` 为 **4 值**（含 `failed`），"首次执行即失败是否写幂等行"未定义 | 规格 `:267` vs `:392`；`:436` vs `:526`；契约 `:671` | ✅ 成立（子视角 B 与自己均命中） |
| **R4-4** | **参数规范化算法自相矛盾**：§4.1.4 规则 5「小数去尾随 0」与同节单测判据「`{"a":1}` vs `{"a":1.0}` → **不同**（类型不同）」**无法同时成立** | 规格 §4.1.4（规则 5 与其后判据表） | ✅ 成立（源自归档草稿，并入时未修） |
| **R4-5** | **幂等表 `message_id TEXT NOT NULL` + 外键 与「三态都写幂等行（含 `rejected`）」冲突**：契约对闸门拒绝只承诺 `403/409`，**未承诺追加 assistant 消息**；若被拒不落消息，则幂等行**无法插入** → "重放返回首次的拒绝"不可兑现 | 规格 §4.1.3（`message_id NOT NULL`＋"三态都写"）；契约 `:640`/`:651-659` | — （未逐条亲验，属静态推断，方向明确） |
| **R4-6** | **`approval_id` → `027` 行无映射键**：`027` 列无 `approval_id`（只有 `step_id`），而决议入口以 `approval_id` 为参数、§4.1.6-4 又要 `resume(run_id, approval_id)` 取回冻结动作 → **"审批通过后取回"不可实现** | 规格 §4.1.1 列清单、§4.1.6-4；`app/main.py:1836-1847`；`app/runtime/mock.py:55/101`（`approval_id` 与 `step_id` 不总是同一值） | — |
| **R4-7** | **（合规）`feature-inventory.md` 断言 OpenClaw = MIT 但无来源，且与既有清单冲突**：`docs/superpowers/poc-license-checklist.md:9` 仍标「OpenClaw｜**待审查**」 | `docs/feature-inventory.md` §3.4（C1/C2 括号内直书"MIT"）；`poc-license-checklist.md:9` | ⚠️ **事实正确但留痕不全**：汇总方已在 GitHub 核过 `openclaw/openclaw` 的 `LICENSE` 原文 = **MIT**（`Copyright (c) 2026 OpenClaw Foundation`）→ 需**补来源并同步更新许可证清单**，而非改结论 |
| **R4-8** | **（合规）C3「执行不可检测」约束强度不足**：现文只到"未评估不得作为方案"，**未禁止**在评估完成前进入**评审/立项/排期/原型/对外表达**链路；且未指定评估责任方与留痕载体 | `feature-inventory.md` §3.4 C3；宪法 12.4（文案与实际能力一致）、13.6（不编造引用） | ✅ 成立（用户已裁决该项含义为"执行不可检测"） |

### 9.3 重要项

| # | 问题 | 证据 |
| --- | --- | --- |
| **P1** | **契约自身自相矛盾**：`:590` 写「已决议过 `409`『审批已决议』」，`:592` 又写「同一次运行的并发决议以**最后写入获胜**」；实码是 `409`（`app/runtime/mock.py:112-113` 抛 `ApprovalAlreadyDecided`）→ §4.1.1/§4.1.5 把 LWW 那句当"既有口径"引用，落地必与 `409` 打架 | 契约 `:590`/`:592`；`mock.py:112-113`（**汇总方亲验**） |
| **P2** | **§4.1.6-7 的 `execution` 字段未回改契约**：规格写"须同轮回改契约"，但契约 `:587` 仍只有 `{run_id, approval_id, status, run_status}` | 规格 §4.1.6-7；契约 `:587`（**汇总方亲验**：无 `execution`） |
| **P3** | **§4.1.4 的 6 条规范化单测与用例 13 不对应**（用例 13 是"批 A 改 B → 409"，不含规范化边界）→ 参数摘要等价性口径在 §5 无编号可追溯 | 规格 §4.1.4 与 §5 用例 13 |
| **P4** | **§4 的 10 项新增配置零落点、零守护**（`app/settings.py`/`.env.staging.example`/`tests/test_env_templates.py` 三处零命中）；`WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值仍未定（U11） | 规格 §4；三文件 grep |
| **P5** | **§3.7 残留 1 处措辞**：仍以已推翻的「零新迁移」作为撤销 `origin` 的**生效理由** | 规格 §3.7（"它与「零新迁移」互斥"） |
| **P6** | **变更记录留痕缺口**：宪法 1.4 要求地基变更写入**变更记录**（时间/原因/影响面/回退），而仓库内**无独立变更记录载体**；对既有表 `workbench_run_records` 的 `UNIQUE` 增补目前只写在规格 §4.1.2 自身 | 宪法 1.4；规格 §4.1.2 |
| **P7** | **本轮新增登记物缺授权留痕**：§B14、组 10、`feature-inventory §3.4`、研读报告的授权来源未写入本记录（§8.9 仅覆盖 R5–R8）→ 与本仓库"变更留痕"自要求不一致 | 本记录 §8.9 与各文件 |
| **P8** | **回归用例登记清单缺口**：`agent_key` 收紧后既有 `tests/test_conversation_api.py` 是否仍绿未登记；前端 **422 文案错配**（`admin-web/.../conversation/state.ts` 固定映射为"消息内容不符合要求"）第三轮 P15 未闭环；`027` 未登记 `.env.staging.example`（`tests/test_staging_assets.py` 为全清单相等断言 → 永久 `blocked`）；PWA/桌面**解析未知字段**无回归用例 | 规格 §5/§6 与各文件 |
| **P9** | **组 10 可补 4 条更高优先级自查**：出网归口唯一性、`reason` 不得含自由文本、密钥不进前端产物、`027` 迁移回滚演练 | `delivery-remaining-checklist.md` 组 10 |

### 9.4 汇总方亲自复核的条目（4 条）

1. **R4-1 成立**：规格 §4.1.1 的 `CREATE TABLE`（含复合外键）确在 §4.1.2 的 `ALTER … ADD CONSTRAINT UNIQUE` **之前**；`migrations/013:2` 确只有单列主键。
2. **R4-2 成立**：归档草稿 `:151-157` 的**函数签名变更表确实存在**（`ensure_execution_authorized(..., actions: Sequence[ToolAction])` ＋"缺省保持旧行为" ＋调用点 `service.py:105`），而 §4.1 确未并入。
3. **P1 成立且更严重**：`mock.py:112-113` 实码为 `ApprovalAlreadyDecided`（→ `409`），**而契约 `:590` 与 `:592` 自相矛盾**（409 vs 最后写入获胜）——问题出在**契约内部**，不止"契约 vs 实码"。
4. **P2 成立**：契约 `:587` 的决议端点响应体确无 `execution` 字段。

> **推翻/更正子视角 1 条**：视角 D 判定"`feature-inventory.md` 断言 OpenClaw=MIT 属**无来源的编造**"——经汇总方核实，**该事实本身正确**（已读 `openclaw/openclaw` 的 `LICENSE` 原文 = MIT），问题在于**未留来源、且未同步更新 `poc-license-checklist.md` 的"待审查"状态**。故 R4-7 的处置是"补来源 + 同步清单"，**不是**"改成待核实"。

### 9.5 未验证（不得当作已解决）

1. **`027` 未落地**（`migrations/` 最大编号仍为 `026`）：可重复执行性、升级路径、对既有 1434 项零破坏**均未验证**。
2. **真实运行时取证全缺**：dsh、容器、`restrict` 运行期拦截、沙箱升级通道、U1 四条判据**一律未取证**。
3. **参数规范化算法与 §4.1 DDL 均未实测**（本轮为静态评审）。
4. 基线 1434：文档四处一致；**独立计数**本轮之前已用 `pytest --collect-only` 逐文件加总核过 = 1434（本轮视角 C 受终端策略限制未能重跑）。
5. 前端 147 / 伴侣端 37 / 桌面 19 未复核（U8）。

### 9.6 第四轮修订闭环清单（建议）

**阻断（必须先闭环）**
1. **写死 `027` 文件内语句顺序**：`ALTER … ADD UNIQUE (run_id, tenant_id)` **前置于**两张 `CREATE TABLE`。
2. **把草稿 §2 并入 §4.1**：`ensure_execution_authorized`（或等价网关）新增 `actions`/参数摘要入参的口径与旧行为退化规则；并补「⑥ 落库失败 `503` 不进入等待」「⑥ 不写 `tool.*` 审计」「执行器只执行 `approved` 且 `plan_digest` 一致的行」三条。
3. **统一枚举**：`reason` / `reason_code` 三处（§3.4、契约 `:671`、§4.1.1/§4.1.6）二选一并补 `①` 步取值；`outcome` 3 值 vs 4 值择一，并定义"首次执行即失败是否写幂等行、重放语义"。
4. **修 §4.1.4 矛盾**：规则 5 与单测判据二选一，并同步 §5（为规范化 6 条判据补独立用例编号）。
5. **解决幂等表可插入性**：显式约定"被拒也追加 assistant 消息"，或把 `message_id` 改为可空并说明响应重建口径。
6. **补 `approval_id` → `027` 行的映射定义**（列或唯一映射规则）。
7. **（合规）`feature-inventory.md` 的 OpenClaw/Codex 许可补来源**，并同步 `poc-license-checklist.md`（OpenClaw：待审查 → 已核对 MIT；Codex：已核对 Apache-2.0；Hermes：仍待核实，需上游链接）。
8. **（合规）强化 C3 约束**：改为"未完成**书面合规评估并留痕**前，不得进入任何评审/立项/排期/原型/对外表达链路"，并指定评估责任方与留痕载体。

**重要（放行前）**
9. 修契约自相矛盾（`:590` 的 `409` 与 `:592` 的"最后写入获胜"）并按实码统一口径。
10. §4.1.6-7 的 `execution` 字段**回改契约**（或显式标注"由 §4.1.6-7 定义、实现时同步"）。
11. 10 项配置登记为清单 §B 的"守护测试扩项"判据，并**先定死 `WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值**。
12. 清 §3.7 的「零新迁移」措辞残留。
13. 补"受影响既有测试 → 预期动作（扩/改/新增）"回归登记清单（含前端 422 文案、PWA/桌面解析未知字段、`027` 模板登记）。
14. 落一条**变更记录**（对既有表 `UNIQUE` 增补的时间/原因/影响面/回退），或指定 `§4.1.2` 即变更记录并写入项目文件组织。
15. 把本轮新增登记物（§B14、组 10、§3.4、研读报告）的**授权来源补进本记录**。
16. 组 10 补 4 条（出网归口唯一性、`reason` 无自由文本、密钥不进前端产物、`027` 回滚演练）。

> **重审触发（第四次更新）**：以上 1–8 闭环、9–16 处置后，规格状态方可改为「**已修订（第四轮） · 待第五轮独立复核**」；**在此之前不得建表、不得写第一行实现代码**，清单 §C1 不得判为过闸。

### 9.7 独立性声明（本轮）

- 四个视角**互不共享推理**，均被要求**不得采信**规格 §9/§9.1/§9.2 与记录 §8 的自述结论，须回原文与代码证伪（本轮据此推翻子视角 D 的"编造"定性 1 条）。
- **汇总方偏差声明**：汇总方与前几轮为**同一助手身份**，且**本轮 §4.1 由其本人撰写并入**，故**不构成本规格的独立评审人**；本轮独立性由上述四个只读视角承担。汇总方"亲自复核"仅覆盖 §9.4 列出的 4 条，**其余条目未逐条复现**。**"自己写的部分由自己参与评审"是本轮的一个结构性弱点**——建议第五轮把 §4.1 的确认交由**未参与撰写的独立视角**完成。

### 9.8 第四轮修订的执行与授权留痕（2026-09-13）

**用户裁决**：① 执行**完整修订**（§9.6 的 8 阻断 + 8 重要项）；② 许可证条目（R4-7）**同步两份文档**。

**本轮（第四轮修订）实际改动清单**——**授权留痕**（此前 §9.3 的 P7 指出：第三轮之后新增的登记物缺授权留痕，本节一并补记）：

| 文件 | 改动 | 授权来源 |
| --- | --- | --- |
| `docs/superpowers/specs/2026-09-12-dsh-integration-design.md` | §4.1 头部加"执行顺序"（R4-1）；**新增 §4.1.7**（并入草稿 §2：签名变更＋`ToolAction`＋三条语义，R4-2）；§4.1.1 加 `approval_id` 列＋唯一索引（R4-6）与两处对照表更正；§4.1.3 `message_id` 可空（R4-5）＋`outcome` 扩 4 值（R4-3）＋响应重建口径；§4.1.4 规则 5 修正（R4-4）；§4.1.5 并发口径改「首写获胜」；§3.4 `reason` 扩 9 值（R4-3）；§3.7 清措辞残留；§6 加**受影响既有测试回归登记**表；头部状态 → 已修订（第四轮）· 待第五轮；**新增 §9.3** | 用户裁决：完整修订 |
| `docs/api-contract.md` | `:587` 补 `execution` 字段（重要项 10）；`:592` 并发口径改「首写获胜 `409`」（重要项 9）；`:671` `reason` 扩 9 值（R4-3） | 同上 |
| `docs/dsh-integration-preflight-checklist.md` | **新增 §B15**（10 项配置的落点与守护判据，含 `WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值必先定死） | 用户裁决：完整修订 |
| `docs/feature-inventory.md` | §3.4 C1/C2/C3：**补许可来源**、**强化 C3 约束为「冻结」**（R4-8） | 用户裁决：完整修订 |
| `docs/superpowers/poc-license-checklist.md` | OpenClaw → **已核对 MIT**；Codex → **Apache-2.0**；**Hermes → 已确认上游为 [`NousResearch/hermes-agent`](https://github.com/NousResearch/hermes-agent) 并核对为 MIT**（确认依据：本机知识库 entity 笔记 `Hermes-软件.md` 自述「Nous Research 出品的本地 AI 智能体框架」，且用户已在实际使用）；补 OpenMausBot（Apache-2.0 + `enterprise/` 警示）、OpenWorkBuddy（PolyForm NC）两行 | 用户裁决：同步两份文档（R4-7）+ 用户补充 Hermes 链接（2026-09-13，所给为占位符，由汇总方检索定位） |
| **新增** `docs/change-record.md` | 变更记录载体（宪法 1.4）；登记 `027` 对既有表 `workbench_run_records` 的约束增补（时间/原因/影响面/回退） | 用户裁决：完整修订（重要项 14） |
| `docs/delivery-remaining-checklist.md` | 组 10 补 10.9–10.12（出网归口唯一性、`reason` 无自由文本、密钥不进前端产物、`027` 回滚演练） | 用户裁决：完整修订（P9） |

**仍未闭环（第五轮前必须处置）**：`WORKBENCH_EXEC_TIMEOUT_SECONDS` **默认值仍未定**（U11）——本轮已在清单 §B15 登记为"实现前必须先定死"，但**数值本身需用户裁决**（建议在段二-1 取证后按实测定值，避免拍脑袋）。

**重审触发**：见规格 §9.3 末段——第五轮独立复核须**由未参与 §4.1 撰写的视角**确认数据模型本体；通过后规格方可改为「已评审（附记录）」。

---

## 10. 第五轮独立复核（2026-09-13）

### 10.1 送审对象与方法

- **送审对象**：**第四轮修订后**的 `docs/superpowers/specs/2026-09-12-dsh-integration-design.md`（状态「已修订（第四轮） · 待第五轮独立复核」），含 **§4.1（4.1.1–4.1.7）**、§5 用例 1–27、§6、§8、§9.3。
- **四个只读视角**（互不共享推理，均为**未参与 §4.1 撰写**者）：
  - **A**：§4.1 迁移 `027` 数据模型本体「**独立确认**」（§9.7 要求的那一方）
  - **B**：对抗式——第四轮 16 条是否真闭合、是否引入新矛盾
  - **C**：一致性与可测性（§5 逐条可判定性 + 文档间一致性）
  - **D**：宪法合规、回归风险、第四轮新增物合规性
- **统一纪律**：**不得采信**规格 §9.x、记录 §9.6/§9.8 的任何"已修订/已闭环"字样，须回原文与实码逐条证伪。

### 10.2 四视角结论汇总（**以更严为准**）

| 视角 | 结论 | 要点 |
| --- | --- | --- |
| **A（数据模型本体）** | 🔴 **不予放行** | 真闭合 5 / 表面闭合 3（R4-2、R4-4、R4-5）；2 条阻断 + 5 条重要 |
| **B（对抗式）** | 🟡 附条件放行 | 16 条中 13 真闭合、2 部分、1 夸大；未引入阻断级新矛盾；5 项放行条件 |
| **C（一致性与可测性）** | 🟡 附条件放行 | 结构对齐良好；2 阻断 + 5 重要（均为"承诺已写、判据未落"） |
| **D（宪法合规与回归）** | 🟡 附条件放行 | 未发现 §4.1 新增阻断；8 项条件中 5 项属**硬性合规缺口** |
| **汇总结论** | 🔴 **不予放行** | 依「以更严为准」：A 判不予放行，且 A 正是 §9.7 指定的独立确认方 |

> **本轮已解决 §9.7 的结构性弱点**：§4.1 的确认**首次由未参与其撰写的视角（A）承担**，不再由汇总方自评。

### 10.3 阻断项（**均由汇总方亲验**，见 §10.6）

| # | 阻断项 | 判定依据 |
| --- | --- | --- |
| **R5-1** | **参数落点缺失**：规格 6 处要求"冻结并**取回参数**"（§4.1.1 列注 `:360`、§4.1.6 调用链 `:514`、§4.1.7-1 `:540`、-3 `:542`、§3.2、契约、用例 23），而 **§4.1.1 DDL 列清单（`:357-383`）无参数承载列**（只有 `args_digest`）→ ⑦ 参数摘要比对、⑧ 执行、用例 13/23（均 P0）**不可实现** | A 判「表面闭合」；**B 判「真闭合」——两视角分歧，汇总方亲验判 A 正确**（缺列是文本事实） |
| **R5-2** | **幂等表无结果码列**：`workbench_execution_idempotency` 列清单（`:436-446`）无 `code`/`status_code`，而 `:463` 承诺"缺 `message_id` 时按 `outcome` 返回**确定的**结果码"；`rejected` 可能为 `422/403/409`、`failed` 可能为 `504/502` → **响应重建不可兑现** | A；真阻断同源（R4-5 只做了"可空"一半） |
| **R5-3** | **§4.1.4 规范化 6 条判据仍挂靠用例 13**（`:484` 写"对应 §5 用例 13"），而用例 13 是"批 A 改 B ⇒ 409"，**断言对象不同**；§5 内**无任何用例编号**承载这 6 条 → 规范性判据**无判据落点** | C；**第四轮已指出未改** |
| **R5-4** | **§4.1.7/§4.1.6 新增三项语义无 §5 用例**：①「缺省保持旧行为」退化路径（`:548`）；② 装配三断言（`:506`）；③ 决议端点 `execution` 响应体（`:534`）→ 第四轮**新写入规格的承诺无验收入口** | C |
| **R5-5** | **（合规）`change-record.md` 漏登两张新表**：全文仅登记既有表 `workbench_run_records` 的 `UNIQUE` 增补；`workbench_tool_actions` 与 `workbench_execution_idempotency` **同为数据模型变更**却未登记 → 不满足宪法 **1.4** 变更留痕 | D；汇总方亲验（文件全文） |
| **R5-6** | **（合规）Hermes「MIT ✅ 已核对」证据链不足**：给出的确认依据是**本机知识库笔记 + 用户在使用**，**非上游 `LICENSE` 原文**；而 `poc-license-checklist.md:16` 自身要求"任何复用须另过许可评审" | D；与宪法 12.3 / 13.6 相邻 |
| **R5-7** | **（一致性）跨文档状态漂移**：规格头部 `:7` 已是「已修订（第四轮）· 待第五轮」，但**门禁清单 `:6`**（仍写"已修订（第三轮）· 待第四轮"且"第三轮修订已完成"）、**功能清单 `:73`/`:119`**、**立项 `:447`** 均未回改 → 违反铁律三（单一可信来源） | B/D；汇总方亲验 |
| **R5-8** | **（一致性）本轮前新增的四份文档相互互斥且过度承诺**：`architecture.md:47`「② 搬代码……**目前一个都没有**」 vs `moat-boundaries.md:40`（OpenClaw `computer-use` 列为 **✅ 可搬（vendor）** 示例） vs `capability-ownership-map.md:47`（同物记 **B 类**）→ 构成"许可允许 ⇒ 可直接复用"的读法；且 `capability-ownership-map.md:56` 把 OpenClaw 记为「**D/C**」、`:47` 又记 **B**，**自违**该文 §1「只能四选一 / 同一能力只允许一个归属」 | D；汇总方亲验 |
| **R5-9** | **（行为冲突）**`multi-adapter-coexistence-spec.md` 的 **I3**（"未知事件不得冒充失败"）与 **`api-contract.md:606`**（"未知事件统一映射为 `run.failed`"）及实码 `app/runtime/adapters/common.py:140` **直接冲突**；该规约自述"**草案·待评审**"，却被 `architecture.md:58` 提升为"**动手前先查表**"的强制入口 → 未评审内容未回写真源即生效（铁律三） | D；汇总方亲验 |

### 10.4 重要项（合并去重）

| # | 重要项 | 来源 |
| --- | --- | --- |
| I-1 | §4.1.4 **规则 5 新矛盾**：`:477`「`1.0` 与 `1.00` 亦视为不同」在 `:479` 的 `json.dumps` 口径下**不可实现**（输入为已解析 JSON 值，Python `float` 不保留尾随零）——**第四轮修订引入的新矛盾** | A/B |
| I-2 | **④ 黑名单置 `rejected`**（`:525`）未定义 `decided_by`/`decided_at`，而同表 CHECK（`:379-382`）**强制非空** → 按字面实现撞 CHECK | A/B |
| I-3 | `reason_code` / `decision_source` **无库级 `CHECK`**，规格亦**未登记"应用层+测试守护"的例外理由**（对比 `:412` 软删除的显式例外登记风格）→ 第四轮"二者择一"**只做了一半** | A |
| I-4 | §4.1.7-5「**缺省保持旧行为**」用语未与被 §3.2 禁止的 fail-open（`run_records is None → return`）划清界限，"旧行为"未定义 → 实现者可合法保留 fail-open | A（B 判"张力但非硬矛盾"，以更严记） |
| I-5 | §9.3 **第 8 行自述不实**：称"清除「零新迁移」措辞残留"，实际 `:327` 字面仍在（仅加注作废） → **夸大** | B；汇总方亲验 |
| I-6 | §3.7 规范正文仍有 2 处作废口径字面（`:306`「`message_id` 派生」、`:327`「零新迁移」） | B |
| I-7 | `delivery-remaining-checklist.md:156` 计数未同步（"以下**八条**"→ 实为 **12 条**） | B/C |
| I-8 | `202`（`pending_approval`）幂等行在**审批后执行失败**（`:526-527`）时**不被更新**，其重放语义未定义，与 `:462`"重放与首次完全一致"存在张力 | A |
| I-9 | 用例 18 **无法约束 U11**（用临时超时值即可变绿）→ 验收标准漏掉"默认值"这一交付项 | C |
| I-10 | 用例 26 状态码**不唯一**（`403`/`404` 分界未定死），无法判定"该次拒绝是否正确" | C |
| I-11 | `ToolAction` 8 字段**无任何断言**；幂等表的四态写行/并发收敛/响应重建**无测试判据** | C |
| I-12 | §6 回归表遗漏：`201` 新增 `run_id`、签名变更的**具体测试文件与调用点**、`state.ts` 文案**现状描述与实际代码不符**、`verify_all.py` 与 CI 的联动守护 | D |
| I-13 | `approval_denied` / `approval_expired` 两个枚举值**无生产者**（§4.1.6 失败表未使用） | A |
| I-14 | `args_digest` 带 `sha256:` 前缀（`:480`），而实码 `plan_digest` 无前缀（`app/runtime/authorization.py:64`）→ 两套摘要口径混用 | A |
| I-15 | 该表为**就地更新的状态表**却无 `updated_at`（对照 `023:23` / `runtime_states`） | A |
| I-16 | 组 10 **未覆盖"越权复测"**（宪法第五章第二道防线）；10.1 的"审计不可删"**缺宪法明文依据** | D |
| I-17 | 重跑 ① 的 `409`（`:522`）与契约闸门表 ① 的 `422`（`:654`）**未区分阶段**，易被读作矛盾 | B |
| I-18 | C3 处置行**内部自相矛盾**（"评估结论为负面则移除" vs "默认移除而非待评估"）且**未指定评估责任方** | D |

### 10.5 汇总方亲验记录（11 条，**全部成立，无一条被推翻**）

按记录惯例，汇总方对本轮关键结论亲自回原文复核：

| # | 亲验对象 | 结果 |
| --- | --- | --- |
| 1 | §4.1.1 DDL 列清单（`:357-383`）无参数列 vs `:360`/`:514`/`:540`/`:542` 要求"取回参数" | ✅ **成立**（R5-1 真阻断） |
| 2 | §4.1.3 列清单（`:436-446`）无结果码列 vs `:463` 承诺"确定的结果码" | ✅ **成立**（R5-2） |
| 3 | §4.1.4 判据 `:484` 挂靠用例 13 | ✅ **成立**（R5-3） |
| 4 | §4.1.4 规则 5（`:477`）与序列化（`:479`）、输入定义（`:473`）三者不可兼得 | ✅ **成立**（I-1） |
| 5 | §4.1.6-5 ④ 行（`:525`）只写 `status`/`reason_code` vs CHECK（`:379-382`） | ✅ **成立**（I-2） |
| 6 | `change-record.md` 全文仅 1 条记录，标题即"对既有表增补唯一约束" | ✅ **成立**（R5-5：漏登两张新表） |
| 7 | 门禁清单 `:6`、功能清单 `:73`/`:119`、立项 `:447` 仍写"第三轮/待第四轮" | ✅ **成立**（R5-7） |
| 8 | `architecture.md:47`「一个都没有」 vs `moat-boundaries.md:40` vs `capability-ownership-map.md:47` | ✅ **成立**（R5-8：三处互斥） |
| 9 | `capability-ownership-map.md:56` 记「**D/C**」、`:47` 记 **B** vs 其 §1「只能四选一」 | ✅ **成立**（R5-8：自违规则） |
| 10 | `api-contract.md:606`「未知事件统一映射为 `run.failed`」 vs 规约 I3 | ✅ **成立**（R5-9） |
| 11 | `delivery-remaining-checklist.md:156`「以下八条」 vs 实际 10.1–10.12 | ✅ **成立**（I-7） |

### 10.6 与前四轮的关系

- **推翻**：视角 B 判 **R4-2「真闭合」** 与视角 A 判 **「表面闭合」** 冲突；汇总方亲验后**采信 A**——R4-2 的三条语义与签名确已并入，但**承载它们的参数列不存在**，故 §4.1.7 只是"口径抄回"，⑦ 在重跑路径上仍不可实现（与第三轮 B2 同根）。
- **延续**：R4-3 的"受控枚举守护方式二者择一"**只做了一半**（枚举已统一，守护方式未择一，且未登记例外）——第三/四轮同类问题第三次出现。
- **本轮新发现（第四轮未预见）**：R5-5（变更记录漏登新表）、R5-8（四份新文档互斥且过度承诺）、R5-9（规约与契约行为冲突）——三者均与**本轮前新增的文档**有关，属**流程性问题**（未评审内容被提升为决策入口）。

### 10.7 待用户裁决（**不得由汇总方自行决定**）

| # | 待裁决 | 选项与后果 |
| --- | --- | --- |
| **J1** | **R5-1 参数落点**（决定 §4.1 地基取向） | **甲案**：`027` 增列承载规范化参数（如 `args_json JSONB`），并**显式声明与 §3.4「永不落正文」的边界**（哪些参数正文允许入库、`fs.write` 的 `content` 如何处置），同时回改草稿口径；**乙案**：另建受控参数落点，并**同步修改** §3.2 / §4.1.6 / 契约 / 用例 23 **四处**表述（不得只改一处）。**甲案改动小但触及"正文是否入库"这条既有红线；乙案不动红线但要改四处。** |
| **J2** | **R5-2 结果码落点** | ① 幂等表增列（`status_code`/`code`）；或 ② 在 §4.1.3 定义「`outcome` → 唯一确定码」映射表（须与契约 `:654-661` 相容） |
| **J3** | **四份新增文档的处置**（R5-8/R5-9） | ① 补授权留痕 + 标注「未评审、不得作放行依据」，并修正三处互斥与"唯一归属"自违；或 ② 把 `architecture.md:58` 的"动手前先查表"降级为"参考"。**（当前它们已作强制入口但从未评审）** |
| **J4** | **Hermes 许可证据**（R5-6） | ① 提供上游 `LICENSE` 原文/链接以收口；或 ② 降级为"待核对正文" |
| **J5** | **I3 冲突处置**（R5-9） | ① 走"改契约先改文档 + 影响评估"流程（触及已交付稳定组件）；或 ② 明确该规约**不构成**对既有行为的要求 |
| **J6** | `WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值（U11，第三次挂起） | 建议段二-1 容器实测后定值 |

### 10.8 独立性声明（本轮）

- 四个视角**均为未参与 §4.1 撰写的只读方**，**本轮首次满足 §9.7 对独立确认的要求**（第五轮的全部价值即在此）。
- 汇总方本轮**只做汇总与亲验**（§10.5 的 11 条），**未参与任何视角的判定**，也**未修改任何设计内容**。
- 汇总方**仍未构成对 §4.1 的独立评审人**（§4.1 由其撰写）；本轮 A 视角的判定即为此设立。

### 10.9 重审触发

**J1–J5 裁决 + R5-1~R5-9 闭环后**，规格方可由「已修订（第四轮） · 待第五轮独立复核」改为「**已修订（第五轮） · 待第六轮独立复核**」；**在此之前不得建表（`migrations/027`）、不得写第一行实现代码**，清单 §C1 不得判为过闸。

---

**汇总方在收尾自检中另发现 1 处残留（如实登记，本轮未改，属更早期规格）**：`docs/superpowers/specs/2026-09-11-runtime-state-persistence-design.md:127/132` 的「并发修改 **最后写入获胜**，登记为已知限制」举例写作"如**并发决议**"——与该规格自身无关（它讲的是运行状态整行 upsert），但"并发决议"这一举例已被段二统一为**首写获胜 `409`**（见契约 `:592`）。**处置建议**：保留其原意（运行状态覆盖），但把"如并发决议"改为"如并发状态写入"，属**另一期规格的独立小修**，不并入段二修订（避免越界）。同类措辞亦见 `api-contract.md:580`（运行状态整行 upsert 场景，**语义正确**，无需改）。

---

## 11. 第五轮修订的执行与授权留痕（2026-09-13）

### 11.1 用户裁决

| 项 | 裁决 |
| --- | --- |
| **J1（R5-1 参数落点）** | **按「甲案」执行**——`027` **增列承载参数**，并**显式声明与 §3.4「永不落正文」的边界** |
| 其余 | 用户明确指令：**修复 R5-1 ~ R5-9 全部阻断项** |

**J2 / J3 / J4 / J5 未单独裁决**，本轮按汇总方建议执行（**如不同意可回改**）：
- **J2** → 取「幂等表**加列**」（`http_status`，比"映射表"更硬）；
- **J3** → 取「四份文档**降级为参考** + 补授权留痕 + 标注未评审**不得作放行依据**」；
- **J4** → **不采用原两选项，改为取回上游 `LICENSE` 原文**（经 GitHub 官方 API，记录 SHA）——更优；
- **J5** → 取「明确该规约**不构成**对既有行为的要求」（不动已交付稳定组件）；
- **J6（`WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值）** → **仍挂起**（第三次），建议段二-1 实测后定值。

### 11.2 本轮实际改动清单（**授权留痕**）

| 文件 | 改动 | 授权来源 | 对应阻断项 |
| --- | --- | --- | --- |
| `docs/superpowers/specs/2026-09-12-dsh-integration-design.md` | §4.1.1 增 `args_json` + **「正文边界」六条**；§4.1.3 增 `http_status` + 响应重建改可兑现口径 + 重放与审批后执行关系定死；§4.1.4 规则 5 删除不可实现要求 + 判据改指用例 28；§4.1.6 调用链补参数取回口径；§4.1.7 第 1/3/4 条同步 + 缺省语义消歧 + `ToolAction` 注记 + 评审范围行更正；**§5 增用例 28–31**；头部状态 → 已修订（第五轮）· 待第六轮；**新增 §9.4** | 用户裁决 J1（甲案）+ 修复 R5-1~R5-9 | R5-1、R5-2、R5-3、R5-4（+ I-1、I-4、I-8、N-9） |
| `docs/api-contract.md` | 「审批通过后的推进」补**参数取回口径**（控制参数取 `args_json`；正文从事实源重读并重算摘要比对；正文不落持久层） | 同上 | R5-1 |
| `docs/change-record.md` | **补登两张新表**（`workbench_tool_actions` / `workbench_execution_idempotency`），四要素齐备 | 同上 | R5-5 |
| `docs/superpowers/poc-license-checklist.md` | 头部口径更新；Codex / Hermes / OpenClaw **改为上游 `LICENSE` 原文直取 + 记录 SHA**；Hermes 原证据链更正声明 | 同上 | R5-6 |
| `docs/dsh-integration-preflight-checklist.md` | 头部（日期/状态）、**§C1**、§C3、§E.5 状态同步至第五轮/待第六轮 | 同上 | R5-7 |
| `docs/feature-inventory.md` | §3.3 规格真源状态、§5.3 第 3 条状态同步 | 同上 | R5-7 |
| `docs/superpowers/specs/2026-09-12-conversational-agent-platform-design.md` | §14.1 P2a-2 行的规格状态与评审轮次同步 | 同上 | R5-7 |
| `docs/architecture.md` | ② 行改「无已生效实例 + 唯一候选」；**新增状态声明**（三份文档未评审、降级为参考） | 同上 | R5-8、R5-9 |
| `docs/moat-boundaries.md` | §3 第 ② 类与 `architecture.md` 口径统一；§7 补 **R5-8 更正登记**与**授权/状态登记** | 同上 | R5-8 |
| `docs/capability-ownership-map.md` | 第 20/21/30 行改**单一归属**（删「D/C」），候选归属走「先补一行」规则 | 同上 | R5-8 |
| `docs/multi-adapter-coexistence-spec.md` | I3 作用域修正 + **新增「边界声明」三条** | 同上 | R5-9 |

### 11.3 四份未评审文档的授权与状态登记（**J3 / R5-9 收口**）

| 文档 | 授权来源 | 状态 |
| --- | --- | --- |
| `docs/architecture.md` §外部集成的判据（三模式） | **2026-09-13 用户提问**（"如何选用外部开源项目 / 搬代码还是接适配器"） | **未评审**；已加状态声明 |
| `docs/multi-adapter-coexistence-spec.md` | **2026-09-13 用户提问**（"适配器相互之间会不会冲突"） | **草案 · 待评审**；已加边界声明 |
| `docs/moat-boundaries.md` | **2026-09-13 用户表态**（"要自主可控、有护城河、不能是缝合怪"） | **未评审** |
| `docs/capability-ownership-map.md` | 同上 | **待评审** |

> **共同约束（本轮定死）**：四份文档**均不得作为放行依据**，**不得作为改动既有已交付组件的依据**；`architecture.md` 中"动手前先查表"的引用**已降级为"参考"**。

### 11.4 本轮未做

1. **未建 `migrations/027`**、**未写任何实现代码**、**未改 `app/` / `tests/`**（Grep 复核 `tool.executed|tool_key|workbench_tool_actions|workbench_execution_idempotency` 在 `app/`、`tests/` 内**零命中**）；
2. **未修记录 §10.4 的 18 条重要项**（已登记：I-2 ④ 行撞 CHECK、I-3 枚举无 CHECK 且未登记例外、I-5 §9.3 自述夸大、I-6 §3.7 字面残留、I-7 组 10 计数、I-10 用例 26 状态码、I-11 幂等表判据、I-12 §6 回归表遗漏、I-13 无生产者的枚举值、I-14 摘要前缀不一致、I-15 无 `updated_at`、I-16 组 10 未覆盖越权复测、I-17 ① 的 `409`/`422` 阶段区分、I-18 C3 行自相矛盾等）；
3. **未落地 `027`** → **可重复执行性、FK 列序、对既有 1434 项零破坏、升级/回退演练 一律未验证**；
4. **未提交、未推送**。

### 11.5 重审触发

见规格 **§9.4 末段**：第六轮独立复核须确认 ① §4.1 数据模型本体（含 `args_json` 正文边界六条与 `http_status`）；② §5 用例 28–31 的可判定性；③ R5-5 / R5-7 / R5-8 / R5-9 的落地。**在此之前不得建表、不得写第一行实现代码**，清单 §C1 不得判为过闸。

---

## 12. 第六轮独立复核（2026-09-13）

### 12.1 送审对象与方法

- **送审对象**：**第五轮修订后**的 `docs/superpowers/specs/2026-09-12-dsh-integration-design.md`（头部自称「已修订（第五轮） · 待第六轮独立复核」），含 §4.1（含新增的「`args_json` 的正文边界」六条）、§5 用例 28–31、§6、§8 U14、§9.4。
- **四个只读视角**（均为**未参与 §4.1 及第五轮修订**者）：A = §4.1 本体**二次独立确认**；B = 对抗式（第五轮 11 条是否真闭合 + 旧口径全库扫描）；C = 用例 28–31 可判定性与跨文档一致性；D = 宪法合规、越界核查、未评审文档降级是否真落地。
- **统一纪律**：不得采信规格 §9.4、记录 §11、头部状态及任何「已修订/已闭环」字样；须回原文与实码证伪。

### 12.2 四视角结论汇总（**以更严为准**）

| 视角 | 结论 | 要点 |
| --- | --- | --- |
| **A（§4.1 本体）** | 🔴 **不予放行** | **2 阻断**（B-1 工作卷/正文事实源不可兑现；B-2 control/body 声明无落点）+ 8 重要；判 R5-1 为「表面闭合」 |
| **B（对抗式）** | 🔴 **不予放行** | **1 阻断**（与 A 的 B-1 **独立收敛**）+ 2 处状态残留 + 1 处规约自相矛盾；11 条中多数真闭合 |
| **C（用例与一致性）** | 🔴 **不予放行** | **1 阻断**（① 步结果码 `422` vs `409` 正面冲突）+ 3 重要 |
| **D（合规与越界）** | 🟡 附条件放行 | **零代码改动**已证实；变更留痕三条四要素齐备；3 项条件（回退顺序 / 许可证据链未同步 / 祈使句未降级） |
| **汇总结论** | 🔴 **不予放行** | 依「以更严为准」：A/B/C 三方均判不予放行 |

### 12.3 阻断项（**均由汇总方亲验**，见 §12.5）

| # | 阻断项 | 证据（已亲验） |
| --- | --- | --- |
| **R6-1** | 🔴 **「重跑时 body 从工作卷读」不可兑现**（A 与 B **独立提出、结论一致**） | §4.1.1 边界第 4 条（`:423`）要求「**文件正文从工作卷读**」；而 §3.3 `:254`「工作目录 **per-run 唯一、生成即空；运行结束销毁**」、`:255`「每次运行一个容器，正常路径**任务结束即销毁**」；§3.2 `:152`「⑥ **先落库、再阻塞**」→ **跨请求审批等待期，容器/工作卷存活语义规格完全未定义**。更根本的是：§3.1.1 `:126-127` 中唯二带 `body` 的工具 `fs.write` / `fs.overwrite`，其 `content` 是**模型待写入的新内容**——**任何"从既有事实源重读"都不可能取到它**（`fs.write` 目标文件尚不存在；`fs.overwrite` 工作卷里是旧内容）。→ 按第 5 条落回"无可复读事实源"，这两类动作在审批路径下**永远 `422`**，即**审批与写文件不可兼得** |
| **R6-2** | 🔴 **`control`/`body` 的分类声明**无落点（A） | §4.1.1 边界第 1 条（`:420`）要求「由**工具目录 schema 逐参数声明**」；而 `ToolCatalog` 字段表（§3.1 `:96-103`）**只有 6 个字段**（`key`/`params_schema`/`risk_level`/`has_side_effect`/`requires_approval`/`reversible`）——**无 control/body 声明字段**；§3.1.1 工具表（`:120-129`）的「`params_schema` 要点」列亦**未标注任何参数类别**。→ **声明方与承载字段双重缺失**，属"写了做不到"；且因"默认 `control`"，实现者会把 `content` 当控制参数入库 → **正撞 §3.4「永不落正文」红线** |
| **R6-3** | 🔴 **① 步结果码在契约与规格间正面冲突**（C） | 契约 `api-contract.md:654`「① 白名单｜工具不在当前组装面内｜**`422`**」 vs 规格 `:536`「① 工具已不在组装面｜**`409`**」——**同一步骤、同一失败条件、两个码**；且契约 `:664` 明写重跑"**从 ① 重跑全套闸门**"→ 闸门表 ① 行同样作用于重跑路径。**直接击穿 R5-2 刚建立的「`http_status` 为唯一结果码来源」**（§4.1.3 `:476`） |

### 12.4 重要项（合并去重，**均未修**）

| # | 重要项 | 来源 |
| --- | --- | --- |
| J-1 | **跨文档状态残留 2 处**：`change-record.md:20`「第四轮修订后**待第五轮独立复核**」（同文件 `:36`/`:48` 已是"待第六轮"→ **文件内自相矛盾**）；`capability-ownership-map.md:33`「规格已修订（**待第五轮复核**；未开工）」 | B/C |
| J-2 | **`feature-inventory.md:97` 的 Hermes 许可括注未同步**：仍写「确认依据为**本机知识库 entity 笔记** … 且用户已在实际使用」，且**未记 SHA**、无更正说明 → **R5-6 只落到 `poc-license-checklist.md`** | D |
| J-3 | **`multi-adapter-coexistence-spec.md` 自相矛盾**：`:42` 边界声明「**禁止以本规约文件直接驱动改动**」 vs `:89`「待改 `common.py`」、`:110`「可立即做（小改）」 | B |
| J-4 | **`202` 重放响应体不可重建**：契约 `:642` 的 `202` 体含 `approval_id`，而幂等表 DDL（§4.1.3 `:448-466`）**无 `approval_id` 列** → R5-2 只解决"结果码唯一"，未解决"响应体重建" | A |
| J-5 | **`(outcome, http_status)` 无 CHECK / 无映射表**；`http_status` 无范围 CHECK；且 **⑥ 的 `503` 不在 §4.1.3 的重建枚举与"四态"之内** → 首次 `503` 的幂等语义未定义 | A/C |
| J-6 | **⑥ 的 `422` 未进契约闸门表**（`:659` 只列 `503`）；规格 `:424` 引用的「§4.1.6-5 ⑥ 语义」**悬空**（该表 `:536-543` 无 ⑥ 行） | A/B |
| J-7 | **§3.2 `:164` 与 §5 用例 23 `:598` 仍写"取回**冻结的动作与参数**"**（未按 control/body 口径就地收口）→ R5-1「五处一致」未齐 | A/B |
| J-8 | **条款↔用例双向引用缺失**：§4.1.6-3（`:520`）↔ 用例 30、§4.1.6-7（`:549`）↔ 用例 31 **双向均无引用**（第五轮"新增语义无用例"的未闭合形态） | C |
| J-9 | **§6 回归表缺"决议端点新增 `execution` 字段"行**（R5-4 的客户端可见变更未进回归登记） | C |
| J-10 | **`args_json` 未进「字段规范对照」表**；§4.1.3 幂等表**整表无**规范对照表 | A/B |
| J-11 | **边界第 3 条与第 4 条措辞矛盾**：`:422`「不落**任何**持久层」 vs `:423`「从**其承载表**的既有记录读」（承载表即持久层） | A |
| J-12 | **`«body»` 占位常量未声明"不参与任何比较/重建"**，无防护 | A/B |
| J-13 | **`architecture.md:59` 与 `capability-ownership-map.md:62` 的祈使句未降级**（仍为"动手前先查表…先补表再动手"）→ 与记录 §11.3「引用已降级为'参考'」**不完全相符** | B/D |
| J-14 | **`change-record.md:47` 回退串顺序与 `:19`/`:35`/规格 §4.1.5 相反**（两表无依赖、功能等价，属一致性缺陷） | B/D |
| J-15 | **I-2 / I-3 仍开放**（`:540` ④ 行置 `rejected` 撞 `decision_check`；`:375-376` 枚举无 CHECK 且未登记例外） | A/D |
| J-16 | 用例 29② 的 `409` 未在正文锚定；用例 30 与 10 分工未显式化；用例 4 与 16 高度重叠（预存项）；`delivery-remaining-checklist.md:155`「以下八条」仍与 12 条不符；`§9.4` 自述"状态同步四处"实为 7 处且**漏 2 处** | B/C |

### 12.5 汇总方亲验记录（5 条，**全部成立**）

| # | 亲验对象 | 结果 |
| --- | --- | --- |
| 1 | §3.3 `:254`/`:255`（工作卷生成即空、运行结束销毁；每运行一容器、任务结束即销毁）+ §3.1.1 `:126-127`（`fs.write`/`fs.overwrite` 的 `content` 为**新内容**）vs §4.1.1 `:423`「从工作卷读」 | ✅ **成立 → R6-1 为真阻断** |
| 2 | §3.1 `:96-103` 六字段表**无 control/body 字段** + §3.1.1 `:120-129` 未标类别 vs §4.1.1 `:420`「由工具目录 schema 逐参数声明」 | ✅ **成立 → R6-2 为真阻断** |
| 3 | 契约 `:654` ① 行 = **`422`** vs 规格 `:536` ① 行 = **`409`** | ✅ **成立 → R6-3 为真阻断** |
| 4 | `feature-inventory.md:97` 仍含「确认依据为**本机知识库 entity 笔记** … 用户已在实际使用」 | ✅ **成立 → J-2** |
| 5 | `multi-adapter-coexistence-spec.md:89`「待改 `common.py`」、`:110`「可立即做（小改）」 vs `:42` 边界声明 | ✅ **成立 → J-3** |

### 12.6 与前五轮的关系（**本轮最重要的判断**）

1. **第五轮「甲案」在本体上是不可实现的**——不是"写漏了一处"，而是**选定的机制前提不成立**：甲案＝"`args_json` 只落 `control`，`body` 靠**事实源重读**"。但 §3.1.1 中**唯二带 `body` 的工具**（`fs.write` / `fs.overwrite`）的 `content` 是**模型生成的新内容**，**根本不存在可复读的事实源**；而"工作卷"（被当作文件正文的事实源）在 §3.3 下**运行结束即销毁**，且**跨请求审批等待期的存活语义从未定义**。→ 结论：**在现有 §3.3 + §3.4 约束下，"审批 + 写文件"不可兼得。**
2. **这是第三/四/五轮同一根因的第四次显形**：R3-1/B2 → R4-2 → R5-1 → **R6-1/R6-2**，根因始终是「**参数要在跨请求审批后被完整还原，而正文又不许落库**」这一对硬约束没有共同落点。
3. **A 与 B 独立收敛到同一条阻断**（R6-1）——两个互不共享推理的视角得出相同结论，可信度高。
4. **D 的独立价值**：证实**本轮零代码改动**（`app/`/`tests/`/`migrations/`/前端/CI 全零命中、`migrations/` 最大仍为 `026`）、§11.2 清单与实际相符、且**推翻**了 §11.3「引用已降级为参考」的完整性表述（祈使句未改）。

### 12.7 待用户裁决

| # | 待裁决 | 选项与后果 |
| --- | --- | --- |
| **J7（**新增，**决定段二是否还能按现方向做下去**）** | **R6-1 的出路**：`body` 到底怎么在"审批跨请求"下还原？ | **丙案（推荐）**：`body` **落受控密文**（独立列/子表；应用层加密、密钥不入库）**＋ TTL 与审批同寿、审批落定即删、不导出、审计不落** → **牺牲 §3.4「永不落正文」的绝对性**，换取"审批+写文件"可用；须一并改 §4.1.5（现写"本段不做清理、不设 TTL"）。**丁案**：**不推翻红线**，把 `fs.write` / `fs.overwrite` **移出跨请求审批路径**（等价于本段这两类工具不可用）→ 数字员工**不能写文件**，产品价值受损。**戊案**：审批等待期保留容器+工作卷 → **只能解决"工作卷存活"，解决不了"content 是新内容"**（A/B 已证），**单独采用无效**。 |
| **J8** | **R6-2 的落点**：`ToolCatalog` 是否新增 `param_roles`（control/body）声明字段？ | 需**一并回改 §3.1 字段表 + §3.1.1 工具表**（逐工具标注），否则甲/丙/丁任何一案都无判据可依 |
| **J9** | **R6-3 的收口**：① 步在"执行入口首次"与"审批后重跑"是否**刻意用不同码**？ | 建议**统一**（首次 `422`、重跑保留 `409` 但**在契约闸门表补注**），或**统一为同码**；须**先改一方再改另一方**，不得只在代码里选一个 |
| **J6** | `WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值（**第四次挂起**） | 建议段二-1 实测后定值 |

### 12.8 独立性声明（本轮）

- 四个视角**均为未参与 §4.1 与第五轮修订**的只读方；**本轮全部判定由它们作出**，汇总方只做汇总与 §12.5 的 5 条亲验。
- 汇总方**仍未构成对 §4.1 的独立评审人**（§4.1 与其第五轮修订均由其撰写）；本轮的 A 视角即为此设立。

### 12.9 重审触发

**J7–J9 裁决 + R6-1/R6-2/R6-3 闭环 + §12.4 的 J-1～J-16 处置后**，规格方可由「已修订（第五轮） · 待第六轮独立复核」改为「**已修订（第六轮） · 待第七轮独立复核**」；**在此之前不得建表（`migrations/027`）、不得写第一行实现代码**，清单 §C1 不得判为过闸。

> ⚠️ **流程提示（汇总方如实登记）**：本段自第三轮起已历经 **四轮**「不予放行」。按宪法排障纪律精神（同一问题连续三轮未解决须停手换法），**建议下一轮不再做全量复核**，而是先由用户裁决 **J7（甲/丙/丁案）**、再按裁决**定点收口**三条阻断，然后仅针对三条阻断做**定点复核**（不再全量）。**在 J7 未定之前继续全量复核，预计仍会沿同一根因返回同一结论。**

---

## 13. 第七轮修订（J7 = 丙案）的授权留痕与待同批实施清单（2026-09-13）

### 13.1 用户裁决与本轮范围

- **J7 = 丙案**：`body` 走**受控密文**（+ TTL 与审批同寿 + 审批落定即删 + 不导出 + 审计不落）。
- **本轮范围（用户原话）**：**"先改 §3.4 和 §4.1.5 的红线表述"**。

### 13.2 实际改动（2 处授权内 + 3 处"同类必要"）

| 文件/节 | 改动 | 性质 |
| --- | --- | --- |
| 规格 **§3.4** | 「永不落正文」→「**默认 + 唯一受控例外（受控正文密文列）**」+ **五条约束**（密文/密钥不落库 · TTL 与审批同寿且落定即删 · 不导出 · 审计不落 · 不参与摘要）+ **红线范围声明**（未进入审批等待的 `body` 一律不落库） | ✅ **本轮授权内** |
| 规格 **§4.1.5** | 「清理策略」拆为**两行**：**幂等表**（本段不做、无 TTL，仍属后续段次）/ **受控正文密文列**（**强制 TTL、本段必做**，7 条判据：落定同事务清空 · 到期即清 · 启动时+周期 · 失败告警登记 · 不新增动作码 · 本段必做 · 不影响既有读路径） | ✅ **本轮授权内** |
| 规格 §4.1.1 边界**第 3/4/5 条** | 第 3 条改「默认不落 + 唯一例外」；**第 4 条「从工作卷/承载表重读」作废** → 改「**从受控密文列解密还原**」并写明 R6-1 的两条理由；**第 5 条「⑥ 拒绝 `422`」作废** → ⑥ 只留「落库失败 `503`」 | ⚠️ **同类必要**：不改则与新 §3.4 **同轮自相矛盾**（新例外所授权的机制，正是被这两条否定的） |
| 规格 §4.1.1 边界**第 1 条** | 加显式标注：**落点尚未存在**（R6-2 / **待裁决 J8**）→ 在 `ToolCatalog` 补齐字段前，本条与 §3.4 例外**均不可实现** | ⚠️ **同类必要**（丙案依赖该声明） |
| 规格 **§8** | U12 口径收窄（仅指幂等表）；**新增 U15**（丙案机制未落地） | ⚠️ **同类必要**（未闭环必须显式登记） |

**未做**：**无任何代码改动**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍为 `026`）。另：本轮曾误建一个临时占位文件 `docs/dsh-integration-review-record.md.tmp-hold`，**已即时删除**（如实登记）。

### 13.3 待同批实施（丙案的**机制**部分，尚未做）

| # | 待实施 | 关联 |
| --- | --- | --- |
| 1 | **§4.1.1 DDL** 增受控正文密文列（建议 `body_ciphertext BYTEA NULL` + `body_expires_at TIMESTAMPTZ NULL` + CHECK「两者同有同无」）+ 字段规范对照表补行 | 丙案核心 |
| 2 | **§3.1 / §3.1.1**：`ToolCatalog` 增参数角色声明字段 + **逐工具标注** | **J8 待裁决** |
| 3 | §4.1.6 调用链、§4.1.7 第 1/3/4 条 → 改「密文解密还原」 | R6-1 遗留 |
| 4 | §4.1.3：`(outcome, http_status)` 约束、⑥ 的 `503` 入枚举、`202` 重放体 `approval_id` 落点 | J-4 / J-5 |
| 5 | **① 步结果码收口**（契约 `422` vs 规格 `409`） | **R6-3 / J9 待裁决** |
| 6 | 契约：「审批通过后的推进」段、闸门表 ⑥ 行、`:641` 状态码分支 | R6-1 / J-6 |
| 7 | §5 新增用例：密文落库/TTL/落定即删/不导出/审计不落的可判定判据 | 丙案验收 |
| 8 | `change-record.md`：登记密文列（数据模型变更，宪法 1.4） | 合规 |
| 9 | §6 回归表：补「决议端点新增 `execution` 字段」一行 | J-9 |

### 13.4 仍未闭环

R6-1（**红线已授权，机制未落地**）、**R6-2（J8）**、**R6-3（J9）**、J-1～J-16、J6、U11、U12、U13、U14、**U15**；`027` 未落地（**可重复执行性 / 对既有 1434 项零破坏 / 升级回退演练 一律未验证**）。

### 13.5 状态说明（避免状态漂移）

规格头部**本轮不动**，仍为「**已修订（第五轮） · 待第六轮独立复核**」——依 §12.9，状态变更须待 **J8/J9 裁决 + 三条阻断定点收口**之后；**本轮只是红线授权，不构成"第六轮已修订"**。这一点在此显式声明，以免出现我们在前几轮反复判过的"状态漂移"缺陷。

---

## 14. 第六轮修订的实施与授权留痕（2026-09-13）

### 14.1 用户裁决

| 项 | 裁决 | 我的建议（已按此执行） |
| --- | --- | --- |
| **J8** | **采纳** | `ToolCatalog` 新增 **`param_roles`** 字段（`{参数名: "control"｜"body"}`，**默认 `control`**，内容型必须显式声明 `body`）；§3.1.1 **逐工具标注** |
| **J9** | **采纳** | **保留双码、定死分界**：① 在**首次执行路径**（无授权绑定）＝输入语义错误 → **`422`**；在**审批后重跑路径**（⑦ 之后）＝**状态漂移冲突** → **`409`**；**契约与规格双向补注**，**实现不得自行统一** |
| 范围 | 用户明确指令 | **同批实施 §13.3 的 9 项** |

> **J9 的判断依据（汇总方说明）**：第六轮视角 C 把契约 `:654`（`422`）与规格 `:536`（`409`）判为"同一失败条件两个码"。**汇总方认同一半**：现象确实相同（工具不在组装面），但**阶段与语义不同**——首次是"客户端提了不存在的工具"（输入错误），重跑是"授权时存在、现在没了"（状态冲突）。故**不是"选一个码"，而是"把分界写死"**；这比强行统一更不易误实现。

### 14.2 本轮实际改动清单（对应 §13.3 的 9 项）

| §13.3 | 落点 | 文件 |
| --- | --- | --- |
| 1 | §4.1.1 DDL 增 `body_ciphertext` + `body_expires_at` + **`body_check`**（同有同无）；字段规范对照表增「**字段内容边界**」行 | 规格 |
| 2 | §3.1 增 **`param_roles`** 字段；§3.1.1 逐工具标注（**仅 `fs.write`/`fs.overwrite` 的 `content` = `body`**） | 规格 |
| 3 | §4.1.6 调用链、§4.1.7 第 1/3/4 条 → **「从 `body_ciphertext` 解密还原」**（原「从工作卷读」措辞已清除） | 规格 |
| 4 | §4.1.3 增 **`approval_id`**（重建 `202` 响应体）+ **`result_check`**（`outcome` ↔ `http_status`）+ **⑥ 失败不写幂等表**；§4.1.6-5 ⑨ 行 `200` → **`201`** | 规格 |
| 5 | §4.1.6-5 ① 行补「审批后重跑路径」+ **J9 分界说明**；契约 `:641` 状态码分支与闸门表 ① 行**双向补注** | 规格 + 契约 |
| 6 | 契约「审批通过后的推进」段 → **密文还原口径**（原「从各自事实源重读」已清除） | 契约 |
| 7 | **§5 新增用例 32（密文与 TTL，含 7 条断言）与用例 33（正文不进对外通道，含 4 条断言）** | 规格 |
| 8 | `change-record.md`：记录 2 补密文列 + 2 个 CHECK、记录 3 补 `approval_id` + `result_check`；**三条记录的状态行同步为「待第七轮」**（同时修掉第六轮 J-1 的状态漂移） | 变更记录 |
| 9 | §6 回归表：补「**决议端点新增 `execution` 字段**」行；`027` 静态契约测试**点名断言全部列与 3 个 CHECK**；状态推进 + **§9.5** + U15 更新 | 规格 |

**未做**：**无任何代码改动**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍为 `026`）；**未提交、未推送**；本轮**未新建任何临时文件**。

### 14.3 状态推进

规格头部由「**已修订（第五轮） · 待第六轮独立复核**」→「**已修订（第六轮） · 待第七轮独立复核**」（§13.5 所声明的"待 J8/J9 裁决 + 三条阻断收口"条件已满足）。**§9.5 为第六轮修订说明。**

### 14.4 仍未闭环（不得视为通过）

**J-1～J-16**（第六轮 14 条重要项，其中 J-1 状态漂移**已随本轮修掉**，其余未修）、**J6**、**U11**、**U12**、**U13**、**U14**、**U15**（丙案机制未实现）；`027` 未落地 → **可重复执行性 / FK 列序 / 对既有 1434 项零破坏 / 升级回退演练 一律未验证**；**加解密与密钥管理、TTL 清理任务、全部实现与测试均未写**；§5 用例 21 仍「未通过」。

### 14.5 下一步（按 §12.9 的流程建议）

**第七轮只做「定点复核」**——不再全量，仅确认 **R6-1 / R6-2 / R6-3 三条阻断是否真闭合** + **用例 32/33 的可判定性**。通过后规格方可改为「已评审（附记录）」；**在此之前不得建表（`migrations/027`）、不得写第一行实现代码**。

### 14.6 追加：第六轮三条小项修复（J-2 / J-3 / J-13，2026-09-13）

用户追加指令："先修复 J-2、J-3 和 J-13 这三处小改动"。**已全部修复**，涉及 **4 个文档**，均为**一致性/证据链**问题，**不触及设计**：

| # | 问题（第六轮） | 修复 | 文件 |
| --- | --- | --- | --- |
| **J-2** | `feature-inventory.md:97` 的 Hermes 许可括注**未随 R5-6 同步**——仍写「确认依据为**本机知识库 entity 笔记** + 用户已在使用」，且无 SHA、无更正说明 | 改为**上游 `LICENSE` 原文经 GitHub 官方 API 取回 + SHA `75410e73…`**，并加 **R5-6 更正说明**；与 `poc-license-checklist.md:9` 口径一致 | `feature-inventory.md` |
| **J-3** | `multi-adapter-coexistence-spec.md` **自相矛盾**：`边界声明第 2 条`（禁止以本规约驱动改动） vs §5「待改 `common.py`」、§7「可立即做（小改）」 | §5「事件未知」行的落点改为「**须走「先改契约 + 影响评估」，不得由本规约驱动**」；§7 **加前置声明**（本表不构成立刻修改的要求；仅新增能力类可按规约执行）+ 第 1 行同步 | `multi-adapter-coexistence-spec.md` |
| **J-13** | 两处**祈使句未降级**（与"未评审"状态声明冲突）：`architecture.md:58`「动手前先查表…先补表再动手」、`capability-ownership-map.md:62` 同款；且 `architecture.md` 的**本节未自标未评审**、`capability-ownership-map.md` **缺共同约束文本** | `architecture.md:58` 改为「**参考（非强制入口）**」+ 注明 J-13 更正；**:60 状态声明补"本节本身亦未经评审"**；`capability-ownership-map.md` 文首**加共同约束**（不得作为放行依据/改动依据）+ 使用规则第 1 条改「可先查表作参考…不作强制入口」 | `architecture.md`、`capability-ownership-map.md` |

**未做**：**无代码改动**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动）；未提交、未推送；未新建文件。

**仍未闭环**：**J-4～J-16**（第六轮重要项余 12 条）、**J6**、**U11**、**U12**、**U13**、**U14**、**U15**；`027` 未落地 → 可重复执行性 / FK 列序 / 对 1434 项零破坏 / 升级回退演练**一律未验证**；丙案机制（加解密、密钥管理、TTL 清理、用例 32/33）**全部未实现未测**；§5 用例 21 仍「未通过」。

---

## 15. 第七轮「定点复核」结论（2026-09-13）

### 15.1 送审与范围

**定点复核**（依 §12.9）：只查 **R6-1 / R6-2 / R6-3 三条阻断是否真闭合** + **用例 32/33 可判定性**，**不再全量**。两个只读视角（A = 三条阻断 + 丙案端到端；B = 用例 32/33 + 跨文档一致性），**均为未参与 §4.1 与历轮修订者**。

### 15.2 结论：**未通过（部分闭合）**

| 项 | 判定 |
| --- | --- |
| **R6-3**（① 步双码） | ✅ **真闭合**——规格 §4.1.6-5 ① 行、J9 分界说明、契约闸门表 ① 行、契约 `:641` **四处互指且不矛盾**；无活跃第四处单码；`result_check` 与双码**相容** |
| **R6-1**（甲案本体不可实现） | ❌ **表面闭合**——旧口径已清除、丙案机制在**文档层**成形，但**端到端未闭合**（见 §15.3） |
| **R6-2**（`control`/`body` 无落点） | ❌ **表面闭合**——`param_roles` 已落地并 **8/8 逐工具标注**，但**无强制机制**、**默认值方向相反（fail-open）**、**实现落点未指明且存在同名冲突**（见 §15.4） |
| **用例 32** | ❌ **附条件（断言 ② 单项不可判定）**——`body_ciphertext` 是 `BYTEA`，"grep 原文"**观测手段不可行且不唯一** → 反假点「把明文写进密文列」可能**假绿** |
| **用例 33** | 🟡 **附条件**——① 审计 / ② 响应体可判定；③ 导出通道**观测点缺失**（且工具未实现）；④ 日志的**密文部分**同 32② 不可判定 |
| **跨文档一致性** | ❌ **未达成**——**8 处状态漂移** + **§C3「31 条」事实错误**（实际 33）+ **条款↔用例双向引用缺失** |

### 15.3 R6-1 的端到端缺口（**汇总方亲验成立**）

| # | 缺口 | 证据 |
| --- | --- | --- |
| 1 | **加解密组件无落点**——谁加密、谁解密未写 | §4.1.6-1（`:546`）`ToolExecutionService` 构造依赖 = `ToolCatalog`/`ContainerExecutor`/`WorkspaceManager`/`ToolActionStore`/`RunRecords`/`Audit`——**不含任何加解密器或密钥服务**；§3.4 只写"应用层 AEAD 加密"，**未指组件** |
| 2 | **密钥与清理周期无配置落点** | §4 新增配置（`:347`）共 **10 项**，**无加密密钥、无清理周期**；`app/settings.py` 内 `WORKBENCH_EXEC_*` **零命中**。**清单 §B15 与 §6 回归表"已同步"，但同步的是同一份缺配置的清单** |
| 3 | **加解密失败语义未定义** | 全规格 grep「加解密失败」**零命中**；§4.1.6-5 重跑失败表**无该行**；`reason_code` 9 值中**无**可表达"解密失败"的码 |
| 4 | **密钥轮换无口径** | 规格正文"轮换"零命中（仅有 `AUTH_SECRET`/`BACKUP_ENCRYPTION_KEY` 的既有运维口径，与正文密文无关）|

> ✅ **已确认自洽**（非缺口）：§3.4 五条约束彼此自洽、与 §3.3/§3.5/§3.6 无冲突；**「TTL 本段必做」与「幂等表本段不做清理」的区分已彻底**（§4.1.5 拆两行 + U12 显式排除）；`body_check` × 落定清空 × 到期即清**三者自洽**；§4.1.1 边界第 3 条与 §3.4 字面一致。

### 15.4 R6-2 的缺口——**含一处新引入的 fail-open**（**本轮最重要发现**）

1. **默认值是泄密方向的 fail-open**：§3.1（`:100`）、§3.1.1（`:134`）、§4.1.1 边界第 1 条（`:439`）三处一致写「**默认 `control`**；漏声明即按控制参数入库，**属实现缺陷**」→ **漏声明一个内容型参数，正文即以明文进 `args_json`**，与 §3.4 红线**方向相反**，且**仅靠"属实现缺陷"口头兜底**。
2. **无任何可检出手段**：`param_roles|body_ciphertext|body_expires_at` 全库命中**仅 docs（50 处）**，**`app/` 与 `tests/` 零命中**；§4.1.6-3 启动断言**不含**该项；用例 19 只守风险定档；用例 32 不覆盖"漏声明"。
3. **实现落点未指明 + 同名冲突**：规格 §3.1 的 `ToolCatalog`（含 `params_schema`/`param_roles`/`risk_level`…）与实码 **`app/planner/models.py:60` 的既有 `class ToolCatalog`**（工具仅 `name`/`kind`/`description`，由 `from_config` 构造）**同名不同构**；规格既未声明"新建同名类"也未声明"扩既有类"→ **实现时无从判断**。
4. **逐工具标注本身成立**：§3.1.1（`:134`）8/8 覆盖（仅 `fs.write`/`fs.overwrite` 的 `content` = `body`），与 §3.1 一致。

### 15.5 跨文档一致性（**汇总方亲验**）

| 项 | 判定 |
| --- | --- |
| **状态漂移 8 处** | 门禁清单 `:5`（日期写"随第五轮同步"）`:6`（头部）`:165`（§C1）`:183`（§E.5）、功能清单 `:73`/`:119`、立项 `:447`、`capability-ownership-map.md:35`（**更旧，仍"待第五轮复核"**）——**全部仍停在「第五轮 · 待第六轮」**，与规格头部（第六轮 · 待第七轮）不一致 |
| **§C3「31 条」** | 门禁清单 `:167` 仍写"用例扩到 **31 条**"，**实际 §5 = 33 条**（1–33 连续无重复）→ **事实错误** |
| **条款↔用例双向引用** | **缺失**：§6 回归表已回指用例 31 ✅；但**条款侧（§3.4/§4.1.1/§4.1.3/§4.1.5）无"用例 32/33"字样**，**用例侧也无 `§x.y` 引用**（与第六轮 J-8 同型缺陷**在新用例上重现**） |
| 列清单 ↔ 变更记录 | ✅ 逐列相符（含 `body_ciphertext`/`body_expires_at`/`body_check`/`result_check`/`approval_id`） |
| 契约 ↔ 规格 | ✅ `:641`/`:654`/`:664` 与规格口径**逐句一致** |

### 15.6 汇总方亲验记录（6 条，**全部成立**）

| # | 亲验对象 | 结果 |
| --- | --- | --- |
| 1 | §4 配置清单（`:347`）10 项**无密钥/无清理周期** | ✅ 成立（端到端缺口 2） |
| 2 | §4.1.6-1（`:546`）构造依赖**无加解密组件** | ✅ 成立（缺口 1） |
| 3 | `app/planner/models.py:60` **已存在同名 `ToolCatalog` 且不同构** | ✅ **成立**（实现落点冲突） |
| 4 | `param_roles|body_ciphertext|body_expires_at` 在 **`app/`、`tests/` 零命中** | ✅ 成立（无实现、无守护） |
| 5 | `capability-ownership-map.md:35` 仍「**待第五轮复核**」 | ✅ 成立（漂移最久一处） |
| 6 | 门禁清单 `:167`「31 条」 vs 实际 **33 条** | ✅ 成立（事实错误） |

### 15.7 待处置清单（**建议作为「第六轮补丁」一次性做完**）

| # | 事项 | 需裁决？ |
| --- | --- | --- |
| **P1** | **消除 R6-2 的 fail-open**：把「默认 `control` + 漏声明属缺陷」改为「**必须显式声明全部参数；未声明即拒绝装配（fail-closed）**」，并**同步 §3.1 / §3.1.1 / §4.1.1 边界第 1 条三处** + 新增用例（漏声明 → 装配期拒绝） | **需你确认**（这是口径变更） |
| **P2** | **加解密组件落点**：§4.1.6-1 构造依赖增 `BodyCipher`；§4.1.6 增「加解密失败 → fail-closed」语义与状态码/`reason_code` | 否 |
| **P3** | **配置项**：§4 由 10 项 → **12 项**（增 `WORKBENCH_BODY_ENCRYPTION_KEY`、`WORKBENCH_BODY_CLEANUP_INTERVAL_SECONDS`）；同步 §B15、§6、模板要求 | **需你确认密钥来源**（**建议不复用**既有 `backup_encryption_key`） |
| **P4** | **密钥轮换口径**：双密钥窗口，窗口 = 最大审批超时 | 否 |
| **P5** | **§3.4 明文写出「占位常量 `"«body»"` 不参与任何比较/重建」**（现仅见于用例 32⑥） | 否 |
| **P6** | **审批超时 → `status='expired'` 的执行方写明**（现未定义；否则到期清了密文而状态仍 `pending`，批准后无正文可还原） | 否 |
| **P7** | **用例 32② 改为可执行口径**（`BYTEA` 不可 grep：改为「列文本化后查 + 对 `args_json`/消息表/日志 grep 原文」或等价的字节级判据）；32⑦ 注明**在隔离测试库直写** | 否 |
| **P8** | **用例 32/33 ↔ 条款双向引用**（§3.4/§4.1.1/§4.1.3/§4.1.5 回指用例 32/33） | 否 |
| **P9** | **状态同步 8 处** + **§C3「31 条」→「33 条」** | 否 |
| **P10** | §4.1.3 与 §4.1.1 边界第 3 条**交叉引用**（幂等表不落正文） | 否 |
| **P11** | **`ToolCatalog` 同名不同构**：明确「**扩既有 `app/planner/models.py` 的 `ToolCatalog`**」或「**新建同名类并说明与既有类的关系**」 | **需你裁决**（建议：**新建独立类并改名**，避免同名歧义） |

### 15.8 独立性声明与流程提示

- 两个视角**均未参与 §4.1 与历轮修订**；汇总方只做汇总与 §15.6 的 6 条亲验。
- **流程提示**：本段累计 **六轮**「不予放行 / 未通过」。但**性质已变**——不再有"本体不可实现"级问题（R6-3 已闭合），剩余为**可枚举的 11 条补丁**。**建议：不做第八轮全量复核，而是做完 P1–P11 后仅对 P1–P11 做一次「补丁确认」。**
- **状态**：规格头部**保持「已修订（第六轮） · 待第七轮独立复核」**——第七轮**未通过**，**不得**改为「已评审」。

---

## 16. 第七轮补丁（P1–P11）的实施与授权留痕（2026-09-13）

### 16.1 用户裁决

**用户指令**：「按建议执行：改 fail-closed、新增独立密钥配置、重命名 ToolCatalog，并修复其余 8 条」→ 即 **P1 / P3 / P11 采纳汇总方建议**，**P2 / P4–P10 一并做**。

| 项 | 裁决 |
| --- | --- |
| **P1** | **采纳**：`param_roles` **取消默认值**，**必须为每个参数显式声明**；**未声明 → 拒绝装配（fail-closed）** |
| **P3** | **采纳**：**新增独立密钥配置** `WORKBENCH_BODY_ENCRYPTION_KEY`（**不复用** `backup_encryption_key`，单一职责）+ `WORKBENCH_BODY_CLEANUP_INTERVAL_SECONDS` |
| **P11** | **采纳**：`ToolCatalog` **重命名为 `ToolSpecCatalog`**，**新建独立模块**；并明确与 `app/planner/models.py` 既有同名类的关系（不得复用该名、不得扩该类） |
| P2/P4–P10 | 按推荐直接做 |

### 16.2 实际改动清单

| # | 改动 | 文件 |
| --- | --- | --- |
| **P1** | §3.1 `param_roles` 行、§3.1.1 逐工具标注段、§4.1.1 边界第 1 条**三处同口径改为 fail-closed**；§4.1.6-3 启动期断言**增"参数全覆盖"断言**；§5 **新增用例 34** | 规格 |
| **P2** | §4.1.6-1 依赖增 **`BodyCipher`**；新增 **1.1 加解密失败语义**；§4.1.6-5 失败表**新增「正文解密失败」行**（`502`+`runtime_error`+`tool.blocked`+保持 `approved`） | 规格 |
| **P3** | §4 配置 **10 → 12 项**（并标注"共 12 项"）；§6 回归表改 12 项；门禁 **§B15** 改 12 项（含判据 1） | 规格、门禁清单 |
| **P4** | §3.4 约束 1 增**双密钥窗口**（窗口 = 最大审批超时 + 清理周期） | 规格 |
| **P5** | §3.4 约束 5 增**占位常量不参与比较/判定/响应重建**（按键区分、不按值判定） | 规格 |
| **P6** | §4.1.6 **新增第 8 条**：`expired` 执行方（审批过期处理，与清理同批同事务，写三列 + 清空两列） | 规格 |
| **P7** | §5 用例 32② 改**可执行口径**（hex 子串 + 长度下界 + 四处 grep）；32⑦ 注明**隔离测试库直连写入** | 规格 |
| **P8** | §3.4 红线声明 / §4.1.1 边界 / §4.1.3 / §4.1.5 / §4.1.6-7 **回指用例 32 / 33 / 34** | 规格 |
| **P9** | **状态同步**：门禁清单（头部`:5`/`:6`、§C1、§C3、§E.5、§B15）、功能清单（§3.3/§5.3）、立项 §14.1、`capability-ownership-map.md:35`；**§C3 条数「31」→「34」**（并补 32/33/34）；变更记录三条评审状态同步 | 6 个文档 |
| **P10** | §4.1.3 与 §4.1.1 边界第 3 条**交叉引用**（幂等表不落正文） | 规格 |
| **P11** | §3.1 标题与说明、§3.1.1、§4.1.6-1 **改名 `ToolSpecCatalog`** + 与既有同名类的关系声明 | 规格 |
| 附带 | 规格头部状态 → **已修订（第七轮） · 待补丁确认**；**新增 §9.6**；U15 更新 + **新增 U16**（清理周期默认值未定） | 规格 |

**未做**：**无任何代码改动**（`app/`/`tests/`/`migrations/`/前端/CI 未动；`migrations/` 最大编号仍 `026`）；**未提交、未推送**；**未新建文件**。

### 16.3 仍未闭环（不得视为通过）

**J-4～J-16**（第六轮重要项）、**J6**、**U11**、**U12**、**U13**、**U14**、**U15**（丙案机制未实现）、**U16（新：清理周期默认值）**；`027` 未落地 → **可重复执行性 / FK 列序 / 对既有 1434 项零破坏 / 升级回退演练 一律未验证**；**加解密实现与密钥管理、TTL 清理任务、用例 32/33/34 均未实现未测**；§5 用例 21 仍「未通过」。

### 16.4 下一步

**「补丁确认」**（第七轮第三阶段）：只确认 **P1–P11 是否真闭环**，不再全量、不引入新范围。通过后规格方可改为「**已评审（附记录）**」，并按门禁 §C1 视为过闸；**在此之前不得建表、不得写第一行实现代码**。

---

## 17. 「补丁确认」结论（2026-09-13）

### 17.1 范围与方法

**只确认 P1–P11 是否真闭环**（依 §16.4）；两个只读视角（A = P1–P6/P10/P11；B = P7–P9），**均为未参与 §4.1 与历轮修订者**；纪律：**不得采信** §9.5/§9.6、记录 §13/§14/§16 的"已完成"自述（可把记录 §15.7 当**问题清单**用）。

### 17.2 结论：**未通过（10 条真闭合 / 2 条表面闭合）**

| 补丁 | 结论 |
| --- | --- |
| **P1**（fail-closed） | ✅ **真闭合**——三处口径逐字一致（§3.1、§3.1.1、§4.1.1 边界第 1 条），启动断言 + **用例 34** 双手段；活跃正文无第四处"默认 `control`" |
| **P2**（`BodyCipher` + 失败语义） | ✅ **真闭合**——依赖落点 + 1.1 两条语义 + 失败表新增行；与契约 ⑥ 行 `503`、`result_check` 的 `502` **相容** |
| **P3**（配置 12 项） | ✅ **真闭合**——规格 §4 / §6 / 门禁 §B15（含判据 1）三处同步，且 §4 与 §B15 **逐项名称一致** |
| **P4**（密钥轮换） | ✅ **真闭合**——双密钥窗口 + 窗口定义；与清理/`expired` 时间轴**自洽** |
| **P5**（占位常量） | ✅ **真闭合**——**规格正文**（§3.4 约束 5）明文写出三点约束 |
| **P6**（`expired` 执行方） | ✅ **真闭合**——逐条比对 `decision_check` / `body_check` **原文，相容** |
| **P9**（状态同步） | ✅ **真闭合**——8 处状态全同步；**§C3「34 条」与 §5 实际 34 条一致**（1–34 连续无重复）；变更记录三条同步 |
| **P10**（交叉引用） | ✅ **真闭合** |
| **P11**（改名） | ✅ **真闭合**——活跃正文全改；与既有同名类关系声明齐备；**实码 `app/planner/models.py:60` 同名类经核对确实存在且不同构** |
| **P7**（用例 32② 可执行口径） | ❌ **表面闭合**（见 §17.3） |
| **P8**（双向引用） | ❌ **表面闭合**（见 §17.4） |

### 17.3 P7 未闭合的三点（**汇总方亲验成立**）

| 点 | 问题 | 证据 |
| --- | --- | --- |
| **(b) `+24` 下界无来源且可绕过** | 规格**全文未定义 AEAD 算法**（grep `AES｜GCM｜nonce｜tag｜ChaCha｜Poly1305` → **零相关命中**）→ `+24` **是断言而非推导**；且"明文 + 固定填充 ≥24 字节但不加密"**仍能通过**，与"否则说明未加密"的立意不符 | 规格 §3.4 只写"应用层 AEAD 加密"；§5 用例 32②(b) |
| **(c) 观测方式未定义** | `args_json`/消息表/审计**在库内不能字面 grep**（须用 SQL 查询），而"**日志文件**"**未给路径与范围** | 用例 32②(c) |
| **③ 必须红列未对齐** | 未给 (b)/(c) 的改坏点（如"只追加 nonce+tag 不加密"、"下界改小"、⑥ 占位常量） | 用例 32 末列 |

### 17.4 P8 未闭合（**汇总方亲验成立**）

**双向未成立**：

- **条款侧**：仅 **§3.4 约束 5（`:276`）** 与 **§3.4 红线声明（`:277`）**、**§4.1.5（`:543`）** 回指 **32/33**；而 **§4.1.1 边界第 1 条（`:442`）只回指 34**、**§4.1.3（`:508`）回指 14/31**、**§4.1.6-7（`:582`）回指 31** → **P8 自称覆盖的四处未齐**。
- **用例侧**：**用例 32/33 行内零 `§x.y` 引用**（只写列名/步骤号）→ 第七轮所判"用例侧无条款引用"**原样存在**。

### 17.5 另发现一处状态漂移（A 视角顺带项，**汇总方亲验成立**）

`§3.4:271` 写「…见 §4.1.1；**2026-09-13 已实施**」——与 §8 **U15**「`migrations/027` 未落地、加解密与密钥管理未实现、无任何实现或测试」**直接矛盾**。→ 应改为「**文档层已定案；实现未落地（见 §8 U15）**」。

### 17.6 待处置（**「补丁补丁」三条，均为小改**）

| # | 事项 | 需裁决？ |
| --- | --- | --- |
| **Q1（P8）** | **建立真双向引用**：① 用例 32/33 行内补 `§3.4 / §4.1.1 / §4.1.3 / §4.1.5` 与契约的条款引用；② §4.1.1 边界第 1/2/3/4/5 条、§4.1.3 **补回指用例 32/33** | 否 |
| **Q2（P7）** | ① **定死 AEAD 算法与 overhead**（建议 **AES-256-GCM**：12B nonce + 16B tag = **28B**），断言改为 `≥ 正文字节数 + 28`（**有来源**）；② **②(c) 给可执行检索方式**（库内用 SQL 查询、日志给出路径与范围）；③ **③ 必须红列补 2–3 条**（只追加 nonce+tag 不加密 → 红；下界改小 → 红；⑥ 占位常量参与比较 → 红） | **需你确认算法选型**（AES-256-GCM） |
| **Q3** | `§3.4:271`「已实施」→「**文档层已定案；实现未落地**（U15）」 | 否 |

### 17.7 独立性声明

- 两视角**均未参与 §4.1 与历轮修订**；汇总方只做汇总与 §17.3/§17.4/§17.5 的亲验（含 grep 证实"规格无 AEAD 算法定义"）。
- **状态**：规格头部**保持「已修订（第七轮） · 待补丁确认」**——**补丁确认未通过**，**不得**改为「已评审」。

---

## 18. Q1–Q3（补丁确认未通过项）的实施留痕（2026-09-13）

### 18.1 用户裁决

**用户指令**：「按 AES-256-GCM 执行 Q1-Q3」→ **Q2① 算法选型采纳汇总方建议：AES-256-GCM**；Q1、Q2②③、Q3 一并做。

### 18.2 实际改动

| # | 改动 | 落点 |
| --- | --- | --- |
| **Q1** | **建立真双向引用**：① **用例 32** 行内补「条款引用：§3.4 唯一受控例外（约束 1–5）/ §4.1.1 边界第 2–5 条与 `body_check` / §4.1.5 清理策略 / §4.1.6-8 `expired` 执行方」；**用例 33** 行内补「§3.4 约束 1/3/4 与审计最小集 / §4.1.3 / 契约「审计与落库口径」」；② **条款侧补回指**：§4.1.1 边界**第 2 条→32⑥**、**第 3 条→32⑤/33①**、**第 4 条→32①/32③**、**第 5 条→32**；**§4.1.3→33①**；**§4.1.6-7→33②** | 规格 §5、§4.1.1、§4.1.3、§4.1.6-7 |
| **Q2①** | **定死算法与 overhead**：§3.4 约束 1 增「**AES-256-GCM**：nonce **12B** + tag **16B** → overhead **恒为 28B**；`body_ciphertext` 布局 = `nonce(12B) ‖ ciphertext ‖ tag(16B)`」；用例 32②(b) 断言由 `+24` 改为 **`+28`** 并注明**来源** | 规格 §3.4、§5 |
| **Q2②** | 用例 32②(c) 改为**可执行方式**：**(c1) 库内三处用 SQL 查询**（`args_json` / `conversation_messages` / 审计明细，`LIKE` / `position()`；**不得**用文件级 grep 代替）；**(c2) 日志**——grep `WORKBENCH` 运行日志与 `app/` 日志，**路径须在验收记录写明** | 规格 §5 |
| **Q2③** | 用例 32 末列**补 4 条"必须红"**：只追加 nonce+tag 而不加密 / 长度下界改小或换无 tag 算法 / 占位常量参与比较→重建 / 把正文写进 `args_json`·消息表·审计明细 | 规格 §5 |
| **Q3** | §3.4 例外引言「**2026-09-13 已实施**」→「**文档层已定案；实现未落地——见 §8 U15**」（并注明该更正理由） | 规格 §3.4 |

**另**：§9.6 增 **Q1/Q2/Q3 三行**；**重审触发更新为第八次**（只做「再次补丁确认」，仅确认 Q1–Q3）。

**未做**：**无任何代码改动**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍 `026`）；未提交、未推送；未新建文件。

### 18.3 仍未闭环

**J-4～J-16**、**J6**、**U11**、**U12**、**U13**、**U14**、**U15**、**U16**；`027` 未落地 → **可重复执行性 / FK 列序 / 对既有 1434 项零破坏 / 升级回退演练 一律未验证**；**AES-256-GCM 的实现、密钥管理、TTL 清理任务、用例 32/33/34 均未实现未测**；§5 用例 21 仍「未通过」。

### 18.4 下一步

**「再次补丁确认」**：只确认 **Q1 / Q2 / Q3** 是否真闭环。通过后规格方可改为「**已评审（附记录）**」，门禁 §C1 视为过闸；**在此之前不得建表（`migrations/027`）、不得写第一行实现代码**。

---

## 19. 「再次补丁确认」结论（2026-09-13）

### 19.1 范围与方法

**只判 Q1 / Q2 / Q3**（不引入新范围）；**单一独立视角**（未参与 §4.1、历轮修订与 Q1–Q3 编写），纪律同前（不得采信 §9.5/§9.6、记录 §13/§14/§16/§18 的自述；记录 §17 仅作**问题清单**）。

### 19.2 结论：**部分真闭合**

| 项 | 结论 | 要点 |
| --- | --- | --- |
| **Q1**（真双向引用） | ❌ **表面闭合** | 条款侧第 **2/3/4/5** 条、§4.1.3、§4.1.6-7 与用例侧 **32/33/34** 的引用**均已建立**；**残留 1 处**——**§4.1.1 边界第 1 条（`:441`）只回指用例 34，未按记录 §17.6 明列要求回指 32/33** |
| **Q2**（P7 可执行性） | ❌ **表面闭合** | ① 算法/overhead/布局**已定死且与用例 32②(b) 的 `+28` 一致**，活跃正文无 `+24` 残留 → ✅；③ 必须红**已补足** → ✅；④ 无自相矛盾 → ✅；**但 ②(c) 有可执行性缺口**（见 §19.3） |
| **Q3**（修状态漂移） | ✅ **真闭合** | §3.4 引言已改并指向 U15；**全库活跃正文无其他与 U15 矛盾的措辞**；§9.6 已增 Q1/Q2/Q3 三行、重审触发已更新；记录 §18 自述与规格实际相符 |

### 19.3 Q2② 的可执行性缺口（**汇总方亲验成立**）

| 点 | 问题 | 证据 |
| --- | --- | --- |
| **JSONB 列缺 `::text` 转换** | 用例 32②(c1) 要求对 `args_json` 与审计明细**用 `LIKE`/`position()` 判定**，但二者皆为 **JSONB**——PostgreSQL **对 JSONB 无 `LIKE`/`position()` 的隐式转换**，**按原文照做会直接报错**，必须显式 `args_json::text` / `detail::text` | `workbench_tool_actions.args_json` = **JSONB NOT NULL**（规格 §4.1.1）；**`workbench_audit_log.detail` = `JSONB NOT NULL`**（`migrations/010_audit_log.sql:9`，**汇总方亲验**） |
| **审计表未点名** | 原文只写泛称"审计明细表"，**未点名 `workbench_audit_log`**（实际表名已核实） | 同上 |
| 消息表类型正确 | `workbench_conversation_messages.content` = **`TEXT NOT NULL`**（`migrations/023_conversational_agent.sql:35`，**汇总方亲验**）→ 该处可直接判定，规格未区分 | 同上 |
| 日志路径待定 | (c2) 的"具体路径须在验收记录中写明"→ **验收记录尚未产生**，该通道**当前不可验证**（属"口径已定、落点待定"） | 用例 32②(c2) |

### 19.4 待处置（**R1–R2 必做；R3 建议**）

| # | 事项 | 性质 |
| --- | --- | --- |
| **R1（Q1）** | **§4.1.1 边界第 1 条补回指**：增「判据见 §5 **用例 34**（机制）与 **32⑥ / 33①**（后果）」 | 必做（1 行） |
| **R2（Q2②）** | **用例 32②(c1) 补三点**：① **JSONB 列须显式 `::text` 转换**（`args_json::text`、`detail::text`）；② **点名审计表 `workbench_audit_log`**；③ 注明消息表 `content` 为 `TEXT`、可直接判定 | 必做（1 行） |
| **R3**（建议） | **补 nonce 唯一性/随机性要求**（`AES-256-GCM` 下 **nonce 重用致命**）：「每次加密**随机生成 12B nonce**、**禁止重用**、不得由计数器/时间戳推导」；并**定死密钥格式**（32 字节、编码方式） | **建议**（安全相关，属第七轮补丁新增内容检查出的缺口） |
| R4（建议） | 契约 `:664` 把"AEAD 密文"同步为具名 **AES-256-GCM**（非强制，避免两处口径不一） | 建议 |

### 19.5 越界与状态核查（**汇总方采纳视角结论**）

- **只有文档改动**：全仓非 `.md` 文件对 `AES-256-GCM|BodyCipher|body_ciphertext|param_roles|tool\.executed|ToolSpecCatalog` 检索，**仅命中既有文件 `app/accounts/secrets.py`**（TOTP 种子静态加密的既有 AES-256-GCM，**非本轮改动**）；`migrations/027` **不存在**（最大 `026`）；`tests/`、前端、CI **零命中**。
- **状态**：规格头部**仍为「已修订（第七轮） · 待补丁确认」**，**未被**改为「已评审」——符合流程。

### 19.6 独立性声明

单一独立视角**未参与 §4.1、历轮修订与 Q1–Q3 编写**；只读；不采信任何"已完成"自述。汇总方亲验 §19.3 的两条类型事实（`migrations/010:9`、`migrations/023:35`）与"§4.1.1 边界第 1 条仅回指 34"。

### 19.7 重审触发

**R1、R2 闭环（如采纳 R3/R4 则一并）后**，做**最后一次「补丁确认」**——**仅确认 R1 / R2（及 R3/R4 若采纳）**；通过后规格方可改为「**已评审（附记录）**」，门禁 §C1 视为过闸，**此后才可建 `migrations/027` 与写实现**。

---

## 20. R1–R3 的实施留痕（2026-09-13）

### 20.1 用户裁决

**用户指令**：「执行 R1+R2+R3 并发起最后一次补丁确认」→ **R3 采纳汇总方建议**（补 nonce 唯一性 + 定死密钥格式）；**R4（契约同步具名算法）本轮不做**（用户未列入；已登记为建议，见 §19.4）。

### 20.2 实际改动

| # | 改动 | 落点 |
| --- | --- | --- |
| **R1** | §4.1.1 边界**第 1 条**补回指：「**判据见 §5 用例 34（机制）与 32⑥ / 33①（后果）**」（连接"原因"与"后果"） | 规格 §4.1.1 |
| **R2** | §5 用例 32②**(c1)** 补列类型口径：① `args_json`（**JSONB → 必须 `::text`**）；② **点名 `workbench_audit_log.detail`**（**JSONB → 必须 `::text`**）；③ `conversation_messages.content`（**TEXT → 可直接判定**）；并注明 **JSONB 无隐式转换、漏 `::text` 会报错** | 规格 §5 |
| **R3** | §3.4 约束 1 补 **nonce 唯一性**（每次加密 **12B CSPRNG 随机生成、严禁重用**；不得由计数器/时间戳/密钥派生；**GCM 下重用致命**）+ **密钥格式**（`WORKBENCH_BODY_ENCRYPTION_KEY` = **32 字节原始密钥的 base64**，**启动期校验解码长度**，否则拒绝启用真实执行）；§4 配置项**同步该格式** | 规格 §3.4、§4 |

**另**：§9.6 增 **R1/R2/R3 三行**；**重审触发更新为第九次**（只做「最后一次补丁确认」，仅确认 R1–R3）。

**未做**：**无任何代码改动**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍 `026`）；未提交、未推送；未新建文件。**R4（契约同步具名算法）未做**（用户未列入本轮）。

### 20.3 仍未闭环

**J-4～J-16**、**J6**、**U11**、**U12**、**U13**、**U14**、**U15**、**U16**；**R4**（契约口径同步，建议级）；`027` 未落地 → **可重复执行性 / FK 列序 / 对既有 1434 项零破坏 / 升级回退演练 一律未验证**；**AES-256-GCM 的实现、密钥管理与校验、TTL 清理任务、用例 32/33/34 均未实现未测**；§5 用例 21 仍「未通过」。

### 20.4 下一步

**「最后一次补丁确认」**：**仅确认 R1 / R2 / R3**。通过后规格**方可**改为「**已评审（附记录）**」，门禁 §C1 视为过闸——**此后才可建 `migrations/027`、才可写第一行实现代码**（且仍受门禁 §B 其余条目约束）。

---

## 21. 「最后一次补丁确认」结论（2026-09-13）—— ✅ **全部真闭合**

### 21.1 范围与方法

**只判 R1 / R2 / R3**（不引入新范围）；**单一独立视角**（未参与 §4.1、历轮修订与 R1–R3 编写）；纪律同前（不得采信 §9.5/§9.6、记录 §13/§14/§16/§18/§20 的自述；记录 **§19** 仅作**问题清单**）。**判据从严**：规格自述与实码/其他章节不一致者一律判未闭合。

### 21.2 结论：**R1 / R2 / R3 三条全部真闭合**

| 项 | 判定 | 关键证据 |
| --- | --- | --- |
| **R1** | ✅ **真闭合** | §4.1.1 边界第 1 条**同句**回指 **用例 34（机制）** 与 **32⑥ / 33①（后果）**；所引三处编号在 §5 **实际存在且内容对得上**；无新不一致 |
| **R2** | ✅ **真闭合** | 用例 32②(c1) **四点齐全**（`args_json` JSONB→`::text`、**点名 `workbench_audit_log.detail`** JSONB→`::text`、`content` TEXT→可直接判定、**JSONB 无隐式转换会报错**）；**实码三处类型与规格逐一相符**——`migrations/010_audit_log.sql:9` = `JSONB`、`migrations/023_conversational_agent.sql:35` = `TEXT`、§4.1.1 DDL `:380` = `JSONB`；**两表名与 migrations 一致** |
| **R3** | ✅ **真闭合** | §3.4 约束 1 ① nonce 12B CSPRNG / 严禁重用 / 不得由计数器·时间戳·密钥派生 / GCM 重用致命；② 密钥 = **32 字节原始密钥 base64** + **启动期校验解码长度**；与 **§4 配置项描述一致**、与 **AES-256-GCM（32 字节 = 256 位）自洽**；**四处数字/布局一致**（`28 = 12 + 16`、布局 `nonce(12B)‖ct‖tag(16B)`、断言 `≥+28`、§9.6 留痕）；**全库无 `+24` 残留** |

### 21.3 越界与状态核查（**独立检索结论**）

- **仅文档改动，未越界**：非 `.md` 文件对 `AES-256-GCM|BodyCipher|body_ciphertext|param_roles|ToolSpecCatalog` 检索，**仅命中既有 `app/accounts/secrets.py`**——该文件自述为 **TOTP 种子静态加密**（密钥取 `backup_encryption_key` 经 HKDF），**与正文密文用途及密钥来源均不同**（规格明写"不得复用 `backup_encryption_key`"），**确定非本轮产物**；`workbench_audit_log` 命中均为既有文件（`migrations/010`、`app/audit/store.py`、`tests/test_audit_postgres.py`）；**`migrations/027` 不存在**（最大 `026`）。
- **状态**：规格头部**仍为「已修订（第七轮） · 待补丁确认」**，**未**改为「已评审」——**符合"过闸前不得改"**。

### 21.4 顺带发现（**不作为本轮结论依据**）

| # | 事项 | 处置 |
| --- | --- | --- |
| 1 | 记录 §20.2 写 `conversation_messages.content`（省略 `workbench_` 前缀）——属**留痕侧简写**，规格本体无误 | 可忽略；本轮已在本记录中补全 |
| 2 | **§9.5（`:886`，第六轮修订留痕）仍写「`ToolCatalog` 新增 `param_roles`…默认 `control`」**，与 P1（fail-closed）与 P11（改名）后口径相反——位于**历史修订说明**，但**在过闸前构成一处潜在状态漂移** | **建议过闸前加一行更正注**（见 §21.5） |
| 3 | **R4（契约同步具名 AES-256-GCM）未做** | 建议级；不影响 R3 内部一致性 |
| 4 | 用例 32②(c2) 日志路径仍待实现时确定（登记在案） | 已登记 |

### 21.5 流程后果与建议

1. **本段评审链条至此闭合**：首轮 → 第七轮共 **8 次独立复核**（含 2 次定点/补丁确认 + 1 次再次确认 + 本次最终确认），**最后一次确认全部真闭合**。
2. **规格状态可推进**：由「已修订（第七轮） · 待补丁确认」→「**已评审（附记录）**」，并同步门禁 §C1 / §E.5 与其余状态位（**待用户授权后执行**）。
3. ⚠️ **通过评审 ≠ 可开工**：规格第 2 行已写明，**门禁 §B（除 B12 外）的开工前置未取证前，段二仍不得动第一行代码**；下一步应是**段二-1**（运行时复验 + 容器能力取证，**只读勘察**）。
4. 建议在状态推进的同时**顺手加 §9.5 的更正注**（顺带发现 2），以免留下最后一处口径反例。

### 21.6 独立性声明

单一独立视角**未参与 §4.1、历轮修订与 R1–R3 编写**；全程只读；**未采信**任何"已完成"自述（§9.5/§9.6、记录 §13/§14/§16/§18/§20 一律回原文逐条证伪）；未改文件、未 git、未连库、未跑测试。汇总方本轮只做归档，未参与判定。

---

## 22. 状态推进（①+②）的实施留痕（2026-09-13）

### 22.1 用户裁决

**用户指令**：「推进 ①+②」= ① **状态推进**（规格 → 「已评审（附记录）」并同步其余状态位）；② **补 §9.5 的历史留痕更正注**。

### 22.2 实际改动

| # | 改动 | 文件 |
| --- | --- | --- |
| ①-1 | 规格头部：状态 → **「已评审（附记录） · 2026-09-13」**；演进链补 §17/§19/**§21**；**新增「通过评审 ≠ 可开工」警示段**（§B 未取证前不得动第一行代码） | 规格 `:7` |
| ①-2 | 规格 **§9.6 末**：「重审触发（第九次）」→ **「评审闭环（第十次更新）」**（8 次独立复核已闭环；**下一步是门禁 §B 取证/段二-1**；`migrations/027` 仍不存在） | 规格 |
| ② | **§9.5 的历史留痕加更正注**（该行仍写「`ToolCatalog`…默认 `control`」）：注明后续经 **P1**（fail-closed）与 **P11**（改名 `ToolSpecCatalog`）修订，**当前有效口径以 §3.1 / §3.1.1 / §4.1.1 边界第 1 条为准** | 规格 |
| ①-3 | 门禁清单：**日期行**、**头部状态**（规格状态 + 演进链补至「最后一次补丁确认」+ **"规格通过评审 ≠ 本清单过闸"**）、**§C1**（→ **C1 判为「过」**）、**§E.5**（C1 已过；**但 §B 仍未闭环 → 段二不得开工**）；并修 **§C1 叙述中段残留的「→ 待补丁确认」** | 门禁清单 |
| ①-4 | 功能清单：**§3.3 规格真源**与 **§5.3**（→ 已评审）；并修 **§3.3 引言中"规格未过评审"的陈旧表述** | 功能清单 |
| ①-5 | 立项 §14.1 P2a-2 行（→ 8 次复核闭环 · 已评审） | 立项 |
| ①-6 | 能力归属表 第 7 行（→ 已评审；未开工，待门禁 §B） | 能力归属表 |
| ①-7 | 变更记录 **三条**记录的评审状态（→ 已评审通过） | 变更记录 |

**未做**：**无任何代码改动**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍 `026`，**`027` 未创建**）；未提交、未推送；未新建文件。

### 22.3 残留扫描（状态一致性）

`待补丁确认|未过评审` 全库扫描：**活跃正文 0 残留**（`feature-inventory.md:74` 的"规格未过评审"、门禁 §C1 中段的"待补丁确认"**已修**）；其余命中**全部在评审记录 §17/§19/§21 的历史留痕**中（当时状态陈述，允许保留）。

### 22.4 当前状态与下一步（**关键**）

- **规格**：**已评审（附记录） · 2026-09-13**；门禁 **§C1 已过**。
- ⚠️ **仍不得开工**：**门禁 §B（除 B12 外）的开工前置未取证**——按规格第 2 行与 §E.5，**§B 逐条取证通过前，段二不得动第一行代码**。
- **下一步 = 段二-1**（运行时复验 + 容器能力取证，**只读勘察**，不写业务代码），其前置需**外部资源**（可断网环境、内网归档位置等），**须用户提供**。
- **`027` 未落地**：`workbench_tool_actions` / `workbench_execution_idempotency` 的 DDL 仍只存在于规格 §4.1；**可重复执行性 / FK 列序 / 对既有 1434 项零破坏 / 升级回退演练 一律未验证**。
- **仍未闭环**：J-4～J-16、J6、U11（`WORKBENCH_EXEC_TIMEOUT_SECONDS` 默认值）、U12、U13、U14、U15、U16（清理周期默认值）、**R4**（契约同步具名算法，建议级）。

---

## 23. 段二-1（门禁 §B 取证）的授权留痕与阶段结论（2026-09-13）

> **性质**：本条**不是评审**，是**执行留痕**（记录用户授权范围、实际做了什么、证据落在哪、未越界声明、以及仍未闭环项）。**证据正文在门禁清单** [`dsh-integration-preflight-checklist.md`](file:///d:/徐徐AI学习/公司工作台/docs/dsh-integration-preflight-checklist.md) **§F7 / §F8**，本条只做索引与结论。

### 23.1 用户授权（**三档全做**，逐档授权）

**用户指令**：「开始段二 -1 门禁 §B 取证」→（就"先做哪些"的选择题）**三档全做**：

1. **零依赖项**（无需外部输入即可执行）；
2. **跑「无凭据」容器复验**（不引入任何真实模型凭据）；
3. **授权用真实模型凭据跑真实 turn**（仅用于 B14 判据取证；**凭据不出本机、不进仓库**）。

**边界**：本轮为**只读勘察**（运行期复验 + 容器能力取证），**不写业务代码**。

### 23.2 实际执行与证据落点

| 阶段 | 做了什么 | 证据落点 |
| --- | --- | --- |
| **A** | 环境复验：Windows 宿主 + Docker `29.7.2`（linux/overlayfs/cgroup v2/WSL2 kernel `6.18.33.2`）；镜像 `node:22-slim` 复用（未重拉）；新建**仓库外**隔离目录 `d:\徐徐AI学习\_dsh-linux-verify\`，仅**复制** `package.json`+`package-lock.json` | 门禁 **§F7.1** |
| **B** | 无凭据容器复验：`npm ci --os=linux --cpu=x64 --libc=glibc` → **added 522 packages / EXIT=0**；`--profile sdk --dump-default-config` → **EXIT=0 / 352 行** | 门禁 **§F7.1 / §F7.2**；原始输出 `out\`/`out2\`/`out3\` |
| **B2** | Linux 侧剖面**装配层**结论：86 个 `- id:` 条目；`dsh-tool-bash` / `dsh-bash-sandbox` / `dsh-subprocess-local` 在 Linux **求值为启用**；`dsh-tool-pwsh` 禁用；联网工具 `web*` 全部存在未禁用 | 门禁 **§F7.2** |
| **C1** | 静态勘察（凭据/端点可配置性）：SDK `initialize` **无凭据字段**；`credentials-local` **四级分层**；provider **支持 `baseURL` 覆盖**；`DSH_TELEMETRY_DISABLED` **存在** | 门禁 **§F8.1**；原始输出 `out4\` |

**两处新发现（如实登记）**：

1. **遥测默认外发**：`dsh-session-telemetry-otel` 默认向 `https://harness-telemetry.deepseeksvc.com/v1/logs` 发送**会话事件** → **已并入真源**（规格 §3.5 禁用口径：`DSH_TELEMETRY_DISABLED` 为首选开关）。
2. **B13 决议算术不成立**：实测 Linux 树 `@img/*` = **4 个**、含 **LGPL 2 条**（原决议写"2 个 / LGPL 1 条"）→ **已更正**门禁 §B13 与规格 §8 LGPL 行。

**一次方法错误（如实登记，负向与正向一律以重跑证据为准）**：v1 脚本按 `bin.js`/`bin/dsh.js` 探测入口，**漏了真实入口 `lib/bin.js`** → v1 的 dump 证据**无效**；已由 v2 修正重跑（门禁 §F7.1）。

### 23.3 用户裁决（阶段 C1 之后）—— **路径①「自建模型网关」**

§F8.2 认定 **判据 C「`--network none` 下 turn 仍成功」与 `dsh --profile sdk` 的架构不相容**（dsh 在容器内自驱模型调用，必须能出网或连网关）。就 §F8.4 三条路径，**用户裁决：走路径①**（我给的推荐项）。

**由此产生的范围变更与口径修正（已回写真源）**：段二**新增交付项 F「模型网关」**；网络面 `--network none` → **`--internal` 仅内网桥 + 仅网关可达**；凭据口径定死为**容器内只放短期网关令牌（六条）**；**判据 C 作废 → 改判 C′**；P1 由"待核实"改"已定口径"；**规格 §4 需补网关配置项（待办）**（→ **同批已补，见 §23.5 第 4 条**）。逐项落点见门禁 **§F8.6**。

### 23.4 未越界声明

- **凭据零泄露**：**未读取、未复制、未打印**任何凭据值——探针 `_dsh-verify\.env.dsh.txt` **全程未读取**；容器内**无任何供应商密钥**。
- **仓库零改动**：本轮**未修改任何仓库文件**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍 `026`，**`027` 未创建**）。
- **产物在仓库外**：全部临时勘察产物位于 `d:\徐徐AI学习\_dsh-linux-verify\`，**未进入 `公司工作台` 仓库**；清单见门禁 §F7.6（**待用户确认后删除**）。
- **未提交**：未执行 git 提交 / 推送。

### 23.5 阶段结论与未闭环项

**结论**：**段二-1 的 A / B / B2 / C1 四段已完成**；**B14 七条判据 A–G 与三条红线仍全部未取证**（**C2/C3 真实 turn 未执行**）。

**未闭环（不得视为通过）**：

1. **B14 判据 A / B / E / F 与 C′**：须真实 turn；实验形态已随路径①确定（**容器内 `baseURL` → 容器外最小网关原型 + 短期令牌**）。
2. **§F3.2「最高优先级待验项」**（`sandbox_permissions` 升级通道真实行为）**未验证**。
3. **`DSH_TELEMETRY_DISABLED` 实际语义**（是否真能关掉 exporter）**未验证**。
4. ~~**规格 §4 网关配置项未补**（未补前 §B15 的"12 项"口径不完整）~~ → ✅ **已于同批补齐**：**网关六项**入规格 **§4**（`WORKBENCH_MODEL_GATEWAY_BASE_URL` / `..._TOKEN_TTL_SECONDS` / `..._UPSTREAM_BASE_URL` / `..._UPSTREAM_API_KEY` / `..._UPSTREAM_TIMEOUT_SECONDS` / `..._MAX_RETRIES`），**门禁 §B15 由 12 项改为 18 项**、**§F8.6 第 6 项标"已补"**。**闭环移交 §B15**（18 项进 `settings.py` / `.env.staging.example` / 守护测试——**实现前必办**）。

### 23.6 本条的后续（**下一步**）

**阶段 C2/C3（真实 turn）**：用户已授权使用真实模型凭据取证；**实验形态 = 容器内 `baseURL` → 容器外最小网关原型 + 短期令牌**。**目标判据**：B14 的 **A / B / E / F 与 C′**，以及三条统一红线。**取证前不得声称「容器内不持有模型密钥」**。

**执行结果（2026-09-13，见门禁 §F9）**：实验已跑通，**A / C′ 取得成立证据**（供应商域名的出站连接由**网关容器**发起；执行容器**全程只有一条**到网关的连接、不可达供应商域名；网关停机后 turn 以 `code=TRANSPORT` 指向网关失败）；**容器内无供应商密钥明文**（文件/环境/`/proc/*/environ` 三口径全 0，`docker inspect` 的 `Env`/`Mounts` 均不含）；**短期令牌可终态吊销**。

⛔ **但受阻**：**供应商凭据无效**（上游 `401 Authentication Fails ... ****1fa0 is invalid`，且**同密钥直连上游同样 401**）⇒ **"turn 成功"与判据 B 的因果部分未取证**；**判据 D / E 被暴露与路径①不相容（须重述）**；另发现 **`initialize` 冷启动 52–79s** 与 **`dsh-llm-retry` 自retry 5 次**两条新事实。**须用户提供有效凭据后重跑 C2/C3**。

---

## 24. 门禁 §B14 判据 D / E 的重述（路径①对齐）实施留痕（2026-09-13）

### 24.1 授权

**用户裁决**（就 §F9.3 暴露的 X1/X2）：**按建议重述并回写真源**。

### 24.2 问题（取证暴露的架构不相容）

阶段 C2/C3 的真实 turn 实测（门禁 §F9.3）证明：**路径①下 dsh 必须在容器内**驱动 agent 循环（实测 `pid 13` 即 dsh），且容器 env **必然**含 `DEEPSEEK_BASE_URL=http://gw:8080`。而 §B14 判据 D 要求"dsh **不在**容器 PID 树内"、判据 E 要求"**无内网地址**"——**两条原判据与路径①直接矛盾**。

### 24.3 实际改动（仅门禁清单，**未改规格正文**）

| # | 改动 | 落点 |
| --- | --- | --- |
| 1 | **判据 D 重述**："dsh 不在容器 PID 树内" → "**dsh 在容器内，且其 env / argv / 容器内文件均无供应商密钥**（只含短期网关令牌）" | §B14 判据表 D 行 |
| 2 | **判据 E 重述**：禁止项明确为**供应商密钥**；`baseURL`（**仅网关内网地址**）声明为**预期注入物** | §B14 判据表 E 行 |
| 3 | **三条统一红线第 2 条重述**：命中条件改为"持有**供应商密钥**"或"存在**不经网关**的外网通道"；持有短期网关令牌**不算命中** | §B14 红线 2 |
| 4 | **连带修正两处陈旧口径**：判据抬头第 ③ 条与判据 **G** 成立边界仍写 `--network none` → 改为"容器内不存在指向供应商域名的连接 / 无外网出口"（与 **C′** 对齐） | §B14 抬头、G 行 |

**未改动**：判据 **A / B / C′ / F** 与统一红线第 1、3 条**原文不改**。

### 24.4 依据与边界

- 依据：门禁 **§F9.2**（实测）与 **§F9.3**（冲突）；规格 **§3.5 P1 第 5 条**（判据 C→C′）、**§3.3**（网络面 `--internal`）。
- **边界**：本条**只重述判据**，**不改变任何设计**，**不构成对 B14 的放行**——**判据 A/B/E/F 与 C′ 的最终确认仍待有效凭据下的 C2/C3 重跑**（见 §23.6）。

---

## 25. 阶段 C 的回写清单与真源一致性自检（2026-09-13）

> **性质**：**回写与自检的留痕**（不新增任何设计）。§23/§24 已单独留痕，本条收口其余回写项。

### 25.1 回写清单（阶段 C 期间，§23/§24 之外）

| # | 回写 | 落点 | 性质 |
| --- | --- | --- | --- |
| 1 | **遥测口径实测更正**：原「默认外发 / web 之外第二条默认出网通道」**被对照实验推翻**（设/不设 `DSH_TELEMETRY_DISABLED` **两轮均 0 请求**） | 段二规格 **§3.5** 遥测行；门禁 **§F7.3 / §F8.5 / §F9.5 / §F10** | **改变规格正文的"理由陈述"**（**设计结论不变**：仍保留该开关作兜底） |
| 2 | **两条实测发现并入待决项**：`initialize` 冷启动 52–79s → **U11**；`dsh-llm-retry` 传输失败自 retry 5 次 → **U7** | 段二规格 **§8** | **只记事实与约束，定值仍待裁决** |
| 3 | **U1 补进展**：判据 A/C′ 已取得证据；凭据 401 阻断"成功态" | 段二规格 **§8 U1** | 进展登记 |
| 4 | **P0-4（7 项退出方案）** + **P0-3 断网演练记录（含基础镜像归档与离线恢复）** | 自主可控方案 **§4 / §5 / §8 / §9 / §10** | **非段二规格**，属自主可控方案 |
| 5 | **P0-2 挂起（用户裁决 D）** + 恢复条件 + 后果 | 自主可控方案 §5/§8；门禁 **§B16** | 状态登记 |

**未越界**：全程**未改动任何代码**（`app/` / `tests/` / `migrations/` / 前端 / CI 未动；`migrations/` 最大编号仍 **`026`**）；**未提交、未推送**。

### 25.2 真源一致性自检（同日触发）

在**本日大量改动文档**之后做了一次**跨文档一致性扫描**，**发现并修复 3 处状态漂移**：

| # | 漂移 | 位置 | 修复 |
| --- | --- | --- | --- |
| 1 | 测试计划仍写「新增 **12 项**配置」「**须同步改为 12 项**」，与 §4 / §B15 的 **18 项** 冲突（**今日改动引入**） | 段二规格 **§5**（测试计划表） | 改为 **18 项**（注明 = 原 12 项 + 路径①网关六项） |
| 2 | 许可证行仍写「LGPL 条目**从 14 个压到 1 个**」「随镜像 `@img/*` **共 2 个、LGPL 仅 1 个**」，与 **§F7.4 实测（4 个包 / 2 条 LGPL）** 冲突 | 段二规格 **§4 许可证行** | 加 **2026-09-13 实测更正**注（**不改历史陈述**；口径以 §8 LGPL 行为准） |
| 3 | 归档草稿把上位规格标为「（待评审）」 | `plans/2026-09-12-dsh-foundation-design-draft.md` 头部 | 改为「**已评审（附记录） · 2026-09-13**」 |

**判定为「非漂移」而保留**：`capability-ownership-map.md` / `multi-adapter-coexistence-spec.md` / `key-dependency-autonomy-plan.md` 的「待评审」（**这三份确实尚未评审**）；立项与规格中对 `governance-closure-design.md` / `evoflow-*` 的「待评审」引用（**对端确实未评审**）；评审记录 §16 / §17 / §19 / §21 等**历史留痕**中的旧状态与旧计数（**当时状态陈述，允许保留**）。

**另**：`change-record.md` 经核对**无需新增条目**——其口径为"**只登记影响地基的变更**（技术栈 / 目录结构 / 数据模型 / 权限模型 / 支付逻辑）"，而**今日全部改动均为文档级**，未触及上述任一项。

### 25.3 引用完整性自检（同日）

扫描 `docs/` 下**全部 `.md`**，解析出 **178 个本地链接**（`file:///` 绝对路径 + 相对路径），逐个 `Test-Path` 校验 → **`MISSING_COUNT=0`（无悬空引用）**。

> **⚠️ 过程留痕（一次方法错误，如实登记）**：首轮用 `Get-Content` **未指定编码**，而 PowerShell 5.1 会把**无 BOM 的 UTF-8 文档按 ANSI 解码** ⇒ 含中文路径的链接被读成乱码（如 `寰愬緪AI瀛︿範`），**误报 126 条"缺失"**。**该轮结果作废**；改用 `-Encoding UTF8` 重跑后取数。**负向与正向一律以重跑后为准。**
