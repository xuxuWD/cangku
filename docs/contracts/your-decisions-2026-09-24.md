# 待你选（2026-09-24）

> **性质**：**决策单，不是真源**。把当前**只有你能定**的事项摊成可直接勾的选项。
> **怎么用**：在每项 `【选】` 后填字母（如 `1=B`），一次性回给我即可。
> **本文件由谁写**：AI 在 2026-09-24 会话收束时整理；**所有事实均为实测**，推断处已标注。

---

## 1. 推送凭据 / 远端 —— 【选】____（A / B / C）

**现状（实测）**：`git push` 被拒 ——
`remote: Permission to xuxuWD/cangku.git denied to xj18652534302-dotcom.`（**403**）

- 远端 `origin` = `https://github.com/xuxuWD/cangku.git`
- 本机凭据身份 = `xj18652534302-dotcom`（**不是仓库所有者，且无写权限**）

| 选项 | 你要做什么 | 之后我做什么 |
| --- | --- | --- |
| **A** | 让 `xuxuWD` 把 `xj18652534302-dotcom` 加为该仓库 Collaborator | 我重推 |
| **B** | 在本机换一个有权限的账号 / PAT（Windows 凭据管理器里的 GitHub 凭据） | 我重推 |
| **C** | 把 remote 改成你自己的仓库：`git remote set-url origin <你的仓库>` | 我重推 |

**⚠️ 三选一都要你动手 —— 我不碰凭据、不改 remote。**
**⚠️ 提醒**：推的时候会**连带上去 5 个不是本轮的 commit**（上一轮的审计导出 / 假入口消解 / TabTin 研读 / Q1-Q3 追记）——分支不能只推一半历史。

---

## 2. B3 形态原型：留还是删 —— 【选】____（A / B）

**现状**：`workbench-web/prototype.html` + `workbench-web/src/prototype/`（**1,665 行**）已入库（commit `31b539a`）。
它是**开发期形态原型**，`vite build` 只打 `index.html`，**不进生产包**（已 grep 验证 0 命中）。

| 选项 | 含义 | 后果 |
| --- | --- | --- |
| **A** | **保留** | 留作"B3 形态的可点评审材料"与真壳对照；代价是多 1,665 行需要维护 |
| **B** | **删除** | 真壳（commit `4e19f50`）已取代它；删是一次独立 commit，**可回滚** |

**建议：A（保留到 B3 评审结束）** —— 它正是你评审形态时最有用的东西。

---

## 3. 把「日程 / 快捷入口」搬进壳右栏（B 的另一半）—— 【选】____（A / B / C）

**背景**：`fix(workbench-web)` 已把「我的工作台」改成单列（主区域列宽 517 → **744 px**）。
另一半是**把这两个面板搬进壳右栏** —— 它是**数据所有权换层**（`RightPanel` 要自己拿
`fetchSchedule` / `fetchQuickActions`，`handleQuickAction` 也要挪过去），**不是调布局**。

| 选项 | 含义 | 后果 |
| --- | --- | --- |
| **A** | **做**（我下一轮实施） | 更贴 B3 §5「右栏放当前上下文」；但动到壳与页面两侧，需重跑壳用例 |
| **B** | **不做**（保持现状单列） | 现在的排布已解决"被挤"（744 px 全宽）；右栏继续只放「当前对象」 |
| **C** | **换个做法**：把日程/快捷入口抽成共享组件，页面与右栏都能用 | 最干净，但工作量最大 |

**建议：B（先不做）** —— "被挤"已经解决；A 的收益是形态一致性，可以在 B3 评审时一并定。

---

## 4. 手点那 4 个入口（我那边脚本验不完）—— 【选】____（A / B）

**已验通**：**归档**（点 → 勾选二次确认 → **服务端真的改了状态**）。
**未验完**：**模式 / 删除 / 导出 / 成员增删**。

| 选项 | 含义 |
| --- | --- |
| **A** | **你在真实环境手点一遍**（推荐）—— 起法见下，5 分钟 |
| **B** | 我再调一次脚本（**我已连续三轮失败在脚本时序上**，收益存疑） |

**起法**（你那边）：
```
cd workbench-web
VITE_DEV_PROXY_TARGET=<你的后端> npm run dev
```
进「对话」→ 新建 → 看四个控件：**模式下拉 / 删除 / 导出 / 成员增删**。
预期：删除要**原样输入「删除」**；导出落一个 json 文件；成员区显示真实名单。

---

## 5. B2 立不立项 + V1 / V2 / V4 —— 【选】____

**材料**：[`b2-review-2026-09-23.md`](b2-review-2026-09-23.md)（评审要点 + 施工清单 + 权限闸门分析）

**一句话**：B2 最硬的一块不是"补员工侧接口"，而是**拆一个守着 10 个端点的权限闸门**
（`_require_workforce_directory_admin` 仅 `super_admin`）—— 属**权限模型变更（地基级）**。

### 5.1 立不立项 —— 【选】____（A / B / C）

| 选项 | 含义 |
| --- | --- |
| **A** | **立项**（它是唯一能解锁 SSE / 运行 / 审批真实验证的东西，且方向你已在 D-050⑥ 裁过） |
| **B** | **不立项**（先做别的） |
| **C** | **先补材料**（B2 规格 §2.2 / §4 / §7 / §8 我还没读，可先补齐再评） |

### 5.2 V1 数字员工要不要「主动值班」—— 【选】____（A / B）

| 选项 | 差别 |
| --- | --- |
| **A** | **不做**（本期只做"派活才干活"）—— 规格 §2.3 已把它列进"不做"，且无人值守与九步闸门冲突 |
| **B** | **做** —— 要引入调度 + 上班时段 + **无人值守审批**（与闸门冲突需先解），工作量大幅上升 |

### 5.3 V2 是否允许租户自定义岗位 —— 【选】____（A / B）

| 选项 | 差别 |
| --- | --- |
| **A** | **不允许**（6 类走**代码常量** `app/workforce/role_templates.py`，**不建表**） |
| **B** | **允许** ⇒ 岗位模板**须建表** + 迁移 + 契约 |

### 5.4 V4 本期加不加 `soul_md` —— 【选】____（A / B）

**实测**：`soul_md` **全 `migrations/` 零命中** ⇒ 现在只有 `system_prompt`（"怎么做"），缺"是谁"。

| 选项 | 差别 |
| --- | --- |
| **A** | **加** ⇒ **加一列**（迁移）+ 前端一个展示位；成本小，是"下属"拟人化的那一层 |
| **B** | **不加** ⇒ 只有 `system_prompt` |

**⚠️ V2 与 V4 决定"要不要建表/加列" ⇒ 定晚了迁移要写第二遍。**

---

## 6. 两条只读 SQL（只能在你的真库上跑）—— 【选】____（A / 已跑，结果附上）

**为什么**：B2 迁移要把 `owner_user_id` 回填自 `created_by` 并 `SET NOT NULL`。
实测 `created_by` 是 **`NOT NULL`** ⇒ **`SET NOT NULL` 拦不住空字符串** ⇒ 迁移会"成功"但数据是垃圾的。

```sql
-- ① 动手前：空串/空值各多少
SELECT count(*) FROM workbench_digital_employees WHERE created_by IS NULL OR created_by = '';
-- ② 回填后核对
SELECT count(*) FROM workbench_digital_employees WHERE created_by <> owner_user_id;
```

| 选项 | 含义 |
| --- | --- |
| **A** | 我还没跑（**仅在做 B2 时需要**） |
| **B** | 已跑，结果：① ____ 条 ② ____ 条 |

**若 ① > 0** ⇒ 不能直接照抄 B1 §3.3 的 UPDATE，要先决定兜底归属或清脏数据。

---

## 7. 一并说明：本文件**未验证**的部分

1. 第 5 节引用的 B2 事实来自 [`b2-review-2026-09-23.md`](b2-review-2026-09-23.md)，
   **该文件的 8 条未验证同样适用于此**（尤其：§7 验收标准未读、`create_employee` 未读、
   `GET /workforce/roles` 响应形状未读、闸门 10 处调用点只定位了 8 处）。
2. 第 4 节的"归档已验通"是**真机**（Playwright + 真后端 + 真 TOTP）；另外 4 个**未验完**。
3. 第 3 节的 744 px 是**真机实测**（1440×1000，`ant-col-xs-24`）；未测 1200–1600 以外的视口。
4. 本文件**未改动任何真源**。
