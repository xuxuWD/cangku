# 全部要你定的 —— 一处列全（2026-09-24）

> **怎么用**：看完这份，回我一行即可。例如
> `甲:全部通过 乙:1B 2A 3A 丙:全按建议 丁:Q4=A Q5③=改 B4V1=A 戊:SQL已跑=0条`
> **或者直接回**：**「全按建议 + 五份规格全通过」** —— 那我一晚上就能全落。
>
> **权威仍在五份规格原文**；本文件只负责"把选项摊开、后果写明、给出建议"。
> **本文未改动任何真源。**

---

# 甲 · 五份规格的评审结论（**总闸门**）

**规格自述**：评审批准前不得开工。**这五份不通过，下面的裁决都落不了地。**

| # | 规格 | 一句话 | 我的建议 | 【结论】 |
| --- | --- | --- | --- | --- |
| **B1** | 执行与客户端合并 | 桌面+服务端并存执行；含**数据模型 §3.3**（owner/visibility/shares）与**逐模块归属 §4.2** | **附条件通过**（先补 3 处，见下） | ______ |
| **B2** | 数字员工干线 | **你已立项**（D-057），V1/V2/V4 已裁并回写 | **通过** | ______ |
| **B3** | 信息架构 | 三栏/对话常驻/悬浮助手/侧栏收敛 —— **真壳已按它做完并真机验过** | **通过** | ______ |
| **B4** | 能力外采 | 外部 skill 处置口径（**V1 是本规格入口**） | **通过**（但 V1 必须先裁，见乙-2） | ______ |
| **B5** | 沉淀层计量与控制器 | 记忆到底有没有被用上；直击你"沉淀层没起作用"的不满 | **附条件通过**（先补分页，见下） | ______ |

### ⚠️ B1 的"附条件"是哪 3 处

| # | 问题 | 证据 | 建议怎么补 |
| --- | --- | --- | --- |
| **1** | **岗位模板"不建表"已被你 D-057 推翻，但 B1 原文没留痕** | B1 §3.2 写"代码常量、不建表"；而 B2 施工方案已按**建表**写 | 在 B1 §3.2 就地加推翻标注（像我给 B2 做的那样） |
| **2** | **§3.3④ 第三档可见性缺数据支撑** | `visibility='shared' AND role_key ∈ 我的岗位` —— **仓库没有「用户→岗位」映射**（账号侧 `position` 是自由文本） | 要么补映射模型，要么**砍掉这一档**改成两档 |
| **3** | **§3.4 只写"目录接口改四档"，没给闸门拆法** | 实测那个闸门守着 **9 个端点**；B2 施工方案已给三档拆法，B1 未纳入 | 把 B2 施工方案 §2 的三档拆法**回写进 B1** |

### ⚠️ B5 的"附条件"是 1 处

**B5 全文 `limit`/`offset` 零命中** —— 有列表端点却无分页，与既有契约「列表必须分页」相悖。**补上分页即可。**

---

# 乙 · 🔴 A 级：不裁就无法开工（**只有 4 项**）

> 我原来报"5 项"，现在**压到 4 项** —— 因为 **① 已被 ADR-0003 foreclose**（见下）。

| # | 问题 | 选项 | 后果 | **我的建议** |
| --- | --- | --- | --- | --- |
| **0** | ~~① 前端底座 AntD vs 手写 CSS~~ | **已被 `ADR-0003` foreclose**（2026-09-19 你已签字：采纳 AntD 5 + 规范令牌，且明文把"保留手写组件库"列为**不推荐**） | —— | **不用裁**。真待定的只剩两条实施窄口：(a) 壳层能否手写 CSS；(b) 令牌须改取规范值（主色 `#1677FF`，正文用 `#0958d9`；深色模式规范缺失） |
| **1** | **B4-V1 外部 skill 处置口径** | **A** L0 只读研读自研（零改动/零供应链风险）<br>**B** L1 引进代码（须变更并重评审已通过的 P4）<br>**C** 两者都要（工作量最大） | **不裁 ⇒ B2 的六套岗位模板没有素材 ⇒ B2 核心做不了** | **A（L0）** —— skill 的价值主要在 `SKILL.md` 方法论，自己写效果一样且零风险；且 B4 §9 的 N3/N4 已把默认定为 L0 |
| **2** | **B2 ↔ B4 对「拍剪素材」结论相反** | **A** 以 **B4** 为准（B4:157 列 5 个 **MIT** 素材：`violin`/`jianshuo/claude-skills`/`manim-skill`/`heygen-com/skills`/`elevenlabs/skills`）<br>**B** 以 **B2** 为准（B2:177 写"❌ 无对口现成、**必须自研**"）<br>**C** 两边都不动，等 B4-V1 定完再说 | **同一天写的两份规格结论相反**，都未声明"以哪份为准" ⇒ 违反「单一可信来源」 | **A（以 B4 为准）** —— B4 列了**具体仓库名 + 许可已逐项读 LICENSE 原文**，B2 只是笼统否定 |
| **3** | **Q4 `single_pg` 多租户取舍**（已挂 2 天） | **A** 保留多租户（零改动）<br>**B** 降级单租户<br>**C** 保留机制、只降级"默认形态" | **B2–B5 的数据模型全建在它上面，越晚裁返工越大** | **A 或 C**。⚠️ 文档原写"回退成本高（44 迁移+51 表模板）"**已被证伪**：实测 schema-per-tenant **从未落地**（`app/migrations.py` 无 scope、`app/` 无 `search_path`），B 的真实改动只是文档+3 个生成文件+1 个脚本 |
| **4** | **B1-V1 内容工作台是否属本期边界** | **A** 属本期（合并进新前端）<br>**B** 剥离<br>**C** 保留但冻结 | 不裁 ⇒ 前端合并范围定不下来 | ⚠️ **这要你答一个事实问题**：**内容岗现在实际在用那个公众号工作台吗？**（问了内容岗就知道，我无法替你判断） |

**⇒ 第 1 批真正的硬阻塞只有 4 项（1/2/3/4），其中只有 4 需要你先去问一句。**

---

# 丙 · 🟠 B 级：不裁无法定稿（7 项，**建议"全按建议"**）

| # | 决策 | 我的建议 |
| --- | --- | --- |
| **1** | **B1 §4.2 逐模块归属勾选**（唯一需要你**逐行勾**的） | 已在 [决策选择书](decision-options-2026-09-22.md) 里逐项给了建议（合并 / 进设置 / 剥离 / 归档），**同意全部**即可 |
| **2** | **桌面前端外壳** Tauri vs Electron | **Tauri**（体积优势明显）—— 但**不急**，可放到前端合并之后 |
| **3** | **路由方案** react-router vs 自建 | **已完成**（换壳时落了**自建**、零依赖）。若要改用 react-router，替换点只有 `shellRouter.ts` 一个文件 |
| **4** | **`soul_md` 本期加不加** | ✅ **你已裁：加**（D-057 5.4） |
| **5** | **CRM 现有代码处置**（`app/crm/` 4,836 行 + 16 前端文件 + 1 迁移 12 表） | **归档**（先不删）—— 已交付的删了不可逆 |
| **6** | **Remotion 买不买**（`$25/月/席位` 或 `$0.01/次 + $100/月最低`） | **先不决定**，但**记在案** —— 它是唯一一项**确定性付费义务** |
| **7** | **B1 §2.3 断网处理**（A 本地暂存补传 / B 断网只读 / C 完全禁用） | **B（断网只读）** —— A 的审计空窗期"无法回答断网期间那台机器上发生了什么" |

---

# 丁 · 🟢 施工中新冒出的（3 项，**建议全按建议**）

| # | 问题 | 我的建议 |
| --- | --- | --- |
| **N-a** | 6 类预置模板**怎么落**：每租户各一份，还是平台级一份 + 租户覆盖？ | **平台级一份 + 租户可覆盖**（否则每租户各一份会让"改预置"污染不了别人，但也让基线无法统一升级） |
| **N-b** | **`is_builtin` 要不要**（区分预置与租户自建） | **要** —— 没有它，租户改预置会改到**所有租户**看到的基线 |
| **N-c** | **`duty_window` 形状**：只时段，还是"时段 + 动作白名单" | **时段 + 动作白名单** —— 这是你 V1=C 口径（只做不需审批的动作）的**前提**，只写时段则口径落不了地 |

---

# 戊 · 只需你**动手一次**（不是裁决，3 项）

| # | 做什么 | 命令 / 说明 | 【状态】 |
| --- | --- | --- | --- |
| **1** | **那条只读 SQL**（B2 迁移动手前必须） | `SELECT count(*) FROM workbench_digital_employees WHERE created_by IS NULL OR created_by = '';` | ______ 条 |
| **2** | **手点对话页 4 个入口**（归档我已验通） | `VITE_DEV_PROXY_TARGET=<你的后端> npm run dev` → 对话 → 新建 → 模式 / 删除 / 导出 / 成员 | ______ |
| **3** | **开 PR** | `https://github.com/xuxuWD/cangku/pull/new/feat/workbench-merge`（描述在桌面 `PR-feat-workbench-merge.md`） | ______ |
| **4** | **授权修那 2 行违规绑定？** | `python scripts/repair_dangling_skill_bindings.py`（**默认干跑**；加 `--apply` 才写） | ______ |

---

# 己 · 一个**被我漏掉、现在补上**的

**Q5③ 硬闸门** —— 台账明写「①② 有答案**不构成可以动工**；③ 是硬闸门」。

| 选项 | 含义 |
| --- | --- |
| **A. 改** | 显式修改 `architecture.md` L51 并留痕 ⇒ **桌面执行才可实施**（B1 的前提） |
| **B. 不改** | 保持现状 ⇒ **B1 的桌面执行永久不得实施**（B1 只剩"服务端容器"那一半可做） |
| **我的建议** | **A（改）** —— 因为 B1 §1 已把你原先的反对理由**收窄**为"桌面执行附带第二套用户/权限/审计"，而 D-050④ 恰好吃掉了后者。**但这是架构条文的变更，只有你能拍。** |

---

# 庚 · 全部未验证（本文的边界）

1. 本文的选项与后果**转述自五份规格、两份汇总、台账与三份本会话材料**；规格内部细节以 [`specs-review-brief-2026-09-24.md`](specs-review-brief-2026-09-24.md)（717 行，70 条空洞）为准。
2. **"我的建议"是判断，不是事实**，每条都给了理由，你可逐条推翻。
3. **B1 §4.2 的逐模块归属**文档里是**建议**，真正勾选要你逐行做。
4. 本文**未改动任何真源**；也**未运行任何测试**。
