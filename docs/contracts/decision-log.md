# 决策日志（Decision Log）

> 用途（《三样执行层面的东西》第 3 条）：**你每天会做很多小决策，AI 会忘、你也会忘**。
> 凡影响架构/权限/数据/交付的结论，一律记在这里；两周后有人问"要不要让数字员工继承使用者权限"，翻这里即可。
> 规则：① 只记**已作出**的决策（未定项留在 `adr.md` 的签字行）；② 每条必须有**依据**（文件/原文/实测）；
> ③ 决策被推翻时**不删旧条**，新增一条并注明"取代 D-xxx"。

| 编号 | 日期 | 决策 | 依据 | 状态 |
| --- | --- | --- | --- | --- |
| D-001 | 2026-09-19 | **后端：保留既有 Python 服务**，不重写为 NestJS | 仓库实测（44 迁移 / 2600+ 用例）+ 知识库《总体思路复审-2026-09-03》「不替换 Celery→Temporal」 | ✅ 已裁决（ADR-0001） |
| D-002 | 2026-09-19 | **多租户：改为 Schema 级隔离**（**推翻我推荐的"行级加固"**）；租户上下文仍由后端强制注入，且与 schema 路由同一处解析 | 用户 2026-09-19 裁决 + 《项目核心规范》MT-01 | ✅ 已裁决（ADR-0002）；**须另出《Schema 级多租户改造专项方案》**，专项签字前不得动工 |
| D-003 | 2026-09-19 | **前端：采纳 AntD 5 + ProComponents + 规范令牌值**（存量页面渐进替换） | 用户 2026-09-19 裁决；《项目核心规范》组件规则 | ✅ 已裁决（ADR-0003） |
| D-004 | 2026-09-19 | **与 GEO 前端各自独立、仅共享令牌值**（不统一壳层） | 用户裁决 + `项目边界审计-2026-09-05`「两仓库相互独立，未发现串库」 | ✅ 已裁决（ADR-0003） |
| D-005 | 2026-09-19 | **组织/部门维度：暂不引入实体、保留扩展位**（数据范围写成 `tenant → project → 未来 org`；岗位模板预留 `org_ref`） | 用户裁决；平台无部门数据来源；推翻"立刻对齐"选项 | ✅ 已裁决（ADR-0004） |
| D-006 | 2026-09-19 | **文档 ACL 采用 4 级**（公开/内部/机密/绝密）+ 与既有 5 级模型路由建立映射 | 《PRD》KB-01 + 《核心规范》；映射表见 `knowledge-acl.md` §4 | ✅ 已裁决（ADR-0004） |
| D-007 | 2026-09-19 | **第一期范围：扩大** —— 基线闭环（岗位入口→…→人工审批→完成/回滚，试点 自媒体运营 + CEO 看板）**+ 并入 A/B/C**：① 通用智能体 GA-01~04；② 管理后台三件套（OP-01/02 + AD-03）；③ 共享 DE-07 + Agent Teams DE-08。**明确后置**：WH-01~04、商城类、进化/混合劳动力分析 | 用户 2026-09-19 勾选；清单见 `adr.md` ADR-0005 | ✅ 已裁决（ADR-0005） |
| D-013 | 2026-09-19 | **排期**：用户选**选项 A —— 先出 Schema 级改造专项方案**（再谈前端第 1 轮） | 用户指令 | ✅ 已执行（见 D-014） |
| D-014 | 2026-09-19 | **《Schema 级多租户改造专项方案》已产出**：实测 55 租户表 + 6 平台表；十项设计决策（双保险 / 4 张表留平台 / 双链模板 + 一次性搬迁 / 注册即建 / `SET LOCAL search_path` + 统一入口 + fail-closed / 命名与注入防护 / 批量任务改造面 / 回退开关与三段演练 / 性能口径 / 排期）；分期 S0–S5 | `docs/contracts/schema-per-tenant-plan.md` | ✅ **已全采纳**（用户 2026-09-19「全采纳」） |
| D-016 | 2026-09-19 | **前端第 1~3 轮已交付**：① 应用壳（AntD 5 + 规范令牌 + 角色自适应 4/8 项）；② 组件库 9 件（八态 + **状态保真** + `PermissionGuard` 不静默隐藏）+ 依赖修补（React19 补丁 + `path-to-regexp` 钉 8.4.2，audit 3 high→0）；③ **模块「我的工作台」**（待办/日程/最近使用/快捷入口 + 适配层 `mock|http` + **显式"示例数据（未接后端）"标识**）。累计 **88 用例**、tsc/构建全绿 | 提交 `759b50f` / `f19c514` / 本轮；契约 `docs/contracts/my-workbench-api.md` | ✅ 已交付（自跑复核） |
| D-017 | 2026-09-19 | **WH-01「我的工作台」由用户指令提前到第一期**（原 ADR-0005 列为后置）；WH-02/03/04 仍后置。**登记缺口**：「日程」后端无实体（本轮如实显示未接入）；「最近运行列表」后端无对应接口（样例无接口可依） | 用户 2026-09-19 指令；ADR-0005 已回写 | ✅ 已裁决 |
| D-018 | 2026-09-19 | **前端第 4 轮「我的数字员工」已交付**（WH-02 提前 + DE-01/02/03 员工侧）：卡片式列表（非表格）+ 从 **6 个岗位模板**创建（能力继承预览，**逐字段取自模板对象**，与 `role-templates.md` §2 逐字比对用例守护）+ 详情抽屉 + 停用走 `DangerConfirm`；**共享给我的员工**：配置/停用渲染为禁用 + 原因；**状态保真**（无运行记录 = 暂无/未验证，无 0 无"成功"）。累计 **117 用例**、tsc/构建全绿 | 本轮提交；契约 `docs/contracts/my-agents-api.md`；ADR-0005 已回写（WH-02 提前） | ✅ 已交付（自跑复核） |
| D-015 | 2026-09-19 | **S0 已完成**：分类清单（51/4/6）+ 租户模板链（51 表 / 53 索引 / 98 约束 / 2 序列 / 4 处 `${PLATFORM_SCHEMA}`）+ 工具链（`build-template` / `verify --apply` / `inventory`）；**S0 期抓到并修复真缺口**：模板漏自增序列（两张表 `id` 无 `nextval` ⇒ 新租户 schema 插入会失败），已补且把 `column_default` 纳入结构比对（反假已证可变红） | `migrations/tenant_template/*`、`scripts/tenant_schema.py`、方案 §5「S0 完成记录」 | ✅ 已交付（`verify --apply` 退出码 0，我方自跑复核） |
| D-012 | 2026-09-19 | **清场扩围 B2+B3 已落盘并推送**（任务与运行层整租户清场，五面齐备） | 提交 `fb002a4`；本地全量 2618 绿 + 真机取证 | ✅ 已提交 |
| D-008 | 2026-09-19 | **权限实时校验必须每次重查 12 项**；**前端隐藏菜单不是安全边界** | 同上 §三.3（原文逐字核对） | ✅ 已入契约（`permission-matrix.md` §2.4） |
| D-009 | 2026-09-19 | **知识检索：角色→能力映射为唯一来源**（`evidence_hub_permissions` 范式）+ **职责分离**（提交≠核验≠撤回）+ 禁止"先取后补判断" | 知识库《GEO-WeKnora 检索适配器 PoC 规格-2026-09-06》（原文核对） | ✅ 已入契约（`knowledge-acl.md` §2.5 / §3） |
| D-010 | 2026-09-19 | **岗位模板 = 岗位合同六要素** + 审批策略量化表；`full_auto` 也不得越过高风险清单 | 知识库 EvoFlow 手册（代理检索，**建议复核原文**） | ✅ 已入契约（`role-templates.md` §3.5） |
| D-011 | 2026-09-19 | **第 0 轮交付物定为 4 份契约文档**（权限矩阵 / 岗位模板 / 知识库 ACL / ADR），**只出文档不写代码** | 用户「先输出契约文档」 | ✅ 已交付 |

| D-019 | 2026-09-19 | **前端第 5 轮「数字员工注册中心」（AD-01）已交付**：全员统一视图（表格，与员工侧卡片对照）+ 指标区 5 卡（全部(含草稿)/启用/停用/草稿/近7天有运行）+ 筛选透传（岗位/状态/创建者/搜索，**服务端为准、不做前端过滤假象**）+ 详情只读抽屉 + 启停走 `DangerConfirm` + 仅管理角色可见（员工 ⇒ 无权限态、不渲染表格）；**状态保真**（无运行 = 未验证/暂无，无 0%）；字段与 `my-agents-api.md` **类型层直取 + 运行时点名 + 文档 32/32 命中**三重一致。累计 **153 用例**、tsc/构建全绿、产物 0 调试串 | 本轮提交；契约 `docs/contracts/agent-registry-api.md`（77 行） | ✅ 已交付（自跑复核） |

| D-020 | 2026-09-19 | **前端第 6 轮「权限配置」已交付**（三块：角色知识范围 / 数字员工知识范围 / 最近变更；四态齐备 + 员工⇒整页无权限且不渲染编辑控件；写失败就地呈现不关抽屉）。**同轮三项决策**：① **候选知识库标识采用方案 A** —— 候选 = **现有绑定（两块）∪ 变更记录 old/new 的并集** + **手动录入**，前端只校验非空/去重/**≤100**（服务端仍是唯一权威）；② **不做只读权限矩阵页签**（矩阵唯一权威是 `permission-matrix.md`，界面另绘必然漂移）；③ **不做文档级四级 ACL**（`knowledge-acl.md` 字段未落库，属 Schema 线） | 用户 2026-09-19 批准方案与缺口处置；本轮真机取证 5 项（`403`/`409` 零写入/纳管后 `200`+查库/审计可读/清理）；契约 `docs/contracts/permissions-api.md`（新增 §8/§9）、`permission-matrix.md` §10 | ✅ 已交付（自跑复核：tsc 退出码 0、vitest **231/231**、build 成功、产物样例字样 0 命中） |

| D-021 | 2026-09-19 | **前端第 7 轮「知识库」已交付**（一页四块：治理指标 / 文档列表 + 登记 / 可检索文档 + 到期扫描 / 知识检索；四态齐备 + 员工⇒整页无权限且不请求数据；写失败就地呈现不关抽屉，成功只用服务端回读值）。**同轮三处口径决定**：① **无权限判定用新能力 `knowledge.manage`（仅 `super_admin` 映射）**，因其为界面呈现层能力、与后端实测门禁一致；② **检索块的空结果按 `reason` 三分呈现**（`empty_whitelist` 给"先登记并发布"指引、`no_binding` 提示先配范围、`no_hits` 只换关键词），`503` 单列「服务未接入」；③ **界面按契约 §2 保守执行状态机**——`draft` 的复核按钮**禁用**，即使真机实测后端允许 `draft + review?approved=true ⇒ 200 → published`（见下"缺口"），**不通过界面放大后端行为** | 用户 2026-09-19 批准契约；本轮真机取证（员工 9 端点全 `403` / 超管列表·指标·清单·扫描 / 发布 `422` owner 闸门 / 归档发布 `409` / 检索 `200 reason=null`）；契约 `docs/contracts/knowledge-api.md`（新增 §2.1 / §2.2 / §9）、`permission-matrix.md` §11 | ✅ 已交付（自跑复核：tsc 退出码 0、vitest **262/262**（原 232 + 新增 30，无删改跳过）、build 成功、产物样例标识 0 命中） |

| D-022 | 2026-09-19 | **第 7 轮发现两项须修缺口（本轮只登记、不改）**：① **知识端点对 `employee` 全量 `403`**（含登记）⇒ `permission-matrix.md` §3「知识：上传/登记 = employee ✅」与实现不符，**KB-02 维持不达标（P0）**；② **`draft` + `review?approved=true` 实测 `200 → published`**，`review_document` 只查状态边不查 owner 闸门 ⇒ **复核通道可代替发布并跳过「发布必须指定负责人」**（P1，建议后端在复核通过路径补 owner 校验或收窄 `draft→published` 边） | 本轮真机实测 + 只读 `app/knowledge_governance/{models,service}.py`；证据见 `knowledge-api.md` §2.1 与交付报告 | ✅ 已登记（待专项裁决） |

| D-023 | 2026-09-19 | **第 7 轮收口验收完成（收口人独立复核，非交付自验）**：门禁重跑（tsc 退出码 0 / vitest **262/262** / build 成功）+ **独立反假 2 组**（状态机 `publish` 闸门改坏 ⇒ 2 条指定用例红；`toReason` 恒 `null` ⇒ `empty_whitelist` 用例红；均还原复绿）+ 真机交叉验证 6 项（**逐字复刻界面登记载荷** `201` + 查库逐字段核对；登记→发布→复核退回全流程；员工 `403` 抽查；检索 1 条真引用）+ **浏览器走查 33/33**（0 控制台错误 / 0 页面异常 / 0 个 4xx5xx，截图 5 张）。**契约补正**：`review?approved=false` 在任意非终态 ⇒ **`200 → archived`**（`published` 行真机实测；`draft` 同机制按代码判定）⇒ `knowledge-api.md` §2 表 `draft`/`published` 两行的「复核退回」格 `409` 不成立（§2.1 已更正）。**新观察**：检索冷启动首次可能 `504`（上游 12.7s > 适配层超时；复测 0.2–0.7s；本机 CPU 环境特性）。**遗留（非本轮引入）**：第 6 轮 `permissions.SAMPLE_DESCRIPTION` 与 `myWorkbench` 的「标记已读…」说明未做 DEV 门控 ⇒ 字符串留在生产产物（`http` 模式不渲染，惰性残留，待整改） | 收口证据见 `knowledge-api.md` §10；走查脚本 `tmp/r7-knowledge-walkthrough.mjs`、截图 `docs/screenshots/ui-v2-r7-knowledge/` | ✅ 验收通过（待用户确认提交） |

| D-024 | 2026-09-19 | **第 7 轮两项缺口修复（用户裁决后实施）**：① **P0**「知识端点对 `employee` 全量 `403`」⇒ 裁决**全量对齐矩阵 §3**，落地三档角色集合（`SEARCH_ROLES`/`REGISTER_ROLES` = 四角色；`GOVERNANCE_ROLES` = `ceo`+`super_admin`）+ `register_document` 改走登记闸门 + 绑定面委派同一集合；**安全要件**：非管理角色检索**自身角色自限**（`role_key == 自身角色`、`agent_key` 不开放）——因 `resolve` 不校验调用者身份，否则可读他人知识范围；② **P1**「`draft` + 复核绕过 owner 闸门」⇒ 裁决**双管**：复核前置状态收窄为 `needs_review`/`under_review`（`draft`/`published` ⇒ `409`）+ 通过路径 owner 非空（空 ⇒ `422`）；③ **界面同轮对齐**（能力分档 + 知识页三视图 + 自限检索不渲染身份选择） | 用户 2026-09-19 四项裁决（模块 / P0 档位 / P1 方式 / 开工顺序）+ 「自身角色自限」「界面同轮对齐」两问；矩阵 §3 为唯一权威（本次是实现对齐、非改口径）；证据见 `knowledge-api.md` §11、`permission-matrix.md` §12 | ✅ 已实施（后端 `pytest` 退出码 0、前端 tsc 0 + vitest 265/265，反假三组） |
| D-025 | 2026-09-19 | **残留缺口（P0 修复带出的跨面依赖）已裁决：维持现状 + 改矩阵标注**。事实：矩阵 §3「知识：授权绑定」对 `ceo` 标 ✅，但写路径还要过**岗位目录**闸门（`app/workforce/store.py::_ensure_admin`，仍仅 `super_admin`）⇒ `ceo` 实际**只能读、不能写**绑定。**裁决（用户 2026-09-19）**：不放宽目录闸门（属 OP-01 岗位目录面的权限模型，**另立专项**），§3 该行按当前事实改标「⚠️ 读可、写待目录放开」，待该面放宽后恢复 ✅。**另一处结构性缺口（登记）**：无 user→岗位 / 数字员工 归属链 ⇒ 非管理角色只能"按自身角色键"命中绑定（"真正按岗位"需等 Schema / 岗位实体线，ADR-0004） | 实施中发现（`tests/test_knowledge_access_directory_gate.py::test_ceo_passes_knowledge_gate_but_directory_gate_still_blocks` 钉住事实）；依据 `app/workforce/store.py:107-109`；用户 2026-09-19 裁决 | ✅ 已裁决（标注见 `permission-matrix.md` §3 脚注 2 / §12） |

| D-026 | 2026-09-20 | **第 8 轮「Skill & MCP」（AD-03）已交付 + 同轮修复两处缺口**：① **矩阵对齐**——`permission-matrix.md` §3「技能：复核/启用/停用 = `ceo` ✅」与实现不符（`REVIEW_ROLES` 原仅 `{super_admin}`，`ceo` 一律 `403`；与第 7 轮知识域 P0 同一类）⇒ **实现向矩阵对齐**：`REVIEW_ROLES` 扩为 `{ceo, super_admin}`，文案改「只有企业负责人或超级管理员可以审核或启用技能包」；② **`bind` 恒 `500` 修复**——`POST /api/v1/skills/bindings` 误声明 `response_model=SkillView`（返回绑定形状 ⇒ 序列化 `ResponseValidationError`）⇒ 新增 `SkillBindingResponse(skill_key, agent_key, status)` 并在 `POST`/`DELETE` 两处声明，**业务逻辑零改动**；③ **绑定口径刻意不动**——矩阵 §3 **未列**「绑定」行 ⇒ 新增独立 `BIND_ROLES`（仍仅 `super_admin`），**不随复核对齐顺带放开**；④ 前端交付 `workbench-web/src/features/skillsMcp/**`（管理视图 = 列表 + 审核/启停 + 详情正文；员工视图 = 提交表单 + 只读列表；MCP 如实「尚未接入」；四态齐备、非法动作禁用 + 原因、**自审按"提交人 = 自己"预置禁用**）、导航四角色可见、能力分档 `skill.manage` + `skill.submit`、会话新增本人账号标识（`workbench.user`）；⑤ 门禁：后端 `2527 passed / 0 failed` + `compileall` 0；前端 `tsc` 0 / `vitest` 300 / `build` 成功 + 产物 grep（本模块样例键 0、`console.log` 0）；⑥ 反假 5 组（回退 `REVIEW_ROLES` ⇒ 2 红；去 owner 闸门 ⇒ 2 红；回退 `response_model` ⇒ 复现 500；导航回退 `ADMIN_ONLY` ⇒ 3 红；去自审预置 ⇒ 2 红）；⑦ 真机（`127.0.0.1:18112` + 真库 `wiring-evidence`）：`bind 200` + `tools ["fs.read"]` + 员工 / ceo 绑定 `403` + ceo 复核 / 启停 `200` + 查库与审计逐条对上 + 浏览器走查两角色（员工**看不到**他人未审包 ⇒ 可见性收敛生效） | 用户 2026-09-20 四项裁决（契约已确认 / 对齐矩阵 / 员工入口同轮开放 / `bind` 本轮修）；契约 `docs/contracts/skills-mcp-api.md` §1/§6/§7/§8；后端契约真源 `docs/api-contract.md` 同步更正 | ✅ 已交付（自跑复核：后端/前端门禁全绿 + 真机取证 + 反假 5 组） |
| D-027 | 2026-09-20 | **第 8 轮口径限制（如实登记，不得读成已验）**：① `ceo` 的**真机**复核 / 启停走的是**开发模式头身份**（`X-User-Role: ceo`，本机**无真 `ceo` 账号**——造 ceo 需走注册 + 审批流程；**生产环境不允许**头身份）⇒ 「真 ceo 走登录令牌」路径未在真机验证（API 层用例覆盖同一份角色判定）；② 前端工具键下拉是**提示集合**（13 键取自 `app/tool_execution/catalog.py`），后端新增键时需前端同步，**该同步缺口未自动化**；③ 产物「示例数据」字样命中 1 处来自第 6 轮 `permissionsService.SAMPLE_DESCRIPTION`（未做 DEV 门控）——**既有缺口，D-023 已登记待整改，本轮未动该文件（`git diff` 空）** | 本轮真机与门禁实测；`skills-mcp-api.md` §6 | ✅ 已登记（待裁决；②③ 不属本模块范围） |

| D-028 | 2026-09-20 | **第 9 轮「技能 ↔ 数字员工绑定」已交付**（契约先行 → 四项裁决 → 真机复测 → 先红后绿 → 反假 → 门禁 → 走查）：① **后端新增读端点** `GET /api/v1/skills/bindings`（此前后端只有 `POST` / `DELETE`，界面无法列出绑定关系），形状 `{items[{skill_key,agent_key,status,created_by,created_at}],total,limit,offset}`（**不含 `tenant_id`**）、`skill_key`/`agent_key` 可选过滤、分页 1–200；② **安全加固**：`SkillService.list_bindings` 原先**零角色判定**（仓储只按租户过滤）⇒ 补 `ensure_can_bind`（服务层 + 路由双层 fail-closed），实测四个非超管角色一律 `403`；③ **同轮修复相邻缺口**：`GET /skills/agents/{agent_key}/tools` 原**只看登录**（实测四角色全放行，含 `customer_admin`）⇒ 新增 `AGENT_TOOLS_ROLES`（四个业务角色）+ `ensure_can_view_agent_tools`，`customer_admin` ⇒ `403`；④ **绑定口径不动**（矩阵 §3 未列该行，仍仅 `super_admin`）；⑤ 前端 `BindingPanel`（技能候选**仅 `enabled`** + 员工候选**目录 ∪ 手动录入** + 解绑**二次确认** + 工具面预览**三种空因分开**）、能力 `skill.bind`、`ceo` 视禁用 + 原因、员工视只给治理面说明且**不请求数据**；⑥ 门禁：后端 `2545 passed / 0 failed`（+18 用例）+ `compileall` 0；前端 `tsc` 0 / `vitest` **333** / `build` + 产物 grep（本模块样例键与 `console.log` = 0）；⑦ 反假四组；⑧ 真机 10 项 + 浏览器走查（4 张截图）—— 并为第 8 轮一处**备注误读**更正：`unbind` 不存在的绑定实测 **`404`**（原记"也返回 200、未区分"） | 用户 2026-09-20 四项范围裁决 + 工具面门禁第五问裁决；契约 `docs/contracts/skill-bindings-api.md`（v2 已交付）；矩阵补登记 `permission-matrix.md` §13；后端真源 `docs/api-contract.md` 同步更正 | ✅ 已交付（自跑复核：门禁全绿 + 反假四组 + 真机 10 项 + 走查） |
| D-029 | 2026-09-20 | **第 9 轮如实登记的三项"未解决/未验证"（不得读成已验）**：① **`bind` 服务端校验缺口仍在**——实测"技能不存在 / 未启用 / 员工键不在目录"三者**均 `200`**，可写入**悬空绑定**（查库确认）；用户裁决**本轮不改**（属业务逻辑改动，超出"读端点 + 界面"范围）⇒ 界面自我收敛（技能候选只给已启用、员工候选给目录）+ 契约 §2/§5 登记，**待专项**；② 未测并发 / 压测；跨租户绑定可见性真机未构造（仅源码判定按 `tenant_id` 过滤）；③ 目录面与绑定面**无联动逻辑**（员工 / 岗位停用不连带解绑），后端无该能力，"修复后行为"无从验证。**另：本轮顺带核实**第 8 轮走查遗留项——退出登录时浏览器报 `ERR_ABORTED`，但**后端日志实测 `POST /api/v1/auth/logout → 204 No Content`** ⇒ 服务端登出**确实完成**，该报错是客户端侧产物 | 本轮真机实测（`tmp/r9-bindings-probe.py` / `r9-bindings-verify.py`）+ 后端访问日志 | ✅ 已登记（待裁决） |

| D-030 | 2026-09-20 | **第 10 轮「审计日志」已交付**（契约先行 → 四项裁决 → 真机复测 10 项 → 先红后绿 → 反假 3 组 → 门禁 → 走查）：① **P0 缺口关闭**——矩阵 §3「审计：查询」的 `department_lead`（✅本租户）与 `employee`（⚠️**仅本人相关**）两档原本**没有实现**（一律 `403`），且 §7 早已登记为 P0；本轮按"实现向矩阵对齐"落地：`app/audit/models.py` 新增 `AUDIT_READ_ROLES` / `AUDIT_SELF_SCOPED_ROLES` / `resolve_actor_filter`，`403` 文案改「当前岗位不能查看审计日志」；② **仅本人相关由服务端强制**：`employee` 一律注入 `actor_id = 调用者`，**显式传他人 ⇒ `403`**（不静默忽略）——真机证据：同租户管理档 `total=140`（2 个 actor）vs `employee` `total=22`（actor 集合仅本人）；③ **新增动作目录端点** `GET /api/v1/audits/actions`（94 项码值，与查询同档）——避免前端复制枚举；**由契约覆盖守卫抓到**（未写进 `api-contract.md` ⇒ 用例红 ⇒ 补文档 ⇒ 复绿）；④ 前端 `features/auditLog/**`（分档两视图 + 四态 + 筛选 + 分页 + 详情抽屉）、能力 `audit.view` / `audit.scope.tenant`、导航 `audit-log` 放开四角色、`buildUrl` 支持数组 ⇒ 重复键；⑤ **走查抓到并当天修掉一条真缺口**：命中 140 条时**看不到总数**（原只在 >200 条时提示）⇒ 改为始终显示「共 N 条记录…」+ 回归用例 + 浏览器复验（「共 145 条记录，本页显示 50 条…」）；⑥ 门禁：后端 `2567 passed / 0 failed`（+22 用例）+ `compileall` 0；前端 `tsc` 0 / `vitest` **360** / `build` + 产物 grep；⑦ 反假三组（回退档位 ⇒ 11 红；去自限 ⇒ 4 红；前端忽略分档 ⇒ 2 红） | 用户 2026-09-20 四项裁决（分档按矩阵对齐 / 越权 `actor_id` ⇒ 403 / 新增动作目录端点 / 导出只登记）；契约 `docs/contracts/audit-log-api.md`（v2 已交付）；矩阵 §13→§14 交付记录 + §7 缺口行关闭；后端真源 `docs/api-contract.md` 同步更正 | ✅ 已交付（自跑复核：门禁全绿 + 反假三组 + 真机 10 项 + 两档走查） |
| D-031 | 2026-09-20 | **第 10 轮如实登记的三项"未解决/未验证"（不得读成已验）**：① **审计导出仍是零实现**（矩阵 §3 给 `super_admin` ✅）⇒ 用户裁决**本轮只登记**，界面不给假入口；② **跨租户不可见性真机未构造**——"伪造 `X-Tenant-Id`"实测无效（带令牌时该头被忽略、仍返回本租户数据，即"租户由服务端注入"的正确行为）⇒ 需要第二个租户账号才能真正验证（现状依据：仓储按 `tenant_id` 过滤 + 单测）；③ **员工档"仅本人相关"的口径边界**：当前只按 `actor_id = 自己`（本人**发起**的操作），**不含"别人对我账号做的操作"**（如管理员改我手机号）；若要覆盖需 `actor_id == 自己 OR target_id == 自己`（仓储当前只支持 AND 组合）⇒ **属口径选择，待裁决**。另：**上一轮遗留项已核实**——退出登录时浏览器报 `ERR_ABORTED`，但后端日志实测 `POST /api/v1/auth/logout → 204`，服务端登出确实完成 | 本轮真机复测与走查（`tmp/r10-audit-probe.py` / `r10-audit-verify.py`）；契约 `audit-log-api.md` §6「仍未验证」 | ✅ 已登记（待裁决） |

| D-032 | 2026-09-20 | **第 11 轮「bind 校验缺口专项」已交付（域内两条；第 ③ 条暂缓）**：契约先行 `docs/contracts/skill-binding-gate-plan.md`（v2 已交付）→ 真机幂等复核（**零新增脏数据**）→ 先红后绿 → 反假两组 → 门禁。① **闸门落地**（`SkillService.bind_skill`，判定序 `ensure_can_bind` → 技能存在 → 技能已启用）：新增 `SkillStore.list_versions`（`Protocol` + 内存 + Postgres 三实现，**刻意不做可见性过滤**，与 `_get_raw` 同口径）；无任何版本 ⇒ `SkillNotFound`（**`404`**）；有版本但无 `enabled` 版本 ⇒ `SkillStateConflict`（**`409`**「只有已启用的技能包可以绑定」）；**闸门刻意放在幂等判定之前**（否则"已存在的绑定行"会成为绕过闸门的后门）⇒ **行为变化**：技能停用后重复 `bind` 由 `200` 变 `409`；被拒请求**不写审计**（与既有 4xx 一致）。② **第 ③ 条（员工键必须在目录）本轮暂缓**——与第 9 轮已确认裁决「员工键 = 目录候选 ∪ **手动录入**」直接冲突（加上它 ⇒ 手动录入必然被拒），**显式留在用户手上**（`skill-binding-gate-plan.md` §10-#1），不静默改。③ **测试**：新增 `tests/test_skill_binding_gates.py`（13 例）+ `tests/test_skill_binding_repair_script.py`（9 例）；第 9 轮两条 `known_gap` 用例按预告翻转（`test_bind_rejects_nonexistent_skill` / `..._not_enabled_skill`），第三条保留为暂缓事实钉子。④ **反假 2 组**：去存在性闸门 ⇒ 3 红；去已启用闸门 ⇒ 6 红；均还原复绿。⑤ **修复脚本产出**（默认干跑 / `--apply` 才写 / **只 `UPDATE SET status='disabled'`、无 `DELETE`** / 必须 `--tenant` / DSN 只从环境变量取）：`scripts/repair_dangling_skill_bindings.py`——**是否执行由用户裁决**。⑥ 门禁：后端全量 `pytest`（`--junit-xml` 实测）**`tests = 2730 / failures = 0 / errors = 0 / skipped = 140`** ⇒ **2590 passed**，退出码 0；`compileall app scripts` 退出码 0；该三个新/改用例文件单跑 **40 passed**。前端本轮 **`git diff` 为空**（`tsc` 0 / `vitest 360` / `build` 成功为同树复跑）。⑦ `docs/api-contract.md` 绑定节同步更正（由"三条全缺"改为"域内两条已补 + 第 ③ 条仍缺"）。⑧ **真机（修复后）**核对（`tmp/r11-bind-gate-verify.py`，实例 `127.0.0.1:18113` + 真库 `workbench_test`）：技能不存在 ⇒ **`404`**、技能未启用 ⇒ **`409`**、员工键不在目录 ⇒ **仍 `200`**（第 ③ 条暂缓）、`employee` 调 `bind`（含不存在的技能）⇒ **`403`**（越权优先、不泄露存在性）、`unbind` 已 `disabled` 行 ⇒ **`200`** 幂等、**绑定行数 `5 → 5`**（零新增）、被拒请求**未新增审计行**。**两条如实观察**（非本轮引入）：ⓐ 长驻的 `18112` 进程是 02:18 启动的、加载**闸门前**代码 ⇒ 复用它做真机核对会得到"闸门无效"的假象（本轮实测踩到）；ⓑ **幂等路径也写审计**——重复 `bind` 同一已 `active` 组合每次都落一条 `skill.enabled`（第 8/9 轮既有行为，会放大审计噪声，是否收敛**待裁决**）。 | 用户指令「继续下一轮：bind 校验缺口专项」；用户**跳过了本轮四项追问** ⇒ 我按"只推进与既有裁决不冲突的部分、其余显式留待定夺"处理；证据：`tmp/r11-bind-gate-probe.py`（修复前）+ `tmp/r11-bind-gate-verify.py`（修复后） | ✅ 已交付（自跑复核：先红后绿 + 反假两组 + 后端门禁全绿 + 真机修复前后对比） |
| D-033 | 2026-09-20 | **第 11 轮如实登记的四项"未解决 / 未验证"（不得读成已验）**：① **第 ③ 条闸门未落地**：绑不在目录的 `agent_key` **仍 `200`**（真机实测）——待裁决三条路：a) 加闸门并取消手动录入；b) 加闸门但保留手动录入（等于自动纳管新键，需定义"纳管"动作归属）；c) 维持现状只靠界面收敛；② **行为变化待确认**：技能停用后重复 `bind` `200 ⇒ 409`（含历史合规行）；若要"幂等优先"，需改为**先判幂等再判闸门**（代价：停用技能后仍能"成功"重复绑定）；③ **真库 2 行 `active` 违规行未修**（`probe-agent ↔ r9-ghost-skill`（技能**不存在**）、`probe-agent ↔ r8-self-probe`（`submitted`））——脚本已就绪，**执行权在用户**；另 2 行 `disabled` 违规行（员工不在目录）**不生效**、只报告不动；④ **未验证**：并发 / 压测未测；**跨租户**（另一租户的技能键）真机未构造（仅源码判定按 `tenant_id` 过滤）；`list_versions` 的"不过滤可见性"口径在多租户真机上未构造反例。**本轮真库零新增**（复核全部只读 / 幂等，打的是已存在的组合）；⑤ **观察待裁决**：重复 `bind` 同一个已 `active` 的组合**每次都写一条 `skill.enabled` 审计**（第 8/9 轮既有行为）⇒ 审计噪声，是否收敛由你定夺 | 本轮真机复核 + 门禁实测；`skill-binding-gate-plan.md` §10 | ✅ 已登记（待裁决） |

| D-034 | 2026-09-21 | **第 10 轮审计界面「实机走查演示」执行完毕（用户指令「执行第 10 轮审计日志界面」→ 选"实机走查演示审计页"）+ 走查抓到并当天修掉 1 个真缺陷**。① **拓扑**：新起后端 `127.0.0.1:18113`（**含第 10/11 轮代码**，连真库 `workbench_test`）+ 前端 dev `127.0.0.1:5273`（`VITE_DEV_PROXY_TARGET` 指向它）；**必须同时设 `VITE_WORKBENCH_API_MODE=http`** —— 开发模式前端默认 `mock`（`resolveServiceMode()`），不设会看到「示例数据（未接后端）」横幅（**这恰证明 sample/真实标识可信**，非缺陷）。② **管理员档实测**（`13600000001`）：导航 **8 项**；「已接入真实数据」+「审计导出尚未接入…不提供导出按钮」；真实记录（`account.login.succeeded` / `skill.*` / `memory.fact.created`）；**共 163 条**（两次测量 161 → 163，随期间 2 次登录增长 ⇒ 真数据非缓存）；分页 1–4 页、点回第 1 页首行恢复最新（`offset` 生效）；`目标类型=skill` ⇒ **共 35 条且 35 行动作全为 `skill.*`**；`操作人=他人`（`acct-36168b02df24`）⇒ 3 条且 actor 唯一（**管理档可按他人筛**）；「重置」回全量；详情抽屉字段齐、无明细如实显示「该记录没有明细。」③ **员工档实测**（`13600000002`）：导航 **6 项**；页面说明「查看你自己的操作记录…不能查看他人记录」；**筛选表单无「操作人」**（界面不给扩大范围的口子，自限在服务端）；**共 27 条且操作人集合仅本人**。④ **走查发现并修**：`SELF_SCOPE_NOTE` 原文写作 `**你自己**`，渲染处是**纯文本**（非 Markdown）⇒ 界面把星号原样显示成「此处只显示\*\*你自己\*\*的操作记录」；改为「」标注（**先红**：把常量改回缺陷值 ⇒ **3 红**；**后绿**：25 passed）。加固：模块级文案 sweep 用例（导出文案不得含 `**` / `__`）+ 员工档**渲染后**文本断言（防"断言常量"式的自洽漏检）。⑤ **门禁**：`tsc` 0；`vitest` **45 files / 362 passed**（审计模块 25）；`build` 成功；产物 grep：`console.log` **0**、`**你自己**` **0**、「示例数据」1 处（第 6 轮既有）、「样例数据」3 处（既有页面横幅）。⑥ **截图 10 张**入库 `docs/screenshots/ui-v2-r10-walkthrough/`（第 8 张 `08-employee-note-fixed.png` 为**修复后的浏览器复验**：员工档文案已无星号、渲染为「此处只显示「你自己」的操作记录…」，同一实例、当次共 28 条；第 9/10 张 `09-actions-multiselect-tags.png` / `10-actions-filter-hit-total.png` 为**动作多选**的真机证据：两项已挂标签 + 查询后共 96 条）。⑦ **未验证（如实登记）**：跨租户与审计导出仍同 D-031 登记。**⑧ 追加复核（2026-09-21 同轮）：撤回"动作多选点不开"的初判——那是自动化手法问题，不是产品缺陷**。真机已打通全链路：`scrollIntoView` 后点 `<label for="actions">`（`→` 焦点落到 `INPUT#actions`，`aria-activedescendant` 随 `↓` 变化）⇒ `↓` + `Enter` 选中 **两项**（`account.login.locked` + `account.login.succeeded`）⇒ 点「查询」⇒ **共 96 条**且当页动作仅 `account.login.succeeded` ⇒ **重复键 `action=…&action=…` 服务端生效**。控件的 DOM 事实：`disabled=false`、`tabIndex=0`、`id=actions`、`role=combobox`、`.ant-select-selector` 的 `pointer-events: auto`、`label[for=actions]` 关联齐备（对真人与屏幕阅读器均可用；jsdom 用例 `getByLabelText('动作')` 同样打通）。**本轮未改任何产品代码**（改的只是文档与截图）。手法见文末「浏览器走查手法（AntD Select）」。 | 用户 2026-09-21 指令（实机走查演示）+ 裁决（修 + 补用例 + 登记并提交）；证据：`docs/screenshots/ui-v2-r10-walkthrough/`（8 张）+ 本轮真机取证 | ✅ 已交付（自跑复核：先红后绿 + 前端门禁全绿 + 产物 grep 干净） |

| D-035 | 2026-09-21 | **第 12 轮「bind 校验缺口专项（第二批）」已交付**（契约先行 `skill-binding-gate-plan.md` v3→v4；用户按 §11.4 裁决 **③A / ④A / ⑥A / ⑦维持**）：① **③ 员工必须已纳管且启用**——**路由层**复用目录面 `ensure_agent_binding_available` ⇒ `409`「该标识尚未纳入目录，请先在「数字员工设置」中纳管」（**不落技能服务层**，避免反向依赖目录面）；**界面同轮取消"手动录入"**（员工 Select 去掉 `mode="tags"`，只给目录候选，目录为空给"先去「数字员工管理」纳管"引导）；② **④ 键归一化 + 字符集校验**——`bind`/`unbind` 复用目录域同一份 `normalize_key`（去空白 + 小写 + 字符集）⇒ 非法 `422`；`skill_key` 不动（技能域口径）；**解绑同样归一化但不设目录闸门**（清理通道必须保留）；③ **⑥ 幂等调用不重复写审计**——`bind`/`unbind` 先读既有绑定状态（`_binding_of`），仅"新建 / 状态翻转"时写（真机实测原先同一 target 最多重复 **8 条** `skill.enabled`）；④ **⑦ 维持**：闸门先于幂等（技能停用后重复 `bind` 仍 `409`）；⑤ **判定顺序**：`403` → `422` → `404` → `409` → `409`（越权者永远先拿 `403`，不泄露任何存在性）；⑥ **矩阵零改动**（`BIND_ROLES` 仍仅 `super_admin`，§3 未列该行 ⇒ 本轮只改校验与界面，不改角色口径）；⑦ **测试**：先红 **9 条** → 后绿；`test_skill_binding_gates.py` 扩到 24 例；第 11 轮两条"钉住暂缓"的用例**翻转为断言被拒**；三个测试文件加 `managed_directory` 夹具（先把用到的员工键纳管）；⑧ **反假 4 组**（去目录闸门 ⇒ 4 红 / 去归一化 ⇒ 3 红 / 去审计收敛 ⇒ 1 红 + 2 红 / 前端回退 `mode="tags"` ⇒ 3 红）；⑨ 门禁：后端全量 `tests = 2740 / failures = 0 / errors = 0 / skipped = 140`（2600 passed）+ `compileall` 0；前端 `tsc` 0 / `vitest` **362** / `build` 成功 + 产物 grep（`console.log` 0、旧"手动录入"文案 0）；⑩ **真机 6 项**（未纳管 `409` 且行数 `5 → 5` / 纳管后 `"  PROBE-AGENT-R12  "` ⇒ `200` 且回读规范键、行数 `5 → 6` / 连续两次重复 `bind` ⇒ 审计 `1 → 1` / 非法键 `422` / `employee` 传非法键与未纳管键均 `403`） | 用户 2026-09-21 指令「继续下一轮：bind 校验缺口专项」+ 四项裁决；契约 `skill-binding-gate-plan.md` §11（v4 已交付）；真源 `docs/api-contract.md` 绑定节同步更正；`skill-bindings-api.md`（第 9 轮绑定面契约）§2/§3/§4/§5/§6 同步标注"已收口"；矩阵 §16 交付记录 | ✅ 已交付（自跑复核：先红后绿 + 反假 4 组 + 两端门禁全绿 + 真机 6 项） |
| D-036 | 2026-09-21 | **第 12 轮如实登记的四项"未解决 / 未验证 / 副作用"（不得读成已验）**：① **目录面与绑定面仍无联动**——停用员工 / 停用岗位**不会**自动解绑（后端无该逻辑），本轮真机**未构造**该场景（与 D-029 同一登记）；② **历史非规范键行的解绑**未验证——若存在含大写 / 空白的旧绑定行（本轮真机实测 **0 行**），API 按规范键解绑会打不中（`404`），需修复脚本兜底；**③ 真机写入面（本轮为验证闸门而产生；用户 2026-09-21 裁决：保留）**：`workbench_digital_employees` 新增 **1 行** `probe-agent-r12`（挂既有岗位 `evidence-ops`，`active`）、`workbench_skill_bindings` 新增 **1 行** `probe-agent-r12 ↔ r8-probe-skill`（`active`）——**这是"加目录闸门后绑定功能在本机可用"的最小前置**（目录原本 0 员工），**保留**；若日后清掉，本机绑定会重新不可用；④ **观察（本轮发现，未改，待裁决）**：`permissions`（知识范围）模块的候选仍是"现有绑定并集 + **手动录入**"，而它的写路径 `PUT /api/v1/knowledge-access/agents/{agent_key}` **早已有目录闸门**（第 6 轮）⇒ 那里存在**同类"能填、必拒"的假入口**；本轮**未动该模块**（超出本专项范围），登记待办。**⚠️ ④ 定性更正（2026-09-21 第 15 轮，见 D-040）**：「**能填、必拒**」这个措辞**不准**——手输**不会被拒**：写路径 `app/knowledge_policy.py::_normalize` **只 `strip()`、不校验存在性**，`PUT` 会正常 `200` 并回读一致（第 15 轮真机复核过该事实）⇒ 实为「**能填、无人校验、写错静默无效**」（写进去也配错，检索时静默无结果，操作者从界面上看不出）。第 15 轮以 **B2′** 消解（上游只读清单当候选真源 + `binding_only` 显形 + 不阻断黄色提醒）。另：错误码文案里写的是「数字员工**设置**」，而界面导航项叫「数字员工**管理**」——**既有文案（3 处复用），本轮未改**，登记为文案一致性问题；**⑤ 界面改动未做浏览器走查**——"取消手动录入 + 目录为空的引导"只由**前端用例**（`BindingPanel.test.tsx`：自由输入不产生值 + 必填校验拦下 + 引导文案）覆盖，**未在本轮真机浏览器里点过**（本轮真机只走 HTTP 接口；如需截图证据，下轮可补一次走查） | 本轮真机复核 + 门禁实测；`skill-binding-gate-plan.md` §11.6 | ✅ 已登记（待裁决） |

| D-037 | 2026-09-21 | **第 13 轮「审计导出」已交付**（契约先行 `docs/contracts/audit-export-plan.md` v1→v2；用户四项裁决 **A/A/A/A**：只做导出 / CSV 默认 + 可选 JSON / 超限 `422` 拒绝 / 留审计）：① **关闭 §7 最后一条审计面缺口**——矩阵 §3「审计：导出」行本就标 `super_admin` ✅，但后端**零实现**（D-031① 登记）⇒ 本轮落地 `GET /api/v1/audits/export`，**角色口径零改动**（新增 `AUDIT_EXPORT_ROLES = {super_admin}`，与矩阵本行逐字一致；`ceo` / `department_lead` 属"能查不能导"，一律 `403`）；② **筛选与列表端点同名同义**（`action` 可重复 / `target_type` / `target_id` / `actor_id` / `since` / `until`），另加 `format`（`csv` 默认｜`json`）与 `limit`（默认 `1000` / 上限 `5000`）；③ **超限拒绝而非静默截断**——命中数 > `limit` ⇒ **`422`**（**绝不返回"看起来完整、实际被截断"的文件**）；④ **导出本身留痕**——每次成功落一条 `audit.exported`（新增动作码，`ALLOWED_DETAIL_KEYS` 同步加 `format` / `rows` / `filters`）；明细**只记形式与计数 + 筛选条件字符串，不落被导出的记录内容**；**被拒请求不写审计**；⑤ **判定顺序**沿用安全要件 `403` → `422`（真机与用例钉住"`ceo` 同时传非法 `format` ⇒ 仍 `403`"，不靠错误码探测参数面）；⑥ **掩码列原样保留** ⇒ 导出文件**不含明文手机号**（真机 grep 核验）；⑦ **前端**：能力 `audit.export`（仅 `super_admin`）+ 审计页「导出」按钮；无权限者**不渲染按钮、改给说明**（不做"隐藏当按钮控制"）；下载新抽 `requestRaw` + `Blob`（与 `request<T>` 共用同一 `performRequest` 实现，不复制令牌注入 / 超时 / 错误映射）；样例（`mock`）模式**不导出**并如实提示；⑧ **测试**：`tests/test_audit_export_api.py` **15 例** + `test_audit_models.py` 动作码 `94 → 95`；**先红 → 后绿**；**反假 3 组**（降级权限档位 ⇒ **6 红** / 去掉 `total > limit` 拒绝 ⇒ **2 红** / 去掉 `audit.exported` 留痕 ⇒ **2 红**）；⑨ 门禁：后端全量 `tests = 2758 / failures = 0 / errors = 0 / skipped = 140`（**2618 passed**）+ `compileall` 0；前端 `tsc` 0 / `vitest` **371 passed**（审计模块 34）/ `build` 成功 + 产物 grep（`console.log` 0、旧"导出尚未接入"文案 0）；⑩ **真机 6 项**（CSV 形状 + BOM + `X-Exported-Rows` == 列表 `total`（103）/ JSON `total` 一致 / 无明文手机号 / `employee` `403` / 超限 `422` / 每次成功导出留痕 +1（实测 `3 → 4`））；⑪ **本轮额外收益（守卫抓到真缺口）**：`tests/test_frontend_audit_labels.py` 在门禁中**报红**——`admin-web/src/features/auditLog/types.ts` 的 `AUDIT_ACTION_LABELS` 缺 `audit.exported`（前端会回落显示动作码）⇒ 补标签 ⇒ 复绿（**该守卫按"前后端动作码集合相等"断言，新动作码必须同步标签，本轮实测有效**） | 用户 2026-09-21 指令「执行第 13 轮审计日志界面」+ 四项裁决；契约 `docs/contracts/audit-export-plan.md`（v2 已交付）；矩阵 §14④ / §7 同步标注"已收口" + 新增 §17 交付记录；`docs/api-contract.md` 新增该端点节；`docs/contracts/audit-log-api.md` 五处"已知限制 / 不做"改为"已收口" | ✅ 已交付（自跑复核：先红后绿 + 反假 3 组 + 两端门禁全绿 + 真机 6 项） |
| D-038 | 2026-09-21 | **第 13 轮如实登记的三项"边界 / 未验证 / 副作用"（不得读成已验）**：① **异步导出未做**——本轮是**同步生成**（请求内查库 + 拼文件，一次最多 `5000` 行）；超大数据集需异步任务 + 对象存储交付，**本轮契约 §6 已列为非目标**；② **真机未构造的验证面**：**跨租户**导出的不可见性仍与 D-031② 同一处境（需要第二个租户账号才能真验；现状依据＝仓储按 `tenant_id` 过滤 + 单测）；**并发 / 压测**未做；**`limit` 与 `total` 恰好相等**的边界只在单测覆盖、未真机构造；③ **本轮真机副作用（功能本身留痕，非脏数据）**：为验证 6 项跑了多轮真实导出 ⇒ 真库新增若干 `audit.exported` 行（实测 `3 → 4` 一轮），**属"导出必留痕"这一设计本身的产物，不建议删**（删审计即破坏审计面不可变性）；如需彻底清零只能由你在服务器按"保留策略"处置。另：**`admin-web` 另有一份审计导出原型**（用环境变量注入身份、生产禁用）——与本轮 `workbench-web` 的真实端点是**两套东西**，本轮**未动 `admin-web` 的任何原型逻辑**（仅同步了 `AUDIT_ACTION_LABELS` 标签表，因守卫要求前后端动作码集合相等） | 本轮真机取证 + 门禁实测；`audit-export-plan.md` §2「非目标」/ §6「边界」/ §9 交付记录 | ✅ 已登记（待裁决） |

| D-039 | 2026-09-21 | **第 14 轮「审计页浏览器走查」未执行 ⇒ 用户裁决按"未验证"登记（如实登记，不得读成已验）**：① **起因**——第 13 轮审计导出的真机核对**全部在 HTTP 层**（直接打 `127.0.0.1:18113`），**「导出」按钮从未在浏览器里被点过**；用户指令「继续审计日志界面」⇒ 经裁决选定方向为**实机走查审计页**（超管 + 员工两档）。② **环境已就绪**：后端 `127.0.0.1:18113`（含第 13 轮代码）、前端 dev `127.0.0.1:5273`（`VITE_WORKBENCH_API_MODE=http` ⇒ 走代理打真实后端、真数据），走查清单与截图归档目录（`docs/screenshots/ui-v2-r14-walkthrough/`）均已备好。③ **阻断点**：浏览器实测停在**登录页**（「账号（手机号）」+「密码」）⇒ 走查需要登录口令；口令**不在仓库、无默认口令可试**（这是**正确的安全设计**，不是缺陷），我**无法自取**（不猜口令、不用任何默认口令登录）。④ **用户裁决**：先选「我现在提供口令」但未给出文本 ⇒ 随后明确裁决「**跳过界面走查，把导出按钮登记为未验证**」。⑤ **登记结果（四处一致）**：`audit-export-plan.md` §9 新增「⚠️ 界面层未验证」段、`audit-log-api.md` §6「仍未验证」新增末条 + §8 变更留痕、`permission-matrix.md` §17⑤ 前端项补注、本条 D-039。⑥ **登记内容（四项无实机证据、仅 jsdom 用例覆盖）**：点「导出」后**下载是否真的落盘**（`triggerDownload` 在 jsdom 下静默跳过 ⇒ 浏览器行为未验）；**成功提示的条数是否确实取自响应头 `X-Exported-Rows`**；**非超管三档是否确实"无按钮、有说明"**；**导出失败（超限 `422`）的提示是否可读、是否如实说明"本次没有生成任何文件"**。⑦ **补齐所需的最小动作（留档）**：用超管账号在审计页点一次「导出」，核对**下载文件名 / 提示条数 / 列表 `total` 三者一致**。⑧ **本轮无代码改动、无真机写入、无截图**（走查未执行到任何一步） | 用户 2026-09-21 指令「继续审计日志界面」→ 方向裁决「实机走查审计页」→ 走查受阻 → 裁决「跳过界面走查，登记为未验证」；证据：browser 子代理实测停在登录页（本轮阻断点） | ✅ 已登记（未验证，待日后补齐） |

| D-040 | 2026-09-21 | **第 15 轮「permissions 假入口专项（消解 D-036④）」已交付**（契约先行 `docs/contracts/permissions-fake-entry-plan.md` v1→v2→v2.1；用户裁决：处理该待办 → 「你来弄」授权自行推进）：① **定性更正**——D-036④ 原措辞「**能填、必拒**」不准，实为「**能填、无人校验、写错静默无效**」（`_normalize` 只 `strip()`、不校验存在性；`PUT` 正常 `200` 且回读一致；写错后检索静默无结果、界面看不出）；② **上游能力确证（无凭据可自取时的最强证据）**——本机无上游实例/凭据，改读**上游（腾讯 WeKnora）公开官方文档**，确证 `GET /api/v1/knowledge-bases`（"返回当前空间拥有的全部知识库"）存在；**未实调**如实登记；③ **裁决落地 B2′**——新增**只读**代理端点 `GET /api/v1/knowledge/bases`：判定**复用**知识范围管理闸门（`ceo` + `super_admin`，**不新造口径**）、`403` 在**任何上游动作之前**判定、响应四键 `{upstream_available, source, items[{knowledge_base_id,name,origin}], note}`；**降级非协商**：上游不可用 ⇒ `upstream_available=false` + `source="local_only"` + `items`=本租户绑定并集（`origin="binding_only"`）+ `note` 写原因（未配置服务 / 超时 / 服务暂不可用 / 返回内容无法识别），**绝不返回空数组冒充"没有知识库"**；**只发 GET**、响应/日志不含 API Key；④ **多键兼容解析**（因未实调）：ID 键 `id|knowledge_base_id|kb_id`、名称键 `name|title`；形状异常（`data` 非数组 / 元素非对象 / 缺标识）一律 `UpstreamShapeError` ⇒ 降级；⑤ **前端**：候选来源改新端点（**打开抽屉才请求**，`enabled: target !== null`）+ 四态来源 Alert；`collectCandidates` 删除，改 `unknownCandidateIds()`——**只把 `origin="upstream"` 当"清单里真有"**，其余给**不阻断的黄色提醒**「以下标识未出现在知识库清单中：…」；**手输必须保留**（若取消则从未绑定过的租户永远无法完成第一次绑定 ⇒ **死锁**——这是对第 12 轮"取消手动录入"的**显式否决**）；`listKnowledgeBases()` 形状校验含"降级却不带 `note` 也抛错"（防被读成"没有知识库"）；⑥ **测试**：新增 `tests/test_knowledge_bases_api.py` **25 例**（正常 3 / 多键兼容 4+1 / 降级 7+1 / 权限 2+3+401+跨租户 1 / 安全 2）；前端 `permissionsService` 4 例 + `ScopeDrawer` 重写 8 例；⑦ **反假 2 组**（均已还原）：降级改成**返回空列表** ⇒ **8 红**（7 个降级 parametrize + 无绑定降级）；去掉 `binding_only` 合并 ⇒ **1 红**；⑧ **门禁**：后端全量 `pytest` **exit 0**（`--collect-only` = **2783** = 上轮 2758 + 25）、`compileall` OK；前端 `typecheck` 0 / `vitest` **45 files / 378 passed** / `build` 成功 + 产物 grep（`MOCK_KNOWLEDGE_BASES`、`kb-legacy-sample`、样例横幅、`kb-00000001`）= **0 命中**；⑨ **真机 30/30**（`tmp/r15-bases-verify.py`，两个模式各起真实 uvicorn + 真实 httpx；情形 A 未配置上游 13/13：降级四要素齐 + `items` 非空 + `ceo`/`200`、员工 `403`、匿名 `401`、跨租户不串；情形 B 本机**桩上游**按文档形状 17/17：清单 ∪ 绑定两类 `origin` 齐 + 多键解析在真实 HTTP 上生效 + 桩侧**恰好一次 `GET` 且带 `X-API-Key`** + 越权 `403` 时**零上游调用**；情形 C `500` / D 形状异常 ⇒ 均降级且 `note` 写对原因）；⑩ **测试工具链教训沉淀**（`user-event` v14 **不写 `event.which`**，而 `rc-select` 按键处理依赖 `which` ⇒ 单测里 `{enter}` 恒无效；必须用 `userEvent.tab()` 走 blur 提交路径——已写进 `permissions-api.md` §9.1 与用例注释）；⑪ **文档回写**：`docs/api-contract.md` 新增该节、`permissions-fake-entry-plan.md` §12、`permissions-api.md` §4/§5/§6/§7/§8/§9.1、`decision-log.md` D-036④ 定性更正 + 本条 | 用户 2026-09-21 指令「开始处理 permissions 模块的假入口登记」+ 裁决「你来弄」；契约 `permissions-fake-entry-plan.md`（v2.1）；`app/knowledge.py` / `app/main.py` / 前端 `features/permissions/**` / `docs/api-contract.md`；**未改** `app/knowledge_policy.py`、上游配置状态、知识分级与检索链路、`skills` 模块 | ✅ 已交付（待用户确认提交） |

| D-041 | 2026-09-21 | **第 15 轮如实登记的四项"未验证 / 边界"（不得读成已验）**：① **上游 `GET /api/v1/knowledge-bases` 仍未实调**——本机无可用上游实例/凭据，官方文档形状由**本机桩上游**模拟；真实 ID 键名与名称字段待用只读实例升级为"已实调"（补验步骤见 `permissions-fake-entry-plan.md` §11）；② **真库（postgres）路径未验证**——真机核对走**内存仓储**；`PostgresKnowledgeAccessRegistry` 现有用例（`tests/test_knowledge_policy.py`、`tests/test_workforce_roster_store.py`）用的是**假连接 + SQL 形状断言**，**不连真库**；本轮**未新增**真库用例 ⇒ `list_bindings` 的真实 SQL 行为**无证据**；③ **并发 / 负载未测**——超时分支仅在单测用假上游触发，真实 `WORKBENCH_WEKNORA_TIMEOUT_SECONDS` 下的超时降级未在真机压力下验；④ ~~**界面渲染层未走查**~~ ⇒ **已于 2026-09-21 补走查**（`super_admin` 档 + 真实 HTTP，见 D-042）——已核对来源 Alert 两种口径 / 黄色提醒不阻断 / 员工侧无「权限配置」入口 / 保存成功回读；**仍未走查**的是 ~~`loading` / `error` 两态~~ ⇒ **已于同日补走查（见 D-044）**、~~保存失败（`403`/`409`/`422`/网络）的界面表现~~ ⇒ **已于同日补走查（见 D-045）**。~~`ceo` 档~~ ⇒ **已于同日补走查（见 D-043）**（详见 `permissions-api.md` §9.3）。另：**假入口问题本轮只做到"可见化"而非"阻断"**——平台仍**不校验标识存在性**（写错仍 `200`），黄色提醒是**提示而非拦截**，这是刻意选择（不阻断 + 保留手输，避免死锁与误伤）；若要升级为硬校验，需另立专项（涉及上游可达性与离线场景取舍）。 | 本轮真机与门禁实测；`permissions-fake-entry-plan.md` §12「仍未验证」、`permissions-api.md` §6 | ✅ 已登记（待裁决） |

| D-042 | 2026-09-21 | **第 15 轮「permissions 界面走查」补证（消解 D-039 同类阻断与 D-041④）**：① **起因**——用户指令「补界面走查，登录口令是 admin/123456」；此前 D-039（审计页）与 D-041④ 均因"无口令可自取"把界面层登记为未验证。② **口令定性（如实登记）**——`admin`/`123456` **不可能**由平台注册/改密路径产生（`app/accounts/passwords.py` 拒 6 位纯数字），故走查账号由一次性脚本 `tmp/r15-walk-api.py` **直插 scrypt 哈希**（`verify_password` 可校验），**仅存在于本机内存仓储**；`WORKBENCH_REQUIRE_ADMIN_TOTP=false` 只为让该账号取得 full scope（默认 True 时超管登录返回 `scope=totp_enrollment`，业务端点不可用）。③ **环境**——真实 `uvicorn`（18117，内存仓储）+ vite dev（`http://localhost:5199/`，`VITE_WORKBENCH_API_MODE=http`）；**两个模式**：不配上游（降级态）与**本机桩上游**（18118，按上游官方文档形状，含 `id/name` 与 `knowledge_base_id/title` 双键）。④ **取证结果（逐字原文 + 12 张截图，入 `docs/screenshots/ui-v2-r15-permissions/`）**——横幅「已接入真实数据」；降级态 Alert「未能获取知识库清单（未配置知识库服务）…」；清单可用态 Alert「候选来自知识库清单…」+ 候选**带名称**；手输清单外标识 ⇒ 黄色 Alert「以下标识未出现在知识库清单中：kb-not-in-list」且**保存按钮仍可用（不阻断）**；保存成功「已保存：知识范围已更新。」+ 回读 3 个标识；员工侧导航 7 项**无「权限配置」**。⑤ **两处走查者误报已更正（关键）**——把"降级态无黄色提醒"误记为缺陷（实为**设计口径**：无清单即不作"不在清单中"判断，与"降级不谎报"同源）、把员工导航误记为「入口存在但禁用」（实为**入口不存在**，经复核截图 + 第二轮逐字列举更正）；**均非代码缺陷**。⑥ **文档回写（四处一致）**——`permissions-api.md` §6③ + §7 + 新增 **§9.2**、`permissions-fake-entry-plan.md` §12④ + 新增 **§13**、`permission-matrix.md` §18⑤、本条 D-042 + D-041④ 标注已走查。⑦ **仍未验证（不得读成已验）**——~~`loading` / `error` 两态~~ ⇒ **同日已补走查（见 D-044）**、~~保存失败（`403`/`409`/`422`/网络）的界面表现~~ ⇒ **同日已补走查（见 D-045）**，两态此前**未在浏览器里造过**；~~`ceo` 档~~ ⇒ **同日已补走查（见 D-043）**。⑧ **本轮无产品代码改动**（走查只读；仅新增一次性走查脚本 + 截图归档） | 用户 2026-09-21 指令「补界面走查，登录口令是 admin/123456」；证据：`tmp/r15-walk-api.py`（走查后端）+ 12 张截图 + 子代理 `b91efeb9` 两轮走查记录 | ✅ 已交付（待用户确认提交；含 `tmp/r15-walk-api.py` 清理申报） |

| D-043 | 2026-09-21 | **`ceo` 档走查（补 D-041④ / D-042⑦ 的最后一处"未走查档位"）已交付**：① **起因**——用户指令「先补 ceo 档的走查」；此前 §9.2/§13 只走了 `super_admin` 档。② **走查目标由矩阵口径倒推**（矩阵是唯一权威，**不新造口径**）：§3「知识：授权绑定」`ceo` = **⚠️ 读可、写待目录放开**（脚注 2，用户 2026-09-19 裁决「维持现状 + 改矩阵标注」）⇒ 要验的是"**读放行 / 写被目录闸门拦**"以及**界面是否与之一致**。③ **环境**——一次性脚本 `tmp/r16-walk-api.py`（真实 `uvicorn` `127.0.0.1:18127` + 内存仓储 + 桩上游 `127.0.0.1:18128`；`WORKBENCH_REQUIRE_ADMIN_TOTP=false`）+ vite dev `http://localhost:5199/`；账号 `ceo-walk`/`123456`（角色 `ceo`，口令**直插 scrypt 哈希**、**仅本机内存**）。④ **HTTP 层实测（双向钉住脚注 2）**——`ceo` 登录 `200`（`scope=full`、`role=ceo`）；`GET /api/v1/knowledge/bases` **`200`**、`GET /api/v1/knowledge-access/roles/content-operator` **`200`** ⇒ **读可**；`PUT` 同一路径 **`403`** `{"detail":"只有超级管理员可以管理岗位与数字员工目录"}` ⇒ **倒在第二步目录闸门**，**写待目录放开**成立；员工 `GET /bases` `403`；`super_admin` `PUT` 同路径 `200`（**对照**，证明写路径本身可用 ⇒ `ceo` 的 403 是角色所致）。⑤ **界面层实测**——顶栏角色显示「**CEO**」；左侧导航逐字 7 项（我的工作台 / 我的数字员工 / 知识库 / Skill & MCP / 团队协作 / 审计日志 / 组件样品）**无「权限配置」**（亦无「数字员工管理」，两者同为 `ADMIN_ONLY`）；「我的工作台」快捷入口处「权限配置」为**禁用态** + 原文「需要「权限配置」权限，当前角色「CEO」不可用（入口保留以便申请）。」（文案锚点 `QuickActions.tsx`）；整页「无权限」提示**未出现**（无可达入口）；控制台**无任何记录**（工具读数）。⑥ **结论：与矩阵 §3 脚注 2 / §160「界面已可用（仍仅 `super_admin`）」一致，非缺陷**——`ceo` 不能写 ⇒ 界面不给编辑页（入口不显示 + 快捷入口禁用并给可读原因）。⑦ **如实登记的落差与未验证（待裁决，未擅自改动）**——(a) 脚注 2 说 `ceo`「读可」但**界面无任何 `ceo` 只读入口**（读能力只在 HTTP 层）；(b) 页面内 `PermissionGuard` 的整页无权限态**界面不可达**（应用壳无 URL 路由：`AppShell` 用 `activeKey` 状态切换，仓库 grep 无 `location.hash/search/pathname` 与 `URLSearchParams`）⇒ 只能由组件测试覆盖；(c) 控制台**无截图证据**（CDP 页面截图不含 DevTools 面板；走查者首轮给的 `03-ceo-console.png` 与 `02` **同字节**、补截那张仍是页面全貌 ⇒ **据实改名为 `03-ceo-topbar-role.png`** 作顶栏证据，不冒充控制台）。⑧ **文档回写（四处一致）**——`permissions-api.md` 新增 **§9.3** + §6③/§7、`permissions-fake-entry-plan.md` 新增 **§13.1** + §12④、`permission-matrix.md` §18⑥、本条 + D-041④/D-042⑦ 标注已走查。⑨ **本轮无产品代码改动、真库零新增**（走查全程内存仓储；仅新增一次性脚本 + 4 张截图入库） | 用户 2026-09-21 指令「先补 ceo 档的走查」；证据：`tmp/r16-walk-api.py` + HTTP 实测 6 项 + 4 张截图（`docs/screenshots/ui-v2-r16-permissions-ceo/`）+ 浏览器子代理走查记录；口径依据 `permission-matrix.md` §3 第 66 行 + 脚注 2（第 79-83 行） | ✅ 已交付（待用户确认提交；含 `tmp/r16-walk-api.py` 清理申报） |

| D-044 | 2026-09-21 | **`loading` / `error` 两态界面走查（补 D-041④ / D-042⑦ 的最后两处"未走查态"）已交付**：① **起因**——用户指令「先补 loading 和 error 两态的界面表现」；此前 D-042⑦ 把两态登记为"未在浏览器里造过"。② **环境**——一次性脚本 `tmp/r17-walk-api.py`（真实 `uvicorn` `127.0.0.1:18137` + 内存仓储；账号 `admin`/`123456` 角色 `super_admin`，口令**直插 scrypt 哈希**、**仅存在于本机内存**、**不是**平台任何真实账号）+ vite dev `http://localhost:5199/`（`VITE_WORKBENCH_API_MODE=http`、`VITE_DEV_PROXY_TARGET=http://127.0.0.1:18137`）。**模式开关**挂在 `/api/__r17/mode?set=slow|error|normal`（**仅走查用**：产品界面**无任何代码**请求它；**不碰产品代码**，只在最外层包了一层 ASGI 中间件）。③ **触发方式（如实）**——`slow`：对三处前缀延迟 **9s**（`REQUEST_TIMEOUT_MS = 10s` 是**每请求独立计时**，须压在超时线以内，否则会落成"服务不可达"而非 `loading`）；`error`：三处前缀返回 `500`，且 `detail` **故意**含 `/api/…`、`Traceback …`、`File "/srv/app/main.py"`（用于验证净化是否生效）。④ **界面层实测**——`loading`：三块**各自**骨架屏（`SkeletonList` 3 条），**绝不显示"暂无数据"**、**不显示失败说明**、**无全页遮罩**；等待后自动出表格（**不是卡死**）；**逐块就绪**（角色 / 数字员工因 `1+N` 两个请求比「最近变更」晚就绪 ⇒ **块间不同步**，属**真实观测**）。`error`：三块各自红叉 +「出错了」+ 各自专属文案（`STATE_TEXT`：`岗位知识范围加载失败，请稍后重试。` / `数字员工知识范围加载失败，请稍后重试。` / `变更记录加载失败，请稍后重试。`）+「重试」；服务端 `detail` 里的 `/api/…`、`Traceback …`、`File "…"` **一个字未落界面**（`safeDetail` 生效）；点「重试」实测**新发出** `GET /api/v1/workforce/roles`（仍 `500`，界面仍 `error`）；切回 `normal`（**不刷新**，避免清 react-query 缓存）再点**同一颗**「重试」⇒ 该块恢复、另两块仍 `error`（**分块重试**，互不牵连）。⑤ **结论：与实现口径一致，非缺陷**（`ContentState` / `SkeletonList` / `DataTable` 三处实现与实测表现逐条对上）。⑥ **仍属未走查（不得读成已验）**——~~**取数超时（>10s）/ 网络中断**那条分支~~（本轮刻意只压超时线以内，超时后落"服务不可用"文案的界面表现未造）⇒ **已于同日补走查（见 D-046：取数侧 `status=0` 两种触发）**；~~**保存失败**（`403`/`409`/`422`/网络）的界面表现~~ ⇒ **已于同日补走查（见 D-045）**（`409`/`422`/网络·`status=0` 三支已取证；`403` 登记为界面不可达、真实网络中断未造）。⑦ **据实改名（沿用 §9.3③ 同一纪律）**——走查者首轮交付的 `08-loading-audit.png` 视口内**只有**「角色 / 数字员工」两块骨架、**没截到**「最近变更」骨架（走查者自己也如实报了 FAIL）⇒ **据实改名**为 `08-loading-two-blocks.png`，并**追加补截** `09-loading-audit-skeleton.png`（**不以错名归档**）。⑧ **文档回写（四处一致）**——`permissions-api.md` §6③ + §7 + §9.2 末条 + 新增 **§9.4**、`permissions-fake-entry-plan.md` §12④ + §13 末行 + 新增 **§13.2**、`permission-matrix.md` §18⑤ 末行 + 新增 **§18⑦**、本条 + D-041④/D-042⑦ 标注"已于同日补走查"。⑨ **本轮无产品代码改动、真库零新增**（走查全程内存仓储；仅新增一次性脚本 + **10 张截图入库** `docs/screenshots/ui-v2-r17-permissions-states/`，均由我**逐张亲自读图核对**、未采信走查者口头结论） | 用户 2026-09-21 指令「先补 loading 和 error 两态的界面表现」；证据：`tmp/r17-walk-api.py` + 10 张截图（`docs/screenshots/ui-v2-r17-permissions-states/`）+ 浏览器走查记录（含一次"截图名不符实"的据实改名）；实现侧锚点 `workbench-web/src/components/{ContentState,SkeletonList,DataTable}.tsx` 与 `features/permissions/{PermissionsPage.tsx,components/ScopePanel.tsx,components/AuditPanel.tsx}` | ✅ 已交付（待用户确认提交；含 `tmp/r17-walk-api.py` 清理申报） |

| D-045 | 2026-09-21 | **「保存失败」三态界面走查（补 D-041④ / D-044⑥ 的最后一处"未走查态"）已交付**：① **起因**——用户指令「先补「保存失败」三态的走查」；此前 D-042⑦ / D-044⑥ 把"保存失败（`403`/`409`/`422`/网络）"登记为"未在浏览器里造过"。② **方法（先判可达、再造）**——逐支判定"界面能否自己产出"，**造不出来的登记为不可达、不硬造**：`409` **能**（行来自目录，但目录可在抽屉打开后被停用）⇒ 用**真实产品端点** `PATCH /api/v1/workforce/roles/content-operator {"status":"disabled"}` 停用后点「保存范围」，命中**真实业务闸门**（`app/workforce/service.py::ensure_role_binding_available`）；`422` **不能**（表单只发一个已知字段 `extra="forbid"` 无从触发、前端 `validateIds` 已限 ≤100）⇒ 服务端侧用 `tmp/r18-walk-probe.py real422` **直调真实端点**多带 `extra`（真实响应 `detail` 是**数组**），界面侧由走查后端在**传输层合成** `422`（`detail` 形状**照抄**真实响应）并**如实标注"传输层合成、非业务闸门产生"**；网络·`status=0` **能**——走查后端把写路径延迟 **12s** > 前端 `REQUEST_TIMEOUT_MS` 10s ⇒ **前端真实 `AbortController.abort()`**；`403` **不能**（本页入口 `ADMIN_ONLY`，非超管拿不到该界面）⇒ **登记为界面不可达，不造**。③ **环境**——一次性脚本 `tmp/r18-walk-api.py`（真实 `uvicorn` `127.0.0.1:18137` + 内存仓储 + 纯 ASGI 包装；模式开关 `GET /api/__r18/mode?set=invalid|slow|normal` **只作用于写路径 PUT**，产品代码**零引用**；账号 `admin`/`123456` 角色 `super_admin`，口令**直插 scrypt 哈希**、**仅存在于本机内存**、**不是**平台任何真实账号）+ `tmp/r18-walk-probe.py`（HTTP 取证）+ vite dev `http://localhost:5199/`。④ **界面层实测（逐字）**——`409`：抽屉顶部红色 Alert「**未能保存：该标识尚未纳入目录，请先在「数字员工设置」中纳管**」+ 描述「本次没有写入任何数据；…后重试。」，**抽屉未关、无「已保存」**；把岗位 `PATCH` 回 `active` 后**在同一个抽屉里**重试 ⇒「**已保存：知识范围已更新。「内容运营」当前可见 2 个知识库。**」+ 抽屉关闭。`422`：「**未能保存：请求参数不合法，已拒绝。**」+「本次没有写入任何数据；请检查填写内容后重试。」，页面**任何位置都未出现** `extra_forbidden`/`loc`/`msg`/`input` 原始 JSON 片段。网络：「**未能保存：服务不可达：请检查网络后重试。**」+「本次没有写入任何数据，请稍后重试。」，输入值**原样保留**。⑤ **HTTP 层取证**——真实 `422` 的 `detail` 确为**数组**（界面据此落本地文案）；`409` **零写入**（停用后 `PUT kb-r18-should-not-write` ⇒ `409`，随后 `GET` 回读仍 `["kb-r18-1","kb-r18-2"]`）；目录 `PATCH` 启停均 `200`。⑥ **结论：与实现口径一致，非缺陷**（`PermissionsPage.handleSubmit` → `ScopeDrawer` 的"就地 Alert（`error.message` + `WRITE_FAILURE_HINT[kind]`）、不关抽屉、不假装成功、不假刷新"，与 `permissions-api.md` §8「失败分类/写失败」逐字对上）。⑦ **仍属未验证（不得读成已验）**——`403` **界面不可达**（其服务端语义已有第 6 轮证据 + 单测）；**真实网络中断（连接被拒 / 断网）未造**（本轮是客户端 10s 超时中止，**共用同一条 `status=0` 前端路径但触发不同**）⇒ **取数侧已于同日补走查（见 D-046）**，**写侧（断网期间的 `PUT` 保存）仍缺**；~~`GET` 取数侧的同一条分支仍未走查~~ ⇒ **已于同日补走查（见 D-046）**；`409` 的前置停用动作是经**真实端点**而非「数字员工设置」界面完成；另有一处**待裁决观察**（非缺陷）——抽屉内「对象…（**已启用**）」是**打开时快照**，停用后该行与页面表格均未刷新（本次**未出现**被误导的后果，失败 `409` 文案已把真实原因写清）。⑧ **据实改名（沿用 §9.3③ / D-044⑦ 同一纪律）**——走查者另交的 `r18-01-page-loaded-full.png` 实为**同一页面的下滚视口**、非全页截图 ⇒ **据实改名**为 `r18-01b-page-loaded-scrolled.png`（**不以错名归档**）。⑨ **文档回写（四处一致）**——`permissions-api.md` §6③ + §7 + §9.4 末条 + 新增 **§9.5**、`permissions-fake-entry-plan.md` §12④ + §13 末条 + §13.2 末条 + 新增 **§13.3**、`permission-matrix.md` §18⑦ 末行 + 新增 **§18⑧**、本条 + D-041④/D-044⑥ 标注"已于同日补走查"。⑩ **本轮无产品代码改动、真库零新增**（走查全程内存仓储；仅新增一次性脚本 + **8 张截图入库** `docs/screenshots/ui-v2-r18-permissions-save-fail/`，均由我**逐张亲自读图核对**、未采信走查者口头结论） | 用户 2026-09-21 指令「先补「保存失败」三态的走查」；证据：`tmp/r18-walk-api.py` + `tmp/r18-walk-probe.py` + 8 张截图（`docs/screenshots/ui-v2-r18-permissions-save-fail/`）+ 浏览器走查记录（含一次"截图名不符实"的据实改名）；实现侧锚点 `workbench-web/src/features/permissions/{PermissionsPage.tsx,components/ScopeDrawer.tsx,services/permissionsService.ts,types.ts}` 与 `workbench-web/src/api/client.ts::safeDetail` | ✅ 已交付（待用户确认提交；含 `tmp/r18-walk-api.py` / `tmp/r18-walk-probe.py` 清理申报） |

| D-046 | 2026-09-21 | **取数（`GET`）侧 `status=0` 两种触发走查（补 D-045⑦ 登记的最后两条未验证）已交付**：① **起因**——用户指令「先补 GET 取数侧的 status=0 分支与真实网络中断」；D-045⑦ 把两条登记为未走查：**真实网络中断（连接被拒 / 断网）** 与 **`GET`（取数）侧的同一条 `status=0` 分支**（§9.5 只造了**写路径**的 10s 超时中止）。② **造法判定的关键（先判可达、再造）**——vite dev 的 `/api` 代理在后端**进程不存在**时**不把 `ECONNREFUSED` 传导到浏览器**，而是代理侧回一个 HTTP 错误响应 ⇒ 前端落 `failed` / `unavailable`、**不是** `status=0` ⇒ "杀后端进程"造**真实连接被拒**必须**直连**：给前端显式注入 `VITE_API_BASE_URL=http://127.0.0.1:18139`（开发模式 CORS 已放行 `https?://(localhost\|127\.0\.0\.1):\d+`，实测预检 `access-control-allow-origin: http://localhost:5199`）。③ **环境**——一次性脚本 `tmp/r19-walk-api.py`（真实 `uvicorn` `127.0.0.1:18139` + 内存仓储 + 纯 ASGI 包装；模式开关 `GET /api/__r19/mode?set=slow&delay=12\|set=normal` **只作用于取数 GET**（`/api/v1/workforce/`、`/api/v1/knowledge-access/`、`/api/v1/knowledge/`），登录路径**不延迟**，产品代码**零引用**；账号 `admin`/`123456` 角色 `super_admin`、口令**直插 scrypt 哈希**、**仅存在于本机内存**、**不是**平台任何真实账号）+ vite dev `http://localhost:5199/`。④ **两根触发（性质如实标注）**——**超时中止**：延迟 **12s** > 前端 `REQUEST_TIMEOUT_MS` 10s ⇒ 每请求独立 `AbortController` 于 10s **前端真实 `abort()`**（"服务端慢"为**合成**）；**真实网络中断**：页面加载完后**杀掉后端进程**（已核对 `18139` 连接被拒）再刷新 / 重试 ⇒ **触发为真实**（连接层失败、无任何响应）；**断网恢复**：重起同一脚本后点「重试」。⑤ **界面层实测（逐字）**——超时：三块各自红叉 +「**出错了**」+ 各块**固定文案**（`岗位知识范围加载失败，请稍后重试。` / `数字员工知识范围加载失败，请稍后重试。` / `变更记录加载失败，请稍后重试。`）+「重试」，表格区**无数据行残留**（同时证明**加载态不误标为空态**：12s 慢期间三块均为骨架屏、**无"暂无数据"字样**）；逐块「重试」后各自恢复为数据表格。断网：刷新后先落**「我的工作台」**（旁证整机不可达）——其「待办」块「出错了」+「**服务不可达：请检查网络后重试**」；点导航进「权限配置」后三块**逐字与超时态完全相同**；后端仍死时点「重试」**仍为错误态**（不静默重试、不假装成功）；后端恢复后点「重试」三块恢复数据。⑥ **传输层原文（区分两触发的唯一硬证据）**——控制台逐字：超时 `net::ERR_ABORTED` × 3；连接被拒 `net::ERR_CONNECTION_REFUSED` × 3（同一三条 URL）。**旁证**：走查者从页面上下文调模式开关报 `net::ERR_FAILED`（控制端点**不带 CORS 头**，服务端副作用**已生效**、用 `curl` 读得 `{"mode":"slow"}` 证实，JS 读不到响应 ⇒ `fetch` 返回 `undefined`）——这是**走查脚手架**的性质，与产品代码无关。⑦ **本轮最重要发现（如实登记，**不擅自改**、列待裁决）**——取数失败文案取**各块固定文案**（`PermissionsPage.STATE_TEXT[kind].error`）、**不渲染请求层已 sanitize 的 `message`** ⇒ **"客户端超时中止"与"真实网络中断"在界面上逐字节不可区分**（`r19-02a/02b/02c` 与 `r19-05a/05b/05c-outage-*` 的 SHA-256 前 12 位**两两相同**：`DA9FF514D403` / `1DD7236C55C0` / `A9DBA2DA8E9F`）；是否需要让读侧区分两种原因（或把请求层文案上屏）属**产品口径变更** ⇒ **待裁决、未动手**。**同轮正面结论**：取数失败落 `error` 态（可重试），**不显示**「暂无…」空态文案 ⇒ 对 `permissions-fake-entry-plan.md` 关心的"**假入口数据没取到时界面会不会谎报成没有内容**"，实测**不会**。⑧ **据实登记（一轮证据作废重取 + 据实改名）**——走查者第一轮断网走查自报"角色块已错误态"，但其两张图 **SHA-256 完全相同**、内容实为**已恢复的数据表** ⇒ 该轮**证据作废、改用更可靠方法重取**（断网后**刷新** → 落「我的工作台」→ 点导航进「权限配置」→ 逐块滚动截图），归档的断网态三张为**重取后**的图；`r19-05-prepage-myworkbench-service-unreachable.png` 系**据实改名**（非权限配置页）；另有 4 个同名覆盖产生的冗余命名文件**已删除**。⑨ **文档回写（四处一致）**——`permissions-api.md` §6③ + §7 + 新增 **§9.6**、`permissions-fake-entry-plan.md` §13.3 末条第 2 项 + 新增 **§13.4**、`permission-matrix.md` §18⑧ 末段 + 新增 **§18⑨**（**口径零改动**：未新造角色、未改闸门、未动 §3 矩阵）、本条 + D-044⑥ / D-045⑦ 标注"已走查（取数侧）"。⑩ **仍属未验证（不得读成已验，5 条）**——(a) 断网期间的**写入**路径（`PUT` 保存）未走查；(b) 本轮"真实网络中断"是**连接被拒**、**不是物理断网**（拔网线 / 关 Wi-Fi / DNS 失败 / 证书失败——后两者需域名与 TLS 环境，本机直连造不出来）；(c) 断网 / 超时**持续时间**未测（10s 之外的长时不可达、重试连打、页面长挂）；(d) `429`、令牌自然过期、并发取数（多标签页同开同页）未测；(e) 断网时"我的工作台"等**其它模块**只有 1 张旁证图、不属本轮范围。⑪ **本轮无产品代码改动、真库零新增**（走查全程内存仓储；仅新增一次性脚本 + **17 张截图入库** `docs/screenshots/ui-v2-r19-permissions-network/`，均由我**逐张亲自读图核对**、未采信走查者口头结论）。 | 用户 2026-09-21 指令「先补 GET 取数侧的 status=0 分支与真实网络中断」；证据：`tmp/r19-walk-api.py` + 17 张截图（`docs/screenshots/ui-v2-r19-permissions-network/`）+ 浏览器走查记录（含一轮"截图名不符实"的证据作废重取 + 一次据实改名）；实现侧锚点 `workbench-web/src/api/{client.ts::performRequest,config.ts::REQUEST_TIMEOUT_MS}`、`workbench-web/src/features/permissions/PermissionsPage.tsx::STATE_TEXT`、`workbench-web/vite.config.ts`、`app/settings.py::resolve_cors_options` | ✅ 已交付（待用户确认提交；含 `tmp/r19-walk-api.py` 清理申报） |

| D-047 | 2026-09-21 | **TabTin（`www.tabtin.com` / `tabtin-ai/TabTin`）只读研读结论 + 三项落地（用户裁决）**：**① 定性＝「只读研读（连接都不接）」**——License = **AGPL-3.0-only**（`gh api` 一手）+ README 自述"可咨询单独商业授权" ⇒ **搬代码绝对禁止**（我方为营利业务系统，源码入库即触发向使用者提供对应源码的义务） ⇒ **⚠️ 前提已于同日变更（见 D-049）：用户改为「仅内部使用」，本条理由构成由"绝对禁止"修正为"高风险且不可逆 + 债不可拆"，结论不变（仍不搬）**；**接适配器同样不做**——理由不在许可而在**我方既有硬约束**：[`architecture.md` L51](file:///d:/徐徐AI学习/公司工作台/docs/architecture.md#L51) 明文禁止"把用户本机 CLI 当引擎 / 单机单用户形态的 harness 搬进来（会带进第二套用户、权限与审计）"，**TabTin 正是该形态**（桌面端为执行环境 + 自建账号 / 权限 / 协作体系）⇒ **命中既有"一类明确不可搬"**；唯一安全路径＝读源码、提炼设计、自己实现（与 EvoFlow / OpenWorkBuddy 同口径）。**② 一处必须纠正的误判（一手 code search 核实）**：子代理转述"TabTin 也做 schema 隔离 / 与我方同思路"**不成立**——`search_path` 11 处集中在 `settings*` 与 `tabdata`（用户自定义数据库连接），`tenant` 59 处但 **`tenant_id` 仅 3 处且全是飞书 / MS Teams 外部平台的 tenant key**，`.env.example` 为 `TABTIN_DATABASE_MODE=single_pg` ⇒ **它的隔离单位是"一次部署 = 一个客户"**，**不是 SaaS 多租户底座**；不得写成"同思路"。**③ 三项落地**（用户 2026-09-21 裁决）：**(a) 新建专项调研文档** `docs/tabtin-source-study-and-adaptation-plan.md`（含 §0 证据等级图例 ✅一手/◐抓取/○未核实、§4 十维度对照、§5 许可红线判定、§6 A/B/C 三档可借鉴清单、§7 待裁决 Q1–Q3、§8 未核实 8 项、§9 血缘声明）；**(b) 两项候选能力只登记 + 出待裁决项** —— `feature-inventory.md` §3.4 新增 **C4「任务交接 / 接手」**（我方 P2c-6 成员表 `read`/`write` 已具雏形，**缺显式交接语义**；障碍：交接什么 / 权限如何收敛 / 交接后原发起人是否失权 / 会再次改动运行级读路径与审批列表）与 **C5「执行检查点回退 + 回退审批」**（有产物版本与重做，**无检查点回退原语**；障碍：属运行域契约扩展，须先答"回退是否等于新一次运行 / 是否重跑九步闸门 / 与终态 `409` 的关系"）；**(c) 六项开源卫生**：`SECURITY.md` / `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md` / `ROADMAP.md` / `.gitleaks.toml` **五项已落地**（均为**如实版**：明确标注"内部仓库、未建立正式响应时限承诺、不构成排期与功能承诺"，`CODE_OF_CONDUCT` 为**自撰稿**不引用第三方模板以免引入未核对许可的文本，`CONTRIBUTING` 口径与 `delivery-gates.md` 六条**逐字一致**），**第 6 项 `lint-baseline.json` 未落地**——勘察发现**全仓无任何 linter 配置**（无 eslint / 无 `[tool.ruff]`，`ci.yml` 也不跑 lint）⇒ **没有可"基线化"的对象，照抄空壳属造假**（宪法九章），故停下并出 **Q3 待裁决**（是否引入 linter 属**依赖新增决策**）。**④ 验证**：`.gitleaks.toml` 已做 **TOML 可解析性校验**（`tomllib` 解析通过：keys=title/extend/allowlists，allowlists=2）——**但 gitleaks 未安装、CI 未接入 ⇒ 本文件"未实跑"，不得读成"密钥扫描已上线"**；我方血缘已本地 `grep` 核实**零命中** `tabtin`/`TabTin`/`摹范`/`Mofan`/`larchiveai` ⇒ **无任何代码或文档血缘**。**⑤ 本轮零产品代码改动、零接口改动、零契约口径改动**（`permission-matrix.md` / `api-contract.md` / `knowledge-acl.md` **均未动**）。**⑥ 未核实 8 项**（官网 `/about` 等路由全 404、公有云是否可自助注册、定价细则、工商与融资、**我方从未运行过对方产品**、对方 CI 与生产部署与完整 git 历史、官网自述 48 应用包 / 80 Skill 未逐条核对、97 个 fork 归属）逐条登记在专项文档 §8 | 用户 2026-09-21 指令「深度调研 www.tabtin.com + github.com/tabtin-ai/TabTin，再对照我们的项目做深度详细分析」+ 三项裁决（新建专项文档 / 六项卫生一起做 / 两条候选登记并出待裁决项）；证据：`gh api repos/tabtin-ai/TabTin`（417 star / 97 fork / AGPL-3.0 / created 2026-08-19 / pushed 2026-08-31）、`apps/tabtin_django/requirements.txt` 与根 `package.json` 直读、code search（`tenant` / `tenant_id` / `search_path`）、官网与 README / ROADMAP 抓取；我方侧勘察：`.github/workflows/ci.yml`、`docs/delivery-gates.md` §「每次提交必须满足」、根目录治理文件清单 | ✅ 已交付（文档 + C4/C5 登记 + 五项卫生；**待用户确认提交**）· **Q1 / Q2 / Q3 已于同日答复「三项都暂缓」**（歧义消解，见「待接续（未定项）」） |

> **⚠️ 第 7 轮取证副作用（如实登记，承接 D-023）**：执行 `review?approved=true` 探针时，`probe-admin-1` 由 `draft` **被置为 `published`**
> （`owner_id` 仍为空），真库指标随之变为 `published=2 / archived=1 / total=3 / freshness_ratio≈0.667`。
> 后端**无"取消发布/回到草稿"接口**（`archived` 为终态），本轮**未做**任何补偿性写入；处置待你裁决（保留现状 / 归档该行 / DB 重置由你在服务器执行）。

| D-048 | 2026-09-21 | **时间相关用例腐化（既有的红）已修复 + 同类排查（用户裁决「修这一条 + 排查同类」）**：① **现象**——本轮（TabTin 主线）按交付门禁跑后端全量，出现 **1 条红**：`tests/test_commercial_api.py::test_customer_admin_can_fetch_own_export_package_only` ⇒ `assert 404 == 200`。② **先判归属（不先改码）**——隔离单跑该用例**同样失败**、单跑整个文件**同样失败**，且 `git status --short` 显示工作树只有**文档类改动**（5 个 md 修改 + 6 个新文件 + 4 个截图目录，**`app/` 与前端零改动**）⇒ 判定为**既有失败、与本轮改动无关**，**未擅自修**，先向用户报告并请裁决。③ **根因（定位到行）**——测试把 `now` **写死**为 `datetime(2026, 9, 14, 8, 0, tzinfo=UTC)` 并令 `expires_at = now + timedelta(days=7)` ⇒ 过期时刻 **2026-09-21 08:00Z**；而 `app/commercial/lifecycle.py::get_export_package` 以 `expires_at <= (now or datetime.now(UTC))` 判过期 ⇒ `ExportPackageExpired`（路由映射 `404`）。**今天是 2026-09-21**，本地时钟过 16:00（= 08:00Z）后该用例即变红 ⇒ **属时间相关的用例腐化（test decay），非产品缺陷**（同一时刻口径「正点即过期」本身有专门用例 `test_get_export_package_expiry_boundary_is_inclusive` 守护，**未改动**）。④ **修法（最小、且贴合全仓既有范式）**——取回端点**不接受注入 `now`**（服务层默认取真实时间）⇒ 只能改用**相对时间** `now = datetime.now(UTC)`；`expires_at` 仍为 `now + 7 天`（**测试语义不变**：仍验"新鲜包可取回"，非"过期包被拒"）。**溯源留痕**同步写入测试注释（为什么不能用写死日期）。⑤ **同类排查（用户要求）**——对全仓 `tests/` 的硬编码日期（99 处 / 30 文件）逐一判类，结论：**(a) 绝大多数是"把固定时间当数据值相互比较"**（排序 / 往返 / 快照断言，如 `test_run_metrics.py`、`test_run_records.py`、`test_orchestration_store.py`、`test_account_postgres.py`、`test_content_sqlite_store.py`）⇒ **不会腐化**；**(b) 商业导出族全部显式注入 `now=`**（`test_commercial_lifecycle.py` 的 `get_export_package` / `purge_expired_export_packages` / `run_pending_jobs` 调用点、`test_commercial_lifecycle_postgres.py::export_store.purge_expired(now=now)`）⇒ **不会腐化**，这也是**应保留的正确范式**；**(c) 故意造过期**（`test_commercial_api.py` 的两处 `past = datetime(2026, 9, 1, …)`）⇒ **永久安全，未动**；**(d) 真正同类隐患仅 1 处**——`test_commercial_api.py` 的列表用例（原 `now = datetime(2026, 9, 19, 8, 0, UTC)`，过期时刻 2026-09-26 08:00Z）：它**只走列表**（过期行在清理前仍可见，见 `test_list_export_packages_includes_expired_rows_until_purged`）故**尚未致红**，但同属"写死 now + TTL"⇒ 一并改为相对时间（**预防性**，非修红）。**未引入任何时间冻结依赖**（不新增 `freezegun` 等，属依赖新增决策）。⑥ **验证（只认证据）**——修后 `tests/test_commercial_api.py` **10 passed**；**反假测试**：把该用例的 `expires_at` 故意改成 `now - 1s` ⇒ **变红**（`assert 404 == 200`）⇒ 证明该断言**不是恒绿的假测试**，还原后复绿；**后端全量** `python -m pytest -o addopts=""` ⇒ **2643 passed / 140 skipped**（229.84s，**0 failed**）；`python -m compileall -q app tests extract_pdf.py scripts` ⇒ **OK**。⑦ **范围与留痕**——本轮**唯一代码改动**为 `tests/test_commercial_api.py` 两处（**测试文件，非产品代码**；`app/`、迁移、前端、契约口径零改动）；**未新增临时脚本**（本轮直接用 pytest，无清理申报项）。⑧ **未验证（不得读成已验）**——前端三端未重跑（本轮未改前端）；除上述判类外**未对 99 处硬编码日期逐条运行时验证**（判类依据是"该值是否与真实 `datetime.now()` 比较"的静态阅读，非运行期实验）；**未做**系统级时间回拨 / 跨时区实验 | 用户 2026-09-21 裁决「修这一条 + 排查同类」（四问答复第 1 项）；证据：`git status --short`（无 `app/` 改动）→ 隔离复现（单用例 / 单文件均 FAILED）→ `tests/test_commercial_api.py#L103-L106`、`app/commercial/lifecycle.py#L444`、`app/commercial/lifecycle.py:EXPORT_PACKAGE_TTL`；修后 `pytest tests/test_commercial_api.py`（10 passed）+ 反假一次变红 + 全量 2643 passed / 140 skipped + compileall OK | ✅ 已修复并验证（**待用户确认提交**） |

| D-049 | 2026-09-21 | **口径变更：与 TabTin 的关系由"绝对不可搬"改为"分域只读研读 + 自研"；两处地基级冲突启动重评审；本轮交付《能力域研读提纲》**：① **前提变更（决定性）**——用户明确「**我们做出来目前也不是商用，只是用作工作内部使用。后面如果商用以后再说**」⇒ **D-047① 的"搬代码绝对禁止"建立在"我方是营利业务系统"这一前提上，前提已变 ⇒ 推理链必须重写而非沿用**；同时 v1 把**许可**与**架构**混在一格判，本轮**拆成两层**（**许可**会随"商不商用"改变；**架构**不随其改变）。② **三项裁决（用户 AskUserQuestion）**——(a) 与上游的关系 = **分域只读研读 + 自研**；(b) **两处"地基级冲突"启动重评审** = `single_pg` 取舍 + 桌面执行模型；(c) 本轮先做 = **出《能力域研读提纲》**。③ **许可层 v2 判定（结论不变、理由构成改写）**——组织内部自用**一般不触发** AGPL 源码提供义务（GPL 系核心是 **conveying / 对外分发**），但**三处风险不为零**：**主体边界**（员工 / 外包 / 多法人是否算 §13 的"用户"有解释空间）/ **一次对外交付即触发且时点通常不由我们决定** / **债不可拆**（本轮新增决定性事实：仓库实测 **20,672 文件 ≈ 145 MB 纯源码** ⇒ "到商用那天再剥离"在工程上**不可行**）。**架构层判定不变**：[`architecture.md` L51](file:///d:/徐徐AI学习/公司工作台/docs/architecture.md#L51) 明文禁止"把用户本机 CLI 当引擎 / 单机单用户形态的 harness 搬进来（会带进**第二套用户 / 权限 / 审计**）"，**TabTin 正是该形态**；但**用户已裁决启动重评审（Q5）⇒ 该条降级为"待评审"、不是终局**；若推翻**必须显式修改 `architecture.md` 并留痕，不得绕过**。④ **本轮交付物（均为一手取证）**——(a) 专项文档 **§5 整节重写为 v2**（许可 / 架构两层 + 三条路径 v2 判定表 + v2 结论）；(b) **新增 §10《能力域研读提纲》**：依 `gh api repos/tabtin-ai/TabTin/git/trees/HEAD?recursive=1`（**`truncated=false`**）一手取回，把 **90 个 package 归并为 8 个能力域** —— D1 Agent 内核 **11 包 / 1,308 文件**、D2 文档·表格协同 CRDT **20 / 1,454**、D3 执行环境 **9 / 325**、D4 应用宿主与外壳 **20 / 745**、D5 安全与策略 **5 / 78**、D6 爬取·站点·检索 **9 / 258**、D7 CLI·SDK·基础设施 **16 / 659**，每域标注**构成 / 我方现状 / 内部使用价值 / 合规红线 / 建议**；**交叉校验通过**：8 域包数合计 **90** = 取回的 `packages/` 目录数、文件数合计 **4,827** = §3 取回的 `packages/ 4,827`（**无遗漏、无重复计入**）；(c) **§6 C 档第 1 / 5 条由"明确不可动"降级为"待评审"**、**§7 新增 Q4 / Q5**、**§9 追加 v2 留痕**（含"新增节一律追加文末"的顺序说明，避免既有 §7 外部引用漂移）；(d) 本节 D-049 + **D-047① 加前提变更标注**。⑤ **两条新硬事实（直接影响用户清单可行性）**——(a) **上游全仓 0 命中"鸿蒙"**（HarmonyOS / hvigor / ArkTS 等均无）⇒ 用户清单里的「鸿蒙端」**在上游没有参照物**，要做只能**纯自研**；(b) **`apps/` 8 应用 / 15,337 文件**（`tabtin-electron` 6,951 / `tabtin_django` 5,017 / `tabtin-android` 1,406 / `tabtin-ios` 1,012 / `admindash` 407 / `tabtin-daemon` 233 / `tabtin-web` 207 / `collab-live` 104）+ **巨型单文件信号**（`agent-runtime/src/subagent/agent-tool.ts` **154.8 KB**、`capability/core/__tests__/shell.test.ts` 131.9 KB、`capability/core/shell.ts` 95.9 KB、`engine/tooling/tool-orchestration.ts` 93.6 KB）⇒ **只可学设计、不可学组织方式**（已在 §10.3 作**反例**登记）。⑥ **合规红线（不得参照）**——`packages/anti-detect`（22 文件 / 141 KB）⇒ 与我方 `architecture.md` 明文"**不实现风控规避、设备伪装和代理轮换**"直接冲突，候选 **C3** 已冻结 ⇒ §10 D5 标注 **`不适用（红线）`**；另 D6 爬取域（`crawlspace` / `tabsite`）标注**合规外部性**、建议**暂不**。⑦ **范围与留痕**——本轮**零产品代码改动、零后端接口改动、零契约口径改动**（`architecture.md` / `docs/contracts/schema-per-tenant-plan.md` / `permission-matrix.md` / `api-contract.md` **全部未动**；两处重评审**只启动、不改动**）；改动文件 = 专项文档 4 处（状态行 / §5 / §6C / §7 / §9 + 新增 §10）+ 本节。⑧ **未验证（不得读成已验）**——(a) §10 的 8 域是**我方归并**、**非上游官方分类**，成员包**只按包名 + 实测文件数归并、未逐条读上游实现** ⇒ §10.3 是**问题清单**、**不是结论**；(b) 「我方现状」列来自**我方仓库直读**，**未与上游做功能级二次比对**；(c) 两处重评审（**Q4 / Q5**）本轮**只登记，未开展任何评审工作**；(d) 我方**从未运行过 TabTin 产品**（无账号、无下载、无部署），一切产品结论仅来自**公开文案与公开源码**；(e) §10 归并基于**单次 tree 取回**，未在第二个时点复核（上游最后一次 push 停在 2026-08-31）。 | 用户 2026-09-21 指令「以上这些也是我想要的…tabtin 是开源项目…希望你能把他的源代码的开发逻辑完整的学会…如果能直接拿过来用最好…**我们做出来目前也不是商用，只是用作工作内部使用**」+ 三项裁决（**分域只读研读 + 自研** / **两处地基启动重评审** / **先出能力域研读提纲**）；证据：`gh api repos/tabtin-ai/TabTin/git/trees/HEAD?recursive=1`（`truncated=false`；**90 包 / 4,827 文件**；**8 应用 / 15,337 文件**；全仓 **20,672 文件**）；专项文档 §5 v2 / §10 | ✅ 已交付（文档；**待用户确认提交**）· **待裁决 Q4 / Q5**（Q1–Q3 已于同日答复「三项都暂缓」，歧义消解） |
| D-050 | 2026-09-22 | **产品形态定稿 + 能力来源核实（本轮为方案层，零产品代码改动）**：① **本轮性质**——用户在连续多轮对话中做出 **30+ 条裁决**，覆盖产品形态、边界、执行模型、数字员工、能力来源、沉淀层、反检测七个方面。**全部属方案层**，本条目为**回写留痕**（宪法 1.4 / 2.1：临时想法回写文档才算生效）。② **信息架构（用户裁决）**——**三栏**：侧栏（**≤10 入口**，含常驻的最近会话列表）/ 主区域（**对话常驻**：切走时 `display:none` 但**不卸载**，保 SSE 连接；其他页面与之**互斥显示**）/ 右栏（当前业务对象面板，默认简要、**可展开全屏**，展开时左对话隐藏）；**对话是唯一创建入口，页面只负责看进度与结果**；**悬浮窗助手常驻**（挂在 `body`、路由切换不卸载、**规则层不用 LLM**）；**管理类（人员/权限/审计/用量）收进桌面设置弹窗**，不再占侧栏。现状对照：`admin-web` 17 个平级入口、`workbench-web` 9 个平级页，目标 ≤10。③ **产品边界（用户裁决）**——**CRM 剥离**（后期作为独立系统通过版本化适配器接入；注意：`app/crm/` 4,836 行 + `admin-web/src/features/crm/` 16 文件 + 5 张迁移表**代码仍在仓库**，"剥离"是**产品边界决策、不等于现在删代码**，代码处置**待裁决**）；**GEO 暂不考虑**（沿用既有口径：独立项目 + 后期适配器接入）。④ **⭐ Q5 裁决（本轮关键，原为待裁决）**——用户明确「**我要的就是桌面端，不要所谓的 web 端**」+「**桌面当执行引擎需要的**」⇒ **Q5 由「待重评审」转为「已裁决」**：桌面执行与服务端容器九步闸门的关系 = **并存、按任务类型分**（需要本机能力（浏览器/本地文件/本机命令）→ 桌面执行；标准化/强审计/无本机依赖 → 服务端容器，九步闸门不变）；**铁律：审计只有一套、落在服务端**（桌面执行时闸门可灵活，但**审计必须回传**，否则等于没有审计；断网期间本地暂存后补传，或不允许执行需审计的动作）。**⛔ 但生效前提未变**：`architecture.md L51` 明文禁止"把用户本机 CLI 当引擎"⇒ **必须显式修改 `architecture.md` 并留痕，不得绕过**（用户已裁决启动，但**本轮仍未动架构文档**）。**连带影响（台账自记的硬耦合）**：**Q2 的耦合条件随之确定**——"检查点回退是否重跑九步闸门"现在取决于**任务落在桌面还是服务端**，不再是悬空问题。⑤ **前端形态（用户裁决）**——**1 份前端产物 + 2 种投递**（桌面内嵌为主 / 浏览器仅作管理远程）；`workbench-web`（19,068 行 / 9 页，员工视角）与 `admin-web`（19,621 行 / 17 视图，管理视角）**合并为一份**，基座重组（workbench-web 骨架 + admin-web 已接线模块）。⑥ **数字员工（产品心脏，用户裁决 + 建议）**——现状实测：**后端无员工侧创建接口**（前端注释："DE-01 本批未接入 ⇒ 入口保留但禁用"）、**目录接口仅超级管理员**（普通员工打开「我的数字员工」落 `403`）、**无 `owner` 归属列**（迁移 022 只有 `created_by`）、**无岗位模板种子**；裁决方向：`Agent`（能力包，可复用可模板化）+ `Assignment`（`owner_user_id` + 职责 + 作息 + 审批档）**两层表（不合成一张）**+ 员工侧创建接口 + 开放目录 + **6 类岗位模板**（策划/拍剪/开发/销售/管理/文案）+ 角色模型加 **SOUL 层**（气质/价值观，与 `system_prompt` 的「步骤/边界」分工）+ 创建流程护栏（先澄清再写 / 最小权限 / 名称必须来自真实目录）。⑦ **能力来源（本轮完成核实）**——**24 个 skill 已逐项读到 LICENSE 原文、可商用**（拍剪 5：`violin`/`jianshuo/claude-skills`/`manim-skill`/`heygen-com/skills`/`elevenlabs/skills` 均 MIT；文案 6：含 **`de-ai-polish` = MIT 三重证据一致**；策划 1：`Weizhena/Deep-Research-skills` MIT；开发 3：`anthropics/knowledge-work-plugins`(Apache-2.0)/`obra/superpowers`(MIT)/`mattpocock/skills`(MIT)；销售 4；管理 3）；**7 个需先处理**（`creator-buddy` **全仓无 LICENSE 文件**、`claude-blog` 的 `brain/` 子目录许可与根 MIT 冲突、`vtake-skills` 内置 **GSAP 规定"终端用户不得为产品付费"**、`vibe-creating-skill` 的范式归字节、`claude-code-recipes` 自定义专有许可、`trailofbits/skills` CC BY-SA 传染、`divinesense` 署名瑕疵）；**不可引**（`Claude-Code-Video-Toolkit` 与 `topic-to-digital-human-video-skill` **均无 LICENSE**、`borghei/Claude-Skills` **MIT + Commons Clause**（内部可用、对外收费禁止——**恰覆盖最想要的策划+管理能力**）、`legal-skills` 的 **18 个 CC-BY-NC + 7 个无许可文件**）；**🚨 一项确定性付费义务**——**Remotion 免费门槛 = 营利组织 ≤3 人**，超门槛**必须买 Company License**（Creators $25/月/席位；**Automators $0.01/次渲染 + 最低 $100/月**），且 5.0 起禁止运营"让终端用户自带 Remotion 代码"的渲染服务；**两个必须自研**：**GEO 特化内容能力**（全网无）+ **跨岗位串联层**（全网无）；**7 步准入链**（回源核实→许可核实→静态扫描→SKILL.md 审查→脚本审查→沙箱试跑→白名单）。**核实方法**：全部经 GitHub API 递归文件树 + `raw.githubusercontent.com` 读 LICENSE 原文，**未采信二手转述**。⑧ **沉淀层（建议，用户认可）**——加**计量**（`use_count`/`cite_count`/hot/cold）+ 加**控制器**（零引用 N 天 → 告警/重写/淘汰，**这是 EvoFlow 缺的一环**）+ **craft→skill 硬晋升通道** + 记忆层确定性衰减/GC/合并 + 注入字符级硬预算。⑨ **⚠️ 反检测（本轮最大分歧，如实登记）**——用户要求包含 **验证码绕过 / 滑块绕过 / 设备伪装（用户原话含"用于欺诈"）/ 学 TabTin 的 JS 注入层**；**我方拒绝**，理由三条：**(a)** 用户 **`architecture.md §发布边界` 自己明文禁止**"不实现私有接口模拟、风控规避、设备伪装和代理轮换"，且写明"**遇到验证码、风控或页面变化立即人工接管**"（是"接管"不是"绕过"）；**(b)** 用户 **C3 判据自己写明**"在完成书面合规评估并留痕之前，不得进入任何评审、立项、排期、原型或对外表达链路"；**(c)** "设备伪装**用于欺诈**"属**我方绝对边界**，不因规则可变而改变。**事实更正**：核实后 **TabTin 并未实现验证码绕过**（`CaptchaSolver` 是 `NoOpCaptchaSolver`，`canSolve()` 恒 `false`；注释自述"仅定义接口，不提供实现"），且其 `crawl-integration` **默认 `robots: 'obey'`**。**保留项（可做）**：指纹保护（防跨站追踪）、账号隔离、**「遇阻即人工接管」做成一流体验**（用户架构天然适合：桌面为主 + 悬浮窗做通知入口 + 审计记录接管全程 + PWA 做远程接管——**这是 EvoFlow/TabTin 都做不到的差异点**）。**前置门槛（C3 合规评估）本轮未过。**⑩ **明确不做（用户认可）**——搬 TabTin/EvoFlow 代码（AGPL / PolyForm Noncommercial）/ 铺开办公套件 / 引入 CRDT（**与既有 409 首写获胜语义冲突**）/ 抄 `collab/` / 单 SQLite + 无迁移机制。⑪ **六批次计划（我方提出，用户授权执行）**——B0 修地基（免评审）/ B1 架构定稿（须评审）/ B2 数字员工干线（须立项，**产品心脏**）/ B3 信息架构改造（须立项）/ B4 能力引进（走准入链）/ B5 沉淀层计量与控制器 / B6 反检测（**前置门槛未过**）；**硬约束：B2 完成前冻结一切 B2 档工作**。⑫ **范围与留痕**——本轮**零产品代码改动、零后端接口改动、零契约口径改动**（`architecture.md` / `api-contract.md` / `permission-matrix.md` **本轮均未动**；Q5 虽已裁决但**架构文档修改未执行**）。⑬ **未验证（不得读成已验）**——(a) **未实际引进任何 skill**（仅核实许可）；(b) 24 个"可引"里 **2 个官方仓库的服务条款未核实**（HeyGen API 条款 / ElevenLabs 声音商用授权）；(c) **Remotion 是否必买未做商业决策**；(d) 三处待裁决事项（`creator-buddy` 是否去要 LICENSE / `borghei` 内部用还是要商业授权 / CRM 代码处置）**均未决**；(e) B1–B6 **均未开工**；(f) 反检测的合规评估**未开展**。 | 用户 2026-09-21/22 多轮指令与裁决（形态 / 边界 / 执行模型 / 数字员工 / 能力 / 沉淀层 / 反检测）+ 我方核实（3 路 agent 逐仓读 LICENSE 原文 + Remotion 条款我方独立复核）；证据：`docs/` 直读（`moat-boundaries.md` / `ROADMAP.md` / `feature-inventory.md` / `architecture.md`）、`app/main.py` 接线注释、`workbench-web/README.md`、GitHub API 递归树 + `raw.githubusercontent.com` LICENSE 原文（核实 URL 见各轮报告） | ✅ 已登记（**待用户确认**）；B0–B6 计划已出，**尚未开工**。**Q5 由"待裁决"转为"已裁决"**（见下「待裁决（未定项）」段同步更新） |
| D-051 | 2026-09-22 | **B0 已交付（真源口径统一 · 六份文档）+ B1 规格已出（未评审）**：① **B0 交付（本轮已执行，六份 docs 改动、零产品代码）**——**(a) 新增 D-050**（本对话 30+ 条决策入台账，补齐宪法「回写文档才算生效」）；**(b) 修 M2 四处过期陈述**：`moat-boundaries.md` §2 M2 行（原「🔴 全部未开工」→ 三重证据的「P3/P4/P6a 已交付 · P6a 默认关闭 · 六要素待补」）+ §7 未决第 1 条（原「M2 未开工、唯一结构性缺口」→ **缺口性质转移为「有没有真起作用」**）、`ROADMAP.md` P3/P4 行（「待细化」→「已交付 · 六要素待补」）、`feature-inventory.md` §2 期次表 P3/P4 行（与**其表头**的「已交付」口径对齐——原表头与表体**自相矛盾**）；**(c) 登记 `workbench-web`**（19,068 行 / 9 页，此前 **README / architecture.md / ROADMAP 三份顶层真源零提及**）进三份真源；**(d) 判据②定可度量指标**（四个载体各给可计算指标 + 明确「**当前四项均无采集 ⇒ 判据②不可验收**」，**同时构成 B5 立项依据**）；**(e) 登记 CRM / GEO 产品边界**（CRM 剥离为后期独立系统接入、GEO 暂不考虑；**并如实标注 `app/crm/` 4,836 行代码仍在仓库、「剥离」≠「现在删代码」、处置待裁决**）。**验证**：逐项 grep 复核（M2 过期陈述零命中 / P3·P4「待细化」清零 / workbench-web 在三份真源各 2 处 / 判据②度量口径 1 处 / CRM 边界双处 / D-050 行**列数=5** 与表头一致）。**② B1 规格已出（未评审）**——新建 `docs/superpowers/specs/2026-09-22-desktop-execution-and-client-merge-design.md`，含四件交付：**(a) 执行模型（桌面+服务端并存）**——唯一分流判据「**是否需要本机能力**」+ **反向约束（不需要本机能力的禁止走桌面）** + 分流由**服务端判定**（客户端只能请求不能决定）；**(b) ⭐ 审计单源机制**——三阶段回传（①授权：服务端跑闸门发短期令牌，桌面**不自行判定权限** ②执行：事件异步可缓冲可丢失 ③结算：**唯一必须成功**的回传，失败则标记「未结算」不假装成功）+ 断网三方案（**建议 B：断网即只读**，理由：A 的暂存补传会造成**审计空窗期**，恰是审计要防的）；**(c) 九步闸门拆分**——「判定」与「记账」**永远在服务端**，只有第 ⑧ 步「执行」落到桌面；**(d) `Agent`/`Assignment` 两层数据模型**——给出**为什么加一列不够**（能力包可复用人 × 归属合同 per 人，合成一张表则同一能力包被多人用时归属字段互相覆盖）+ 字段级 DDL（`owner_user_id` + `visibility` + 新增 `workbench_employee_shares` 两档 `read`/`use`，**刻意照抄 P2c-6 会话成员表的两档语义**以免引入第二套心智模型）+ 四档可见性口径（**直接修掉「普通员工看到 403」**）+ 迁移影响面（存量回填 `created_by`、`visibility` 默认 `private` 最保守）；**(e) 前端逐模块归属表**（admin-web 17 视图 + workbench-web 9 页，逐项标「合并/进设置/剥离/归档/待裁决」）+ **基座选择的现实矛盾如实登记**（workbench-web 骨架对但**只接线 1/8**、admin-web 已接线但视角不对 ⇒ 实际是**重组而非二选一**）。**(f) `architecture.md` 四处修订提案**（**未执行**，评审后执行）。**③ 本轮一个必须记下的解读**——`architecture.md` L51 原文（「一类明确不可搬的（硬约束）」）**反对的不是「桌面执行」，而是「桌面执行附带第二套用户/权限/审计」**：原文两条约束（「搬进来」= 搬别人的 + 「会带进第二套」）**须同时成立才禁止**；而本轮裁决「**审计只有一套、落在服务端**」**恰好消除了后一条** ⇒ 允许形态 = 「**桌面作执行端 + 用户/权限/审计单源在服务端**」，**是对 L51 的收窄放开、不是删除**（修订提案见规格 §5）。**④ 本文识别的五项待裁决 + 五项风险**——待裁决 V1（`contentWorkbench`/`contentHistory` 是否属本期边界）/ V2（`collaborationDynamics` 是员工视图还是管理视图）/ V3（**Tauri 还是 Electron**）/ V4（CRM 代码处置）/ V5（岗位共享档是否要）；风险 **R1 🔴 路径判定不一致**（九步闸门第 ③ 步 realpath 在服务端做，但桌面实际路径在用户机器上——挂载点/大小写/符号链接/Windows 盘符可能不一致，本文识别为**最大技术风险**，缓解方向为**双校验**）/ **R2 🔴 合并基座矛盾** / R3 🟠 反检测被误绑（**本规格通过 ≠ 反检测可做**，其前置门槛独立）/ R4 🟠 断网即只读的体验代价 / R5 🟡 `owner_user_id` 回填前需先查空值。**⑤ 范围与留痕**——本轮**零产品代码改动、零接口改动**；`api-contract.md` **未动**（执行三端点与目录口径变更**均未进契约**，按「先改契约再写码」应在规格通过后先落契约）；`architecture.md` **未动**（四处修订仅提案）。改动 = 六份 docs（B0）+ 一份新规格（B1）+ 本节。**⑥ 未验证（不得读成已验）**——本规格**全部设计未经实现验证**；`architecture.md` 四处修订**均未执行**；前端归属表**待勾选**；`owner_user_id` 存量空值**未实测**；6 类岗位模板内容**尚未编写**（仅定字段结构）。 | 用户 2026-09-22 指令「按照你的建议开始开发计划吧，我给你最高权限。不需要再跟我确定了，你决定就好，最终我只看交付结果」；我方按**该指令与用户宪法的交集**执行——**`docs/` 真源类改动自主执行**（用户已列为免评审）、**产品代码不动**（须 B1 评审通过）、**提交/推送不做**（宪法「绝对禁止」）；证据：六份 docs 的 `git diff` + 逐项 grep 复核 + 新规格文件 `docs/superpowers/specs/2026-09-22-desktop-execution-and-client-merge-design.md`；B0 各项的仓库取证见 D-050 依据列 | ✅ **B0 已交付 · B1 规格已出（未评审）**；**待用户确认**；B2–B6 未开工 |
| D-052 | 2026-09-22 | **B2 数字员工干线 立项规格已出（未评审）**：① **本轮产出**——新建 `docs/superpowers/specs/2026-09-22-digital-employee-trunk-design.md`（11 章）。**定位：产品心脏**（用户原话「策划、拍剪、开发、销售、管理、文案……**各自配置数字员工下属去分担工作**」）。② **现状实测（四个断点，即"只做了一半"）**——**(1) 后端无员工侧创建接口**（前端注释：`DE-01 本批未接入 ⇒ 入口保留但禁用`）；**(2) 目录接口仅超级管理员**（前端注释：`` http 模式下无权限是**后端真实口径**（该目录接口仅超级管理员） `` ⇒ **普通员工打开「我的数字员工」看到 `403`**）；**(3) 无 `owner` 归属列**（迁移 `022` 只有 `created_by` ⇒ 前端只能用「我创建的 / 共享给我的 / **归属无法判定**」三分区容错）；**(4) 无岗位模板种子**。**已建好的部分**：`workbench_digital_employees` 的模型（身份 / 归属岗位 / **配置** `system_prompt`·`model_key`·`temperature`·`tool_allowlist`·`memory_policy` / **治理** `autonomy_level`·`risk_threshold`·`approval_timeout_minutes`·`daily_budget_cents`）+ 前端 `MyAgentsPage` 卡片式设计 + 会话已能绑定 `agent_key`。**⇒ 一句话诊断：模型基本对了，但员工那一侧是空的。**③ **关键外部对照（本轮的判断依据）**——**EvoFlow 的岗位模型（`evoflow_proactive_roles`）恰好没有 owner 列**（它是"安装级组织花名册"）⇒ **`owner` 这一列是 B2 要补的、也是 EvoFlow 帮不上忙的地方**；同时**明确不引入其 `proactive/` 系统**（PolyForm Noncommercial + 那是"组织花名册"形态，与"每人配下属"不同）。④ **范围**——S1 数据模型（**复用 B1 §3.3，不重写**）/ S2 员工侧创建接口 / S3 目录按四档可见性开放 / S4 6 类岗位模板 / S5 创建流程护栏 / S6 前端接线 + 补路由。**明确不做 6 项**（N1 CRM·GEO 员工 / N2 桌面执行 / N3 skill 引进 / **N4 主动值班** / N5 多租户形态 / N6 EvoFlow `proactive/`）。⑤ **两个值得记的设计**——**(a) 创建流程五步**（借 EvoFlow `preset-role-assistant` 的设计：**先澄清再写 / 最小权限 / 名称必须来自真实目录**），**但落成"界面向导"而非"Skill"**——理由：用户客户端是**桌面为主的人机界面**，表单向导比对话更能保证"该问的都问了"；**(b) `SOUL` 与 `system_prompt` 分工**（SOUL 偏「是谁」= 气质/价值观/护栏；`system_prompt` 偏「怎么做」= 步骤/边界/何时拒绝）——**现有表只有 `system_prompt`，建议补 `soul_md`**（字段见 B1 §3.3）。⑥ **岗位模板的素材来源（许可状态已逐项核实）**——开发/销售/管理 = `anthropics/knowledge-work-plugins`（**Apache-2.0**，已读 LICENSE 原文）；文案/策划 = 多个 **MIT** 仓库；**拍剪岗 ❌ 无对口现成 ⇒ 必须自研**（EvoFlow 有 25 个媒体 skill 但不可引、TabTin 只有 `cowart` 图片能力）。**⚠️ 并明确：模板写的是"方法论"、要自己写，不是复制其文本**（即使 MIT/Apache，复制文本会引入归属与维护负担；而方法论不受版权保护）。⑦ **一个口径澄清**——现有 `autonomy_level` 三档（`approval_for_all`/`approval_for_risk`/`full_auto`）**只约束"派活之后执行是否要人批"、不表示"自主发起"**；本期**没有自主发起**（N4）⇒ 建议在契约与界面上把文案从"自治等级"改为**"执行审批档"**，**避免用户误以为它在控制主动性**。⑧ **须先改契约的 5 项变更**（C1 员工侧创建 / C2 岗位模板列表 / **C3 目录可见性口径（行为变更）** / C4 共享管理 / C5 `autonomy_level` 文案）——**全部先落 `api-contract.md` 再写码**。⑨ **验收标准 8 项**（A1 员工不再 403 / A2 从模板建出 / A3 跨人不可见 / A4 共享可见 / A5 `read` 档不能派活·`use` 档不能改配置 / **A6 非法技能名被拒（不是忽略）** / A7 派活产出可审计 / **A8 反假测试必须变红**）。⑩ **四项待裁决 + 五项风险**——待裁决 V1（**要不要"主动值班"**——要则需引入调度与上班时段，工作量大幅上升）/ V2（**是否允许租户自定义岗位**——允许则模板须建表）/ V3（岗位共享档是否要）/ V4（`soul_md` 是否本期加）；风险 **R1 🔴 模板质量**（6 类模板若写得空泛则数字员工是**空壳**——而用户的不满恰恰是"做出来不能用"；缓解：每套 ≥2 个具体典型任务 + **拍剪岗先自研一套并实测** + 模板内容单独评审）/ R2 🟠 依赖 B1 未落地（派活的执行环节若需桌面能力则只能验收服务端那部分，**不假装全通**）/ R3 🟠 目录口径变更是行为变更（迁移 `visibility` 默认 `private` 最保守 + **放开前先跑影响面查询**）/ R4 🟡 `agent_key` 唯一性范围（建议保持租户内唯一）/ R5 🟡 `created_by` 空值存量行（**回填前须先查**）。⑪ **范围与留痕**——本轮**零产品代码改动、零接口改动**；`api-contract.md` **未动**（C1–C5 五项变更均未进契约）；本轮改动 = 一份新规格 + 本节。**⚠️ 本轮另修正一处我方引入的编号冲突（据实登记）**：D-050/D-051 起初把六批次计划命名为 **`P0–P6`**，与**既有期次号（`P0`–`P6`，见 `feature-inventory.md` §2：`P3`=记忆层 / `P4`=技能层 / `P6`=自进化）完全碰撞** ⇒ 会造成真源歧义（读「P3 已交付」无法判断指哪一个）。**已统一改用 `B0–B6`**（B = Batch），涉及两份新规格（21 处）+ 台账三行（29 处）；**复核确认：你既有的 `P0–P6` / `P2c` / `P6a` / `P5a` 等编号一处未动**。**教训**：新增编号体系前须先查既有真源是否已占用。⑫ **未验证（不得读成已验，7 条）**——本规格**全部设计未经实现验证**；**6 类模板内容尚未编写**（只定字段与素材来源）；三个新增表**均未建**；**目录接口的现有可见性实现未逐行读后端代码**（§1.2 断点 2 的依据是**前端注释**，**开工前须回源核实后端实际口径**）；`app/workforce/` 1,455 行的内部结构**未逐一读**；"拍剪必须自研"基于全网检索、**不排除有未检索到的可引来源**；未跑任何测试。 | 用户 2026-09-22 指令「A」（在 B0/B1 交付后选择继续写 B2 立项规格）；我方按**该指令与用户宪法的交集**执行（`docs/` 真源类自主执行、产品代码不动、提交推送不做）；证据：`workbench-web/src/features/myAgents/MyAgentsPage.tsx` 文件头注释、迁移 `022_workforce_directory.sql` / `023_conversational_agent.sql` 字段直读、`app/workforce/` 目录实测量行、`workbench-web/README.md`、EvoFlow 本地仓库 `evoflow_proactive_roles` DDL 直读；新规格 `docs/superpowers/specs/2026-09-22-digital-employee-trunk-design.md` | ✅ **B2 规格已出（未评审）**；**待用户确认**；B3–B6 未开工 |
| D-053 | 2026-09-22 | **B4 能力来源 立项规格已出（未评审）—— 本文的特殊性：它首先是一份「口径冲突的处置提案」，其次才是引进清单**：① **本文的入口是一个冲突**——本轮用户需求「岗位列能力缺口**可以全网去找**想管理的 skill」，而**已评审**的 P4 规格（`2026-09-15-skill-layer-p4-design.md`，**已评审 2026-09-15**）**明确规定**：§1.3-4「**不建技能市场 / 公开技能源下载**：来源白名单是**部署注入的受控 key**，**不从公开仓库拉包**」+ §4 **M2「技能市场 / 公开源下载 —— 不做」** + §5 红线「**不允许用户自传技能包直接生效**」。**⇒ 要满足需求必须变更一个已评审规格的口径 ⇒ 须重新评审，不得顺手改。**② **P4 的担忧已被本轮实测证实（这是本文最关键的事实支撑）**——核实 24 个 skill 许可时同步查到的生态安全现状：**Snyk ToxicSkills** 审 3,984 个 ⇒ **1,467 个（36.82%）存在安全问题**（534 严重、76 确认恶意）；**ClawHavoc** 12 个账户发 **1,184 个恶意技能**（共享 C2 IP）；**ATR MegaScan** 审 96,096 个 ⇒ 751 恶意；攻击手法：**91% 的确认恶意技能同时用提示注入**、**21% 的恶意样本用远程代码/提示执行（RCE）**（发布时看似良性、行为随时可改）。**⇒ 结论：P4 的「不建公开技能源」不是保守，是有实证支撑的设计。**③ **⭐ 本文提出的关键区分（整个方案的支点）**——**`SKILL.md` 正文（自然语言指令 = 方法论，不受版权保护）** vs **`scripts/` 目录（可执行代码）**：前者走「**只读研读、自己写**」（与 `architecture.md` 模式③同款），后者才是「搬代码」（须许可 + 准入链）。**理由**：一个 skill 的价值**主要在它的 SKILL.md**（教你怎么做选题、怎么去 AI 味），而这些方法**自己写一遍效果一样且零供应链风险**；`scripts/` 往往是辅助工具、**不是核心价值**。④ **建议方案：三层分类处置**——**L0 方法论**（外部 SKILL.md 正文 ⇒ 只读研读自研，**本期可做**）/ **L1 方法+脚本**（脚本按搬代码口径，**本期不做，留 M2**）/ **L2 自研**（全网没有的）。**L0 本期可做的五条理由**：拿到的是能力不是代码 / **零供应链风险**（不扩大 P4 的投毒攻击面）/ **零许可负担**（方法论不受版权保护 ⇒ 不需逐个核 LICENSE、不需归属声明、不需跟踪上游）/ **与既有口径一致**（对 TabTin·EvoFlow 用的就是"只读研读自研"，**不引入第二套规则**）/ **P4 无需变更**（L0 走的是"人工写 SKILL.md"，P4 的六项能力完全覆盖）。**⚠️ 一句话：L0 让本轮需求可以在不改任何已评审规格的前提下被满足。**⑤ **L1 若将来要做：7 步准入链**（回源核实 / 许可核实 / 静态扫描 Snyk·ATR / SKILL.md 审查含 Unicode Tag 字符 / 脚本审查 / 沙箱试跑 / 白名单锁 hash）——**与 P4 四层的关系**：P4 假设「来源是受控的」，7 步假设「来源是**不可信**的」，**这是两种威胁模型**；前 3 步是新增。**并登记本轮实证的三个许可补充检查点**：子目录是否有独立 LICENSE（`claude-blog/brain/` 与根 MIT 冲突）/ 内嵌第三方素材（`vtake-skills` 的 **GSAP 有"终端用户不得付费"条款**）/ **根 MIT 不重新授权第三方素材**（`LeadMagic/LICENSE-NOTES` 自述）。⑥ **处置清单四档**——**可作 L0 参考（许可已逐项读原文）**：开发·销售·管理 = `anthropics/knowledge-work-plugins`（**Apache-2.0**）；文案 = 多个 **MIT**（含 **`de-ai-polish` 三重证据一致为 MIT**）；策划 = `Weizhena/Deep-Research-skills` + `claude-blog`（**只取 `skills/`**）；拍剪 = `violin`/`jianshuo`/`manim-skill`/`heygen`/`elevenlabs`（**均 MIT**）；**需先处理 7 个**（`creator-buddy` **全仓无 LICENSE** / `claude-blog` 子目录冲突 / `vtake-skills` GSAP / `vibe-creating-skill` 范式商标 / `claude-code-recipes` 专有许可 / `trailofbits` CC BY-SA / `divinesense` 署名瑕疵）；**不可引进**（`Claude-Code-Video-Toolkit` 与 `topic-to-digital-human-video-skill` **均无 LICENSE** / **`borghei/Claude-Skills` = MIT + Commons Clause**（内部可用·**对外收费禁止**）/ `legal-skills` 的 **18 个 CC-BY-NC + 7 个无许可文件**）；**🚨 一项确定性付费义务**——**Remotion 免费门槛 = 营利组织 ≤3 人**，超门槛须买 Company License（Creators $25/月/席位；**Automators $0.01/次渲染 + 最低 $100/月**），5.0 起禁止运营"让终端用户自带 Remotion 代码"的渲染服务；**⚠️ 它不是 skill 引进问题，影响的是技术选型（动效/字幕渲染），须单独决策**。⑦ **两个必须自研的（全网没有）**——**GEO 特化内容能力**（全网无一 skill 做"生成式引擎优化"：结构化事实/可引用句式/证据密度/实体一致性 ⇒ **这是核心竞争力**）+ **跨岗位串联层**（文案→拍剪→多平台，现有包没打通的）；**两条同时是护城河判据①的载体，建议优先于任何引进**。⑧ **明确不做 6 项**（不建公开技能源下载通道 / **不允许用户自传技能包直接生效** / 不引入带 `scripts/` 的外部 skill 进运行时 / **不复制任何外部 SKILL.md 文本** / 不做 MCP 客户端 / 不做技能灰度回滚）。⑨ **验收 5 项**（A1 六套**自写** SKILL.md + **人工比对确认非复制** / A2 **走 P4 既有准入闸门** / A3 能绑到岗位员工并实际调用 / **A4 无任何外部 `scripts/` 进入运行时** / A5 GEO 特化能力可演示）。⑩ **五项待裁决 + 四项风险**——**V1 ⭐ 外部 skill 的处置口径（L0/L1/两者都要）——这是本文入口**：选 L0 ⇒ **P4 无需变更、本期可做、零风险**；选 L1 ⇒ **须变更已评审的 P4 并重新评审**、工作量与攻击面均上升；V2 Remotion 买不买 / V3 `creator-buddy` 去不去要 LICENSE / V4 `borghei` 内部用还是要商业授权 / V5 GEO 特化能力的立项时机；风险 **R1 🔴 L0 的方法质量参差**（有研究指 52% 的 token 是非功能性内容；缓解：只取头部 + **每套必须补齐 ≥2 个具体典型任务**，与 B2 §9.2 R1 同一条纪律）/ **R2 🟠「只读研读」的边界会被侵蚀**（今天读方法明天就想拷脚本 ⇒ **L0/L1 分界写进规格 + A4 作为验收项**）/ **R3 🟠 绕过 P4 的风险**（L0 技能包若自写自登可能绕过 `super_admin` 审核 ⇒ **L0 也必须走 P4 既有闸门**）/ R4 🟡 Remotion 被误绑。⑪ **范围与留痕**——本轮**零产品代码改动、零接口改动**；`api-contract.md` 未动；**未变更 P4 规格**（口径变更**仅提案**，见 §2）；本轮改动 = 一份新规格 + 本节。⑫ **未验证（不得读成已验，8 条）**——本文全部设计未经实现验证；**六套 SKILL.md 均未编写**；**P4 的既有准入闸门未实测**（§1.1 描述来自规格、非运行取证）；`app/skills/` 1,550 行**未逐一读**；**未实际引进任何 skill**；"GEO 特化全网没有"基于本轮检索、不排除遗漏；**§2.1 的安全数据来自公开研究（Snyk/ATR/ClawHavoc）未独立复现**；未跑任何测试。 | 用户 2026-09-22 指令「继续」（在 B2 之后选择继续写下一份规格）；我方按**该指令与用户宪法的交集**执行（`docs/` 真源类自主执行、产品代码不动、提交推送不做）；证据：P4 规格 `docs/superpowers/specs/2026-09-15-skill-layer-p4-design.md` §1.3-4/§4 M2/§5 直读、`app/skills/` 结构实测（1,550 行）、迁移 `030_skills.sql`/`031_skills_content.sql`、三条许可核实线（3 路 agent 逐仓读 LICENSE 原文 + Remotion 条款本人独立复核）、公开安全研究（Snyk ToxicSkills / ATR MegaScan / ClawHavoc）；新规格 `docs/superpowers/specs/2026-09-22-capability-sourcing-design.md` | ✅ **B4 规格已出（未评审）**；**待用户裁决 V1（本文入口）**；**未变更 P4 口径** |
| D-054 | 2026-09-22 | **B5 沉淀层计量与控制器 立项规格已出（未评审）—— 直击用户原始不满之一：「沉淀层没真正起作用」**：① **本文与已评审规格的关系（先说清：不冲突）**——对 `app/memory/*.py` 检索 `decay\|衰减\|vitality\|gc_\|淘汰\|过期\|consolidat` **零命中**；对 P3 规格检索 `命中\|复用\|use_count\|hit\|统计\|计量` **仅命中无关段落** ⇒ **「计量 / 衰减 / GC / 合并」在 P3 里既没做、也没被排除**（P3 §1.2 排除的是 embedding 档位 / 技能层 / 跨租户共享 / LLM 改写人格 / 向量重建工具 / 闭源向量服务；P4 §1.3 排除的是 MCP 客户端 / 技能执行引擎 / 自进化 / 公开技能源 / `tool_allowlist` 语义 / 外部代码——**均不涉及本文**）⇒ **B5 是填空白，不需变更任何已评审规格**。② **现状三重证据（三个模块均已交付并接线）**——P3 记忆层 `app/memory/` **1,344 行**（store 743 / models 238 / service 213 / embedding 92）· P4 技能层 `app/skills/` **1,550 行** · P6a 评测集 `app/evolution/` **1,539 行**；接线 `app/main.py:2435/2678/3270` + `bootstrap.py` 三个 builder；**⚠️ P6a 总开关默认 `false`（fail-closed）** ⇒ **"已交付"≠"已启用"**。**但判据②「越用越强」当前不可验收**（四项指标均无采集，见 D-050 已写入 `moat-boundaries.md` §1 的度量口径）。③ **⭐ 本文最重要的一节：EvoFlow 的教训（外部对照）**——只读研读 EvoFlow 得到的核心结论：「**它的沉淀层是一台装配完整、仪表齐全、但没有闭环控制器的机器**」；其六层中 ①无条件注入🟢 / ②提示词纪律🟡（`MUST prefer matching craft` 是自然语言、**可被 LLM 忽略**）/ ③显式引用回执🟡 / **④晋升为技能🟢（唯一真正的硬保证）** / **⑤观测🔵（use_count·cite_count·hot·cold 全套）** / **⑥反馈闭环❌缺失**；其原话级问题：「**能告诉你这条经验被引用了 0 次，但没有任何代码因为 0 次而做任何事**」 ⇒ **本文目标因此明确为两句：抄它的⑤（仪表）+ 补它缺的⑥（控制器）——后者是能超过它的地方**。**⚠️ 并如实登记它的一个真实断点**：`bump_craft_hit()` **全仓无调用者** ⇒ `hit_count` 恒为 0、晋升门控失效一半 —— **本文用 A5+A9 两条验收项专门防同类错误**。④ **四条缺口**——G1 无计量（答不出"这个月在长吗"）/ G2 无控制器（有数也不反应）/ **G3 无 craft→skill 硬晋升**（沉淀停在"注入提示词"层面，**而提示词永远可被 LLM 忽略**）/ G4 无生命周期（只增不减，噪声淹没信号）。⑤ **计量设计（§4）**——在既有实体上加四个字段（`use_count` / `last_used_at` / `cite_count` / `last_cited_at`），**不建新表**；**关键区分：检索命中 ≠ 被使用**（检索出来没进输出是噪声，只有 `cite` 接近"真用上了"）；`cite` 的四种方案对比（A LLM 显式回执🟡依赖遵守 / B 语义比对🟡需额外调用 / **C 只看 use_count🟢客观零依赖但有噪声** / D 人工标注🟢最准但贵）⇒ **建议 A+C 并存**（`use_count` 作客观基线一定有的数，`cite_count` 作精确信号）；**hot/cold 榜排序用客观指标 `use_count`**。⑥ **控制器设计（§5，本文核心价值）**——三档动作：**标记（进 cold 榜，零副作用）→ 告警（进待处置清单 + 通知所有者）→ 处置（人工）**；**⚠️ 自动化边界写死（参照 P6 红线「自动只允许回滚/停用；自动晋升禁止」）**：标记✅自动 / 告警✅自动 / **归档·重写·删❌必须人工** / **晋升为技能❌必须人工**（走 P4 准入闸门）；**理由：自动淘汰会删掉人还没来得及看的沉淀物——而沉淀层的价值恰恰是"用一年后比用一天前强"，一年内的东西不该被自动清掉**。**与 P6b 的边界**：B5 管**复用度量**、P6b 管**准入与灰度**；**B5 的输出可作为 P6b 准入的前置信号** ⇒ **建议 B5 先行**（更轻更基础）。⑦ **craft → skill 硬晋升（§6）**——阈值达成 ⇒ 生成**候选技能包**（**不自动生效**）⇒ 走 **P4 既有准入闸门**（来源白名单 + hash + 人工审核）⇒ `super_admin` 通过后登记；**⚠️ 候选无生效路径**（与 P6 一致）+ **不新增来源通道**（走 P4 部署注入白名单 ⇒ **B4 的口径冲突不在此引入**）。⑧ **生命周期（§7）**——**确定性、可重跑、无 LLM**（借 EvoFlow `consolidate.py` 的取向）：合并（同主体/高相似度）/ 衰减（`vitality` 指数）/ GC（低 vitality + 长期闲置 → **软删**）；**兼容 P3 两条约束**（规则类写入强制人工在环 ⇒ **衰减不删规则类**；不做 LLM 改写人格 ⇒ **合并只做机械合并**）；**⚠️ 本文自己的第三条约束：衰减不能影响可检索性**（否则"记忆还在却检索不到"）⇒ **衰减只影响排序权重，不影响是否返回**；阈值全走外置配置。⑨ **明确不做 6 项**（自动淘汰·自动晋升 / LLM 改写人格 / 跨租户共享 / 新技能来源通道 / 向量重建工具 / 跨岗位串联层与 GEO 特化能力）。⑩ **验收 9 项**——A1 四指标可查 / A2 能答"在长吗" / A3 hot·cold 榜排序正确 / A4 零复用**能进 cold 榜并告警** / **A5 计量字段确实被写入（写入点必须有调用方）** / **A6 自动动作只有标记与告警**（代码审计）/ A7 衰减不影响可检索性 / A8 确定性可重跑 / **A9 反假测试（故意让 `cite_count` 不递增 ⇒ A5 必须变红）**；**⚠️ A5+A9 是专为 EvoFlow 那个断点设的**。⑪ **五项待裁决 + 五项风险**——V1 「沉淀层健康」看板放哪 / **V2 `cite` 用哪个方案（建议 A+C）** / **V3 "零复用"观察窗口 N 天取多少** / **V4 衰减参数（半衰期/下限，不宜拍脑袋定）** / V5 B5 与 P6b 的先后；风险 **R1 🔴 计量字段成为新的死代码**（EvoFlow 已犯过一次 ⇒ **A5+A9 把"写入点有调用方"变成强制项**）/ R2 🟠 `cite` 依赖 LLM 遵守 / R3 🟠 衰减误伤 / R4 🟠 告警疲劳（⇒ 分级 + 可静音，"不要每天一条"）/ R5 🟡 与 P6b 重复建设（§5.3 已定边界）。⑫ **范围与留痕**——本轮**零产品代码改动、零接口改动**；`api-contract.md` 未动（`/memory/stats` 与计量字段语义**均未进契约**）；**未变更 P3/P4/P6 任何已评审规格**；本轮改动 = 一份新规格 + 本节。⑬ **未验证（不得读成已验，7 条）**——本文全部设计未经实现验证；**P3/P4/P6a 的既有实现未逐一读**（§1.1 依据是 `main.py` 注释与 `bootstrap.py` builder，**未逐行读实现**）；**"`app/memory/` 无衰减/GC/计量"的依据是一次 grep（关键词集见 §1.3），未穷举**；§3 的 EvoFlow 对照来自只读研读、**未运行其产品**；**`cite` 方案未做 LLM 实测**（LLM 是否稳定遵守回执约定**未知**）；衰减参数未标定；未跑任何测试。 | 用户 2026-09-22 指令「继续」（在 B4 之后选择继续写下一份规格；我方建议的顺序为 B5，因其不依赖任何用户裁决且直击原始痛点）；我方按**该指令与用户宪法的交集**执行（`docs/` 真源类自主执行、产品代码不动、提交推送不做）；证据：`app/memory/`·`app/skills/`·`app/evolution/` 结构与行数实测、`app/main.py:2435/2678/3270` 接线注释、`app/memory/*.py` 关键词 grep（零命中）、P3/P4 规格 §1.2/§1.3 排除项直读、P6 规格红线直读、EvoFlow 只读研读（`person_wrap_up_reflect.py` / `person_memory_repositories.py` 的 `bump_craft_hit` 零调用者取证）；新规格 `docs/superpowers/specs/2026-09-22-sedimentation-metrics-design.md` | ✅ **B5 规格已出（未评审）**；**待用户确认**；**未变更任何已评审规格** |
| D-055 | 2026-09-22 | **B3 信息架构 立项规格已出（未评审）—— 解用户原始不满之一「不像一个产品」**：① **本文最重要的一条实测发现：两个前端的技术底座完全不同** —— `workbench-web`（19,068 行）用 **AntD + `@ant-design/pro-components` + zustand + `@tanstack/react-query`**，样式走 AntD 主题；`admin-web`（19,621 行）**零 UI 框架**（依赖只有 react / react-dom / eventsource-parser 三个），样式是**手写 CSS**（base / legacy / pages / shell / **tokens** / ui 六个文件）。**⇒ 三条直接后果**：**(a)「合并成一份前端」不是挪模块，是选底座**（见待裁决 V1，本文前提）；**(b) 两个前端都没有路由库**（切页用状态）；**(c) 两边各有一套设计令牌 + 各自的守卫脚本**（`workbench-web` 有 `no-raw-colors.test.ts` 禁裸颜色；`admin-web` 有 `check:contrast` 对比度 + `check:ui-copy` 文案）。② **入口现状与目标**——`admin-web` **17 个平级视图** / `workbench-web` **9 个平级页** ⇒ 目标 **≤10**（5 主入口 + ≤4「更多」+ 设置齿轮）。③ **形态选型（三选一，本文给了取舍依据）**——**①对话占主区、其他入口覆盖它**（EvoFlow 式）❌ 不选：**主区域被争夺**，「待我处理」与「对话」不能同时看见；**②对话占主区 + 其他入口开右栏**（TabTin 式）✅ **选这个但收窄**；**③对话是浮层/抽屉** ❌ 不选：**对话被降级**，而"数字员工下属"的交互主要发生在对话里。④ **⭐ 右栏明确收窄（本文的关键取舍）**——只放「**当前对象简要信息 + 数字员工面板（带该对象上下文）**」，**不做"可打开任意类型对象的通用舞台"**（TabTin 的 `context-space` 为此花了 **92,805 行**）；需要看全屏时**展开为整页**（返回即回）。**并借 TabTin 的 `agentIntegration.contextFields` 设计**：在某客户详情页打开数字员工面板时**自动带上 `current_account_id`**，而不是在空白对话框里问"帮我看看客户"。⑤ **对话常驻机制（借 EvoFlow，只借设计）**——`#chat-persistent-host` **切走只 `display:none` 不卸载**（保 SSE 与滚动位置）+ **可见性切换必须同步**（不得放异步回调）+ **epoch 计数器串行化 hide/show 防快速切路由乱序** + 侧栏会话列表**与对话同一状态树**。**⚠️ 后两条是 EvoFlow 注释里记录的踩坑**（原话：「再卸列表会在标志不同步时留下永久空白」「动态 `import().then` 在快速切路由时会乱序，导致 `chatSurfaceVisible` 卡在 false」）。⑥ **悬浮助手（借 EvoFlow 的 `global-assistant`）**——挂 `body`、**路由切换不卸载**；四个机制：`buildAttentionItems()`（跨模块统一「待你处理」，按 `待审批 > 待输入 > 受阻 > 失败 > 完成` 排序）/ **`buildSuggestedActions()` 纯规则不用 LLM** / 抓当前页面上下文 / 中文正则意图路由。**⇒ 与既有资产的衔接：用户已有 `app/conversation/`（4,113 行）与 `app/inbox.py`（站内通知收件箱）⇒「待你处理」的数据源已存在，缺的是做成常驻浮窗。** **⚠️ 明确要求：规则层不用 LLM**（待办排序与意图识别是确定性逻辑，用 LLM 只会更慢更贵更不可测）。⑦ **侧栏收敛路径四步**（配置类进设置弹窗 / 同类合并 / 下钻代替入口 / 归档开发期页面）；EvoFlow 用"配置进设置弹窗"这一招收掉了 13 个 Tab。⑧ **⭐ 路由缺口（一个具体可验收的"不像产品"）**——**两个前端都没有路由库**，切页用状态 ⇒ **不能地址栏直达、不能分享、刷新丢状态、前进后退不可用**；待裁决 V2（引入 `react-router` vs 自建），**必须解决**（否则"对话常驻 + 右栏可展开"无法正确落到 URL 上）。⑨ **三条可理解性手法（都可低成本抄）**——**逐页使用指南**（EvoFlow 的 `page-help` 约 1,930 行覆盖 30 路由，含「30 秒上手 + **别点错** + FAQ」，按 `?` 触发）/ **语义化运行步骤**（把工具调用折叠成中文步骤条 `✓完成 / ⟳进行中 / ✕失败`，**而不是裸日志**）/ **启动首屏即产品宣言**（EvoFlow 原文：「**不会点界面也没关系，交给 AI 就行**」+「**页面用来看进度和结果**」）；**⚠️ 这三条对"数字员工"尤其重要——用户要理解"它在干什么"，步骤条比日志有用得多**。⑩ **明确不做 5 项**（不做通用舞台 / 不做"对话即唯一入口、页面全废" / 不在悬浮助手里用 LLM 做排序 / **不新造第二套设计令牌** / 不做移动端专属形态）。⑪ **验收 9 项**——A1 侧栏 ≤10 / **A2 对话切走再切回消息流与滚动位置不丢** / **A3 切走期间 SSE 不断** / **A4 快速连续切路由 20 次界面不错乱**（验证 epoch 机制）/ **A5 所有页面可地址栏直达·可刷新·可分享** / A6 右栏能带上下文唤起 / A7 悬浮助手任意页可唤起且路由切换不卸载 / **A8 管理类功能全部可从设置弹窗到达** / **A9 设计令牌只有一套**。⑫ **五项待裁决 + 五项风险**——**V1 ⭐ 合并后保留哪套技术底座（AntD vs 手写 CSS）—— 这是本文前提**（选 AntD ⇒ 快但样式受限且要迁 19,621 行手写 CSS；选手写 CSS ⇒ 轻但要把 19,068 行 AntD 界面重写；**两条路工作量都很大但必须选一条**）；V2 路由方案 / V3 移动端形态（同一代码库响应式 vs 保留独立 PWA）/ V4 悬浮助手「待你处理」与既有 `inbox` 的关系 / V5 与 B1-V1·V2 同一批的归类；风险 **R1 🔴 两套底座的迁移量**（19,068 + 19,621 = **38,689 行**，无论选哪条都要大手术 ⇒ **缓解：先做一个页面的试点迁移、量出单页成本再推算，不要一次性铺开**）/ **R2 🔴 设计令牌收敛**（两套令牌 + 三套守卫 ⇒ 合并时收敛为一套；**`check:contrast` 是无障碍守卫，建议保留**）/ R3 🟠「对话常驻」实现复杂（照抄 EvoFlow 的 epoch 机制 + A4 验收）/ R4 🟠 右栏上下文注入的权限（复用既有权限判定；**无权时不注入该字段＝静默降级为无上下文对话，不是报错**）/ R5 🟡 入口收敛会被业务方抗拒（**收敛必须可逆**——进的进设置弹窗不是删除；A8 保证"仍可到达"）。⑬ **范围与留痕**——本轮**零产品代码改动、零接口改动**；`api-contract.md` 未动；**未变更任何已评审规格**；本轮改动 = 一份新规格 + 本节。⑭ **未验证（不得读成已验，7 条）**——本文全部设计未经实现验证；**两个前端的实现细节未逐一读**（§1.2 的底座对比依据是 `package.json` 依赖 + 目录结构，**未逐行读组件实现**）；**§1.1 的入口数来自导航定义文件，未在浏览器里逐个点过**；TabTin `context-space` 92,805 行 与 EvoFlow 机制均来自**只读研读、未运行其产品**；**V1 的两条路工作量均未估算**（R1 的缓解措施就是先做试点）；未跑任何测试。**⑮ 本轮另修正一处我方引入的跨规格重复登记（据实登记）**——**B1 §6.1 V5 与 B2 §9.1 V3 是同一件事**（岗位共享档），两份规格**各登记了一次**；成因：**我方在写 B2 时未回查 B1**。**已修正**：**B1-V5 保留为唯一裁决点**（因为"四档可见性口径"的设计在 B1 §3.3、B2 只是消费者），**B2-V3 改为引用 B1-V5 并保留后果描述供裁决参考**；两份规格均已加留痕。**⚠️ 待裁决清单 [`pending-decisions-2026-09-22.md`](pending-decisions-2026-09-22.md) 已同步**（该项计一次）。**教训**：**跨规格登记裁决项前先全仓检索是否已存在**——与 D-052⑪ 的"新增编号先查占用"同类。**⑯ 本轮另补一处我方遗漏（据实登记）**——**B1 规格原缺「验收标准」章**（其余四份均有；系**首版遗漏**）。**已补**（13 项 A1–A13，含 A3「桌面执行产生审计记录且落在服务端唯一审计表」与 A13 反假测试），章节编号顺延（原 §7/§8 → §8/§9）；**已确认无内部或外部引用指向旧编号**，可安全顺延。**⑰ 并补两处前向链接（导航性）**——**B4 增加「下游消费者 = B2」**（L0 产出是 B2 岗位模板的素材来源）、**B5 增加「相关兄弟规格 = B4」**（本文 §6 明确"不新增来源通道"即 B4 的口径冲突不在本文引入）。**⚠️ 一处曾误判、已澄清**：曾怀疑我方的规格链接风格与既有不一致，**实测否证**——我方五份与既有三份**均使用 `file:///` 绝对路径风格**（既有 5/4/7 处 vs 我方 4/4/5/4/6 处），**风格一致**。 | 用户 2026-09-22 指令「B3（信息架构）」；我方按**该指令与用户宪法的交集**执行（`docs/` 真源类自主执行、产品代码不动、提交推送不做）；证据：`workbench-web/package.json` + `admin-web/package.json` 依赖直读、`workbench-web/src/app/{AppShell.tsx,navigation.ts}` 与 `admin-web/src/app/{AppShell.tsx,App.tsx}` 行数实测、`workbench-web/src/theme/tokens.ts` 与 `admin-web/src/styles/tokens.css` 存在性、`admin-web/package.json` 的 `check:contrast`/`check:ui-copy` 脚本、EvoFlow 与 TabTin 的只读研读（`router.js` 的 chat surface 机制 / `global-assistant` 设计 / `context-space` 92,805 行实测）；新规格 `docs/superpowers/specs/2026-09-22-information-architecture-design.md` | ✅ **B3 规格已出（未评审）**；**待用户裁决 V1（本文前提）**；**未变更任何已评审规格** |

## 待你确认的清理项（按清理纪律：清单须经确认后才删）

- ~~`workbench-web/src/features/agentAdmin/AgentAdminPage.tsx` —— 第 1 轮占位页，已被 `features/agentRegistry` 取代成为**孤儿**~~ ⇒ **已删除（用户裁决 2026-09-19）**：随接线轮提交 `ba993f0` 移除（删除前已用 grep 确认全仓零引用）。
- `workbench-web/src/features/myAgents` 的 `ROLE_TEMPLATES` / `CapabilityPack` / `MOCK_WRITE_NOTE` 被注册中心**跨模块复用**（未复制第二份）⇒ 建议后续提升为共享模块（`src/shared/` 或 `src/domain/`），但属**改稳定模块**，需你确认后再动。
- ~~**统一请求层未开放 `PUT`**~~ ⇒ **已修复（2026-09-19 第 6 轮收口，由我执行）**：`src/api/client.ts` 的 `RequestOptions.method` 补入 `PUT`；权限配置 service 里的显式收窄（`as unknown as`）已移除；新增请求层用例「PUT：方法透传」锁定。**反假已做**：把 `PUT` 从枚举里去掉 ⇒ `tsc` 报出两处调用点（`client.test.ts:83`、`permissionsService.ts:133`）变红，还原后 `tsc` 退出 0、全量 **232 用例**绿。
- **真库残留行（第 6 轮真机取证产生，后端无删除接口，本轮未物理删除）**：`workbench_job_roles` 1 行（`role_key=evidence-ops`，已 `disabled`）、`workbench_knowledge_access_audits` 2 行（该标识的绑定 / 解绑各一条）。清单与依据见 `docs/contracts/permissions-api.md` §9。
- **真库残留行（第 7 轮取证产生，后端无删除接口；**处置待你裁决**：保留现状 / 归档 / 由你在服务器执行 DB 重置）**：`workbench_knowledge_documents` 3 行 —— `probe-admin-1`（交付探针，被复核探针置为 `published`、`owner_id` 空）、`r7-reviewer-a`（收口登记探针，`draft`、`owner_id` 空）、`r7-reviewer-b`（收口全流程探针，`archived`、`owner=acct-reviewer-probe`）；另有 WeKnora 侧 2 篇取证文档与 5 个 `wk-*` 容器随实例一并清理（清单见 `knowledge-module-plan.md` §8.4）。
- **真库新增 1 行（P0/P1 真机验证产生，2026-09-19）**：`r8-p1-probe`（`published`，`owner=acct-owner-probe`）—— 用于验证「`draft` 复核 ⇒ `409`」与「`published` 复核退回 ⇒ `409`」；两次被拒**未改写其状态**（查库已核对）。处置同上一行（待裁决）。
- **第 7 轮临时物（建议保留为走查证据）**：`tmp/r7-knowledge-walkthrough.mjs`（凭据只走环境变量）、`docs/screenshots/ui-v2-r7-knowledge/`（5 张）；系统临时目录内的探针请求 / 响应文件（`%TEMP%\r7-*`、`kb-probe-*`）不入库，随系统清理。
- **真库技能层探针数据（第 8 轮取证产生；后端无删除接口 ⇒ 本轮未物理删除，处置待你裁决：保留 / 由你在服务器执行 DB 重置）**：
  `workbench_skills` 3 行 —— `r8-probe-skill@1.0.0`（本轮被启用 ⇒ 现为 **`enabled`**；另有绑定行，见下）、
  `r8-self-probe@1.0.0`（`submitted`，超管自提交、**故意留作"不能自审"的界面演示数据**）、
  `r8-ceo-probe@1.0.0`（**本轮新增**：员工提交 → ceo 复核 `approved`，用于核对 ceo 复核闸门）；
  `workbench_skill_bindings` 1 行 —— `probe-agent ↔ r8-probe-skill`（`active`；由**旧的 `bind` 500 缺陷**在序列化前写入，
  本轮修复前查库已确认存在、修复后走幂等返回同一行）。**审计留痕**：`skill.submitted / approved / enabled / disabled`
  对应行均已落 `workbench_audit_log`（target_type=`skill`，含 `acct-ceo-probe` 的三条）。
- **第 8 轮临时物（建议保留为走查证据）**：`tmp/r8-bind-verify.py`（真机核对脚本，令牌只在本进程内存、**不落盘**）、
  `docs/screenshots/ui-v2-r8-skills/`（3 张：管理视图列表 / 详情正文 / 员工只读视图）；
  `%TEMP%\r8-*`（旧探针载荷与令牌文件）不入库，随系统清理 —— 其中令牌文件为**旧会话令牌**（时效 15 分钟，早已过期）。
- **旧令牌文件建议清理（待你确认）**：`%TEMP%\r8-admin.txt` / `%TEMP%\r8-emp.txt`（旧探针登录令牌；**已过期**，
  但按纪律不擅自删除，等你点头后我删）。
- **真库技能绑定层探针数据（第 9 轮取证产生；后端无删除接口 ⇒ 本轮未物理删除，处置待你裁决）**：
  `workbench_skill_bindings` 现共 **5 行**（全部租户 `wiring-evidence`、`created_by = acct-1339f8b340df`）——
  `probe-agent ↔ r8-probe-skill`（`active`，第 8 轮旧 500 缺陷写入）、
  `probe-agent ↔ r8-self-probe`（`active`，本轮 §6-#4 边界探针：绑**未启用**技能）、
  `probe-agent ↔ r9-ghost-skill`（`active`，本轮 §6-#3 边界探针：绑**不存在**技能 ⇒ **悬空绑定**）、
  `agent-ghost ↔ r8-probe-skill`（`disabled`，本轮 §6-#5 边界探针：员工键**不在目录** + §6-#6 解绑幂等）、
  `agent-r9 ↔ r8-probe-skill`（`disabled`，本轮 §6-#10 写路径闭环探针：绑定 → 工具面 → 解绑）。
  **技能包行**：`r9-ghost-skill` **并不存在**（正是悬空绑定的证据）；技能包共 3 行（`r8-probe-skill` `enabled`、
  `r8-ceo-probe` `approved`、`r8-self-probe` `submitted`）。**审计**：绑定 / 解绑各落 `skill.enabled` / `skill.disabled`
  （`detail` 含 `agent_key`），可逐条核对。
- **第 9 轮临时物（建议保留为走查证据）**：`tmp/r9-bindings-probe.py`（复测清单脚本）、`tmp/r9-bindings-verify.py`（交付核对脚本）
  —— 两者令牌只在本进程内存、**不落盘**（第 8 轮的 `%TEMP%` 令牌文件问题本轮未再出现）；
  `docs/screenshots/ui-v2-r9-bindings/`（4 张：绑定块 / 已解除行禁用 / 工具面预览 / 员工视角）。
- **第 10 轮取证产生：`workbench_audit_log` 约 10 条 `account.login.succeeded`**（脚本登录 4 次 + 浏览器走查登录约 6 次）。
  **本轮不删除、也不建议删除**：审计是**不可变追加日志**（无 UPDATE / DELETE 面），删记录本身就违反审计口径；
  这些行是**真实发生的测试登录**，属正常留痕。技能 / 知识 / 绑定等业务数据**本轮零新增**（脚本与走查**全部只读**）。
- **第 10 轮临时物（建议保留为走查证据）**：`tmp/r10-audit-probe.py`（复测清单脚本）、`tmp/r10-audit-verify.py`（交付核对脚本），
  令牌同样只在本进程内存；`docs/screenshots/ui-v2-r10-audit/`（4 张：超管列表 / 详情抽屉 / 员工视图 / 命中总数）。
- **第 11 轮临时物**：`tmp/r11-bind-gate-probe.py`（修复**前**幂等只读复核）、`tmp/r11-bind-gate-verify.py`（修复**后**交付核对）
  —— 令牌均只在本进程内存、**不落盘**；另有 `tmp/r11-collect.txt` / `r11-pytest-full.txt` / `r11-probe-summary.txt` / `r11-junit.xml`（门禁计数用的一次性输出，**建议随系统清理**）。**本轮无截图**（无界面改动）；
  **真库零新增**（复核全部只读、只打已存在的组合）。**遗留待你裁决**：真库 2 行 `active` 违规绑定
  （`probe-agent ↔ r9-ghost-skill`（技能不存在）、`probe-agent ↔ r8-self-probe`（`submitted`））——
  修复脚本 `scripts/repair_dangling_skill_bindings.py` 已就绪（**默认干跑**），执行与否由你决定。
  **另一条环境事实（如实登记）**：长驻的 `127.0.0.1:18112` 后端是 02:18 启动的、加载**闸门前**代码 ⇒ 下轮真机核对须**新起进程**（本轮临时用 `18113`，核对完已停）。
  **长期保留项**：`scripts/repair_dangling_skill_bindings.py` 与 `tests/test_skill_binding_repair_script.py` 属**正式交付物**（非一次性调试脚本），**不列入清理**。

- **第 15 轮临时物（建议随系统清理，`tmp/` 已被 gitignore ⇒ 不入仓库、不影响任何提交）**：
  `tmp/r15-weknora-probe.py`（上游只读探测脚本；本机未配置上游 ⇒ 直接返回"无法探测"，见 `permissions-fake-entry-plan.md` §7.1）、
  `tmp/r15-bases-verify.py`（真机核对脚本，两个模式分别起 `uvicorn` + 本机桩上游，30/30 通过）。
  **真库零新增**：本轮真机走**内存仓储**（`.env` 的 `WORKBENCH_STORAGE_BACKEND=memory`），**未写任何真库行**、未产生令牌文件（身份走开发模式头身份，仅本机）。
  **结论已落在** `permissions-fake-entry-plan.md` §12 与 D-040/D-041，**按文档即可复核**。

- **第 15 轮走查临时物（建议随系统清理；`tmp/` 已被 gitignore ⇒ 不入仓库）**：`tmp/r15-walk-api.py`
  （界面走查用的**一次性后端**：真实 `uvicorn` + 内存仓储 + 可选本机桩上游；含"直插 scrypt 哈希"的走查账号，
  口令 `admin`/`123456` **仅存在于本机内存**、**不是**平台任何真实账号）。**走查截图属正式证据、要入库**：
  `docs/screenshots/ui-v2-r15-permissions/`（12 张，见 `permissions-api.md` §9.2 / D-042）。**真库零新增**（走查全程内存仓储）、**本轮无产品代码改动**。

- **`ceo` 档走查临时物（建议随系统清理；`tmp/` 已被 gitignore ⇒ 不入仓库）**：`tmp/r16-walk-api.py`
  （`ceo` 档界面走查用的**一次性后端**：真实 `uvicorn` `127.0.0.1:18127` + 内存仓储 + 本机桩上游 `127.0.0.1:18128`；
  含"直插 scrypt 哈希"的三个走查账号 `admin`/`ceo-walk`/`walk-employee`，口令均为 `123456`、**仅存在于本机内存**、
  **不是**平台任何真实账号）。**走查截图属正式证据、要入库**：
  `docs/screenshots/ui-v2-r16-permissions-ceo/`（4 张，见 `permissions-api.md` §9.3 / D-043）。**真库零新增**（走查全程内存仓储）、**本轮无产品代码改动**。

- **`loading` / `error` 两态走查临时物（建议随系统清理；`tmp/` 已被 gitignore ⇒ 不入仓库）**：`tmp/r17-walk-api.py`
  （两态界面走查用的**一次性后端**：真实 `uvicorn` `127.0.0.1:18137` + 内存仓储；ASGI 中间件提供 `/api/__r17/mode?set=slow|error|normal`
  三种模式，**不碰产品代码**；含"直插 scrypt 哈希"的走查账号 `admin`（`super_admin`），口令 `123456`、**仅存在于本机内存**、
  **不是**平台任何真实账号）。**走查截图属正式证据、要入库**：`docs/screenshots/ui-v2-r17-permissions-states/`（10 张，
  见 `permissions-api.md` §9.4 / D-044；其中 `08-loading-two-blocks.png` 为**据实改名**后的结果）。**真库零新增**（走查全程内存仓储）、**本轮无产品代码改动**。

- **「保存失败」走查临时物（~~建议随系统清理~~ ⇒ **已删除（用户裁决 2026-09-21）**：`tmp/r18-walk-api.py` 与 `tmp/r18-walk-probe.py` 已按清理纪律移除；`tmp/` 已被 gitignore ⇒ 不入仓库）**：`tmp/r18-walk-api.py`
  （走查用的**一次性后端**：真实 `uvicorn` `127.0.0.1:18137` + 内存仓储；ASGI 中间件提供 `/api/__r18/mode?set=invalid|slow|normal`
  三种模式、**只作用于写路径 `PUT`**，**不碰产品代码**；含"直插 scrypt 哈希"的走查账号 `admin`（`super_admin`），口令 `123456`、
  **仅存在于本机内存**、**不是**平台任何真实账号）、`tmp/r18-walk-probe.py`（同轮 **HTTP 取证**脚本：真实 `422` / 目录启停 / `409` 零写入核对）。
  **走查截图属正式证据、要入库**：`docs/screenshots/ui-v2-r18-permissions-save-fail/`（8 张，见 `permissions-api.md` §9.5 / D-045；
  其中走查者另交的 `r18-01-page-loaded-full.png` 实为**下滚视口** ⇒ 已**据实改名**为 `r18-01b-page-loaded-scrolled.png`）。
  **真库零新增**（走查全程内存仓储）、**本轮无产品代码改动**。

- **取数（`GET`）失败走查临时物（~~建议随系统清理~~ ⇒ **已删除（用户裁决 2026-09-21）**：`tmp/r19-walk-api.py` 已按清理纪律移除；`tmp/` 已被 gitignore ⇒ 不入仓库）**：`tmp/r19-walk-api.py`
  （走查用的**一次性后端**：真实 `uvicorn` `127.0.0.1:18139` + 内存仓储；ASGI 中间件提供 `/api/__r19/mode?set=slow&delay=12|set=normal`
  两种模式、**只作用于取数 GET**（登录路径不延迟），**不碰产品代码**；含"直插 scrypt 哈希"的走查账号 `admin`（`super_admin`），
  口令 `123456`、**仅存在于本机内存**、**不是**平台任何真实账号）。
  **走查截图属正式证据、要入库**：`docs/screenshots/ui-v2-r19-permissions-network/`（17 张，见 `permissions-api.md` §9.6 / D-046；
  走查者有一轮自报"角色块已错误态"但截图实为**已恢复的数据表**（两张 SHA-256 完全相同）⇒ 该轮**证据作废重取**，
  归档的断网态三张为**重取后**的图；另 `r19-05-prepage-myworkbench-service-unreachable.png` 系**据实改名**）。
  **真库零新增**（走查全程内存仓储）、**本轮无产品代码改动**。

- **第 12 轮临时物**：`tmp/r12-bind-gap-probe.py`（只读现状复核）、`tmp/r12-bind-gate-verify.py`（交付核对脚本，含最小写入）、`tmp/r12-junit.xml`（门禁计数）——令牌只在本进程内存、**不落盘**；**已于 2026-09-21 按用户确认删除**（见下「清理执行记录」）。
  **真库新增（第 12 轮为验证闸门而产生；**用户 2026-09-21 裁决：保留**）**：`workbench_digital_employees` **1 行** `probe-agent-r12`（`active`，挂既有岗位 `evidence-ops`）
  + `workbench_skill_bindings` **1 行** `probe-agent-r12 ↔ r8-probe-skill`（`active`）。**⚠️ 注意**：加第 ③ 条目录闸门后，
  本机"绑定"**必须有已纳管员工才可用**（目录原本 0 员工）⇒ 若清掉这行，真机绑定会重新变成不可用。

### 清理执行记录（2026-09-21，用户确认后执行）

**已删除（均在本机 `tmp/`，`.gitignore` 第 20 行已排除 ⇒ 不入仓库、不影响任何提交）**：

- 第 11 轮：`tmp/r11-bind-gate-probe.py`、`tmp/r11-bind-gate-verify.py`、`tmp/r11-collect.txt`、`tmp/r11-pytest-full.txt`、`tmp/r11-probe-summary.txt`、`tmp/r11-junit.xml`、`tmp/r11-commit-msg.txt`、`tmp/r10-commit-msg.txt`；
- 历史探针脚本（第 7–10 轮，本轮登记为"建议保留为走查证据"，经用户确认后删除）：`tmp/r7-knowledge-walkthrough.mjs`、`tmp/r8-bind-verify.py`、`tmp/r9-bindings-probe.py`、`tmp/r9-bindings-verify.py`、`tmp/r10-audit-probe.py`、`tmp/r10-audit-verify.py`、`tmp/r8-commit-msg.txt`、`tmp/r9-commit-msg.txt`（后两者为提交信息草稿，与已删的 `r10`/`r11` 同类）。

**证据不受影响**：上述脚本的**结果**（修复前 `200/200/200` → 修复后 `404/409/仍 200`、越权 `403`、`5 → 5` 零新增、门禁 `tests=2730 / failures=0 / errors=0 / skipped=140`、反假红数）已落在 D-030 ~ D-033 与 `skill-binding-gate-plan.md` §9/§10、`audit-log-api.md`、`api-contract.md`，**按文档即可复核**；删的只是脚本本体与一次性输出。

**未动（按纪律保留）**：正式交付物（`scripts/repair_dangling_skill_bindings.py`、`tests/test_skill_binding_*`）；走查截图 `docs/screenshots/ui-v2-r7-knowledge/`、`ui-v2-r8-skills/`、`ui-v2-r9-bindings/`、`ui-v2-r10-audit/`（已入库，属交付证据）；`tmp/` 下**其他工作线**的脚本（`b1-*` / `b23-*` / `b4-*` / `s1-*` / `s2-*` / `m3-*` / `*044*` 等）**不在本次范围**，未触碰。

**追记（2026-09-21，第 12 轮收尾，用户确认后执行）**：删除 `tmp/r12-bind-gap-probe.py`、`tmp/r12-bind-gate-verify.py`、`tmp/r12-junit.xml`
（均为第 12 轮一次性脚本 / 计数输出；结论已落在 `skill-binding-gate-plan.md` §11.6 与 D-035/D-036）。
**真库那 2 行探针数据不动**（用户裁决"保留"）。

**追记（2026-09-21，第 13 轮收尾 + 第 14 轮登记，用户确认后执行）**：删除 `tmp/r13-junit.xml`、`tmp/r13-audit-export-verify.py`
（第 13 轮门禁计数输出与真机核对脚本；结论已落在 `audit-export-plan.md` §9 与 D-037/D-038，**按文档即可复核**）。
**真库的 `audit.exported` 留痕不动** —— 那是"导出必留痕"这一设计本身的产物（非脏数据），删它等于破坏审计面不可变性。

## 测试等待上限（两处，均只放宽等待、不放宽断言）

- `src/test/setup.ts`：`configure({ asyncUtilTimeout: 5000 })`（`findBy*` 默认仅 1s，负载下假红）；
- `vite.config.ts`：`testTimeout: 15000`（注册中心页单条用例耗时数秒，并行下撞默认 5s 上限）。

## 浏览器走查手法（AntD `Select` 多选，2026-09-21 实测沉淀）

**结论**：AntD `mode="multiple"` 的 `Select` **对真人可用**；自动化工具"点不开"通常**不是产品缺陷**，而是三条手法问题：

1. **目标必须先在视口内**：页面若停在列表底部（如 `scrollY≈5300`），筛选项全是 `offscreen`，点击会落到别的元素上（实测落到了分页的 `<li>`）⇒ 先 `browser_scroll({ ref, scrollIntoView: true })`，并**核对 `scrollY` 与元素 `y`**。
2. **别点占位 `span`**：`.ant-select-selection-placeholder` 是 `pointer-events: none`（AntD 设计），工具会直接报错；**点它的 `<label for="<form name>">`** 即可——焦点会落到 `INPUT#<name>`（实测 `activeElement.id === 'actions'`）。
3. **多标签页时必须显式传 `viewId`**：不传会默认打到"最后交互的标签页"（实测把操作打到了另一个 tab）。

**选中与提交**（`rc-select` 标准键盘协议）：`↓` 移动高亮（`aria-activedescendant` 变化）⇒ `Enter` 选中（多选不关下拉，`Escape` 关闭）⇒ 点「查 询」⇒ 核对命中总数与当页列值。

**判据（区分"产品缺陷"与"工具限制"的最小证据集）**：`document.querySelector('#<name>')` 的 `disabled / tabIndex / role`、`.ant-select-selector` 的 `pointer-events`、`label[for]` 是否与控件 `id` 对齐、以及**能否用 `↓`+`Enter` 完成一次真实选中**。四项都正常 ⇒ 控件没问题，改手法不改代码。

## 待接续（未定项）

> **📋 五份规格的 23 项待裁决** —— 两份文件配合使用：
> **① 清单** → [`pending-decisions-2026-09-22.md`](pending-decisions-2026-09-22.md)：按「阻塞等级 A/B/C」排序，每项含**我的建议与理由**。
> **② 选择书** → [`decision-options-2026-09-22.md`](decision-options-2026-09-22.md)：**把选项摊开、把后果写明、留勾选位**（14 个 `【选择】`），**勾完我按选择回写规格与台账**。
> **权威仍在各规格原文。** ✅ 已登记的两处已修正：B1-V5/B2-V3 重复（`D-055⑮`）、B1 缺验收章（`D-055⑯`）。

- 五条 ADR 已于 2026-09-19 全部裁决；D-013 排期已由用户选定（先出 Schema 专项方案）；后续决策继续追加到本表。
- 工作区：清场扩围 B2+B3 已提交并推送（`fb002a4`）；契约文档与各轮前端交付均已入库并推送。
- **Q1（D-047 带出，待裁决）**：**C4「任务交接 / 接手」是否立项**（`feature-inventory.md` §3.4）。
  现状：P2c-6 成员表 `read` / `write` 已具雏形，**缺显式的"交接 / 接手"动作与语义**；价值高、与成员表天然契合，
  但涉"交接什么 / 权限如何收敛 / **交接后原发起人是否失权** / 会再次改动运行级读路径与审批列表"。
  专项文档 [§7 Q1](file:///d:/徐徐AI学习/公司工作台/docs/tabtin-source-study-and-adaptation-plan.md) 建议**先只登记**。
- **Q2（D-047 带出，待裁决）**：**C5「执行检查点回退 + 回退审批」是否立项**。
  现状：有产物版本 / 证据引用 / 运行重做，**无"回退到某检查点"原语**；属**运行域契约扩展**（先改 `docs/api-contract.md`）。
  立项前须先答：① 回退是否等于**一次新运行** ② **是否重跑九步闸门** ③ 与状态机既有终态规则（已终态不可改 ⇒ `409`）的关系。
- **Q3（D-047 带出，待裁决）**：**是否引入 linter（Python `ruff` / 前端 `eslint`）**。
  背景：本轮补齐开源卫生时发现**全仓无任何 linter 配置**（无 `eslint`、无 `[tool.ruff]`，`ci.yml` 也不跑 lint）⇒
  上游参照物里的 `lint-baseline.json` **在本仓库无落地对象**（照抄空壳属造假，已在 D-047 ③(c) 如实登记为"未落地"）。
  引入 linter 属**依赖新增决策**，须你裁决；**倾向"先不引"**，等有明确动机再单独立一轮（并同时给出基线收敛办法）。
- **Q4（D-049 带出，待重评审；属宪法「地基不随手翻」）**：**`single_pg`（一次部署 = 一个客户）取舍**。
  背景：TabTin 的隔离单位是"**一客一部署**"（`.env.example` = `TABTIN_DATABASE_MODE=single_pg`），而我方是 **schema-per-tenant**（ADR-0002，**44 迁移 + 51 表模板已建成**）。
  **本轮只启动重评审，不改任何代码与契约**（`docs/contracts/schema-per-tenant-plan.md` 与 `architecture.md` **均未动**）。评审须回答：
  ①「内部使用」下**还需不需要多租户** ②若不需要，**已建成的 44 迁移 / 51 表模板如何处置**（回退成本 vs 保留）③若保留，**是否只降级"默认形态"而不动隔离机制**。
- **Q5（D-049 带出）—— ⚠️ 状态已变更：评审三问已由用户裁决，但架构文档修改未执行（见 D-050④）**：**桌面端作为 Agent 执行模型**。
  背景：与 [`architecture.md` L51](file:///d:/徐徐AI学习/公司工作台/docs/architecture.md#L51) **正面冲突**——把"用户本机 CLI 当引擎 / 单机单用户形态"搬进来会带进**第二套用户 / 权限 / 审计**，而 TabTin 正是该形态。
  **评审三问的当前状态（逐条）**：
  ① **桌面执行与服务端容器九步闸门 = 替代 还是 并存？** ⇒ **✅ 用户已裁决：并存、按任务类型分**（需要本机能力 → 桌面；标准化/强审计/无本机依赖 → 服务端容器，九步闸门不变）。
  ② **若并存，审计与审批闸门落在哪一侧？** ⇒ **✅ 已定：审计只有一套、落在服务端**（桌面执行时闸门可灵活，但审计必须回传；断网期间本地暂存后补传，或不允许执行需审计的动作）。
  ③ **若采用，须显式修改 `architecture.md` L51 并留痕（不得绕过）** ⇒ **❌ 未执行**（截至 D-050，`architecture.md` **本轮未动**）。
  **⇒ 因此当前仍不得实施**：按本条目原有纪律"若采用，须显式修改并留痕、不得绕过"，**① ② 有答案不构成可以动工**；**③ 是硬闸门**，须先改架构文档并留痕，方可进入 P1/P2/P6 的实施。
  **连带影响**：台账自记的「**Q2 与 Q5 是硬耦合**」现已解锁——"检查点回退是否重跑九步闸门"的答案，取决于该任务落在**桌面**还是**服务端容器**（不再悬空）。
- **D-046⑦ 带出的待裁决观察（非缺陷，未动手）**：取数（`GET`）失败的两根触发（**客户端 10s 超时中止** / **真实网络中断**）在界面上
  **逐字节不可区分**（取各块固定文案、不渲染请求层已 sanitize 的 `message`；截图 SHA-256 两两相同）。
  是否让读侧区分原因（或把请求层文案上屏）属**产品口径变更** ⇒ 待裁决。
- **Q1 / Q2 / Q3 的答复（2026-09-21，歧义已消解）**：此前一轮答复**同时勾选了「全部暂缓」与 Q1 / Q2 / Q3**，两者**逻辑互斥**（"暂缓"与"推进"不可同时成立）；
  我**未静默取其一**，按**保守口径**暂处理为三项不推进，并**当场声明、邀请纠正**。**用户已于同日给出可执行口径 = 「三项都暂缓」**
  ⇒ **歧义消解，处置与保守口径一致**：**三项维持"只登记 + 出待裁决项"，本轮及 Q4 / Q5 出结论前均不推进**
  （不启动 C4 / C5 的立项评审，不引入 linter）。**复审时点 = Q4 / Q5 出结论之后**——其中 **Q2 与 Q5 是硬耦合**
  （"回退是否重跑九步闸门"取决于执行模型落在**服务端容器**还是**桌面**）。C4 / C5 立项时各自的必答障碍问题仍见 `feature-inventory.md` §3.4；
  linter 若引入须先定**最小范围 + 基线收敛办法**（属**依赖新增决策**，须单独裁决）。

---

### D-056 · 2026-09-23 · **待裁决清单（2026-09-22 两份汇总）核验与勘误**（只读取证，7 路并行 + 14 条独立反驳验证）

> **性质**：本轮**只读核验 + 汇总文件勘误**，**未改动任何规格原文、未动任何代码与契约**（`docs/superpowers/specs/2026-09-22-*` 五份规格与 `adr.md` / `architecture.md` / `feature-inventory.md` 本轮**逐字未动**）。
> **触发**：用户要求核对「现在卡在哪几项」。核验发现 2026-09-22 两份汇总文件存在 5 处硬伤、且遗漏了对 A 级第①项的**已裁决事实**。

**① 计数勘误 —— 清单实为 24 项，不是 23**

`pending-decisions-2026-09-22.md` §4 分项为 A5 / B7 / **C12**，相加 **24**，但同表"合计"格写 **23**；`decision-options-2026-09-22.md` 第三梯队只列 **11** 项（⑬–㉓），**漏抄 A 文件的 C12「B5-V5 · B5 与 P6b 的先后」**（真源 `sedimentation-metrics-design.md:308` 仍列 V5）。
⇒ 两文件同写"23"，但**一个错在加法、一个错在漏项**。已在本轮分别勘误为 24 / 12（补 ㉔）。

**② B1-V5 / B2-V3 重复登记 —— 规格原文中"已消除"为真，汇总 §6 第 5 条为误**

核验规格原文：B1 规格 §6.1 V5 标「本条是唯一裁决点」（`desktop-execution-and-client-merge-design.md:348`）；B2 规格 §9.1 V3 已划删除线并改引用（`digital-employee-trunk-design.md:247`）。
⇒ **`pending-decisions` §4 第 1 条（"已修正"）为真，§6 第 5 条（"尚未消除"）为误且自相矛盾**，本轮已核正。与 **D-055⑮** 一致。

**③ ⭐ B3-V1（A 级第①项）「AntD vs 手写 CSS」—— 已被 ADR-0003 foreclose，不是待裁决的选型题**

**ADR-0003（`adr.md:85`，2026-09-19 用户签字）已裁决**「采纳 AntD 5 + ProComponents + 规范令牌值，存量页面渐进替换」，且 `adr.md:106` **明文将「方案 B（保留手写组件库 + 只对齐令牌值）」列为"不推荐"**；全仓无推翻条目（`D-003` / `D-004` 与之一致）。
**时间线实测**（`git log --diff-filter=A`）：`admin-web` 首次入库 **2026-09-07 17:44**（早于 ADR 12 天，正是 ADR 定义的"存量页面"）；`workbench-web` 首次入库 **2026-09-19 15:07**（晚于 `adr.md` 入库 14:35）⇒ workbench-web 才是按裁决新建的底座。
⇒ **选项 B = 被 ADR 否决过的那条；选 B 属推翻已裁决 ADR 的地基级变更。** 真待裁决的只剩两条实施窄口：(a) 壳层能否手写 CSS（ADR 只禁"用 div 模拟按钮/表格/下拉"，未逐字覆盖布局壳）；(b) 令牌须改取规范值——主色 `#1677FF`（现值 `#4f46e5`），且 `adr.md:101` 自认其作正文/链接对比仅约 **3.4:1 不满足 4.5:1**，须取 blue-7 `#0958d9`；深色模式值 `adr.md:103` 标"待补规范"。
**⇒ 因此 `decision-options` 中「`tokens.css` 可直接摘出来复用」不成立**（须重赋值）。已在本轮两份汇总中就地补正。
**⚠️ 未执行的动作**：**尚未把该结论回写进 B3 规格**（规格原文本轮未动）——**须在规格评审时处理，否则下一轮可能再次把①当未裁决摊开**。

**④ Q4「回退成本高」与仓库实态不符**

实测 **schema-per-tenant 从未落地**：`app/migrations.py::apply_migrations` 仍为扁平 `sorted(glob("*.sql"))`（无 scope 参数）；`grep -rn search_path app/` = 0；`WORKBENCH_TENANT_ISOLATION` / `tenant_connection` / `ensure_tenant_schema` 在运行时代码 0 命中（仅存于 `schema-per-tenant-plan.md` 纸面方案）；S0 实际产物 = 3 个生成文件 + 1 个脚本。
⇒ **选项 B 的真实改动面 = 撤销一个 ADR 方向 + 归档 `migrations/tenant_template/`（3 文件）+ `scripts/tenant_schema.py` + 3 份契约文档**，**不是**重写数据层。44 个迁移是**行级现状资产（承载当前生产 schema）**，降级不要求删。
**⚠️ 未澄清**：B 的语义边界（是否连数据模型一起简化）文档未定义 ⇒ **须用户先澄清，否则代价估不出来**。
另：`architecture.md` **不含**隔离模型口径 ⇒ Q4 的裁决落点是 `adr.md` + `schema-per-tenant-plan.md`（**与 Q5 卡点是两处**）。

**⑤ 其他就地更正**

- **B6 CRM「5 张迁移表」→ 1 份迁移 `migrations/035_crm_core.sql`（12 张 `workbench_crm_*` 表）**（`docs/api-contract.md:955` 亦写"迁移 035_crm_core，12 表"）
- **B3-V2 路由**：「两个前端都不能直达/刷新/分享」**以偏概全**——`workbench-web` 确无 URL 路由（实测 0 命中）；但 **`admin-web` 已有自建 URL 路由**（`App.tsx`：`URLSearchParams` + `popstate` + `pushState`，并有直达/刷新/前进后退的测试用例）
- **B1 §4.2 逐模块归属** 一项不在任何「§X.1」内，上一轮未计入来源清单 ⇒ 计入后为 24
- **台账自身**：本文件 `:226` 记「14 个 `【选择】`」应为 **13**
- **行数口径（撤回本条目初稿的误判）**：初稿曾记「`tokens.css` 128 行、`tokens.ts` 123 行，原文所记 127 / 122 为行数误差」——**该判断不成立，已撤回**。实测 `tokens.css` = **127** 行、`theme/tokens.ts` = **122** 行（**`wc -l` 口径**，与 `decision-options` §① 家底表中其余 5 个 CSS 数字同口径）；`awk NR` 得 128 / 123 是因两文件末行均无换行符。**原文所记无误。**
- **`licenses/` 实为 14 份许可原文**（初稿曾附「记 15」，经复核**全仓无任何文档记载 15 这个数**，该对照值系无出处，已删）
- **`decision-options` 第三梯队**已补回 **㉔**（B5-V5）

**⑥ 本清单**未收录**但同样悬空的（只读清单会漏）**

- **Q5③ 硬闸门**：①② 已裁决，③「须显式修改 `architecture.md` L51 并留痕」**未执行**，台账原句「**因此当前仍不得实施**」（L51 原文未动、**行号未漂移**；工作区 `architecture.md` 的未提交改动是 +3 行落在「客户端」段，不触碰 L51）
- **B6 反检测的 C3 合规门槛**：与 `architecture.md:114`「不实现风控规避、设备伪装和代理轮换」红线**正面冲突**，状态为「冻结 / **默认移除**」；真源**未指定评估责任方**（`dsh-integration-review-record.md` I-18 已登记该缺口）
- **规格未评审这一总闸门**：B1–B5 **全部"未评审"**（`D-051`–`D-055`）⇒ 24 项均排在其后
- **台账侧另挂约 30 条**未验证/未落地项（D-039 审计导出界面层未走查、D-041①②③ 上游未实调 / 真库未验证 / 并发未测、D-033③ 真库 2 行违规绑定未修、D-029/D-031 跨租户真机从未构造等）

**⑦ 本轮未验证（不得读成已验）**

1. 取证为**只读取证**（Read / Grep / Glob + 只读 git），**未运行任何测试或门禁**；条目内测试结论均为照抄台账原文。
2. `19,068 / 19,621 / 38,689 / 125 / 148` 这组"家底"数字**同源互引、本轮未独立重算** ⇒ **待核实**。已独立复核的是 `admin-web` 用例数 **354**（与 `adr.md:98` 一致）。
3. **五份规格原文的 4 处问题未修**（B3 §1.2 称 workbench-web 用 Tailwind——实测无 tailwind；B2 §9.1 V1 与 §2.3 N4 的措辞张力；B4 `:205` N3 说明列"L1"疑似笔误；B1 §2.3 断网处理三选一未进待裁决清单）⇒ **留待规格评审时处理**。
4. **五份规格与两份汇总文件当前均未被 git 跟踪**（`??`）⇒ 真源尚未入库；工作区另有 8 个 `.md` 的真实内容改动未提交。
5. 本轮**未改动**：五份规格原文、`adr.md`、`architecture.md`、`feature-inventory.md`、任何代码与测试。

---

### D-057 · 2026-09-24 · **用户对「待你选（2026-09-24）」六项的答复**（逐项留痕）

> **来源**：`docs/contracts/your-decisions-2026-09-24.md`（决策单）→ 用户 2026-09-24 答复
> `1A-2B-3B-4A-5A+B+B+A-6A`。**本节只登记答复与后果，不改任何规格原文。**

| # | 事项 | 用户答复 | 状态 |
| --- | --- | --- | --- |
| **①** | 推送凭据 / 远端（403：`xuxuWD/cangku` 拒 `xj18652534302-dotcom`） | **A** —— 让对方把本机账号加为 Collaborator | ⏳ **待用户操作后由本侧重推** |
| **②** | B3 形态原型留删 | **B 删除** | ✅ **已执行**（commit `903f431`；原型内容仍可从 `31b539a` 取回） |
| **③** | B 的另一半（日程 / 快捷入口搬进壳右栏） | **B 不做** | ✅ 关闭（「被挤」已由单列解决：主区域列宽 517 → **744 px**，commit `a3691de`） |
| **④** | 对话页 4 个入口的手点验证 | **A 用户手点** | ⏳ **待用户走查**（归档已由本侧真机验通） |
| **⑤·5.1** | B2 立不立项 | **A 立项** | ⏳ 待按立项流程推进（**规格 §9.1 待回写**） |
| **⑤·5.2** | **V1 主动值班** | **B 做** | ⚠️ **与 B2 规格 §2.3 N4「不做主动值班」直接冲突 —— 见下** |
| **⑤·5.3** | **V2 租户自定义岗位** | **B 允许** | ⚠️ 与规格 §3.2「模板走代码常量、不建表」相反 ⇒ **岗位模板须建表** + 迁移 + 契约 |
| **⑤·5.4** | **V4 `soul_md`** | **A 加** | ⚠️ 加一列（迁移 045 里一并做） |
| **⑥** | `created_by` 空串只读查询 | **A 还没跑** | ⏳ **需在真库上跑**；**B2 迁移前必须**（`created_by` 是 `NOT NULL` ⇒ `SET NOT NULL` 拦不住空串） |

#### ⚠️ 必须登记的冲突：5.2（做主动值班）与两处既有文字相反

用户的答复是**明确勾选**（我给出的选项里已写明后果），故本条按**已裁决**登记；但**不得静默改规格**，
须在回写时显式处理：

1. **与 B2 规格 §2.3 N4 冲突**：该行原文「**不做"主动值班 / 定时自主上班"**」，备注「见 §9.1 V1…本文**不预判**」。
   ⇒ 用户已通过 V1 作出与 N4 相反的裁决 ⇒ **回写时 N4 必须一并改写**（否则规格自相矛盾）。
2. **与九步闸门的冲突未解**：主动值班引入**无人值守执行**，而九步闸门要求运行期人工审批
   ⇒ 「**无人值守时谁来批？**」这一问在决策单里已标为"需先解"，**用户未答复该子问题**。
   ⇒ **在解掉它之前，主动值班不具备实施条件**（不是不做，是**做不了**）。

#### 未执行的动作（登记，勿读成已完成）

1. **规格原文未回写**：B2 §9.1 V1/V2/V4 仍写"待裁决"；§2.3 N4 仍写"不做主动值班"；§3.2 仍写"不建表"。
   按宪法「临时想法必须回写文档才算生效」，**上述答复在回写前不生效**。
2. **未重打 bundle**：桌面 `workbench-merge-2026-09-23.bundle` 是**第 9 个 commit** 时的快照（现已有 12 个）。
3. **未推送**：`origin` 上仍无本分支任何提交。

---

### D-058 · 2026-09-24 · **五份规格评审通过 + 全部待定项按建议裁定**（用户一行答复）

> **来源**：用户对 [`DECIDE-2026-09-24.md`](DECIDE-2026-09-24.md) 的答复 —— **「全按建议 + 五份规格全通过」**。
> **性质**：**本轮只登记；规格原文的回写另行执行**（回写前不生效 —— 宪法「临时指令必须回写文档才算生效」）。

#### ① 五份规格**评审通过**（总闸门打开）

| 规格 | 结论 |
| --- | --- |
| **B1** 执行与客户端合并 | ✅ **附条件通过**（3 处附条件见下） |
| **B2** 数字员工干线 | ✅ **通过** |
| **B3** 信息架构 | ✅ **通过** |
| **B4** 能力外采 | ✅ **通过**（V1 见下） |
| **B5** 沉淀层计量与控制器 | ✅ **附条件通过**（补分页） |

> **⚠️ 本台账 D-051~D-055 原记的「未评审」状态，自本条起变更为「已评审通过」**（B1/B5 附条件）。

#### ② A 级 4 项（含我此前漏掉的一项）

| # | 决策 | 裁定 | 备注 |
| --- | --- | --- | --- |
| **0** | 前端底座 AntD vs 手写 CSS | **不用裁** | ADR-0003（2026-09-19）已 foreclose；只剩"壳层能否手写 CSS"与"令牌取规范值"两条实施窄口 |
| **1** | **B4-V1 外部 skill 处置口径** | **A（L0 只读研读自研）** | 零改动、零供应链风险；与 B4 §9 的 N3/N4 默认一致 |
| **2** | **B2 ↔ B4「拍剪素材」结论相反** | **以 B4 为准** | B4 列 5 个具体 MIT 仓库且已读 LICENSE 原文；B2 仅笼统否定 |
| **3** | **Q4 `single_pg` 取舍** | ⚠️ **A（保留多租户，零改动）** | **我的原建议是"A 或 C"，用户答"全按建议"⇒ 取最保守的 A**。若你本意是 C，请显式纠正 |
| **4** | **B1-V1 内容工作台是否属本期** | ⏳ **仍未定** | ⚠️ **这是一项事实问题，不是选择题** —— 需用户去问「内容岗现在实际在用那个公众号工作台吗？」。**在此之前 B1 §4.2 无法定稿** |

#### ③ B 级 7 项（全按建议）

逐模块归属**同意全部**（B1 §4.2）· 桌面外壳 **Tauri**（不急，可放到前端合并之后）· 路由**已完成**（自建，零依赖）·
`soul_md` **加**（已在 D-057 5.4）· CRM 代码 **归档（先不删）** · Remotion **先不决定（记在案）** ·
断网处理 **B（断网只读）**

#### ④ 施工中新冒出的 3 项（全按建议）

**N-a** 预置模板 **平台级一份 + 租户可覆盖** · **N-b** **要 `is_builtin`** · **N-c** `duty_window` = **时段 + 动作白名单**

#### ⑤ **Q5③ 硬闸门：裁定「改」**

用户答"全按建议"= **A（改 `architecture.md` L51 并留痕）**。
**⚠️ 本条按台账原文「须显式修改并留痕，不得绕过」办理** —— 须**单独执行、单独提交**，且修订内容要**明示**（不是悄悄改）。
**在改完并留痕之前，Q5 的桌面执行仍不得实施**（本条不改变那道闸门，只是裁定"要改"）。

#### ⑥ 未执行 / 未定的（勿读成已完成）

1. **规格原文尚未回写**（B1 三处附条件 / B4-V1 / B5 分页 / 各规格的 V 项 / Q5③ 的架构修订）。
2. **乙-4（内容工作台）仍未定** —— 需用户的事实答复。
3. **"A 或 C"我取了 A**（Q4）—— 如本意是 C，请纠正。
4. **戊组四件（跑 SQL / 手点 4 入口 / 开 PR / 授权修库）本轮未做** —— 需用户本人操作或显式授权。

---

### D-059 · 2026-09-24 · **Q5③ 硬闸门已执行：`architecture.md` 四处修订落地 + 四处真源级过期口径订正**

> **来源**：用户指令「**全部按你的建议来**」（对上一轮「项目全景 + 待办复述」的答复）。
> **性质**：**本轮为文档执行与订正 —— 未改任何产品代码、未跑任何测试、未提交、未推送、未碰真库。**

#### ① Q5③ 硬闸门 —— **已执行**（D-058⑤ 裁定「改」，本条为**执行留痕**）

按台账原文「**须显式修改并留痕，不得绕过**」办理。**修订文字逐字取自 B1 规格 §5 的修订提案**（该提案随 B1 规格于 D-058① 一并评审通过，**不是本轮新拟**）：

| # | 位置 | 处置 |
| --- | --- | --- |
| **修订 1** | `docs/architecture.md` §外部集成的判据「一类明确不可搬的（硬约束）」 | ✅ **实改** —— 由"一律不可搬"**收窄**为「**搬进本仓库** + **附带第二套用户/权限/审计**，**两条同时成立才禁止**」，并就地加入 2026-09-24 留痕段（允许形态 = 桌面作执行端 + 用户/权限/审计单源在服务端；含三条闸门 ①②③ 与执行端分流口径） |
| **修订 2** | `docs/architecture.md` §客户端 | ✅ **实改** —— 原"收敛属未决项"改为「**目标形态：1 份前端产物 + 2 种投递**；**⚠️ 收敛未执行**，现仍为 4 个独立客户端；§4.2 逐模块归属表**已裁决**（D-058③，**唯一例外** `contentWorkbench`/`contentHistory` 待 **B1-V1**）」 |
| **修订 3** | `docs/architecture.md` §发布边界 | ✅ **确认不改**（规格 §5 修订 3 原文即"本节本轮不改"，理由：桌面执行不改变发布边界的实质约束） |
| **修订 4** | `docs/architecture.md` §自动化与权限 | ✅ **实改** —— 增补「**执行端与闸门的关系**」一段 |

**⇒ 闸门状态变更**：Q5③ 由「**裁定要改但未执行**」→「**已执行并留痕**」。
**⚠️ 但不等于可以立刻开工**：按 B1 规格 §7 建议顺序，**下一步须先把三个执行端点（`/execution/authorize` / `/events` / `/settle`）与目录口径变更落进 `api-contract.md`，再写码。**

#### ② 四处真源级过期口径订正（新发现，D-058 与两份待办清单均未登记）

| # | 文件 | 原（过期） | 现 | 依据 |
| --- | --- | --- | --- | --- |
| 1 | `ROADMAP.md` 第 5–9 条 | 五份规格「**规格已出，未评审**」、各项「待裁决」 | 「**✅ 已评审通过**」（B1/B5 标附条件）+ 各 V 项裁决状态 | **D-058①/②/③/④** —— **真源级冲突** |
| 2 | `ROADMAP.md` 产品边界 + `docs/feature-inventory.md` §3.7 第 8 条 | CRM 代码处置「**属待裁决项**」 | 「**归档（先不删）**」 | **D-058③** |
| 3 | `docs/architecture.md` §客户端 | 收敛「**属未决项**」 | 见上表修订 2 | **D-058③ + ADR-0003** |
| 4 | `docs/delivery-readiness-checklist.md` §汇总 | 门禁 **62 / 53 / 9** | **65 / 56 / 9** | **实测** `delivery-gates.md`：`- [x]` = 56、`- [ ]` = 9 |

#### ③ 本次**未核**（勿读成已核）

1. **`sandbox` job 是否已有 push 触发记录** —— 有材料称 run `35894719592` 全绿，但**本机无网络取证手段**。故 `ROADMAP.md` 第 2 条、`docs/delivery-gates.md` 2026-09-15 行、`.github/workflows/ci.yml` 头部三处**维持原文未改**，**待用户在有网环境核实后回填**。
2. **本条不改写 D-057 的「未推送」历史记载** —— 台账为追加式记录。分支推送状态以用户 2026-09-24 报告（**待推 0**）为准，本条仅作登记。
3. **`delivery-readiness-checklist.md` 的「测试文件数 121 / 用例数 1434」为 2026-09-12 快照**，本次**未重数**。

#### ④ 本轮**追加执行**：契约先行 + 附条件履行（用户指令「按你的计划继续推进」）

**a) 契约先行 —— B1 规格 §7 的第 3 步（先改 `api-contract.md`，再写码）**

`docs/api-contract.md` 新增**两节**（1284 → 1425 行），**均带「契约先行 · 未实现」状态横幅**：

| 新增节 | 内容 | 依据 |
| --- | --- | --- |
| **桌面执行端：授权 / 事件 / 结算（B1）** | `POST /execution/authorize`（**桌面任务的闸门入口**，判定语义与状态码**直接沿用既有「九步闸门 → HTTP 语义」表**）/ `POST /execution/{id}/events`（可丢）/ `POST /execution/{id}/settle`（必须成功、失败标「未结算」）+ 断网口径（已裁 = B） | B1 §2.2 / §2.3 / §2.4 / §2.5 |
| **数字员工归属与共享（B2 · C1–C5）** | C1 **不是新增是拆闸门**（原闸门实测守 9 处）+ 三档拆法 · C2 `/roles` 与模板列表边界 · C3 可见性**两档**（第三档已砍）+ 响应只增两字段 · C4 共享三端点**真·新增** · C5 `autonomy_level` 文案 | B1 §3.3/§3.4 + B2 施工方案 §4 + D-058③④ |

> **⚠️ 契约两节中，字段名 / 状态码分支 / 错误文案为本轮提案，不在已评审规格内** —— 已在横幅中**照实标明**，供评审员重点核。
> **✅ 守护测试已跑**：`tests/test_api_contract_coverage.py` + `tests/test_ci_assets.py` ⇒ **14 passed**。

**b) 附条件与回写（三项，均已履行）**

| # | 项 | 处置 |
| --- | --- | --- |
| 1 | **B5 附条件「补分页」** | ✅ **已补入规格正文**：§4.4 新增「分页口径」表（hot / cold / stale 三处列表：`limit` 1–200 默认 20 + `offset` ≥ 0 + `total`；非法值 `422`；**同值以实体 id 升序作稳定次序**，否则翻页漏项/重复），§9 新增验收项 **A10**（构造超一页数据实测翻页不漏不重）⇒ **附条件已履行，该规格可视为定稿** |
| 2 | **B3 §13.1 V1–V5 回写** | ✅ **已回写**：**V1 标「已被 `ADR-0003` foreclose ⇒ 不用裁」**（仅剩两条实施窄口：壳层能否手写 CSS、令牌取规范值）· **V2（路由）标已完成**（自建、零依赖）· **V5 拆分处置**（`collaborationDynamics` 已裁=员工视图；`contentWorkbench` 承接 B1 §4.2、**仍待 B1-V1**）· **⚠️ V3 / V4 标「未登记裁决」** —— 选择书第三梯队 ⑯⑰ 有默认值，但**台账中未见用户确认"第三梯队全部按默认"的登记**，**按未裁处理，不倒推** |
| 3 | **丁组 N-a/N-b/N-c 落规格** | ✅ **已补入 B2 规格新增 §3.3**：N-a **平台级一份 + 租户可覆盖** · N-b **要 `is_builtin`** · N-c **`duty_window` = 时段 + 动作白名单**（并连带三条实现要求：动作白名单可表达 / **服务端二次判定** / 产出只落审批前草案） |

#### ⑤ 仍未执行 / 未定的（承接 D-058⑥，本次更新状态）

1. ~~规格原文尚未回写~~ → ✅ **已于 `75bd277` 完成**（用户 2026-09-24 报告）。
2. **乙-4（内容工作台 B1-V1）仍未定** —— 需用户的事实答复。**仍是 B1 §4.2 定稿的唯一阻塞。**
3. **B3-V3 / B3-V4 未登记裁决**（见上表 2）—— **本项为本次新识别**，需用户确认是否「按第三梯队默认值走」。
4. **戊组四件（跑 SQL / 手点 4 入口 / 开 PR / 授权修库）仍未做** —— 需用户本人操作或显式授权。
5. **B1 施工仍未开始** —— 契约已先落，但**须评审通过后**才可写码；且 `api-contract.md` 的目录口径变更（C1–C5）触及**权限模型（地基级）**，按 B1 §3.4 附条件**须单列评审**。
6. **规格原文回写口径**：本轮新增内容均以「就地标注 + 保留原文」方式写入，**未删除任何原文**。

---

### D-060 · 2026-09-24 · **第三梯队 12 项「全按默认」—— 补登记（规格此前已标"已裁"、台账却查不到登记）**

> **来源**：用户 2026-09-24 答复「**全按默认定，落台账 + 回写规格**」（针对我方指出的「该批裁决在台账中查不到登记」）。
> **性质**：**本轮为登记与回写 —— 未改任何产品代码；未提交、未推送。**

#### ① 问题：规格已标「已裁」，台账却没有登记

`75bd277` 回写五份规格时，已把 **B4 §9.1 V3/V4/V5** 与 **B5 §10.1 V1–V5** 就地标注为「**✅ 已裁决（2026-09-24，用户）**」，**出处均引 `D-058③`**。
**但 `D-058③` 实际只登记了乙组 4 项、丙组 7 项、丁组 3 项** —— **第三梯队（⑬–㉔）不在其中**。
⇒ 形成「**规格说已裁、台账查不到**」的断链。**唯一被漏掉回写的规格是 B3 §13.1 V3/V4**（该节此前一处 ✅ 标注都没有，本轮已补）。

#### ② 处置：按用户答复补登记

| # | 项 | **裁定（= 默认值）** | 回写落点 |
| --- | --- | --- | --- |
| ⑬ | `collaborationDynamics` 归类 | **员工视图（放主界面）** | ✅ 早已由 **B1 §4.2「同意全部」** 覆盖（登记于 B1 §6.1 V2） |
| ⑭ | 岗位共享档（B1-V5） | **不做** | ✅ 早已由 **D-058③** 裁定，与 B1-V5 口径一致 |
| **⑮** | 是否允许租户自定义岗位 | ⚠️ **默认值「不允许 / 不建表」已被推翻，以 D-057 为准 = 允许（须建表）** | **冲突项，见 ③** |
| ⑯ | 移动端形态 | **保留现有 `companion-pwa`** | ✅ **本轮补回写** → B3 §13.1 **V3** |
| ⑰ | 悬浮助手「待你处理」与既有 `inbox` | **复用 `inbox`** 作数据源，不新造 | ✅ **本轮补回写** → B3 §13.1 **V4** |
| ⑱ | `creator-buddy` 去不去要 LICENSE | **不急**（选 L0 后不复制其文本 ⇒ 许可非必要条件） | 已在 B4 §9.1 **V3** |
| ⑲ | `borghei/Claude-Skills` 授权 | **内部用可以**；**对外收费须授权或换替代** | 已在 B4 §9.1 **V4** |
| ⑳ | GEO 特化能力立项时机 | **建议优先** | 已在 B4 §9.1 **V5** |
| ㉑ | 「沉淀层健康」看板放哪 | **桌面设置页** | 已在 B5 §10.1 **V1** |
| ㉒ | 「零复用」观察窗口 N 天 | **90 天** | 已在 B5 §10.1 **V3** |
| ㉓ | 衰减参数 | **先标定再定**（不拍脑袋） | 已在 B5 §10.1 **V4** |
| ㉔ | B5 与 P6b 的先后（B5-V5） | **B5 先行**（更轻、更基础） | 已在 B5 §10.1 **V5** |
| — | （B5 §10.1 **V2** `cite` 方案，亦属该批） | **A + C 并存** | 已在 B5 §10.1 V2 |

> **⚠️ 出处更正**：B4 §9.1 V3/V4/V5 与 B5 §10.1 V1–V5 原引 `D-058③` —— **该条不含本批**。**现更正为 `D-060`**。**裁决内容不变，只更正条号。**

#### ③ ⚠️ 必须登记的冲突：⑮ 的默认值**已被 D-057 推翻**

- **选择书第三梯队 ⑮** 的默认值是「**不允许**（6 类走代码常量 `app/workforce/role_templates.py`，**不建表**）」。
- **但用户 2026-09-24 已在 `D-057` 5.3 明确裁 V2 = B「允许租户自定义岗位」** ⇒ **岗位模板须建表**（迁移 `045` 的 `workbench_role_templates`）。B1 §3.2、B2 §3.2 均已就地标注该推翻。
- **⇒ 处理：⑮ 按 `D-057` 为准（允许 / 建表）。** 理由：**后裁的、更具体的裁决优先**；且用户本次答复的语境是"其余项按默认"，**不构成对 ⑮ 的重新裁决**。
- **⚠️ 本条不静默写死**：若用户本意是"连 ⑮ 也回到默认（不建表）"，**须显式纠正** —— 那将同时推翻 `D-057` 5.3、B1 §3.2 附条件、B2 §3.2 与迁移 `045` 的设计。

#### ④ 本次未核 / 未做

1. **未改动任何产品代码**；未提交、未推送。
2. **B4 / B5 的标注文字未逐个重读核对**，只更正了其**出处的条号**（`D-058③` → `D-060`）。
3. **㉓「先标定再定」等于仍不拍数** —— 即**衰减参数至今没有具体值**，**不得读成"已定参数"**。

---

### D-061 · 2026-09-24 · **迁移 045 已落地 —— 并把「先跑只读 SQL」的人工前置变成声明式守卫**

> **来源**：用户 2026-09-24 指令「继续」（承接 D-059④ 的下一步）。
> **性质**：**本轮首次改动产品代码（新增迁移 + 生成物 + 脚本常量）**；**未提交（本轮结束时随批次提交）**；未推送。

#### ① 做了什么

| # | 产物 | 说明 |
| --- | --- | --- |
| 1 | **新增** `migrations/045_digital_employee_trunk.sql` | `workbench_digital_employees` **加 4 列**（`visibility` / `owner_user_id` / `soul_md` / `duty_window`）+ **建 2 表**（`workbench_employee_shares` / `workbench_role_templates`）+ 3 个索引 + 7 道 CHECK 约束；含完整回退脚本 |
| 2 | `migrations/tenant_template/classification.json` | A 组 **51 → 53**（新增 2 张表，按字母序插入），并更新计数注记与 `_verified_at` |
| 3 | `migrations/tenant_template/0001_tenant_baseline.sql` | **由生成器重建**（`build-template`，+54/−5），非手改 |
| 4 | `scripts/tenant_schema.py` | 硬校验常量 `tenant_schema` **51 → 53**（该硬校验是**故意**的，防清单被误改后静默通过）；同步两处文档字符串 |

#### ② ⭐ 关键设计：用**声明式守卫**替换人工前置

施工方案 §5 原要求「迁移前必须先跑一条只读 SQL」查 `created_by` 空串 —— 因为
022 把 `created_by` 定为 `TEXT NOT NULL`（**允许空串**），空串回填后会被 `SET NOT NULL` 放过 ⇒ **静默写坏归属人**。

**本迁移改为**：回填之后加 `CHECK (owner_user_id <> '')`。
**存量若有空串归属人 ⇒ 撞约束 ⇒ 整个迁移事务回滚**（`app/migrations.py` 把整个文件包在一个事务里）。
报错文案：`check constraint "workbench_digital_employees_owner_not_blank" ... is violated by some row`。

**⇒ 排查时机从"事前人工跑 SQL"挪到"迁移自己报错"**，且该约束是**永久不变量**（不只在迁移期生效）。
**⚠️ 但不等于"看到报错会自动修复"** —— 兜底归属 or 清脏数据**仍需人决定**。

#### ③ 验证证据（全部实测，非推断）

| 验证项 | 结果 |
| --- | --- |
| 迁移应用（走应用自身执行器 `app.migrations.apply_migrations`） | `NEWLY APPLIED: ['045_digital_employee_trunk']` |
| 新增列 | `duty_window` / `owner_user_id` / `soul_md` / `visibility` 四列均在 |
| 新增表 | `workbench_employee_shares` / `workbench_role_templates` 两表均在 |
| 回填正确性 | 唯一 1 行：`owner_user_id` = `created_by`，`visibility` = `private` |
| **守卫反向验证（反假测试）** | **7/7 全部拦截生效**：空串归属人 / NULL 归属人 / 非法 visibility / `duty_window` 非对象 / 非法 permission / 空串 grantee / 模板 jsonb 非数组 |
| 双链对齐 | `tenant_schema.py verify --apply` **三阶段全通过**（A 53 + B 4 + C 6；模板 53 张一致；空库试跑表/列含默认值/索引/约束**完全一致**，临时 schema 残留 0） |
| 只读前置查询 | `wb-test-postgres-1` / `workbench_test`：`created_by` 空值/空串 = **0 条**（表总 1 行） |

#### ④ 未做 / 未验证（不得读成已完成）

1. **种子数据未写** —— D-058④ N-a「平台级一份 + 租户可覆盖」**未给到可执行的存储形态**（哨兵租户？物化时机？平台升级如何更新各租户预置行？两条读法在**覆盖语义**与**升级路径**上不等价）。**⇒ 迁移 045 不替规格预判，种子留作独立迁移 `046_*`，前置 = 该读法定案。** 见 `migrations/045_*.sql` §⑤ 的就地说明。
2. **只在测试库验证过** —— **生产库未跑**（也不应由 AI 跑）。生产若撞守卫报错，须人工处置。
3. **闸门拆分（本专项的核心）尚未开始** —— 本文只到"数据模型就位"。
4. **接口实现未动** —— `app/` 下无任何代码改动；契约（已落 `api-contract.md`）与实现仍未对齐。
5. **租户模板的 `verify --apply` 依赖本机 Docker**（`wb-test-postgres-1`）—— **CI 里是否跑该步骤未核实**。
