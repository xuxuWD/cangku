# 「技能绑定校验」专项方案（第 11 轮）

> 状态：**v4 · 2026-09-21 · 第 12 轮已交付（③A / ④A / ⑥A 落地 + ⑦ 维持；见 §11.6 交付记录）**。
> **唯一权威在别处**：权限口径 = [`permission-matrix.md`](file:///d:/徐徐AI学习/公司工作台/docs/contracts/permission-matrix.md)（§3「技能」两行 + §13 绑定面补登记）；
> 绑定面契约 = [`skill-bindings-api.md`](file:///d:/徐徐AI学习/公司工作台/docs/contracts/skill-bindings-api.md)（§2 三条服务端事实 / §5-#1 待裁决 / §7 安全要件）；
> 后端契约真源 = `docs/api-contract.md`（**改接口先改它**）。
>
> **证据级别**：本版为**源码判定 + 第 9 轮真机证据**（见 §1 证据链）；**复测列为写码第一步**（§6）。

## 1. 背景：被登记三轮的同一个缺口

第 9 轮真机复测**实测**（工作树后端 + 真库，仅本机非生产）——`POST /api/v1/skills/bindings` **三条校验全缺**：

| # | 事实 | 真机证据（2026-09-20） |
| --- | --- | --- |
| 1 | **不校验技能是否存在** | `{"skill_key":"r9-ghost-skill"}` ⇒ **`200`**，且**查库确认**写入该行 ⇒ **悬空绑定** |
| 2 | **不校验技能是否 `enabled`** | 绑 `r8-self-probe`（`submitted`）⇒ **`200`**（工具面不放大：展开侧只取 `enabled`） |
| 3 | **不校验 `agent_key` 是否在数字员工目录内** | 绑 `agent-ghost` ⇒ **`200`**（本租户目录实测 `total: 0`） |

⇒ 第 9 轮用户裁决「**本轮不改**（属业务逻辑改动，超出"读端点 + 界面"范围）」并**明确登记为待专项**（契约 §5-#1、`decision-log` D-029）。
界面只做了"自我收敛"（技能候选仅 `enabled`、员工候选给目录 + 允许手动录入）——**那不是安全边界，也没修数据完整性**。

## 2. 目标与非目标

**目标**：把「绑定」收回到"只允许绑定**真实存在、已启用**的技能，且员工标识**已纳管且启用**"；
让**服务端**成为唯一判定点（界面收敛只是体验层）。

**非目标（本轮不做）**：
- **联动**（停用技能 / 停用员工 / 停用岗位 ⇒ 自动解绑或自动停用绑定行）——属运行域与目录面联动，另立（D-029 已登记"后端无该逻辑"）；
- 跨租户技能市场、第三方技能包、MCP（均第 8 轮已登记后置）；
- 绑定行**加字段**（如有效期 / 授权人 / 逐工具授权）——属新的数据模型改动；
- **删除**任何绑定行（现有接口只有 `active ⇄ disabled` 状态迁移）。

## 3. 校验设计（三条闸门）

| # | 闸门 | 落地位置 | 失败语义（**复用既有文案/口径**） |
| --- | --- | --- | --- |
| 1 | 技能**必须存在**（本租户内，且**对调用者可见**） | `SkillService.bind_skill`（域层，fail-closed） | `404`（`SkillNotFound`） |
| 2 | 技能**必须 `enabled`** | `SkillService.bind_skill`（域层） | `409`「只有已启用的技能包可以绑定」 |
| 3 | `agent_key` **必须在目录且启用** | **路由层**（`app/main.py` 的 `bind_skill`） | `409`（`DirectoryNotManaged`）「该标识尚未纳入目录，请先在「数字员工设置」中纳管」**——本轮暂缓，未落地（见 §7-#1 / §9）** |

**为什么第 3 条放路由层而不是注入服务层**：仓库既有**同类先例**——`ensure_agent_binding_available` 已在
`app/main.py:3089`（会话执行入口）与知识范围绑定写路径按**路由层闸门**使用，`DirectoryNotManaged ⇒ 409`
映射亦已存在（`api-contract.md`「会话协作」节 + 第 6 轮知识范围契约）。
本轮**沿用同一手法**，不新造跨模块注入（避免 SkillsService 反向依赖 workforce 面）；
而**域内**两条（技能存在 / 已启用）放 `SkillService`，保证非路由调用方（脚本 / worker）同样 fail-closed。

**顺序（重要）**：**权限判定优先**（`ensure_can_bind` ⇒ 403）→ 三条闸门（404 / 409 / 409）。
即：越权者永远拿 `403`，不会通过"绑定不存在的技能"探测到技能是否存在。

**幂等语义不变**：同一 `(tenant, agent_key, skill_key)` 已 `active` ⇒ 仍**原样返回**（幂等），但**先过闸门**——
若该技能此后被停用，重复 `bind` 会得到 `409`（而不是继续 200），这一点**必须写清**（行为变化）。

## 4. 现存违规行的处置（**待裁决**）

真库现状（`wiring-evidence` 租户，第 9/10 轮取证产生，**均为测试探针**）：

| 行 | 状态 | 违规点 |
| --- | --- | --- |
| `probe-agent ↔ r9-ghost-skill` | `active` | 技能**不存在**（悬空） |
| `probe-agent ↔ r8-self-probe` | `active` | 技能存在但**未启用**（`submitted`） |
| `agent-ghost ↔ r8-probe-skill` | `disabled` | 员工不在目录（**已解除，不生效**） |
| `agent-r9 ↔ r8-probe-skill` | `disabled` | 员工不在目录（**已解除，不生效**） |
| `probe-agent ↔ r8-probe-skill` | `active` | **合规**（技能存在且 `enabled`） |

**本轮实际交付**：**只读盘点脚本**（`scripts/repair_dangling_skill_bindings.py`，默认**干跑**只打印）+ 上表登记清单 + **可选修复动作**（`--apply` ⇒ 把"生效中且违规"的行置为 `disabled`；**只改状态、无 `DELETE`**；必须 `--tenant`，不做全库一把改）。
**是否执行修复由你裁决**——按纪律，**未经确认不执行回滚 / 重置 / 删数据**；且新闸门**只影响后续请求**，不会自动修正历史行。

## 5. 兼容性与影响面

| 影响面 | 说明 |
| --- | --- |
| **会被翻转的用例** | `tests/test_skill_bindings_api.py` 的三条 `known_gap` 用例——**它们本就是为了在闸门补上时变红**（契约 §2 明示）。本轮：前两条改为**断言被拒**（`test_bind_rejects_nonexistent_skill` ⇒ `404` + `total == 0`；`test_bind_rejects_not_enabled_skill` ⇒ `409`）；第三条 `..._agent_key_outside_directory...` **保留为"暂缓事实"钉子**（改名注释为第 ③ 条暂缓），不在本轮翻转。 |
| 既有正常路径 | `tests/test_skills_layer.py` 的绑定用例绑的是**已启用**技能（`review → enable → bind`）⇒ **不受影响**；`test_skills_postgres.py` 同理。 |
| 前端 | 界面已自我收敛（技能候选仅 `enabled`），**无需改动**——本轮前端 `git diff` 为空。服务端新增的 `404` / `409` 由既有 `WRITE_FAILURE_HINT` 呈现原文（含"没有写入任何数据"）。**注**：因第 ③ 条暂缓，"手动录入不在目录的员工标识"**仍 `200`**（未变 `409`）。 |
| 审计 | 绑定成功仍复用 `skill.enabled` + `detail{skill_key, agent_key}`（**不变**）；被拒请求**不写审计**（与既有 4xx 一致）。 |
| `unbind` | **不变**（解绑不校验，且本就允许解绑"技能已停用 / 员工已停用"的绑定——这是清理通道，必须保留）。 |

## 6. 未验证 / 本轮复测清单（写码第一步执行）

1. 现行三条缺口的**真机复核**（确认第 9 轮结论仍成立，且**同时再查库**核对现存违规行）。
2. 目录面 `ensure_agent_binding_available` 的**真机语义**：不存在 / 已停用 / 岗位已停用 三种情形是否都 `409` + 同一文案。
3. 绑定**已启用**技能 + **已纳管**员工 ⇒ `200`（正向路径不能被新闸门误伤）。
4. 幂等路径：已 `active` 时重复 `bind` ⇒ `200`；技能随后被停用再 `bind` ⇒ `409`（**行为变化**须实测确认）。
5. `unbind` 对"技能不存在 / 员工不在目录"的历史行仍可解绑 ⇒ `200`（清理通道保留）。
6. 未测并发 / 压测；跨租户（另一租户的技能键）真机未构造（源码判定按 `tenant_id` 过滤）。

## 7. 待裁决（四项）→ 本轮实际口径

> 用户于第 11 轮**跳过了这四项追问**（未给显式裁决）。按纪律，我**不静默替换方案**，改为：
> **只推进"与既有裁决不冲突"的部分**，凡与既有裁决冲突 / 需你点头的，**显式留在你手上**（下表末列）。

| # | 问题 | 选项 | 我的建议 | 本轮实际 |
| --- | --- | --- | --- | --- |
| 1 | 校验范围 | **A** 三条全做；**B** 只做域内两条，目录校验另立专项 | **A** | **按 B 推进（临时代理）**：域内两条已落地；第 ③ 条**暂缓**——它与第 9 轮已确认的裁决「员工键 = 目录候选 ∪ **手动录入**」**直接冲突**（加上它 ⇒ 手动录入必然被拒），须由你定夺（见 §9-#1） |
| 2 | 现存违规行处置 | **A** 只登记 + 只读盘点 SQL；**B** 出一次性修复脚本（**你在服务器执行**）；**C** 授权我直接改本机测试库 | **B** | **按 B 产出**：`scripts/repair_dangling_skill_bindings.py`（默认干跑、只改状态、必须 `--tenant`、DSN 只从环境变量取）；**是否执行仍由你决定**（见 §9-#3） |
| 3 | 错误码语义 | **A** `404` / `409` / `409`；**B** 三条统一 `422` | **A** | **按 A 落地**（复用既有 `SkillNotFound` / `SkillStateConflict` 语义与文案，未新造） |
| 4 | 是否顺带做"停用技能 ⇒ 自动解绑"联动 | **A** 不做；**B** 做 | **A** | **按 A**：本轮只做校验闸门；联动另立（`decision-log` D-029 已登记） |

## 8. 变更留痕

- 2026-09-20：v1 建立（第 11 轮，用户指令「继续下一轮：bind 校验缺口专项」）。**证据级别 = 源码判定 + 第 9 轮真机证据**；
  §6 复测清单列为写码第一步；§4 现存行处置与 §7 四项待裁决。
- 2026-09-20：**v2 交付**。域内两条闸门落地（先红后绿 + 反假两组）；第 9 轮三条 `known_gap` 用例翻转；修复脚本 + 脚本用例产出；`api-contract.md` 同步更正；§9 交付记录与遗留项。

## 9. 交付记录（v2）

**已落地（域内两条，`SkillService.bind_skill`）**——判定顺序 `403` → `404` → `409`：

1. `list_versions(context, skill_key)` 新增（`app/skills/store.py`，`Protocol` + 内存 + Postgres 三处实现；**不做可见性过滤**，与 `_get_raw` 同口径，可见性由服务层判）；
2. 无任何版本 ⇒ `SkillNotFound`（`404`）；有版本但无 `enabled` ⇒ `SkillStateConflict`（`409`「只有已启用的技能包可以绑定」）；
3. **闸门放在幂等判定之前**（否则已存在的绑定行会成为后门）；被拒请求**不写审计**。

**测试**：新增 `tests/test_skill_binding_gates.py`（13 例：三条闸门 + 域层 fail-closed + 正向 + 幂等 + **技能停用后重复 `bind` ⇒ `409`** 的行为变化 + `403` 优先于存在性 + `unbind` 清理通道保留 + 被拒不写审计 + 钉住"第 ③ 条暂缓"）；`tests/test_skill_bindings_api.py` 三条 `known_gap` 用例**按 §5 预告翻转**为 `test_bind_rejects_nonexistent_skill`（404）/ `..._not_enabled_skill`（409），第 ③ 条用例更名保留为暂缓事实；`tests/test_skill_binding_repair_script.py`（9 例）。

**反假（2 组，均实测）**：去掉存在性闸门 ⇒ **3 红**；去掉已启用闸门 ⇒ **6 红**；均已还原复绿。

**真机**（工作树后端 + 真库 `workbench_test`，**只打已存在的组合、零新增脏数据**）：
- **修复前**复核（`tmp/r11-bind-gate-probe.py`）：三条缺口均仍 `200`，目录 `total = 0`，真库 5 行绑定（2 行 `active` 违规）；
- **修复后**核对（`tmp/r11-bind-gate-verify.py`，实例 `127.0.0.1:18113` + 同真库）：技能不存在 ⇒ **`404`**；技能未启用 ⇒ **`409`**（文案「只有已启用的技能包可以绑定」）；员工键不在目录 ⇒ **仍 `200`**（第 ③ 条暂缓，如实登记）；`employee` 调 `bind`（含不存在的技能）⇒ **`403`**（越权优先，不泄露存在性）；`unbind` 已 `disabled` 行 ⇒ **`200`** 幂等（清理通道保留）；**绑定行数 `5 → 5`**（零新增）；被拒的两次请求**未新增审计行**。

**两条如实观察（非本轮引入，登记备查）**：① **长驻的 `127.0.0.1:18112` 后端进程是 02:18 启动的、加载的是闸门前的代码** —— 复用它做真机核对会得到"闸门无效"的假象（本轮实测踩到）；后续真机核对须用**新起的进程**（或重启该实例）。
② **幂等路径也会写审计**：重复 `bind` 同一个已 `active` 的组合每次都会落一条 `skill.enabled`（第 8/9 轮既有行为，非本轮引入）⇒ 会放大审计噪声，**是否收敛属口径选择，待你定夺**。

**修复脚本**：`scripts/repair_dangling_skill_bindings.py`（纯函数 `violation_reason` / `find_violations` + 薄 DB 层；**默认干跑**、`--apply` 才写、**只 `UPDATE SET status='disabled'` 无 `DELETE`**、必须 `--tenant`、DSN 只从环境变量取）。

## 10. 本轮如实登记的"未解决 / 未验证"（不得读成已验）

1. **第 ③ 条闸门未落地**：绑不在目录的 `agent_key` **仍 `200`**（真机实测）。它与第 9 轮「员工键可手动录入」冲突 ⇒ **待你定夺**：a) 加闸门并**取消手动录入**；b) 加闸门但**保留手动录入**（等于自动把新键纳管，需定义"纳管"动作归属）；c) 维持现状只靠界面收敛。
2. **行为变化待确认**：技能停用后重复 `bind` 由 `200` ⇒ `409`（含历史合规行）。若你要求"幂等优先、不受技能状态影响"，需改为**先判幂等再判闸门**（代价：停用技能后仍能"成功"重复绑定）。
3. **真库 2 行 `active` 违规行未修**（`probe-agent ↔ r9-ghost-skill`（技能不存在）、`probe-agent ↔ r8-self-probe`（`submitted`））——脚本已就绪，**执行权在你**。
4. **未验证**：未测并发 / 压测；**跨租户**（另一租户的技能键）真机未构造（仅源码判定按 `tenant_id` 过滤）；`list_versions` 的可见性口径"不过滤"在多租户真机上未构造反例。

## 11. 第 12 轮续做（bind 校验缺口专项·第二批）

> 用户指令：`继续下一轮：bind 校验缺口专项`。本批**先出契约、再等裁决**（不静默改）。
> **证据级别**：源码判定 + **2026-09-21 只读真机实测**（`tmp/r12-bind-gap-probe.py`，全程 `GET`、零写入）。

### 11.1 只读取证（现状事实）

| # | 事实 | 实测 / 源码 |
| --- | --- | --- |
| 1 | 数字员工目录**为空**，但**岗位目录已有 1 个 active 岗位** | `GET /workforce/agents` ⇒ `total = 0`；`GET /workforce/roles` ⇒ `total = 1`（`evidence-ops`，`active`） |
| 2 | 绑定行 5 条，键全是规范形态（无大写 / 无空白） | `GET /skills/bindings` ⇒ `agent-r9` / `agent-ghost` / `probe-agent` |
| 3 | **幂等 `bind` 每次都写审计**（同 target 多条 `skill.enabled`） | `GET /audits?action=skill.enabled&limit=100` ⇒ `r8-probe-skill@probe-agent` **8 条**、`r8-self-probe@probe-agent` 3 条、`r9-ghost-skill@probe-agent` 3 条、`r8-probe-skill@1.0.0` 3 条 |
| 4 | `agent_key` **无归一化 / 无字符集校验**（`SkillBindRequest` 仅 `min_length=1, max_length=64`） | 源码：`SkillService.bind_skill` 原样透传；而目录面 `normalize_key` 做 `strip()+lower()+` 字符集校验 |
| 5 | 第 ③ 条闸门**可复用的实现已存在** | `app/workforce/service.py:143` `ensure_agent_binding_available` ⇒ `DirectoryNotManaged`（`409`「该标识尚未纳入目录，请先在「数字员工设置」中纳管」），已用于会话入口 `app/main.py:3089` |
| 6 | 自动纳管的**前置条件**：需 `agent_key + name + role_key`，且 `role_key` 必须已 `active`；目录写路径仅 `super_admin`（与 `bind` 同角色） | `create_employee` + `_require_active_role` + `_ensure_admin`（`app/workforce/store.py:108`） |

### 11.2 候选修复项（四项）

| # | 缺口 | 方案 A | 方案 B | 影响面 |
| --- | --- | --- | --- | --- |
| ③ | `agent_key` 未纳管也能绑（实测 `200`） | **加闸门**（路由层，复用 `ensure_agent_binding_available`）⇒ `409`；**界面同轮取消"手动录入"**（`mode="tags"` ⇒ 只读目录候选），目录为空时给"先去纳管"的引导 | 加闸门 + **自动纳管**（绑定顺手在目录建员工） | A：**真机当前 0 个员工 ⇒ 加闸门后"绑定"须先用「数字员工管理」建 1 个员工**（岗位已有，只差员工）；**翻转第 9 轮「手动录入」裁决（需你确认）**；前端 1 处 + 用例。**B 需扩 `SkillBindRequest` 加 `role_key`/`name`** ⇒ 把绑定接口变成目录写接口（一个接口又读又写），且要重定目录写的审计/权限口径 ⇒ **不建议** |
| ④ | `agent_key` 无归一化 ⇒ `"Probe-Agent"` 与 `probe-agent` 是**两行**，目录面按小写比对 ⇒ 绑定"看不见" | **加**：`bind` / `unbind` 前走目录域同一份 `normalize_key`（去空白 + 小写 + 字符集校验）；非法 ⇒ `422` | 不加（维持原样键） | A：与**目录域**口径一致（`agent_key` 属目录域）；**历史 5 行已全规范 ⇒ 无需迁移**；`skill_key` **不动**（技能域既定口径就是只限长度，且有第 11 轮存在性闸门兜底） |
| ⑥ | 幂等 `bind` 每次都写 `skill.enabled`（实测重复最多 **8 条**） | **收敛**：只有"新建"或"`disabled` 翻回 `active`"时写；幂等命中**不写** | 维持（如实记录每次调用） | A：实现为"先读既有绑定状态再决定写不写"（写路径是幂等 UPSERT ⇒ 争用下最坏少写一条审计，不产生错误状态）；需同步既有断言；前端无影响 |
| ⑦ | 行为变化：技能停用后重复 `bind` 由 `200` ⇒ `409`（第 11 轮已实现） | **维持**（闸门在幂等之前） | 改"幂等优先" | 维持 = 已交付，不改；改则要把两条闸门移到幂等之后 ⇒ 会重新打开"已存在绑定行"这个后门 |

### 11.3 本轮明确不做（边界）

- **不做**目录面与绑定面的联动（停用员工 / 停用岗位 ⇒ 自动解绑）：`decision-log` D-029 已登记另立；
- **不做**"绑定即纳管"以外的目录写能力（建岗位 / 改岗位）；
- **不改**技能域既有的 `skill_key` 口径（只限长度）；
- **不动**真库现有 5 行绑定（含 2 行 `active` 违规行）——用户已裁决"保留现状"。

### 11.4 待裁决（四项）

1. **第 ③ 条**：A 加闸门 + 取消手动录入（推荐；代价：真机须先建 1 个员工）／B 加闸门 + 自动纳管（需扩接口字段，不建议）／C 维持暂缓；
2. **第 ④ 条（键归一化）**：A 加（推荐）／B 不加；
3. **第 ⑥ 条（审计噪音）**：A 收敛（推荐）／B 维持；
4. **第 ⑦ 条（行为变化）**：确认维持（推荐）／改幂等优先。

### 11.5 复测清单（裁决后按此执行）

1. 单元（③）：未纳管 ⇒ `409`；已纳管且员工 `active`（岗位 `active`）⇒ `200`；员工 `disabled` ⇒ `409`；岗位停用连带 ⇒ `409`；被拒**不写审计**；
2. 单元（④）：`" Probe-Agent "` / `"PROBE-AGENT"` ⇒ 归一化后与 `probe-agent` **同一行**（幂等命中，不新增行）；非法字符 ⇒ `422`；`unbind` 同样归一化；
3. 单元（⑥）：首次 `bind` ⇒ 1 条审计；重复 `bind` ⇒ **审计条数不变**；`unbind → bind`（状态翻转）⇒ 新增 1 条；
4. 真机（零写入或最小写入）：③ 未纳管键 ⇒ `409`；④ 归一化命中幂等 ⇒ **绑定行数不变**；⑥ 连续两次 `bind` ⇒ 审计条数不变；
5. 前端：③ 取消手动录入后，目录为空时给"先去纳管"的引导（不是空白下拉）；写入失败**原样呈现服务端 `409` 文案**。

### 11.6 交付记录（v4，2026-09-21）

**已落地**（用户裁决 `③A / ④A / ⑥A / ⑦维持`）：

| 项 | 落地位置 | 说明 |
| --- | --- | --- |
| ③ 员工必须已纳管且启用 | **路由层** `app/main.py::bind_skill` | 复用 `workforce_directory_service.ensure_agent_binding_available`（与「知识范围」写路径同一先例）⇒ `409`；**不落在服务层**（避免 SkillsService 反向依赖 workforce 面，与第 11 轮同一理由） |
| ④ 键归一化 + 字符集校验 | **路由层** `bind_skill` / `unbind_skill` | 复用目录域同一份 `normalize_key`（`app/workforce/models.py`，main.py 直接 import）⇒ 非法 `422`；`skill_key` **不动**（技能域既定口径）；`unbind` 同样归一化，**但不设目录闸门**（清理通道） |
| ⑥ 幂等调用不重复写审计 | **服务层** `app/skills/service.py` | `bind_skill` / `unbind_skill` 先读既有绑定状态（`_binding_of`）⇒ 仅"新建 / 状态翻转"时 `_record`；读-判-写不会产生错误状态（写路径本身是幂等 UPSERT） |
| ⑦ 闸门先于幂等 | 不变 | 技能停用后重复 `bind` 仍 `409`（行为变化维持） |
| 界面（③A 配套） | `BindingPanel.tsx` + `skillsService.ts` | 员工键**取消 `mode="tags"` 手动录入**，只给目录候选；目录为空给"先去「数字员工管理」纳管"的引导（`AGENT_CANDIDATE_EMPTY_NOTE`）、并把口径写进 `AGENT_DIRECTORY_ONLY_NOTE` |

**先红后绿**：测试先写 ⇒ **9 红**（未纳管 / 员工停用 / 岗位停用连带 / 归一化 / 非法标识 / 解绑归一化 / 幂等 bind 审计 / 幂等 unbind 审计 / 绑定面翻转用例）⇒ 实现后全绿。
既有用例的**兼容性改动**：第 11 轮"钉住第 ③ 条暂缓"的两条用例**翻转为断言被拒**；`test_skill_bindings_api` / `test_skills_role_matrix` / `test_skill_binding_gates` 三个文件加 `managed_directory` 夹具（先把用到的员工键纳管，幂等 `409` 视为成功）。

**反假（4 组，均实测）**：去路由层目录闸门 ⇒ **4 红**；去 `normalize_key` ⇒ **3 红**（含"非法键落到 409 而非 422"—正是两套口径的危害）；去审计收敛的前置读取（bind 侧）⇒ **1 红**、（unbind 侧）⇒ **2 红**；前端把员工 Select 改回 `mode="tags"` ⇒ **3 红**。以上均还原复绿。

**真机（2026-09-21，最小写入）**：未纳管键 ⇒ `409` 且**行数不变**（`5 → 5`）；纳管 1 个探针员工后，`"  PROBE-AGENT-R12  "` ⇒ `200` 且**回读规范键** `probe-agent-r12`（行数 `5 → 6`，只 1 行）；连续两次重复 `bind` ⇒ 该 target 的 `skill.enabled` 条数 **1 → 1**（未新增）；非法键 ⇒ `422`；`employee` + 非法键 / 未纳管键 ⇒ 一律 `403`（越权优先）。

**写入面（**用户 2026-09-21 裁决：保留**；因本机"绑定"依赖它才可用——目录原本 0 员工）**：`workbench_digital_employees` 新增 **1 行**探针员工 `probe-agent-r12`（挂既有岗位 `evidence-ops`，`active`）；`workbench_skill_bindings` 新增 **1 行** `probe-agent-r12 ↔ r8-probe-skill`（`active`）。

**本轮边界（未做）**：目录面与绑定面的联动（停用员工 / 岗位 ⇒ 自动解绑）仍另立；技能域 `skill_key` 口径不改；真库 5 行既有绑定未动。
