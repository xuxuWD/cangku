import { ContentState } from '../../components/ContentState'

/** 我的工作台（占位页，第 1 轮只搭壳，不接业务数据）。 */
export function MyWorkbenchPage() {
  return <ContentState state="empty" description="「我的工作台」尚未接入（第 3 轮实现）。" />
}