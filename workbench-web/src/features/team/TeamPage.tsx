import { ContentState } from '../../components/ContentState'

/** 团队协作（占位页，第 1 轮只搭壳，不接业务数据）。 */
export function TeamPage() {
  return <ContentState state="empty" description="「团队协作」尚未接入（第 4 轮实现）。" />
}