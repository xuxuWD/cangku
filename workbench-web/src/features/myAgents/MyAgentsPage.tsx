import { ContentState } from '../../components/ContentState'

/** 我的数字员工（占位页，第 1 轮只搭壳，不接业务数据）。 */
export function MyAgentsPage() {
  return <ContentState state="empty" description="「我的数字员工」尚未接入（第 3 轮实现）。" />
}