import { ContentState } from '../../components/ContentState'

/** 知识库（占位页，第 1 轮只搭壳，不接业务数据）。 */
export function KnowledgePage() {
  return <ContentState state="empty" description="「知识库」尚未接入（第 4 轮实现）。" />
}