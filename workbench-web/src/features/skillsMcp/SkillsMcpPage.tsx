import { ContentState } from '../../components/ContentState'

/** Skill & MCP（占位页，第 1 轮只搭壳，不接业务数据）。 */
export function SkillsMcpPage() {
  return <ContentState state="empty" description="「Skill & MCP」尚未接入（第 6 轮实现）。" />
}