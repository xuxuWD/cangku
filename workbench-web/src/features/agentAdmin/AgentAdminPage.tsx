import { ContentState } from '../../components/ContentState'

/** 数字员工管理（占位页，第 1 轮只搭壳，不接业务数据）。 */
export function AgentAdminPage() {
  return <ContentState state="empty" description="「数字员工管理」尚未接入（第 5 轮实现）。" />
}