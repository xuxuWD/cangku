# 贡献规范（CONTRIBUTING）

> **当前状态（如实）**：本仓库为**内部项目仓库**（`github.com/xuxuWD/cangku`），
> **不接受外部贡献**。本文件的作用是**把团队内部的提交纪律写在一处**，供本项目协作者与 AI 助手共同遵守。
> 若将来转为公开项目，须**重写并经评审**（对外口径、许可与 CLA 均需另定）。

## 1. 开工前：真源三件套齐了才写代码

| 件 | 位置 | 说明 |
| --- | --- | --- |
| 立项文档 | [`docs/superpowers/specs/`](file:///d:/徐徐AI学习/公司工作台/docs/superpowers/specs) | 定义"这个项目做什么 / 不做什么 / 边界" |
| 功能清单 | [docs/feature-inventory.md](file:///d:/徐徐AI学习/公司工作台/docs/feature-inventory.md) | 只登记"有哪些能力"与六要素骨架 |
| 阶段文档 / 规格 | `docs/superpowers/specs/*-design.md` | 每期开工具备 |

**接口口径的唯一真源**是 [docs/api-contract.md](file:///d:/徐徐AI学习/公司工作台/docs/api-contract.md)；
**权限口径的唯一真源**是 [docs/contracts/permission-matrix.md](file:///d:/徐徐AI学习/公司工作台/docs/contracts/permission-matrix.md)。
实现与真源冲突时**以真源为准**；要改先改真源。

## 2. 每次提交必须满足（口径与 [docs/delivery-gates.md](file:///d:/徐徐AI学习/公司工作台/docs/delivery-gates.md) 一致）

1. **先增加或更新行为测试，再修改生产代码**；
2. `python -m pytest -q` **全部通过**；
3. `python -m compileall -q app tests extract_pdf.py` 通过；
4. **新接口同步更新 `docs/api-contract.md`**（由 `tests/test_api_contract_coverage.py` 守护：每个 `/api/` 路由都必须能在契约中查到完整路径）；
5. **不提交** `.env`、密钥、Cookie、浏览器会话、客户原文或临时媒体；
6. **未通过真实验收的能力不能写成"已上线""已发布""已收录"**。

> **CI 强制范围**：第 2–4 条中机器可判定的部分由 [.github/workflows/ci.yml](file:///d:/徐徐AI学习/公司工作台/.github/workflows/ci.yml) 在 push / PR 上执行。
> **第 1、5、6 条属人工约定，CI 不覆盖**——靠人（和 AI）守。
> 其中 `sandbox` job **尚无 push 触发的运行记录** ⇒ 在实跑回填前**不得声称已通过**（口径见 delivery-gates.md）。

## 3. 测试纪律

- **先红后绿**：高风险功能强制先写失败测试；
- **反假测试**：故意在代码里制造错误（如金额算错、闸门改坏），重跑**必须变红**；仍全绿 = 测试无效，重写；
- **一条命令跑全量**：新增/修改功能后重跑，**一条红先解决再往下**；
- **验收只认证据**：接口要给五类现象实例（正常 / 参数错误 / 未登录 / 无权限 / 访问他人数据）+ 数据前后变化 + 日志记录；
  **没验证的必须写"未验证"**，禁止只回"已完成 / 已优化 / 符合最佳实践"。

## 4. 分层红线（摘要，完整口径见项目宪法）

| 层 | 必须 | 禁止 |
| --- | --- | --- |
| 前端 | 四种占位态齐备（加载/空/错误/无权限）；同类 UI 出现 >2 次抽组件；请求走统一封装 | 隐藏按钮当权限控制；前端定价格/库存/角色/余额；密钥与内部地址进前端代码 |
| 接口 | 统一响应与错误处理；401/403/404/409/429 语义正确；列表分页；关键写接口幂等 | try-except 吞异常；跳过认证中间件；写死管理员密码/测试数据 |
| 数据 | 表名/字段英文+下划线；金额不用浮点；密码只存哈希；重要数据软删除；结构变更走 migration | 明文存密码；生产库跑测试脚本；N+1；`SELECT *` 于生产大表 |
| 业务 | 多表同时变更必须包事务；状态取值显式定义；关键操作预埋日志 | 状态字符串随手写；无事务改多表 |

## 5. 提交与发布

- **不自动提交 / 不自动推送 / 不自动合并 / 不自动部署**——一律**先经确认**；
- 未经确认**不执行**回滚、`reset --hard`、删历史、删数据；
- **临时物清理**：一次性脚本、调试打印、临时测试接口、写死的假数据、一次性脚本，
  必须**列清单 → 经确认 → 再删**；正式回归测试与线上排查用的运行日志**绝不能删**；
- 依赖升级：**一次只升一个升级单元**，不删锁定文件、不绕过兼容检查；安全告警先确认是否真受影响，**别整个项目全升**。

## 6. 文档与留痕

- 凡影响**架构 / 权限 / 数据 / 交付**的结论，记入 [docs/contracts/decision-log.md](file:///d:/徐徐AI学习/公司工作台/docs/contracts/decision-log.md)（只记**已作出**的决策，每条必须有依据）；
- 变更留痕：改了什么、依据是什么、哪些**未验证**；
- **不复制外部项目源码**：外部能力默认"接适配器"，强传染/非商用许可只允许**只读研读**（见 [docs/architecture.md](file:///d:/徐徐AI学习/公司工作台/docs/architecture.md#L41-L53)）。

## 7. 多 Agent 协作（本项目实际使用）

- **写代码与验收分离**：验收方只许看与给结论，不参与实现；
- 同一批文件、同一批验收、架构仍在变的任务**不拆**；
- 成果带回前**先查冲突**；**不许自动提交/合并/推送/部署**。