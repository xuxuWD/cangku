export function ObjectSelect({ value, onChange, options }: { value: string; onChange: (value: string) => void; options: { key: string; label: string }[] }) { return <label className="object-select"><span className="sr-only">选择岗位或数字员工</span><select value={value} onChange={(event) => onChange(event.target.value)}>{options.map((option) => <option key={option.key} value={option.key}>{option.label}</option>)}</select></label> }
