export function SegmentedControl({ value, onChange }: { value: 'role' | 'agent'; onChange: (value: 'role' | 'agent') => void }) { return <div className="segment" role="tablist" aria-label="权限对象类型"><button role="tab" aria-selected={value === 'role'} className={value === 'role' ? 'active' : ''} onClick={() => onChange('role')}>岗位权限</button><button role="tab" aria-selected={value === 'agent'} className={value === 'agent' ? 'active' : ''} onClick={() => onChange('agent')}>数字员工权限</button></div> }
